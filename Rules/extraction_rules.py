import spacy 
import datetime
import regex as re 
import nltk
from nltk.corpus import stopwords
from tqdm import tqdm
import time as time

def join_lists(nested_list):
    """
    Helper function to join lists within a nested list as strings, separated by commas.
    """
    return '; '.join([', '.join(sublist) for sublist in nested_list])


def skill(df):
    s = 0
    o = re.findall
    for index, row in tqdm(df.iterrows(), total=len(df), desc='Extracting Skills...'):
        text = row['Tokens']

        # Matching fragments for report utility meter readings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\butility\s+meter\w*', text)
        match_2 = o(r'\bmetering\w*|\bmeter\s+read\w*', text)
        match_3 = o(r'\butility\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'report utility meter readings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify faults in utility meter meters
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\butility\s+meter\w*|\bgas\s+meter\w*|\belectricity\s+meter\w*|\bheat[i]?[n]?[g]?\s+meter\w*|\belectric\s+meter\w*|\belectrical\s+meter\w*|\bwater\s+meter\w*', text)
        match_2 = o(r'\bfault\w*|\bissue\w*|\bproblem\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify faults in utility meter meters'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calculate utility payments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\butility\s+payment\w*|\bgas\s+payment\w*|\bheat\s+payment\w*|\bheating\s+payment\w*|\belectric\s+payment\w*|\belectricity\s+payment\w*|\butility\s+bill\w*|\bgas\s+bill\w*|\bheat\s+bill\w*|\bheating\s+bill\w*|\belectric\s+bill\w*|\belectricity\s+bill\w*|\butility\s+invoice\w*|\bgas\s+invoice\w*|\bheat\s+invoice\w*|\bheating\s+invoice\w*|\belectric\s+invoice\w*|\belectricity\s+invoice\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calculate utility payments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read water meter
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+meter\w*', text)
        match_2 = o(r'\bmetering\w*|\bmeter\s+read\w*|\bread[i]?[n]?[g]?\s+meter\w*', text)
        match_3 = o(r'\bwater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read water meter'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read electricity meter
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectricity\s+meter\w*|\belectrical\s+meter\w*|\belectric\s+meter\w*', text)
        match_2 = o(r'\bmetering\w*|\bmeter\s+read\w*|\bread[i]?[n]?[g]?\s+meter\w*', text)
        match_3 = o(r'\belectric\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read electricity meter'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read gas meter
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+meter\w*', text)
        match_2 = o(r'\bmetering\w*|\bmeter\s+read\w*|\bread[i]?[n]?[g]?\s+meter\w*', text)
        match_3 = o(r'\bgas\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read gas meter'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read heat meter
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+meter\w*|\bheating\s+meter\w*', text)
        match_2 = o(r'\bmetering\w*|\bmeter\s+read\w*|\bread[i]?[n]?[g]?\s+meter\w*', text)
        match_3 = o(r'\bheat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read heat meter'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use aircraft maintenance and engineering operating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bamos\b|\baircraft\s+maintenance\s+engineer[i]?[n]?[g]?\s+operat[e]?[i]?[n]?[g]?\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use aircraft maintenance and engineering operating system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect aircraft documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baircraft\s+document\w*|\bhelicopter\s+document\w*|\bdocument\s+aircraft\w*|\bdocumentation\s+aircraft\w*|\bdocument\s+helicopter\w*|\bdocument\s+aircraft\w*', text)
        match_2 = o(r'\baircraft\s+maintenance\w*|\bhelicopter\s+maintenance\w*|\bmaintain\s+aircraft\w*|\bmaintain\s+helicopter\w*|\bairworthiness\w*|\baircraft\s+inspect\w*|\bhelicopter\s+inspect\w*|\binspect\s+aircraft\w*|\binspect\s+helicopter\w*|\breview\s+aircraft\w*|\breview\s+helicopter\w*|\baircraft\s+review\w*|\bhelicopter\s+review\w*', text)
        match_3 = o(r'\bdocument\w*|\bdocumentation\w*|\bpaperwork\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect aircraft documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect aircraft for airworthiness
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baircraft\s+maintenance\w*|\bhelicopter\s+maintenance\w*|\bmaintain\s+aircraft\w*|\bmaintain\s+helicopter\w*|\bairworthiness\w*|\baircraft\s+inspect\w*|\bhelicopter\s+inspect\w*|\binspect\s+aircraft\w*|\binspect\s+helicopter\w*|\breview\s+aircraft\w*|\breview\s+helicopter\w*|\baircraft\s+review\w*|\bhelicopter\s+review\w*|\banalyse\s+aircraft\w*|\banalyze\s+aircraft\w*|\banalyse\s+helicopter\w*|\banalyze\s+helicopter\w*|\baircraft\s+analys\w*|\bhelicopter\s+analys\w*', text)
        match_2 = o(r'\bfitness\b|\bfit\b|\bsound\b|\bsoundness\w*|\breliab\w*', text)
        match_3 = o(r'\bfly\b|\bflying\b|\bflight\b|\baircraft\w*|\bhelicopter\w*|\bair\s?worthiness\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect aircraft for airworthiness'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain water infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\water\s+installation\s+engineer\w*|\bwater\s+response\s+team\w*|\bwater\s+network\s+maintenance\w*|\bmanage\s+water\s+network\w*|\bmaintain\s+water\s+network\w*|\bmaintain\s+water\s+infrastructure\w*', text)
        match_2 = o(r'\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\bservice\w*|\bservicing\w*|\bmaintain\w*|\binstallation\w*|\bengineer\w*|\binstall\w*|\bservicing\s+work\w*|\bexperience\w*|\bknowledge\w*', text)
        match_3 = o(r'\bpipework\b|\bpipeline\b|\bnetwork\w*|\bgrid\w*|\binfrastructure\w*|\bsuppl\w*', text)
        match_4 = o(r'\bwater\w*|\bwastewater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain water infrastructure'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform water treatments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+treatment(?! company| firm| industry| technolog)\w*|\btreatment\s+water\w*|\bwater\s+technician\w*|\btmv\s+maintenance\w*|\btmv\s+monitor\w*', text)
        match_2 = o(r'\bpurification\w*|\bhygiene\s+technician\w*|\bhygiene\s+chemist\w*|\bdecontamination\w*|\bhygiene\s+code\w*', text)
        match_3 = o(r'(?<!waste )\bwater\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform water treatments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage and maintain desalination control systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesalination\b', text)
        match_2 = o(r'\bsystem\b|\bsystems\b|\bfiltration\w*|\btreatment\w*|\bwater\w*|\bcontrol\w*|\bequipment\w*|\binstrument\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage and maintain desalination control systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform water treatment procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+procedure\w*|\bwater\s+treatment\s+procedure\w*|\bwater\s+treatment\s+operat\w*|\bwater\s+filtration\w*|\bwater\s+treatment\s+system\w*|\bwater\s+treatment\s+engineer\w*|\bdisinfect\s+water\w*|\bdisinfection\s+water\w*|\bwater\s+disinfect\w*|chlorinat\w*', text)
        match_2 = o(r'\btechnician\w*|\bwater\s+technician\w*', text)
        match_3 = o(r'\bwater\s+treatment\w*', text)
        match_4 = o(r'\bfilter\w*|\bsterile\w*|\bsterilising\w*|\bpurify\w*|\bosmosis\w*|\bozonation\w*|\bcarbon\s+filter\w*|\bcarbon\s+filtration\w*|\bultraviolet\w*|\buv\b|\bwater\s+softener\w*', text)
        match_5 = o(r'\bwater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform water treatment procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for model or study ground water
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'r\bpore\s+pressure\w*|\bgroundwater\s+analy\w*|\banaly[s]?[e]?[i]?[s]?\s+groundwater\w*|\bgroundwater\s+flow\w*|\bmodel[l]?[i]?[n]?[g]?\s+groundwater\w*|\bgroundwater\s+model\w*|\bgroundwater\s+characteristic\w*|\bcharacteristic[s]?\s+groundwater\w*|\bstud[y]?[i]?[n]?[g]?\s+groundwater\w*|\bgroundwater\s+study\b', text)
        match_2 = o(r'\bground\s+water\w*|\bunder\s+ground\s+water\w*|\bwater\s+under\s+ground\w*', text)
        match_3 = o(r'\btest\w*|\basses\w*|\bstudy\w*|\bresearch\w*|\blab\b|\blaboratory\w*|\bscientist\w*|\banaly\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'model or study ground water'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for measure pH
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bph\b|\balkali\b|\balkalinity\w*|\bacidity\b', text)
        match_2 = o(r'\bmeasure\w*|\bmeasuring\w*|\bcollect\w*|\btest\w*|\bsample\w*', text)
        match_3 = o(r'\bwater\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'measure pH'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor temperature of water
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+temperature\w*|\btemperature\s+water\w*', text)
        match_2 = o(r'\btemperature\s+monit\w*|\bmonitor\s+temperature\w*|\bmonitoring\s+temperature\w*|\btake\s+temperature\w*|\bcontrol\s+temperature\w*|\btemperature\s+control\w*', text)
        match_3 = o(r'\bwater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor temperature of water'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform water disinfection
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdisinfect\w*', text)
        match_2 = o(r'\bwater\w*|\bwaste\s+water\w*|\bwastewater\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform water disinfection'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor water systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+system[s]?\s+monitor\w*|\bmonitor[i]?[n]?[g]?\s+water\s+system\w*|\bmaintain[i]?[n]?[g]?\s+water\s+system\w*|\bwater\s+system[s]?\s+maintenance\w*', text)
        match_2 = o(r'\bcontrol\w*|\bmanage\w*|\bmonitor\w*', text)
        match_3 = o(r'\bwater\s+system\w*|\bwater\s+environmnet\w*|\benvironment\s+water\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor water systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carry out waste water treatments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwaste\s+water\s+treatment\w*|\btreatment\s+waste\s+water\w*|\bwastewater\s+treatment\w*|\btreatment\s+wastewater\w*', text)
        match_2 = o(r'\bwaste\s+water\w*|\bwastewater\w*', text)
        match_3 = o(r'\bprocedure\s+identification\w*|\bhygiene\s+technician\w*|(?<!nuclear )\bdecontamination\w*|\bhygiene\s+chemist\w*|\bhygiene\s+code\w*|\bpurification\w*', text)
        match_4 = o(r'\bwaste\s+purification\w*', text)
        match_5 = o(r'\bwaste\s+water\w*|\bwastewater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'carry out waste water treatments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise waste water treatment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwaste\s+water\s+treatment\s+supervis\w*|\bwastewater\s+treatment\s+supervis\w*|\bsupervis[e]?[i]?[n]?[g]?\s+waste\s+water\w*|\bsupervis[e]?[i]?[n]?[g]?\s+wastewater\w*|\bwaste\s+water\s+supervis\w*|\bwastewater\s+supervis\w*|\bmanage\s+waste\s+water\w*|\bmanage\s+wastewater\w*|\bwaste\s+water\s+treatment\s+manage\w*|\bwastewater\s+treatment\s+manage\w*|\bwaste\s+water\s+manage\w*|\bwastewater\s+manage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise waste water treatment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate pressure washer
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpressure\s+washer\w*|\bhigh\s+pressure\s+clean\w*|\bpressure\s+clean\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate pressure washer'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create design for pipeline engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspool\s+design\w*|\bspool\s+engineer\w*|\bpiping\s+design\w*|\bdesign[i]?[n]?[g]?\s+pipe\w*|\bpipe\s+design\w*|\bpipeline\s+design\w*|(?<!rigid )\bpipeline\s+design\w*|\bdesign\s+pipe\w*|\bpipe\s+design\w*', text)
        match_2 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+pipeline\w*|\bpipeline\s+manage\w*|\bgas\s+transmission\w*|\bgas\s+pipeline\w*|\btransfer\s+gas\w*|\bgas\s+transfer\w*|\bpiping\s+engineer\w*|\bpipeline\s+engineer\w*|\bpip\b|\bstructural\s+engineer\w*|\bstructural\s+design\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bplan\w*|\bblueprint\w*|\blayout\w*', text)
        match_4 = o(r'\bgas\b|\boil\b', text)
        match_5 = o(r'\btransmission\w*|\bpipeline\w*|\bpiping\w*|\bpipework\w*|\bpipe\b|\bpipelay\w*|\btransfer\w*|\bwater\s+network\w*|\bwater\s+engineer\w*|\bgrid[s]?\b|\bnetwork\w*', text)
        match_6 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bblueprint\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create design for pipeline engineering'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform and advise on pipe installation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpipeline\s+instal\w*|\binstal[l]?[a]?[t]?[i]?[o]?[n]?\s+pipe\w*|\bpipe\s+instal\w*|\blay\s+pipe\w*|\bpipe\s+lay\w*|\bpipelay\s+equipment\w*|\bpipelay\s+instal\w*|\bpipelay\s+operation\w*|\bpipefit\w*', text)
        match_2 = o(r'\bpi\b|\binstallation\s+engineer\w*', text)
        match_3 = o(r'\bpipeline\w*|\bpipelay\w*|\bassemble\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform and advise on pipe installation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor safety and security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breview\w*|\bmanage\w*|\bensure\w*|\bhandle\w*|\bmonitor\w*', text)
        match_2 = o(r'\bsafety\s+security\w*|\bsecurity\s+safety\w*|\bsafe\s+secure\w*|\bsecure\s+safe\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor safety and security'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply safety management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsafety\s+assessment(?! site)\w*|\bassess[i]?[n]?[g]?\s+safety\w*|\bsafety\s+manage\w*|\bsafety\s+control\w*|\bmanag[e]?[i]?[n]?[g]?\s+safety\w*|\bprovide\s+safe\w*|\bensure\s+safe\w*|\bsafety\s+brief\w*|\bsafe[t]?[y]?\s+manage\w*|\bensure\s+safe\w*|\bsafety\s+analy\w*|\bsafety\s+engineer\w*|\bmonitor[i]?[n]?[g]?\s+safety\w*|\bsafety\s+monitor\w*|\bsafety\s+performance\w*|\bsafety\s+risk\s+assess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply safety management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install or repair plumbing systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplumbing\b|\bplumber\b|\bpipework\w*', text)
        match_2 = o(r'\bengineer\w*|\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*|\bfitter\w*|\bservice\w*|water\w*|\bnetwork\w*|\bsystem\w*|\boperation\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install or repair plumbing systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle complaints
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomplaint[s]?\w*', text)
        match_2 = o(r'\bmanag[e]?[i]?[n]?[g]?\w*|\bhandl[e]?[i]?[n]?[g]?\w*|\bdeal\w*|\brespond\w*|\badmin\w*', text)
        match_3 = o(r'\bcustomer\w*|\bclient\w*|\bstakeholder\w*', text)
        match_4 = o(r'\bcomplaint\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle complaints'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle return
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breturn\w*|\breplacement\w*', text)
        match_2 = o(r'\bmanage\w*|\bproduct\w*|\bhandle\w*|\bdeal\w*|\bprovide\w*|\bhandling\w*|\bmanaging\w*|\bproviding\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle return'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advice on investment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binvestment\s+analy\w*|\banalyze\s+investment\w*|\banalyse\s+investment\w*|\binvestigate\s+investment\w*|\binvestment\s+investigation\w*|\bassess\s+investment\w*|\binvestment\s+assess\w*|\breview\s+investment\w*|\binvestment\s+review\w*|\bmanage\s+investment\w*|\binvestment\s+manage\w*|\binvestment\s+advice\w*|\badvice\s+investment\w*|\badvise\s+investment\w*|\badvice\s+investment\w*|\binvestment\s+advice\w*|\bsuggest\s+investment\w*|\bcounsel\s+investment\w*|\bcompare\s+investment\w*|\binvestment[s]?\s+compar\w*|\bcomparison\s+investment\w*|\binvestment\s+plan\w*|\bnet\s+present\s+cost\w*|\bnet\s+present\s+value\w*|\bnpc\b|\bnpv\b|\bplan[i]?[n]?[g]?\s+investment\w*|\bvalue\s+risk\w*|\bvar\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advice on investment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for review investment portfolios
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binvestment\s+delivery\w*|\binvestment\s+analy\w*|\banalyze\s+investment\w*|\banalyse\s+investment\w*|\binvestigate\s+investment\w*|\binvestment\s+investigation\w*|\bassess\s+investment\w*|\binvestment\s+assess\w*|\breview\s+investment\w*|\binvestment\s+review\w*|\bnet\s+present\s+cost\w*|\bnet\s+present\s+value\w*|\bnpc\b|\bnpv\b', text)
        match_2 = o(r'\bportfolio\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'review investment portfolios'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage and analyse financial risks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinance\s+risk\w*|\bfinancial\s+risk\w*|\bmitigate\s+finance\s+impact\w*|\bmitigate\s+financial\s+impact\w*|\bfinance\s+impact\s+mitigat\w*|\bfinancial\s+impact\s+mitigat\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage and analyse financial risks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on financial matters
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinance\s+advice\w*|\bfinancial\s+advice\w*|\beconomic\s+advice\w*|\bfinancial\s+advisor\w*|\beconomic\s+advisor\w*|\bfinancial\s+matter\w*|\bfinance\s+matter\w*|\bifa[s]?\s+support\w*|\bsupport[i]?[n]?[g]?\s+ifa[s]?\b', text)
        match_2 = o(r'\bifa[s]?\b|\beis\b', text)
        match_3 = o(r'\bfinancial\s+plan\w*|\bfinancial\s+advice\w*|\binvestment\w*|\bportfolio\w*|\bpension\w*|\bsupport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on financial matters'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design wind farm collector systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwind\s+farm\w*|\bwind\s+turbine\w*', text)
        match_2 = o(r'\bcollector\s+system\w*|\bcollect\s+electricity\w*|\bcollect\s+energy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design wind farm collector systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for research locations for wind farms
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brenewable\s+energy\s+engineer\w*|\bwind\s+energy\s+engineer\w*', text)
        match_2 = o(r'\bevaluat\w*|\bresearch\w*|\banaly\w*|\bsearch\w*|\bcheck\w*', text)
        match_3 = o(r'\bwind\s+farm\s+site\w*|\bwind\s+farm\s+location\w*|\blocation\s+wind\s+farm\w*|\bwind\s+site\w*|\bwind\s+location\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'research locations for wind farms'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor work site
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsite\s+manage\w*|\bsupervise\s+site\w*|\bsupervise\s+site\w*|\bsite\s+supervis\w*|\bplan[n]?[i]?[n]?[g]?\s+\w+\s+site\s+work\b|\bmanag[e]?[i]?[n]?[g]?]\s+site\w*|\bmonitor[i]?[n]?[g]?\s+site\w*|\bmanag[e]?[i]?[n]?[g]?]\s+\w+\s+site\b|\bmonitor[i]?[n]?[g]?\s+\w+\s+site\b|\bsite\s+plann\w*|\binspect[i]?[n]?[g]?\s+site\w*|\bsite\s+inspect\w*', text)
        match_2 = o(r'\bsat\b', text)
        match_3 = o(r'\bcommission\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor work site'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install gas heaters
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+heater\b|\blpg\s+heater\w*|\bbutane\s+heater\w*|\bmethane\s+heater\w*|\bair\s+heat\s+equipment\w*|\bair\s+heating\s+equipment\w*|\bair\s+heat\s+tool\w*|\bair\s+heating\s+tool\w*|\bair\s+-\s+heat\s+equipment\w*|\bair\s+-\s+heating\s+equipment\w*|\bair\s+-\s+heat\s+tool\w*|\bair\s+-\s+heating\s+tool\w*', text)
        match_2 = o(r'\binstall\w*|\bassembl\w*|\bbuild\w*|\bfit\b|\bfitting\w*|\btechnician\w*|\bengineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install gas heaters'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install heat pump
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+pump\w*|\bheat\s+exchange\w*', text)
        match_2 = o(r'\binstall\w*|\btechnician\w*|\bengineer\w*|\bcommission\w*|\bdesign\b|\bdesigner\b|\bdesigning\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install heat pump'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design heat pump installations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+pump\s+installation\w*', text)
        match_2 = o(r'\bheat\s+pump\w*', text)
        match_3 = o(r'\bpipework\b|\bpipes\b|\bpipe\b|\bsystem\w*|\bdesign\b|\bdesigner\b|\bdesigning\b|\bvalve\w*|\bpiping\w*|\binstallation\s+engineer\w*', text)
        match_4 = o(r'\bsale\w*|\bselling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design heat pump installations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for instal heating boiler
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bboiler\w*', text)
        match_2 = o(r'\binstal\w*|\bfit\b|\bfitting\b|\bcommission\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'instal heating boiler'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate or repair boiler
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bboiler\w*', text)
        match_2 = o(r'\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binstal\w*|\bassembl\w*|\boperate\w*|\btend\w*|\boperating\w*|\bdiagnos\w*|\btechnician\w*|\bengineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate or repair boiler'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply territory planning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bterritory\s+plan\w*|\bterritory\s+manage\w*|\bmanage\s+territory\w*|\bterritory\s+marketing\w*|\bplan\s+territory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply territory planning'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for climb structures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclimb\s+tower\w*|\bclimb\s+ladder\w*|\bclimb\s+building\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'climb structures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage process engineering activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocess\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage process engineering activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage renewable energy projects
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brenewable\s+energy\w*|\bwind\s+energy\w*|\btidal\s+energy\b|\btidal\s+turbine\w*|\bwind\s+turbine\w*|\bwave\s+turbine\w*|\brenewable\s+power\w*|\bsolar\s+energy\w*|\bsolar\s+power\w*|\bphotovoltaic\w*|\bpv\b', text)
        match_2 = o(r'\bmanage\s+project\w*|\bproject\s+manage\w*|\bproject\s+plan\w*|\bmanage\s+multiple\s+project\w*|\bproject\s+coordination\w*|\bproject\s+-\s+co\s+ordination\w*', text)
        match_3 = o(r'\bmanag[e]?[i]?[n]?[g]?\b|ordination\w*|\bplan\w*', text)
        match_4 = o(r'\bproject\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_1 and match_3 and match_4:
            matched_fragments.extend([match_1, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage renewable energy projects'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plant commissioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplant\s+commission\w*|\bfactory\s+commission\w*|\bplant\s+safety\w*', text)
        match_2 = o(r'\bfat\b', text)
        match_3 = o(r'\bcommission\w*', text)
        match_4 = o(r'\bknowledge\s+commissioning\w*|\bday\s+-\s+-\s+day\s+commissioning\w*|\bcommissioning\s+procedure\w*|\bcommission[i]?[n]?[g]?\s+manager\w*|\bcommission[i]?[n]?[g]?\s+activity\w*|\bcommission[i]?[n]?[g]?\s+process\w*|\bcommission[i]?[n]?[g]?\s+engineer\w*|\bquality\s+control\w*|\bqc\b|\bquality\s+assur\w*|\bmanage\s+quality\w*|\bassess\s+quality\w*|\bquality\s+assess\w*|\bensure\s+quality\w*', text)
        match_5 = o(r'\bplant\b|\bmeica\w*|\bfactory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plant commissioning'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan emergency response
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemergency\s+plan\w*|\bemergency\s+procedure\w*|\bemergency\s+response\w*|\bplan[n]?[i]?[n]?[g]?\s+emergency\w*|\brespond[i]?[n]?[g]?\s+emergency\w*|\bwater\s+response\s+team\w*|\brapid\s+response\w*|\bemergency\s+process\w*|\bemergency\s+security\w*', text)
        match_2 = o(r'\bemergency\w*', text)
        match_3 = o(r'\bresponse\w*', text)
        match_4 = o(r'\bplan\w*|\bstrategy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan emergency response'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage emergency procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemergency\w*', text)
        match_2 = o(r'\bresponse\w*|\bprocedure\w*|\bprocess\w*|\bplan\w*|\bstrategy\w*', text)
        match_3 = o(r'\bmanage\w*|\bmanaging\w*|\boversee\w*|\boversight\w*|ordinat\w*|\bcreate\w*|\bconduct\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage emergency procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage emergency evacuation plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bevacuation\b', text)
        match_2 = o(r'\bplan\w*|\bstrategy\w*|\bresponse\w*|\bprocedure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage emergency evacuation plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and use emergency shutdown system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besd\b|\bemergency\s+shutdown\s+system\w*', text)
        match_2 = o(r'\binstal\w*|\bfit\b|\bfitting\w*|\bassemble\w*|\bengineer\w*|\btechnician\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and use emergency shutdown system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate emergency communication system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becs\b|\bemergency\s+communication\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate emergency communication system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dispatch emergency personnel
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemergency\s+dispatch\w*|\bdispatch[i]?[n]?[g]?\s+emergenc\w*', text)
        match_2 = o(r'\bemergency\w*|\bemergencies\w*', text)
        match_3 = o(r'\bdispatch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'dispatch emergency personnel'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drive helicopter
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhelicopter\b|\bhelideck\w*', text)
        match_2 = o(r'\bmaneuver\w*|\bdrive\w*|\boperate\w*|\bfly\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'drive helicopter'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design strategies for nuclear emergencies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnuclear\s+safety\w*', text)
        match_2 = o(r'\bengineer\b', text)
        match_3 = o(r'\bnuclear\s+system\w*|\bnuclear\s+project\w*|\bnuclear\s+energy\w*', text)
        match_4 = o(r'\bsafety\b|\bemergency\w*|\bhazard\w*|\bfault\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design strategies for nuclear emergencies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide technical expertise
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+specialist\w*|\btechnical\s+specialism\w*|\btechnical\s+authority\w*|\btechnical\s+assist\w*|\btechnical\s+input\w*|\btechnical\s+proposal\w*|\bidentify\s+hardware\w*|\btechnical\s+leadership\w*|\btechnical\s+solution\w*|\bdeliver\s+technical\w*|\btechnical\s+support\w*|\btechnical\s+clerk\w*|\btechnical\s+expertise\w*|\btechnical\s+assurance\w*|\bsystem\s+support\w*|\bsystem\s+expertise\w*|\bsystem\s+administration\w*|\badministration\s+system\w*|\btechnical\s+advice\w*|\btechnical\s+advisory\w*|\btechnical\s+adviser\w*|\btechnical\s+adivsor\w*|\btechnical\s+consult\w*|\bit\s+support\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide technical expertise'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform minor maintenance of equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperation\s+maintenance\w*|\boperational\s+maintenance\w*|\bminor\s+maintenance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform minor maintenance of equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain mechanical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmechanical\s+maintenance\w*', text)
        match_2 = o(r'\bmechanical\s+tools\w*|\bmechanical\s+equipment\w*|\bmechanical\s+machine\w*|\bmachine\s+type\w*|\btype[s]?\s+machine\w*|\brange\s+machine\w*|\btypology\s+machine\w*|\bmechanical\s+tool\w*', text)
        match_3 = o(r'\bengineer\w*|\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain mechanical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate machine tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmachine\s+operat\w*|\boperat[e]?[i]?[n]?[g]?\s+machine\w*|\bus[e]?[i]?[n]?[g]?\s+machine\w*|\bmachine\s+tool\s+operat\w*|\bmachine\s+tool\s+us[e]?[i]?[n]?[g]?\w*|\bus[e]?[i]?[n]?[g]?\s+\w+\s+machine\w*|\boperate\s+\w+\s+machine\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate machine tools'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use specialised tools in electric repairs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectrician\b', text)
        match_2 = o(r'\belectricity\w*|\belectric\w*', text)
        match_3 = o(r'\brepair\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binstall\w*|\bassemble\w*|\bdiagnos\w*', text)
        match_4 = o(r'\bmaintain\s+equipment\w*|\bmaintain\s+machinery\w*|\bmaintain\s+instrument\w*|\bmaintain\s+\w+\s+equipment\w*|\bmaintain\s+\w+machinery\w*|\bmaintain\s+\w+\s+instrument\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4:
            matched_fragments.extend([match_2, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use specialised tools in electric repairs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for repair gas engines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+engine\b', text)
        match_2 = o(r'\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binstal\w*|\bassembl\w*|\bdiagnos\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'repair gas engines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain mechanical installation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'mechanical\s+instal\w*', text)
        match_2 = o(r'\bmonitor\w*|\bmaintain\b|\bmaintenance\b|\bfix\b|\brepair\b|\bidentif\w*|\bcheck\b|\bchecking\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain mechanical installation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for show enthusiasm
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdemonstrate\s+enthusiasm\w*|\bdemonstrate\s+ambition\w*|\bdemonstrate\s+motivation\w*|\bdemonstrate\s+positive\s+attitude\w*|\bdemonstrate\s+positive\s+disposition\w*|\bdemonstrating\s+enthusiasm\w*|\bexhibit\s+enthusiasm\w*|\bshow\s+ambition\w*|\bshow\s+enthusiasm\w*|\bshow\s+motivation\w*|\bshow\s+passion\w*|\bshow\s+positive\s+attitude\w*|\bshow\s+positive\s+disposition\w*|\bshow\s+positive\s+thinking\w*|\bdevelop[i]?[n]?[g]?\s+self\b', text)
        match_2 = o(r'\benthusiasm\b', text)
        match_3 = o(r'\bskill\w*|\bability\w*|\bshow\w*|\bdisplay\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'show enthusiasm'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate control systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontrol\s+system\w*|\bics\s+environment\w*|\boperat[e]?[i]?[n]?[g]?\s+ics\b|\bmaintain[i]?[n]?[g]?\s+ics\b|\biacs\s+environment\w*|\boperat[e]?[i]?[n]?[g]?\s+iacs\b|\bmaintain[i]?[n]?[g]?\s+iacs\b|\blocal\s+control\s+panel\w*|\bmotor\s+control\s+panel\w*', text)
        match_2 = o(r'\bics\b|\biacs\b|\bmcc\b|\blcp\b', text)
        match_3 = o(r'\bcontrol\w*', text)
        match_4 = o(r'\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate control systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform system migration
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsystem[s]?\s+migration\w*|\bsystem\s+design\s+migration\w*|\bmigrat[e]?[i]?[o]?[n]?\s+system\w*', text)
        match_2 = o(r'\bmigration\w*', text)
        match_3 = o(r'\bsystem\b|\bitc\b|\bict\b|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform system migration'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform application engineering tasks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapplication[s]?\s+engineer\w*|\bapp[s]?\s+engineer\w*|\bapplication[s]?\s+developer\w*|\bapp[s]?\s+developer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform application engineering tasks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop desktop app
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesktop\s+app\w*', text)
        match_2 = o(r'\bengineer\w*|\bdevelop\w*|\bcreate\w*|\bprogramm\w*|\bcode\w*|\bfull\s+stack\s+developer\w*|\bend\s+developer\w*|\bdesign\w*|\bsoftware\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop desktop app'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop mobile app
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmobile\s+app\w*|\bphone\s+app\w*', text)
        match_2 = o(r'\bengineer\w*|\bdevelop\w*|\bcreate\w*|\bprogramm\w*|\bcode\w*|\bfull\s+stack\s+developer\w*|\bend\s+developer\w*|\bdesign\w*|\bsoftware\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop mobile app'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design user interfaces
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bui\b|\buser\s+interface\w*|\bsystem\s+interface\w*', text)
        match_2 = o(r'\binterface\w*', text)
        match_3 = o(r'\bengineer\w*|\bdevelop\w*|\bcreate\w*|\bprogramm\w*|\bcode\w*|\bfull\s+stack\s+developer\w*|\bend\s+developer\w*|\bdesign\w*|\bapp\b|\bapplication\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design user interfaces'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for improve user-friendliness
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\buser\s+friend\w*|\buser\s+-\s+friend\w*', text)
        match_2 = o(r'\bdigital\s+design\w*|\bui\b|\buser\s+interface\w*|\bsoftware\s+engineer\w*|\bapplication\s+engineer\w*|\bapplication\s+design\w*|\bsoftware\s+design\w*|\bdesign\s+app\w*|\bdesign\s+software\w*|\bfull\s+stack\s+developer\w*|end\s+developer\w*|\bimprove\w*|\benhance\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'improve user-friendliness'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain responsive design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bresponsive\s+design\w*|\bresponsive\s+app\w*|\bresponsive\s+system\w*|\bresponsive\s+website\w*|\bresponsive\s+web\b', text)
        match_2 = o(r'\bdigital\s+design\w*|\bui\b|\buser\s+interface\w*|\bsoftware\s+engineer\w*|\bapplication\s+engineer\w*|\bapplication\s+design\w*|\bsoftware\s+design\w*|\bdesign\s+app\w*|\bdesign\s+software\w*|\bfull\s+stack\s+developer\w*|end\s+developer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain responsive design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for version control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bversion\s+upgrade\w*|\bversion\s+control\w*|\bcontrol\s+version\w*|\bupgrade\s+version\w*|\bsource\s+control\s+system\w*|\bgithub\b', text)
        match_2 = o(r'\bsource\s+control\w*|\bversion\s+control\w*|\bconfiguration\s+manage\w*|\brevision\s+control\w*|\bversioning\b', text)
        match_3 = o(r'\bgit\b|\bnixos\b|\bnoc\b|\blocal\s+configuration\s+system\w*|\bpikt\b|\bquattor\b|\bradmind\b|\brudder\b|\brundeck\b|\bsmartfrog\b|\bsynctool\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'version control'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop environmental remediation strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremediation\s+plan\w*|\bremedial\s+plan\w*|\bremedial\s+activity\w*|\bremedial\s+strateg\w*|\bremediation\s+strateg\w*|\bland\s+remediation\w*|\bground\s+remediation\w*|\bground\s+risk\s+remediation\w*|\bland\s+assessment\s+remediation\w*|\bground\s+assessment\s+remediation\w*', text)
        match_2 = o(r'\benvironment\w*|\bsustainabl\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop environmental remediation strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop site remediation strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremediation\s+plan\w*|\bremediation\s+work\w*|\bremedial\s+plan\w*|\bremedial\s+activity\w*|\bremedial\s+strateg\w*|\bremediation\s+strateg\w*|\bsite\s+remediation\w*', text)
        match_2 = o(r'\bsite\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop site remediation strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop flood remediation strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bflood[i]?[n]?[g]?\s+manage\w*|\bmanag[e]?[i]?[g]?\s+flood\w*|\bflood[i]?[n]?[g]?\s+defence\w*|\bflood[i]?[n]?[g]?\s+defense\w*', text)
        match_2 = o(r'\bremediation\w*|\bremedial\w*', text)
        match_3 = o(r'\bplan\w*|\bstrateg\w*|\bactivity\w*', text)
        match_4 = o(r'\bflood\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop flood remediation strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan schedule
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplan\s+schedule\w*|\bschedule\s+plan\w*|\bmanag[e]?[i]?[n]?[g]?\s+schedule\w*|\bmaintain[i]?[n]?[g]?\s+schedule\w*|\bmonitor[i]?[n]?[g]?\s+schedule\w*|\borganize\s+schedule\w*|\borganise\s+schedule\w*|\border\s+schedule\w*|\bcoordinate\s+schedule\w*|\bschedule\s+time\w*|\bschedule\s+manage\w*|\bplanning\s+scheduling\w*|\bmaintenance\s+planner\w*|\bconstruction\s+schedule\w*|\bupdat[e]?[i]?[n]?[g]?\s+schedule\w*|\bschedule\s+document\w*|\btask[s]?\s+plan\w*|\bplan\s+\w+\s+schedule\w*|\bschedule\s+\w+\s+plan\w*|\borganize\s+\w+\s+schedule\w*|\borganise\s+\w+\s+schedule\w*|\bcoordinate\s+\w+\s+schedule\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan schedule'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate and monitor construction activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bordinat[e]?[i]?[o]?[n]?\s+construction\w*|\bcoordinat[e]?[i]?[o]?[n]?\s+construction\w*|\bsupervis[e]?[i]?[n]?[g]?\s+construction\w*|\bco\s+-\s+ordinat[e]?[i]?[o]?[n]?\s+construction\w*|\bmanage[i]?[n]?[g]?\s+construction\w*|\bconstruction\s+manage\w*|\breview[i]?[n]?[g]?\s+construction\w*|\bconstruction\s+progress\w*|\bcoordinate\s+engineer\w*|\bcoordination\s+engineer\w*|\bordination\s+engineer\w*|\bconstruction\s+plan\w*|\bplan\s+construction\w*|\bconstruction\s+schedule\w*|\bschedule\s+construction\w*|\bmonitor[i]?[n]?[g]?\s+construction\w*', text)
        match_2 = o(r'\bconstruction\s+activity\w*', text)
        match_3 = o(r'\breview\w*', text)
        match_4 = o(r'\bconstruction\w*', text)
        match_5 = o(r'\bproject\s+execution\w*|\bmanage\w*|\bmonitor\w*|\bordinate\w*|\bcoordinate\w*|\bco\s+-\s+ordinate\w*|\bsupervis\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate and monitor construction activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage schedule of tasks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanage\s+schedule\s+task\w*|(?<!carry out )(?<!carry )\bassign\s+task\w*|\bschedule\s+task\w*|\borganise\s+task\w*|\borganize\s+task\w*|\bmanage\s+task\w*|\btask[s]?\s+schedule\w*|\btask[s]?\s+plan\w*|\btask[s]?\s+manage\w*|\btask[s]?\s+organise\w*|\btask[s]?\s+organis\w*|\bdelegat[e]?[i]?[o]?[n]?\s+task\w*|\btask[s]?\s+delegat\w*|\bdelegat[e]?[i]?[o]?[n]?\s+activit\w*|\bactivit[y]?[i]?[e]?[s]?\s+delegat\w*|\bsupervise\s+task\w*|\bcompile\s+task\w*|\barrange\s+task\w*|\bschedule\s+plan\w*|\bassign\s+duty\w*|\bassign\s+\w+\s+duty\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage schedule of tasks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create financial reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+report\w*|\bfinance\s+report\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create financial reports'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use real time reporting system (hmrc)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brti\b|\breal\s+time\s+report\w*', text)
        match_2 = o(r'\bhmrc\w*|\bpayroll\w*|\bpaye\b|\badminist\w*|\breport\w*|\bsystem\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use real time reporting system (hmrc)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for write technical report or note
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+report\w*|\btechnical\s+\w+\s+report\w*|\btechnical\s+note\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'write technical report or note'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform management reporting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanagement\s+report\w*|\breport[i]?[n]?[g]?\s+management\w*|\breport[i]?[n]?[g]?\s+\w+\s+management\w*|\bmanagement\s+\w+\s+report\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform management reporting'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lift heavy weights
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!gas )\blift(?! valve)\w*|\buphold\w*|\blifting\w*', text)
        match_2 = o(r'\bheavy\w*', text)
        match_3 = o(r'\bequipment\w*|\bweight\w*|\bbox\w*|\bobject\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'lift heavy weights'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply various lifting techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blifting\s+method\w*|\blifting\s+technique\w*|\blifting\s+activity\w*|\blift\s+activity\w*|\brequire[d]?\s+lift\w*', text)
        match_2 = o(r'(?<!gas )(?<!artificial )\blift(?! valve)(?! flow)\w*|\buphold(?! standard)(?! company)(?! companys)\w*', text)
        match_3 = o(r'\bheavy\w*|\bequipment\w*|\bweight\w*|\bbox[e]?[s]?\b|\bobject[s]?\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply various lifting techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect insulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+recovery\w*|\bwaste\s+heat\w*|\bheat\s+waste\w*|\binsulation\w*|\binsulator\w*', text)
        match_2 = o(r'\binspect\w*|\basses\w*|\bcheck\w*|\binspect\w*|\bmonitor\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect insulation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply insulation strips
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binsulation\s+strip\w*|\binsulator\s+strip\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply insulation strips'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install insulation blocks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binsulator\s+block\w*|\binsulation\s+block\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install insulation blocks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install insulation material
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binsulation\s+engineer\w*|\binsulation\s+material\w*|\binsulator\s+material\w*|\bfit\s+insulation\w*|\bfitting\s+insulation\w*|\bfix\s+insulation\w*|\bfixing\s+insulation\w*|\binstal[s]?\s+insulation\w*|\binsulate\s+strucutre\w*|\binsulating\s+strucutre\w*|\binsulate\s+building\w*|\binsulating\s+building\w*|\binsulate\s+room\w*|\binsulating\s+room\w*|\binsulation\s+instal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install insulation material'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use rigging equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blifting\s+accessor\w*|\blift\s+accessor\w*|\blifting\s+equipment\w*|\blift\s+equipment\w*|\blifting\s+tool\w*|\blift\s+tool\w*|\brigging\s+cable\w*|\brigging\s+chain\w*|\brigging\s+net\w*|\brigging\s+wire\w*|\brigging\s+tool\w*|\brigging\s+equipment\w*|\brigging\s+gear\w*|\bexperience\s+rigging\w*|\brigging\s+experience\w*|\brigger\b', text)
        match_2 = o(r'\bcable\w*|\brope\w*', text)
        match_3 = o(r'\bpulley\w*|\bwinch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use rigging equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate cutting equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'bcutting\s+machine\w*|\bcutting\s+equipment\w*|\bcutting\s+tool\w*|\bcutter\b|\bcutters\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate cutting equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate and maintain drilling equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrill[i]?[n]?[g]?\s+engineer\w*|\bdrill\s+opearation\w*|\boperat[e]?[i]?[n]?[g]?\s+drill\w*|\bus[e]?[i]?[n]?[g]?\s+drill\w*|\bhandl[e]?[i]?[n]?[g]?\s+drill\w*', text)
        match_2 = o(r'\bdrill\b', text)
        match_3 = o(r'\bequipment\w*|\btool\w*|\bmachine\w*|\bpneumatic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate and maintain drilling equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design and calulate rigging plots
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brigging\s+plot\w*|\brig\s+plot\w*|\brigging\s+training\w*|\bplot[s]?\s+rigging\b|\bplot[s]?\s+rig\b|\bpoint\s+load\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design and calulate rigging plots'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide rigging plans
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brigging\s+plan\w*|\brig\s+plan\w*|\brigging\s+strategy\w*|\blift\s+plan\w*|\blifting\s+plan\w*|\bplan\s+rigging\w*|\bplan\s+rig\b|\bplan\s+lift\b|\bplan\s+lifting\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide rigging plans'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle crane equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrane\b|\bcranes\b', text)
        match_2 = o(r'\brepair\w*|\binstal\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binspect\w*|\bdiagnos\w*|\bmachine\w*|\bequipment\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle crane equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform crane operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrane\s+load\w*|\btower\s+crane\w*|\bmobile\s+crane\w*|\bload[i]?[n]?[g]?\s+crane\w*', text)
        match_2 = o(r'\blift\w*|\brigging\w*|\brig\b|\bopearte\w*|\boperation\w*|\bperform\w*', text)
        match_3 = o(r'\bcrane\b|\bcranes\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform crane operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate tower crane
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btower\s+crane\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate tower crane'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate mobile crane
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmobile\s+crane\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate mobile crane'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate deck crane
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeck\s+crane\w*', text)
        match_2 = o(r'\bdeck\b', text)
        match_3 = o(r'\brigging\w*|\blifting\b|\bcrane\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate deck crane'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform slinger signalling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bslinger\b', text)
        match_2 = o(r'\bsignaller\w*|\bsignal\b', text)
        match_3 = o(r'\bcrane\b|\bcranes\b|\bslinger\w*|\blifting\w*|\brigging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform slinger signalling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design automation components
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautomate\w*|\bautomation\w*', text)
        match_2 = o(r'\belement\w*|\bcomponent\w*|\bpart\w*|\bsystem\w*|\bdevice\w*|\bequipment\w*|\btool\w*|\bmethod\w*', text)
        match_3 = o(r'\bdesign\w*|\bcreate\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design automation components'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install automation components
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautomate\w*|\bautomation\w*', text)
        match_2 = o(r'\belement\w*|\bcomponent\w*|\bpart\w*|\bsystem\w*|\bdevice\w*|\bequipment\w*|\btool\w*|\bmethod\w*', text)
        match_3 = o(r'\binstal\w*|\bfit\b|\bfitting\w*|\bassembl\w*|\bimplement\w*|\bincorporat\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install automation components'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for legal research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blegal\s+research\w*|\banalyse\s+law\b|\banalye\s+laws\b|\banalyse\s+regulation\w*|\banalyse\s+legislation\w*|\banalyze\s+law\b|\banalyze\s+laws\b|\banalyze\s+regulation\w*|\banalyze\s+legislation\w*|\blegal\s+analy\w*|\blaw\s+analy\w*|\bjudical\s+analy\w*|\bjudical\s+inquir\w*|\bjudical\s+investigat\w*|\bjudical\s+prob\w*|\blegal\s+inquir\w*|\blegal\s+investigat\w*|\blegal\s+prob\w*|\binvestigate\s+legal\w*|\binvestigate\s+law\w*|\binvestigate\s+regulation\w*|\binvestigate\s+legislation\w*|\banalyse\s+policy\w*|\banalyze\s+policy\w*|\bpolicy\s+analy\w*|\bassess\s+policy\w*|\bpolicy\s+reserach\w*|\bresearch\s+policy\w*|\binvestigate\s+policy\w*|\bpolicy\s+investigat\w*|\banalyse\s+policies\w*|\banalyze\s+policies\w*|\bassess\s+policies\w*|\bpolicies\s+reserach\w*|\bresearch\s+policies\w*|\binvestigate\s+policies\w*|\bpolicies\s+investigat\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'legal research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assist with litigation matters
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blitigation\w*|\blegal\s+dispute\w*', text)
        match_2 = o(r'\bdispute\w*', text)
        match_3 = o(r'\blegal\w*|\blaw\w*|\bassisst\w*|\bguidance\w*|\bmatter\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assist with litigation matters'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend compressor engine
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcompress\s+air\w*|\bcompressor\s+system\w*|\bcomporess\s+gas\w*|\bgas\s+comporess\w*|\bair\s+compress\w*|\blp\s+compress\w*|\blow\s+pressure\s+air\s+compress\w*|\bmanage\s+compressor\w*|\bcompressor\s+engine\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend compressor engine'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install oil rig
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boil\s+rig\w*|\boil\s+drill\w*|\boil\s+well\w*', text)
        match_2 = o(r'\binstal\w*|\bassembl\w*|\bbuild\w*|\bfit\b|\bfitting\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install oil rig'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate and plan rig movement
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boil\s+operat\w*|\bgas\s+operat\w*|\boil\s+gas\s+operat\w*|\bgas\s+oil\s+operat\w*|\brig\s+management\w*|\bengage\s+drill\w*|\boperate\s+drill\w*|\boperate\s+rig\w*|\brig\s+operat\w*|\bdrill\s+operat\w*|\brig\s+positioning\w*|\bdrill\s+rig\s+positioning\w*|\brig\s+drill\s+positioning\w*', text)
        match_2 = o(r'\bdrill\w*|\brig\b|\bplatform\w*|\bvessel\w*', text)
        match_3 = o(r'\boperat[e]?[i]?[n]?[g]?\s+\w+\s+rig\b|\boperat[e]?[i]?[n]?[g]?\s+\w+\s+platform\w*', text)
        match_4 = o(r'\boil\w*|\boilgas\w*|\bgasoil\w*|\bgas\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate and plan rig movement'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design engineering components
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b(?<!closely )(?<!close )\bengineer[i]?[n]?[g]?\s+design\b|\b(?<!closely )(?<!close )\bdesign[i]?[n]?[g]?\s+engineer\w*|\bdengineer[i]?[n]?[g]?\s+product\w*|\bengineer[i]?[n]?[g]?\s+component\w*|\binstrumentation\s+design\w*|\bdesign[i]?[n]?[g]?\s+instrument\w*', text)
        match_2 = o(r'\bdesign\w*|\bcreate\w*|\bdesigning\w*|\bcreating\w*|\bdevelop\w*', text)
        match_3 = o(r'\bcomponent\w*|\binstrument\w*|\bmachinery\w*|\bpart\b', text)
        match_4 = o(r'\bengineer\w*|\belectric\w*|\bmanufactur\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design engineering components'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for approve engineering design or plans
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapprove\s+engineer[i]?[n]?[g]?\s+design\w*|\bapprove\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bapprove\s+engineer[i]?[n]?[g]?\s+solution\w*|\bapprove\s+engineer[i]?[n]?[g]?\s+plan\w*|\barrange\s+engineer[i]?[n]?[g]?\s+plan\w*|\barrange\s+engineer[i]?[n]?[g]?\s+solution\w*|\barrange\s+engineer[i]?[n]?[g]?\s+design\w*|\barrange\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bmodel[l]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+design\w*|\bmodel[l]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+plan\w*|\bmodel[l]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+solution\w*|\bmodel[l]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bdevelop[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+design\w*|\bdevelop[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+plan\w*|\bdevelop[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+solution\w*|\bdevelop[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bproduc[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+design\w*|\bproduc[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+plan\w*|\bproduc[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+solution\w*|\bproduc[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bmanag[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+design\w*|\bmanag[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+plan\w*|\bmanag[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+solution\w*|\bmanag[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+strateg\w*|\bengineer[i]?[n]?[g]?\s+design\s+experience\w*|\bexperience\s+engineer[i]?[n]?[g]?\s+design\w*|\bengineer[i]?[n]?[g]?\s+design\s+develop\w*|\bengineer[i]?[n]?[g]?\s+design\s+approval\w*|\bengineer[i]?[n]?[g]?\s+design\s+assess\w*|\bengineer[i]?[n]?[g]?\s+design\s+develop\w*|\bengineer[i]?[n]?[g]?\s+plan\s+approv\w*|\bengineer[i]?[n]?[g]?\s+plan\s+develop\w*|\bengineer[i]?[n]?[g]?\s+plan\s+assess\w*|\bengineer[i]?[n]?[g]?\s+approv\w*|\bengineer[i]?[n]?[g]?\s+strategy\s+assess\w*|\bengineer[i]?[n]?[g]?\s+strategy\s+develop\w*', text)
        match_2 = o(r'\bengineer[i]?[n]?[g]?\s+design\w*|\bdesign\s+engineer\w*', text)
        match_3 = o(r'\bapprov\w*|\bmodel\w*|\bproduce\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'approve engineering design or plans'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for evaluate project plans and carry out project activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproposal[s]?\s+engineer\w*|\bevaluate\s+project\s+plan\w*|\bassess\s+project\s+plan\w*|\bmanage\s+project\s+plan\w*|\bproject\s+plan\s+manage\w*|\bjudge\s+project\s+plan\w*|\bevaluate\s+proposal\s+plan\w*|\bassess\s+proposal\s+plan\w*|\bmanage\s+proposal\s+plan\w*|\bproposal\s+plan\s+manage\w*|\bjudge\s+proposal\s+plan\w*|\bproject\s+schedule\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'evaluate project plans and carry out project activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create floor plan template
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfloor\s+plan\s+creator\w*|\bfloor\s+plan\s+designer\w*|\bfloor\s+plan\s+template\w*', text)
        match_2 = o(r'\bfloor\s+plan\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bcreate\w*|\bdesigning\w*|\bcreation\w*|\barchitect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create floor plan template'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use adobe creative suite software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcreative\s+cloud\s+software\w*|\bcreative\s+suite\s+software\w*|\badobe\s+suite\w*', text)
        match_2 = o(r'\bcreative\s+cloud\w*|\bcreative\s+suite\w*', text)
        match_3 = o(r'\badobe\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use adobe creative suite software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop a specific interior design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binterior\s+design\w*|\bdesign[i]?[n]?[g]?\s+interior\w*', text)
        match_2 = o(r'\barchitecture\w*|\barchitect\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop a specific interior design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for integrate building requirements in the architectural design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegrate\s+design\s+build\w*|\bintegrate\s+build[i]?[n]?[g]?\s+requirement\w*', text)
        match_2 = o(r'\bbuild[i]?[n]?[g]?\s+requirement\w*|\bconstruction\s+requirement\w*', text)
        match_3 = o(r'\bdesign\w*|\bdesigning\w*|\bintegrat\w*|\badher\w*|\barchitect\w*', text)
        match_4 = o(r'\brequirement\w*', text)
        match_5 = o(r'\bdesign\w*|\bdesigning\w*|\barchitect\w*|\bplan\w*', text)
        match_6 = o(r'\bconstruct\w*|\bbuild\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'integrate building requirements in the architectural design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle chemicals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchemical\w*', text)
        match_2 = o(r'\bhandle\w*|\bhandling\w*|\buse\b|\busing\w*|\bexperiment\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle chemicals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for transport dangerous goods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdangerous\s+good\w*|\bdanger\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+good\w*|\bhazard\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+material\w*|\bhazard\s+material\w*', text)
        match_2 = o(r'\bdeliver\w*|\bshipment\w*|\btransit\w*|\btransport\w*|\blogistic\w*|\bstorage\w*|\bfork\s?[-]?\s?lift\w*|\bwarehouse\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'transport dangerous goods'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle dangerous goods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdangerous\s+good\w*|\bdanger\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+good\w*|\bhazzard\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+material\w*|\bhazard\s+material\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle dangerous goods'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle shipment documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bshipment\w*|\btransit\b|\blogistic\w*|\btransport\b|\brelocation\w*', text)
        match_2 = o(r'\bdocument\w*|\bpaperwork\w*|\breceipt\w*|\bprocedure\w*', text)
        match_3 = o(r'(?!customer )(?<!project )delivery(?! safe)(?! project)(?! strategy)\w*', text)
        match_4 = o(r'\bproduct[s]?\b', text)
        match_5 = o(r'\bdocument\w*|\bpaperwork\w*|\breceipt\w*|\bprocedure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle shipment documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage dangerous goods documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdangerous\s+good\w*|\bdanger\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)hazardous\s+good\w*|\bhazard\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)hazardous\s+material\w*|\bhazard\s+material\w*', text)
        match_2 = o(r'\bdocument\w*|\bpaperwork\w*|\breciept\w*|\breceipt\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage dangerous goods documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for revise certifications for dangerous good transportation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdangerous\s+good\w*|\bdanger\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+good\w*|\bhazzard\s+good\w*', text)
        match_2 = o(r'\bcertificate\w*|\bcertification\w*|\bregulation\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'revise certifications for dangerous good transportation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for research and improve manufacturing processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocess\s+scientist\w*|\bprocess\s+science\w*|\bprocess\s+development\s+scientist\w*|\bimprove\s+manufactur\w*|\benhance\s+manufactur\w*|\boptimize\s+manufactur\w*', text)
        match_2 = o(r'\bimprove\s+process\w*|\benchance\s+process\w*|\boptimise\s+process\w*|\boptimize\s+process\w*', text)
        match_3 = o(r'\bmanufactur\w*|\bfabrication\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'research and improve manufacturing processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate forwarder
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforwarder\b', text)
        match_2 = o(r'\bwood\w*|\blogging\w*|\bfelling\w*|\bforest\w*|\bwood\w*|\boperate\w*|\bdrive\w*|\buse\w*|\bload\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate forwarder'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on efficiency improvement
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimprov[e]?[i]?[n]?[g]?\s+efficiency\w*|\befficincy\s+improve\w*|\befficiency\s+save\w*|\bbusiness\s+efficiency\w*|\befficiency\s+suggestion\w*|\boptimis[e]?[a]?[t]?[i]?[o]?[n]?\s+process\w*|\bsimplif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+process\w*|\bsuggest\s+efficiency\w*|\befficiency\s+improvement\w*|\benchanc[e]?[i]?[n]?[g]?\s+efficiency\w*|\benchanc[e]?[i]?[n]?[g]?\s+capacity\w*|\bsuggestion\s+efficiency\w*|\bconsult[i]?[n]?[g]?\s+efficiency\w*|\badvise\s+efficiency\w*|\badvice\s+efficiency\w*|\befficiency\s+constult\w*|\befficiency\s+advis\w*|\befficiency\s+advice\w*|\bstreamline\s+operation\w*|\benhanc[e]?[i]?[n]?[g]?\s+efficiency\w*|\bwork\s+flow\s+efficien\w*|\befficient\s+work\s+flow\w*|\bsimplif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+process\w*|\bimprov[e]?[i]?[n]?[g]?\s+\w+\s+efficiency\w*|\bprocess\s+\w+\s+efficien\w*|\bimprov[e]?[i]?[n]?[g]?\s+\w+\s+operating\s+efficiency\w*|\bstreamline\s+\w+\s+operation\w*|\benchanc[e]?[i]?[n]?[g]?\s+\w+\s+efficiency\w*|\benchanc[e]?[i]?[n]?[g]?\s+\w+\s+capacity\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on efficiency improvement'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for write proposals or support proposal activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+bid\w*|\bproposal[s]?\s+engineer\w*|\btechnical\s+proposal\w*|\bbid\s+project\w*|\bprepare\s+bid\w*|\bmake\s+bid\w*|\bbid\s+process\w*|\bbid\s+defense\w*|\bprepare\s+proposal\w*|\bmake\s+proposal\w*|\bwrite\s+proposal\w*|\bmanage\s+proposal\w*|\bevaluate\s+proposal\w*|\bassess\s+proposal\w*|\bsupervise\s+proposal\w*|\bjudge\s+proposal\w*|\bproposal\s+develop\w*|\bproposal\s+bid\w*|\bbid\s+proposal\w*|\bdesign\s+proposal\w*|\bbid\s+support\w*|\bproposal\s+support\w*|\bsupport\s+bid\w*|\bbbid\s+develop\w*|\bbid\s+representation\w*|\bsupport\s+proposal\w*|\bprepar[e]?[i]?[n]?[g]?\s+\w+\s+proposal\w*|\bmak[e]?[i]?[n]?[g]?\s+\w+\s+proposal\w*|\bwrit[e]?[i]?[n]?[g]?\s+\w+\s+proposal\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+proposal\w*|\bassess[i]?[n]?[g]?\s+\w+\s+proposal\w*|\bsupport[i]?[n]?[g]?\s+proposal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'write proposals or support proposal activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare building permit applications
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbuild\w*|\bhouse\w*|\bconstruction\w*|\brenovation\w*|\barchitect\w*', text)
        match_2 = o(r'\bpermit\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare building permit applications'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for issue permits
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bissue\s+permit\w*|\bafford\s+permit\w*|\bgrant\s+permit\w*|\bissuing\s+permit\w*|\baffording\s+permit\w*|\bgranting\s+permit\w*|\bprovide\s+permit\w*|\bproviding\s+permit\w*|\bpermit\s+provision\w*|\bcorrect\s+permit\w*|\bmanage\s+permit\w*|\bgood\s+permit\w*|\bissue\s+\w+\s+permit\w*|\bafford\s+\w+\s+permit\w*|\bgrant\s+\w+\s+permit\w*|\bissuing\s+\w+\s+permit\w*|\baffording\s+\w+\s+permit\w*|\bgranting\s+\w+\s+permit\w*|\bprovide\s+\w+\s+permit\w*|\bproviding\s+\w+\s+permit\w*|\bpermit\s+\w+\s+provision\w*|\bcorrect\s+\w+\s+permit\w*|\bmanage\s+\w+\s+permit\w*|\bgood\s+\w+\s+permit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'issue permits'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for interpret scientific data to assess water quality
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+quality\s+data\b|\bwater\s+qualiy\s+datum\w*|\bwater\s+data\b|\bwater\s+datum\w*|\banalyse\s+water\w*|\banalyze\s+water\w*|\bwater\s+analys\w*|\binterpret\s+water\w*', text)
        match_2 = o(r'\bwater\s+quality\w*|\bwater\s+test\w*|\btest[i]?[n]?[g]\s+water\w*|\bassess[i]?[n]?[g]\s+water\w*|\bwater\s+assess\w*|\bwater\s+sample\w*|\bwater\s+quality\w*|\bquality\s+water\w*', text)
        match_3 = o(r'\bscientific\s+data\b|\bscientific\s+datum\w*|\blab\b|\blaboratory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'interpret scientific data to assess water quality'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor and maintain water quality
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+quality\w*|\bwater\s+hygiene\w*|\bwater\s+contaminat\w*|\blegionella\w*|\bwater\s+treatment\s+hygiene\w*|\bwater\s+hygiene\s+code\w*', text)
        match_2 = o(r'\bhealth\s+safety\w*|\btest\w*|\bassess\w*|\bmonitor\w*|\bmeasure\w*|\bmaintain\w*|\bmaintenance\w*|\btest\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor and maintain water quality'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install utility equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\butility\s+engineer\w*', text)
        match_2 = o(r'\butility\s+equipment\w*|\butilities\s+equipment\w*|\butility\s+tool\w*|\butilities\s+tool\w*', text)
        match_3 = o(r'\binstall\w*|\bassembl\w*|\bbuild\w*|\bfit\b|\bfitting\w*|\bmaintain\w*|\bmaintenance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install utility equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dispose of sewage sludge
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btanker\w*|\bdispose\w*|\bdisposal\w*|\bremove\w*|\bremoval\w*', text)
        match_2 = o(r'\bsewage\w*|\bsludge\w*|\bseptic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'dispose of sewage sludge'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain septic tanks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseptic\s+tank\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain septic tanks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install septic systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseptic\s+tank\w*|\bseptic\s+system\w*', text)
        match_2 = o(r'\binstal\w*|\bfit\b|\bfitting\b|\bcommission\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install septic systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sort waste and assess waste type
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsegregate\w*|\bsort\w*', text)
        match_2 = o(r'\bwaste\w*|\blandfill\w*|\bmaterial\w*|\brecycling\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'sort waste and assess waste type'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate centrifuge
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcentrifuge\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate centrifuge'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate control panels
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperate\s+control\s+panel\w*|\buse\s+control\s+panel\w*|\brun\s+control\s+panel\w*|\bcontrol\s+panel\s+experience\w*|\bexperience\s+control\s+panel\w*|\bcontrol\s+panel\s+switch\w*|\bswitch\s+control\s+panel\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate control panels'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for wire control panel
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontrol\s+panel\w*', text)
        match_2 = o(r'\bwire\w*|\bconnect\w*|\bcircuit\w*|\binstal\w*|\bassembl\w*|\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\bdiagnos\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'wire control panel'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conserve water resource
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+resource\w*', text)
        match_2 = o(r'\bconserv\w*|\bmanage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conserve water resource'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use integrated management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegrated\s+management\s+system\w*|\bims\b(?! health)\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use integrated management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use informaiton security management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\s+management\s+system\w*|\bisms\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use informaiton security management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use quality management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquality\s+management\s+system\w*|\bqms\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use quality management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use environment management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment[a]?[l]?\s+management\s+system\w*|\bems\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use environment management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use manufacturing operations management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmom\b|\bmanufactur[e]?[i]?[n]?[g]?\s+operation[s]?\s+manage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use manufacturing operations management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use database management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badabas|\badabas\s+d|\badaptive\s+server\s+anywhere|\badaptive\s+server\s+enterprise|\badvantage\s+database\s+server|\bcloudscape|\bdatabase\s+management\s+system|\bdatabase\s+system|\bdatacom|\bdb.linux|\bdb\s+.\s+linux|\bdb\s+linux|\bdb.star|\bdb\s+.\s+star|\bdb\s+star|\bdb2\s+everyplace|\bdb\s+engine|\bdbms\b|\bidms\b|\bims\s+db|\binformix\s+dynamic\s+server|\binformix\s+extended\s+parallel\s+server|\bingres\s+ii|\binterbase|\bjasmine\s+ii|\bnonstop\s+sql|\bobjectivity\s+db|\boracle\s+8i|\boracle\s+lite|\bpervasive.sql|\bpervasive\s+.\s+sql|\bpervasive\s+sql|\bpoet\s+object\s+server\s+suite|\bpointbase\s+embedded|\bpointbase\s+mobile|\bpointbase\s+network\s+server|\bquadbase\s+sql\s+server|\br:base|\bbr\s+:\s+base|\bbr\s+base|\braima\s+database\s+manager|\brdb|\bsql|\bsqlbase|\bsupra|\btimesten|\bunidata|\bunisql|\bvelocis|\bversant|\byard-sql|\breltio\b|\benterworks\b|\bcolibra\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use database management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use booking management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbooking\s+management\s+system\w*|\bbokking\s+management\s+software\w*|\bbooking\s+system\w*|\bbooking\s+software\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use booking management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for process bookings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bensur[e]?[i]?[n]?[g]?\s+booking\w*|\baccept[i]?[n]?[g]?\s+booking\w*|\bhandl[e]?[i]?[n]?[g]?\s+booking\w*|\bmanag[e]?[i]?[n]?[g]?\s+booking\w*|\borganis[e]?[i]?[n]?[g]?\s+booking\w*|\borganiz[e]?[i]?[n]?[g]?\s+booking\w*|\bprocess[i]?[n]?[g]?\s+booking\w*|\breservation\s+service\w*|\btak[e]?[i]?[n]?[g]?\s+booking\w*|\bprocess[i]?[n]?[g]?\s+\w+\s+booking\w*|\btak[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*|ensur[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*|\baccept[i]?[n]?[g]?\s+\w+\s+booking\w*|\bhandl[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*|\borganis[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*|\borganiz[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+booking\w*', text)
        match_2 = o(r'\bhelpdesk\w*', text)
        match_3 = o(r'\breception\b', text)
        match_4 = o(r'\brestaurant\w*|\bbar\b|\bhotel\b', text)
        match_5 = o(r'\bbook\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_5:
            matched_fragments.extend([match_2, match_5])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'process bookings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use building management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbuilding\s+management\s+system\w*', text)
        match_2 = o(r'\bbms\b|\bibms\b', text)
        match_3 = o(r'\bconstruction\w*|\bbuild\w*|\bsystem\w*|\buse\w*|\boperate\w*|\bexperience\w*|\bknowledge\w*|\bsoftware\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use building management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use computerised maintenance management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputerised\s+maintenance\s+management\w*|\bcomputerized\s+maintenance\s+management\w*|\bcmms\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use computerised maintenance management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use business management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+management\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use business management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use asset management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basset\s+management\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use asset management system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use asset integrity management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basset[s]?\s+integrity\s+management\s+system\w*', text)
        match_2 = o(r'(?<!main )(?<!project )\baim[s]?\b(?! work)\w*', text)
        match_3 = o(r'\basset\w*|\bintegrity\w*|\bsystem\w*|\bexperience\w*|\bknowledge\w*|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use asset integrity management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use material management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaterial\s+management\s+system\w*', text)
        match_2 = o(r'\bmms\b', text)
        match_3 = o(r'\bmaterial\w*|\binventory\w*|\bsystem\w*|\buse\w*|\boperate\w*|\bexperience\w*|\bknowledge\w*|\bsoftware\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use material management systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use human resources information system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhris\b|\bhuman\s+resource[s]?\s+information\s+system\w*|\bhr\b\s+information\s+system\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use human resources information system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess and manage environmental impact
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment[a]?[l]?\s+impact\w*|\bimpact\s+environment\w*|\benvironment[a]?[l]?\s+risk\w*|\brisk\s+environment\w*|\benvironment[a]?[l]?\s+consult\w*|\bconsult[i]?[n]?[g]?\s+environment\w*|\benvironment[a]?[l]?\s+assess\w*|\benvironment[a]?[l]?\s+survey\w*|\becology\s+survey\w*|\benvironment[a]?[l]?\s+effect\b|\benvironment[a]?[l]?\s+performance\w*|\benvironment[a]?[l]?\s+mitigat\w*|\benvironmnet[a]?[l]?\s+appraisal\w*|\benvironment[a]?[l]?\s+monitor\w*|\benvironmental\s+mitigat\w*|\beia\s+assess\w*|\bwfd\s+assess\w*', text)
        match_2 = o(r'\bwfd\b|\bhra\b|\bhras\b|\bmcz\b|\benvid\b|\bbat\b|\bbpeo\w*|\beia\b|\besia\b|\beias\b|\becias\b|\berms\b|\bcedm\b|\berm\b(?! group)\w*|\bimpact\s+model\w*', text)
        match_3 = o(r'\benvironment\w*|\becolog\w*|\brenewable\w*|\bsustainab\w*|\bassess\w*|\bmanage\w*|\bmanaging\w*|\banaly\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess and manage environmental impact'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain power plant equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpower\s+plant\w*', text)
        match_2 = o(r'\bequipment\w*|\btool\w*|\binstrument\w*|\bmachine\w*|\bdevice\w*', text)
        match_3 = o(r'\btest\w*|\bassess\w*|\bmonitor\w*|\bmeasure\w*|\bmaintain\w*|\bmaintenance\w*|\brepair\w*|\bfix\b|\bfixing\w*|\bdiagnos\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain power plant equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design electric power systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectric\s+power\s+system\w*|\bpower\s+plant\s+system\w*', text)
        match_2 = o(r'\bcreate\w*|\bdesign\b|\bdesigner\b|\bdesigning\b|\bplan\w*|\bconstruct\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design electric power systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carry out cooling tower cleaning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcooling\s+tower\w*|\bcool\s+tower\w*', text)
        match_2 = o(r'\bchlorination\w*|\bclean\w*|\bcleaning\w*|\bmaintain\w*|\bmaintenance\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'carry out cooling tower cleaning'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean ventilation systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bventilation\s+system\w*|\blev\b|\bhvac\b|\bodour\s+control[l]?[i]?[n]?[g]?\s+system\w*|\bmechanical\s+ventilation\w*|\bsystem[s]?\s+ventilation\w*', text)
        match_2 = o(r'\bclean\w*|\bcleaning\w*|\bcondition\s+monitor\w*|\breliability\s+analy\w*', text)
        match_3 = o(r'\bcable\s+design\w*|\bcable\s+system\w*|\bcable\s+transmission\w*|\bhvdc\b', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean ventilation systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and maintain ventilation equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bventilation\s+system\w*|\blev\b|\bhvac\b|\bodour\s+control[l]?[i]?[n]?[g]?\s+system\w*|\bmechanical\s+ventilation\w*|\bsystem[s]?\s+ventilation\w*', text)
        match_2 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*|\bmaintain\w*|\bfit\b|\bfitting\b|\bdiagnos\w*|\bmonitor\w*|\breliability\w*|\bassess\w*|\banaly\w*', text)
        match_3 = o(r'\bcable\s+design\w*|\bcable\s+system\w*|\bcable\s+transmission\w*|\bhvdc\b|\bsale\w*|\bselling\w*', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and maintain ventilation equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and maintain hydraulic systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydraulic\s+system\w*', text)
        match_2 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*|\bmaintain\w*|\bdiagnos\w*|\bmonitor\w*|\breliability\w*|\banaly\w*|\bassess\w*', text)
        match_3 = o(r'\bsale\w*|\bselling\w*', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and maintain hydraulic systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design ventilation systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bventilation\s+system[s]?\s+plan\w*|\bhvac\s+plan\w*|\blev\s+plan\w*|\bventilation\s+network\s+plan\w*|\bplan[n]?[i]?[n]?[g]?\s+ventilation\s+system\w*|\bplan[n]?[i]?[n]?[g]?\s+ventilation\s+network\w*|\bplan[n]?[i]?[n]?[g]?\s+hvac\b|\bplan[n]?[i]?[n]?[g]?\s+lev\b', text)
        match_2 = o(r'\bventilation\s+system\w*|\blev\b|\bhvac\b|\bodour\s+control\s+system\w*|\bventilation\s+flow\s+diagram\w*|\bfan\s+siz[e]?[i]?[n]?[g]?\b|\bventilation\s+network\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigning\b|\bdesigner\b|\bcreate\w*|\bcreating\w*', text)
        match_4 = o(r'\bcable\s+design\w*|\bcable\s+system\w*|\bcable\s+transmission\w*|\bhvdc\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design ventilation systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct linear modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blinear\s+model\w*|\blp\s+model\w*|\blinear\s+programm\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct linear modelling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify new opportunities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bidentif[y]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bidentification\s+new\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bopportunity\s+identificat\w*|\banalyse\s+opportunit\w*|\bidentify\s+opportunit\w*|\bdetermine\s+opportunit\w*|identif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+opportunit\w*|\bidentifying\s+\w+\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+\w+\s+opportunit\w*|\bidentif[y]?[i]?[n]?[g]?\s+new\s+upgrade\w*|\bidentification\s+new\s+upgrade\w*|\bdetermin[e]?[i]?[n]?[g]?\s+new\s+upgrade\w*|\bupgrade\s+identificat\w*|\banalyse\s+upgrade\w*|\bidentify\s+upgrade\w*|\bdetermine\s+upgrade\w*|identif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+upgrade\w*|\bidentifying\s+\w+\s+upgrade\w*|\bdetermin[e]?[i]?[n]?[g]?\s+\w+\s+upgrade\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify new opportunities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify new recycling opportunities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bidentif[y]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bidentification\s+new\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bopportunity\s+identificat\w*|\banalyse\s+opportunit\w*|\bidentify\s+opportunit\w*|\bdetermine\s+opportunit\w*|identif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+opportunit\w*|\bidentifying\s+\w+\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+\w+\s+opportunit\w*', text)
        match_2 = o(r'\brecycling\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify new recycling opportunities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify new business opportunities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bidentify[i]?[n]?[g]?\s+new\s+business\s+opurtunit\w*|\bbusiness\s+oppurtunity\s+identification\w*|\bdevelop\s+business\s+oppurtunit\w*|\bdevelop[m]?[e]?[n]?[t]?\s+new\s+business\w*|\bbusiness\s+transformation\w*|\bsecur[e]?[i]?[n]?[g]?\s+business\s+opportunit\w*|\bsecur[e]?[i]?[n]?[g]?\s+new\s+business\w*|\bidentiy[i]?[n]?[g]?\s+new\s+business\w*', text)
        match_2 = o(r'\bidentif[y]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bidentification\s+new\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+new\s+opportunit\w*|\bopportunity\s+identificat\w*|\banalyse\s+opportunit\w*|\bidentify\s+opportunit\w*|\bdetermine\s+opportunit\w*|identif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+opportunit\w*|\bidentifying\s+\w+\s+opportunit\w*|\bdetermin[e]?[i]?[n]?[g]?\s+\w+\s+opportunit\w*', text)
        match_3 = o(r'\bbusiness\w*|\bnew\s+customer\w*|\bnew\s+client\w*|\bnew\s+market\w*|\bsale\s+opportunit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify new business opportunities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for align efforts towards business development
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+development\s+manager\w*|\bbusiness\s+development\s+principle\w*|\bmanage\s+business\s+development\w*|\bbusiness\s+development\s+supervis\w*|\bsupervise\s+business\s+development\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'align efforts towards business development'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for deploy ict system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bict\s+delivery\s+plan\w*|\bitc\s+delivery\s+plan\w*|\bimplement\s+ict\b|\bimplement\s+itc\b|\bict\s+implement\w*|\bitc\s+implement\w*|\bproduce\s+ict\b|\bproduce\s+itc\b|\bict\s+plan\w*|\bitc\s+plan\w*', text)
        match_2 = o(r'\bdeploy\w*|\bintegrate\w*|\bintegration\w*', text)
        match_3 = o(r'\bsystem\w*|\bnetwork\w*|\bserver\w*', text)
        match_4 = o(r'\bict\b|\bitc\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'deploy ict system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse ict system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bitc\s+consult\w*|\bict\s+consult\w*|\bitc\s+advis\w*|\bict\s+advis\w*', text)
        match_2 = o(r'\bict\b|\bitc\b', text)
        match_3 = o(r'\bsystem\w*|\bserver\w*|\bnetwork\w*', text)
        match_4 = o(r'\banaly\w*|\basses\w*|\bconsult\w*|\badvise\w*|\badvice\w*|\badvisor\w*|\badviser\w*|\bmonitor\w*|\bcontrol\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse ict system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and configure ict systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bict\b|\bitc\b', text)
        match_2 = o(r'\bsystem\b|\bnetwork\w*|\bserver\w*', text)
        match_3 = o(r'\bmaintain\w*|\bmaintenance\w*|\bmanage\w*|\bmanaging\w*|\bconfigure\w*|\bconfiguration\w*|\bservice\w*|\btechnical\s+support\w*|\btechnical\s+clerk\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and configure ict systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design information system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bict\s+system\w*|\bitc\s+system\w*|\binformation\s+system\w*|\binformation\s+communication\s+technology\w*|\binformation\s+comunnication\s+system\w*|\binformation\s+technology\s+communication\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bcreate\w*|\bprogram\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design information system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use ict systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bict\s+system\w*|\bitc\s+system\w*|\binformation\s+system\w*|\binformation\s+comunnication\s+system\w*|\binformation\s+communication\s+technology\w*|\binformation\s+technology\s+communication\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use ict systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test circuitry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcircuit\b|\bcircuitry\w*|\bcable\b|\bcircuit\s+breaker\w*', text)
        match_2 = o(r'\btest\w*|\bmange\w*|\bmanagign\w*|\bassess\w*|\bsafe\w*|\bsafety\w*|\bquality\w*|\bqc\b|\bqa\b|\bmonitor\w*', text)
        match_3 = o(r'\bpscad\w*', text)
        match_4 = o(r'\bcircuit\w*|\belectricity\w*|\bsoftware\w*|\bprogram\w*|\btest\w*|\bcable\w*|\bcircuitry\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test circuitry'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate chainsaw
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchainsaw\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate chainsaw'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform felling and logging procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfelling\w*|\bfell\s+tree\w*|\bcut\s+tree\w*|\blogging\b|\bfell\s+trees\w*|\bchop\s+down\s+tree\w*|\bchopping\s+down\s+tree\w*|\bcut\s+down\s+tree\w*|\bcutting\s+down\s+tree\w*|\bchop\s+tree\w*|\bchopping\s+tree\w*|\bfell\s+tree\w*|\btree[s]?\s+fell\w*|\bremove\s+tree\w*|\btree[s]?\s+remov\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform felling and logging procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calculate carbon emissions/footprint
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcarbon\s+calculat\w*|\bcalculate\s+carbon\w*', text)
        match_2 = o(r'\bcarbon\w*|\bfootprint\w*|\bemission\w*|\bmodel\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calculate carbon emissions/footprint'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and maintain solar energy systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsolar\s+energy\s+system\w*|\bphotovoltaic\w*|\bdesign\s+pv\s+system\w*|\bpv\s+design\s+software\w*', text)
        match_2 = o(r'\bsolar\w*|\bpv\b', text)
        match_3 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*|\bmaintain\w*|\bfit\b|\bfitting\b|\bcondition\s+monitor\w*|\breliability\s+analy\w*|\bgrid\s+connect\w*|\bnetwork\s+plan\w*|\bprivate\s+wire\s+network\w*', text)
        match_4 = o(r'\bsale\w*|\bselling\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and maintain solar energy systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and operate mixer truck
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmixer\s+truck\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and operate mixer truck'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mix concrete
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmix[i]?[n]?[g]?\s+concrete\w*|\bconcrete\s+mix\w*|\bconcrete\s+repair\w*|\bplace\s+concrete\w*|\bconcrete\s+work\w*|\bconcrete\s+finish\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'mix concrete'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pour concrete
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconcrete\s+pour\w*|\bpour[i]?[n]?[g]?\s+concrete\w*|\bload[i]?[n]?[g]?\s+concrete\w*|\boff\s+load[i]?[n]?[g]?\s+concrete\w*|\boff\s+-\s+load[i]?[n]?[g]?\s+concrete\w*|\bdischarg[e]?[i]?[n]?[g]?\s+concrete\w*|\bconcrete\s+repair\w*|\bplace\s+concrete\w*|\bconcrete\s+work\w*|\bconcrete\s+finish\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'pour concrete'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carry out repair of vehicles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmet\s+technician\w*|\bvehicle\s+technician\w*|(?<!quantum )\bmechanic\b', text)
        match_2 = o(r'\bvehicle\w*|\bcar\b|\btruck\b', text)
        match_3 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\bmaintenance\w*|\bmaintain\w*|\bcondition\w*|\bmot\b|\bservice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'carry out repair of vehicles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carry out repair of motorcycles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmet\s+technician\w*|\bvehicle\s+technician\w*|\bmechanic\w*|\brepair\w*|\bfix\b|\bfixing\b|\bmaintenance\w*|\bmaintain\w*|\bcondition\w*|\bmot\b|\bservice\w*', text)
        match_2 = o(r'\bmotorcycle\w*|\bbike\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'carry out repair of motorcycles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform tire repairs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmet\s+technician\w*|\bvehicle\s+technician\w*|\bmechanic\w*|\brepair\w*|\bfix\b|\bfixing\b|\bmaintenance\w*|\bmaintain\w*|\bcondition\w*|\bmot\b|\bservice\w*', text)
        match_2 = o(r'\btyre\b|\btyres\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform tire repairs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for serve drinks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbartender\w*|\bbartending\w*|\bserve\s+drink\w*|\bserve\s+beverage\w*|\bbeverage\s+associate\w*|\bserve\s+wine\w*|\bserve\s+coffee\w*|\bserve\s+champagne\w*|\bserve\s+sparkling\w*|\bserve\s+house\s+wine\w*|\bserve\s+still\s+wine\w*|\bserve\s+fortified\s+wine\w*|\bserve\s+beer\w*|\boffer\s+beer\w*|\boffer\s+brew\w*|\boffer\s+ale\w*|\bserve\s+brew\w*|\bserve\s+ale\w*|\bbeverage\s+\w+\s+associate\w*|\bserve\s+\w+\s+coffee\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'serve drinks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare mixed beverages
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcocktail\w*', text)
        match_2 = o(r'\bmix\w*|\bprepare\w*|\bserve\w*|\boffer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare mixed beverages'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure cleaniness of food preparation area
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean\w*|\btidy\w*', text)
        match_2 = o(r'\bkitchen\w*|\bfood\s+preparation\w*|\bfood\s+area\w*', text)
        match_3 = o(r'\bservice\s+area\w*|\bstorage\s+area\w*', text)
        match_4 = o(r'\bfood\w*|\bkitchen\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_1 and match_3 and match_4:
            matched_fragments.extend([match_1, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure cleaniness of food preparation area'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain cleaniness of bar area
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean\w*|\btidy\w*|\bwash\w*', text)
        match_2 = o(r'\bbar\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain cleaniness of bar area'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for setup the bar area
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bset\s+up\s+bar\w*|\bset\s+bar\w*|\bprepare\s+bar\w*|\bsetup\s+bar\w*|\bbar\s+layout\w*|\bprepare\s+bar\w*|\bbartender\w*|\bbartending\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'setup the bar area'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for optimise production
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boptimis[e]?[i]?[n]?[g]?\s+production\w*|\boptimisation\s+production\w*|\boptimiz[e]?[i]?[n]?[g]?\s+production\w*|\boptimization\s+production\w*|\boptimis[e]?[i]?[n]?[g]?\s+manufactur\w*|\boptimisation\s+manufactur\w*|\boptimiz[e]?[i]?[n]?[g]?\s+manufactur\w*|\boptimization\s+manufactur\w*|\bproduction\s+efficiency\w*|\bmanufacturing\s+efficiency\w*|\bproduction\s+optimis\w*|\bproduction\s+optimiz\w*|\benhance\s+production\w*|\bproduction\s+enhancement\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'optimise production'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for verify distillation safety
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\w*', text)
        match_2 = o(r'\bquality\b\s+\bcontrol\w*|\bqc\b|\bquality\b\s+assurance\b|\bmanage\s+quality\w*|\bassess\s+quality\w*|\bquality\s+assess\w*|\bensure\s+quality\w*|\bquality\s+manage\w*|\bquality\s+parameter\w*|\bquality\s+standard\w*|\bpermit\w*|\bsafety\w*|\bcompliance\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'verify distillation safety'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate distillation equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\s+equipment\w*|\bdistillation\s+plant\w*|\bdistillation\s+production\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate distillation equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor and maintain distillation equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\s+equipment\w*|\bdistillation\s+plant\w*|\bdistillation\s+production\w*', text)
        match_2 = o(r'\bmonitor\w*|\bmaintain\b|\bmaintenance\b|\bfix\b|\brepair\b|\bidentif\w*|\bcheck\b|\bchecking\b|\boperate\w*|\bassess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor and maintain distillation equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for measure the strenght of distillation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\w*', text)
        match_2 = o(r'\bstrenght\w*|\bpower\w*|\bconcentration\w*', text)
        match_3 = o(r'\bmeasure\w*|\bassess\w*|\banaly\w*|\bappraise\w*|\bmonitor\w*|\btest\w*|\bcheck\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'measure the strenght of distillation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow given instructions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfollow[i]?[n]?[g]?\s+order\w*|\bfollow[i]?[n]?[g]?\s+instruction\w*|\bcarry[i]?[n]?[g]?\s+out\s+instruction\w*|\bfollow[i]?[n]?[g]?\s+protocol\w*|\bfollow[i]?[n]?[g]?\s+procedure\bcomply[i]?[n]?[g]?\s+instruction\w*|\bgiven\s+instruction\w*|\binstruction[s]?\s+procedure\w*|\bfollow[i]?[n]?[g]?\s+\w+\s+order\w*|\bfollow[i]?[n]?[g]?\s+\w+\s+instruction\w*|\bfollow[i]?[n]?[g]?\s+\w+\s+protocol\w*|\bfollow[i]?[n]?[g]?\s+\w+\s+procedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow given instructions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct field development for oil or gas extraction
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfield\s+develop\w*', text)
        match_2 = o(r'\boil\w*|\bgas\w*|\bcrude\w*|\boffshore\w*|\bonshore\w*|\bwell\b|\brig\b|\barea\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct field development for oil or gas extraction'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain offshore pipeline costruction and facilities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boffshore\w*|\bsubsea\w*|\bplatform\w*|\binstallation\s+engineer\w*|\bconstruction\s+manage\w*|\bfabrication\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+construct\w*|\bmanag[e]?[i]?[n]?[g]?\s+fabricat\w*|\bconstruction\s+experience\w*|\bfabrication\s+experience\w*|\binstallation\s+engineer\w*|\bconstruction\s+manage\w*|\bfabrication\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+construct\w*|\bmanag[e]?[i]?[n]?[g]?\s+fabricat\w*|\bconstruction\s+experience\w*|\bfabrication\s+experience\w*', text)
        match_2 = o(r'\bpipeline\w*|\bpipe\b|\bpipelay\b', text)
        match_3 = o(r'\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binstall\w*|\bassembl\w*|\bdiagnos\w*|\bmanage\w*|\bmonitor\w*|\bmanaging\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain offshore pipeline costruction and facilities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design oil wells and well paths
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brwis\b|\bproduction\s+well\w*|\bwell\s+design\w*', text)
        match_2 = o(r'\bproduction\s+technologist\w*|\bwell\s+engineer\w*|\bwell\s+project\w*|\bwell\s+construction\w*|\bwells\b|\bwell\s+lifecycle\w*|\bwell\s+life\s+cycle\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bplan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design oil wells and well paths'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for select well equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwell\s+-\s+head\w*|\bwellhead\w*|\bwell\s+equipment\w*|\bwell\s+machine\w*|\bplug\s+abandonment\w*|\babandonment\s+plug\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'select well equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor well safety and perform well operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwell\s+integrity\w*|\bwell\s+safety\w*|\bwell\s+operat\w*|\bwell\s+flow\w*|\bmanage\s+well\w*|\bwell\s+test\w*|\bplug\s+abandonment\w*|\bwellbore\s+stability\w*|\bwellbore\s+analy\w*|\bwellbore\s+maintenance\w*|\bcaliper\s+log\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor well safety and perform well operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install water reservoir
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+reservoir\w*', text)
        match_2 = o(r'\binstal\w*|\bfit\b|\bfitting\w*|\bassembl\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install water reservoir'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for measure or monitor reservoir volumes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breservoir\s+manage\w*|\breservoir\s+depletion\w*|\bserservoir\s+character\w*|\bmanage\s+reservoir\w*|\bcharacteristic[s]?\s+reservoir\w*', text)
        match_2 = o(r'\breservoir\w*', text)
        match_3 = o(r'\bwell[s]?\b|\bvolume\w*|depletion\w*|\bmonitor\w*|\bmanage\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'measure or monitor reservoir volumes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for compile appraisal reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bappraisal\s+plan\w*|\bproject\s+appraisal\w*|\bplan[n]?[i]?[n]?[g]?\s+apparisal\w*|\bresource[s]?\s+appraisal\w*|\bappraisal\s+report\w*', text)
        match_2 = o(r'\bpricing\w*|\bvaluation\w*|\bevaluation\w*|\bassessment\w*|\breport\w*|\bplan\w*', text)
        match_3 = o(r'\bappraisal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'compile appraisal reports'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for natural gas liquids recovery processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brecycling\w*|\brecycle\w*|\brecover\w*', text)
        match_2 = o(r'\bfuel\s+produc\w*|\bgas\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'natural gas liquids recovery processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate recycling processing equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brecycling\s+plant\w*|\brecycling\s+disposal\w*', text)
        match_2 = o(r'\brecycling\w*|\brecycle\w*', text)
        match_3 = o(r'\bmetal\w*|\bplastic\w*|\bwood\w*|\bpaper\w*|\bwaste\w*|\bgarbage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate recycling processing equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect recycling procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brecycling\w*|\brecycle\w*', text)
        match_2 = o(r'\bmonitor\w*|\bhealth\s+safety\w*|\bquality\w*|\binspect\w*|\bcheck\w*|\bsafety\w*|\bsafe\w*|\bqc\b|\bqa\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect recycling procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for digitise documents
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigitise\s+document\w*|\bdigitize\s+document\w*|\bdigitise\s+record\w*|\bdigitize\s+record\w*|\bscan\s+document\w*|\bdigitalis[e]?[a]?[t]?[i]?[o]?[n]?\s+document\w*|\bdigitalis[e]?[a]?[t]?[i]?[o]?[n]?\s+record\w*|\bdigitize\s+document\w*|\bdigitalis[e]?[a]?[t]?[i]?[o]?[n]?\s+document\w*', text)
        match_2 = o(r'\bdigitalise\w*|\bdigitalize\w*|\bdigitalisation\w*|\bdigitalization\w*|\bdigitisation\w*|\bdigitization\w*|\bdigital\s+twin\w*', text)
        match_3 = o(r'\bdocument\w*|\bpaperwokr\w*|\brecord[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'digitise documents'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse transportation cost
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransport\s+cost\w*|\btransportation\s+cost\w*|\bcost[s]?\s+transportation\w*|\bcost[s]?\s+transport\w*', text)
        match_2 = o(r'\banaly\w*|\bevaluate\w*|\bassess\w*|\bmanage\w*|\bcheck\w*|\binspect\w*|\bmonitor\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse transportation cost'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate drilling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcoordinate\s+drill\w*|\bcoordinating\s+drill\w*|\bcoordination\s+drill\w*|\bsupervise\s+drill\w*|\bdrill[i]?[n]?[g]?\s+supervis\w*|\boversse[i]?[n]?[g]?\s+drill\w*|\bdrill[i]?[n]?[g]?\s+oversight\w*|\bmanage\s+drill\w*|\bdrill\s+manage\w*|\bdrill[i]?[n]?[g]?\s+service[s]?\s+manage\w*|\bdrilling\s+performance\w*|\bimprove\s+drilling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate drilling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for liaise with managers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliais[e]?[i]?[n]?[g]?\s+manage\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+manage\w*|\breport[i]?[n]?[g]?\s+manager\w*|\breport[i]?[n]?[g]?\s+regular\w+basis\s+\w+\s+manage\w*|\breport[i]?[n]?[g]?\s+regular\s+basis\s+manage\w*|\breport[i]?[n]?[g]?\s+regular[l]?[l]?[y]?\s+\w+\s+manage\w*|\breport[i]?[n]?[g]?\s+regular[l]?[l]?[y]?\s+manage\w*|\bliais[e]?[i]?[n]?[g]?\s+regural[l]?[l]?[y]?\s+\w+\s+manage\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+manage\w*|\bliaise\s+regural[l]?[l]?[y]?\s+\w+\s+manage\w*|\bliaise\s+regural[l]?[l]?[y]?\s+manage\w*|\bcollaborate\s+manage\w*|\bcollaborate\s+\w+\s+manage\w*|\bwork\s+manage\w*|\bliais[e]?[i]?[n]?[g]?\s+supervisor\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+\w+\s+supervisor\w*|\bliais[e]?[i]?[n]?[g]?\s+regural[l]?[l]?[y]?\s+\w+\s+supervisor\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+supervisor\w*|\bliaise\s+regural[l]?[l]?[y]?\s+\w+\s+supervisor\w*|\bliaise\s+regural[l]?[l]?[y]?\s+supervisor\w*|\bcollaborate\s+supervisor\w*|\bcollaborate\s+\w+\s+supervisor\w*|\bwork\s+supervisor\w*|\barticulate\s+manager\w*|\barticulate\s+\w+\s+manager\w*|\bbarticulate\s+\w+\s+\w+\s+manager\w*|\bbliaise\s+\w+\s+\w+\s+manager\w*|\breport[i]?[n]?[g]?\s+\w+\s+\w+\s+manager\w*', text)
        match_2 = o(r'\binteract\w*|\bliaise\w*|\bcommunicate\w*|\bliaison\w*', text)
        match_3 = o(r'\bsenior\s+manage\w*|\bvp\b|\bmanagement\s+team\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'liaise with managers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for liaise with engineers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliais[e]?[i]?[n]?[g]?\s+engineer\w*|\bliaise\s+wireline\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+\w+\s+engineer\w*|\bliais[e]?[i]?[n]?[g]?\s+regural[l]?[l]?[y]?\s+\w+\s+engineer\w*|\bliais[e]?[i]?[n]?[g]?\s+regular\s+basis\s+engineer\w*|\bliaise\s+regural[l]?[l]?[y]?\s+\w+\s+engineer\w*|\bliaise\s+regural[l]?[l]?[y]?\s+engineer\w*|\bcollaborate\s+engineer\w*|\bcollaborate\s+\w+\s+engineer\w*|\bwork\s+engineer\w*', text)
        match_2 = o(r'\bliaise\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'liaise with engineers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on personnel management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpersonnel\s+logistic\w*|\bhuman\s+resource[s]?\s+administrator\w*|\bpersonnel\s+admin\w*|\bpersonnel\s+management\w*|\bpersonnel\s+oversight\w*|\bhr\s+administrator\b|\bpersonnel\s+supervis\w*|\bhuman\s+resource[s]?\s+oversight\w*|\bpersonnel\s+regulation\w*|\bhuman\s+resource[s]?\s+regulation\w*|\bhuman\s+resource[s]?\s+supervis\w*|\bhr\s+regulation\w*|\bhr\s+supervis\w*|\bhr\s+oversight\w*|\bpersonnel\s+logistic\w*|\bpersonnel\s+regulation\w*|\bpersonnel\s+manage\w*|\bemployee[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+employee\w*|\bmanag[e]?[i]?[n]?[g]?\s+personnel\w*|\bhir[e]?[i]?[n]?[g]?\s+manage\w*|\bpersonnnel\s+opeartion\w*|\bpersonnel\s+resources\w*|\boperation[s]?\s+personnel\w*', text)
        match_2 = o(r'\bhuman\s+resources\w*|\bhr\b', text)
        match_3 = o(r'(?<!look )(?<!looking )(?<!seek )(?<!seeking )\brecruit[m]?[e]?[n]?[t]?\b(?! agenc[y]?[i]?[e]?[s]?|  agenc[y]?[i]?[e]?[s]?)\w*', text)
        match_4 = o(r'\brecruiting\b(?! agenc[y]?[i]?[e]?[s]?|  agenc[y]?[i]?[e]?[s]?)\w*', text)
        match_5 = o(r'\bhiring\w*|payroll\w*', text)
        match_6 = o(r'\bemployee\w*|\bpersonnel\w*', text)
        match_7 = o(r'\badvice\w*|\bsupport\w*|\brecommend\w*|\badvis\w*|\bliaise\w*|\bconsult\w*', text)
        if match_1 and match_7:
            matched_fragments.extend([match_1, match_7])
        elif match_2 and match_3 and match_6 and match_7:
            matched_fragments.extend([match_2, match_3, match_6, match_7])
        elif match_2 and match_4 and match_6 and match_7:
            matched_fragments.extend([match_2, match_4, match_6, match_7])
        elif match_2 and match_5 and match_6 and match_7:
            matched_fragments.extend([match_2, match_5, match_6, match_7])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on personnel management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean paper docuemnts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean[i]?[n]?[g]?\s+document\w*|\bdocument\s+clean\w*|\bdocumentation\s+clean\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean paper docuemnts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bglasswash\w*|\bglass\s+\wash\w*', text)
        match_2 = o(r'(?<!safe )\bclean(?! efficient)(?! energy)(?! room)\w*|\bwash\w*', text)
        match_3 = o(r'\bequipment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate wash equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwash\s+bay\w*|\bwashbay\w*|\bwash\s+plant\w*|\bwashing\s+plant\w*|\bwash\s+equipment\w*|\bwheelabrator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate wash equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate workshop crane
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfolding\s+crane\w*|\bworkshop\s+crane\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate workshop crane'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate hydraulic or pneumatic press
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydraulic\s+press\w*|\bhydraulic\s+equipment\w*|\bpneumatic\s+press\w*|\bpneumatic\s+equipment\w*', text)
        match_2 = o(r'\bhydraulic\w*|\bpneumatic\w*', text)
        match_3 = o(r'\bpress\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate hydraulic or pneumatic press'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide improvement strategies and identify improvement actions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimprovement\s+design\w*|\bimprovement\s+strateg\w*|\bimprovement\s+service\w*|\bimprovement\s+product\w*|\bimprov[e]?[i]?[n]?[g]?\s+design\w*|\bimprov[e]?[i]?[n]?[g]?\s+strateg\w*|\bimprov[e]?[i]?[n]?[g]?\s+service\w*|\bimprov[e]?[i]?[n]?[g]?\s+product\w*|\bservice[s]?\s+improve\w*|\bproduct\s+improve\w*|\bimprovement\s+project\w*|\bimprov[e]?[i]?[n]?[g]?\s+service\w*|\bimprov[e]?[i]?[n]?[g]?\s+process\w*|\bproject\s+improvement\w*|\bprovide\s+improvement\w*|\bsuggest\s+improvement\w*|\bcorrective\s+action\w*|\bimprovement\s+technique\w*|\bcontinuous\s+improvement\w*|\bstrateg[y]?[i]?[e]?[s]?\s+improve\w*|\binfluenc[e]?[i]?[n]?[g]?\s+improvement\w*|\bimplement[i]?[n]?[g]?\s+improvement\w*|\bdesign\s+new\s+strateg\w*|\bpropose\s+new\s+strateg\w*|\bprovide\s+new\s+strateg\w*|\bcompile\s+improvement\w*|\bdetermine\s+improvement\w*|\bdevelop\s+improvement\w*|\bidentify[i]?[n]?[g]?\s+improvement\w*|\bselect\s+improvement\w*|\bplan\s+improvement\w*|\bimprove\s+process\w*|\bimprove\s+\w+\s+process\w*|\bprocess\s+improve\w*|\bimprovement\s+plan\w*|\bplan\s+improvement\w*|\bpotential\s+improvement\w*|\bimplement[i]?[n]?[g]?\s+improvement\w*|\bimprovement\s+implement\w*|\bsupport\s+business\s+improvement\w*|\bbusiness\s+improvement\s+need\w*|\bidentify\s+business\s+need\w*|\bdefine\s+business\s+need\w*|\bsupport[i]?[n]?[g]?\s+\w+\s+improvement\w*|\bindicat[e]?[i]?[n]?[g]?\s+\w+\s+improvement\w*|\binfluenc[e]?[i]?[n]?[g]?\s+\w+\s+improvement\w*|\bimplement[i]?[n]?[g]?\s+\w+\s+improvement\w*|\bincorporate\s+\w+\s+improvement\w*|\brevise\s+\w+\s+procedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide improvement strategies and identify improvement actions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for update procedure instruction
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bupdat[e]?[i]?[n]?[g]?\s+procedure\w*|\breview[i]?[n]?[g]?\s+procedure\w*|\brevis[e]?[i]?[g]?\s+procedure\w*|\bdevelop[i]?[n]?[g]?\s+procedure\w*|\bassess[i]?[n]?[g]?\s+procedure\w*|\bprocedure\s+update\w*|\bprocedure\s+review\w*|\bprocedure\s+revis\w*|\bprocedure\s+development\w*|\bprocedure\s+assess\w*|\bnew\s+sop[s]?\b|\bupdat[e]?[i]?[n]?[g]?\s+sop[s]?\b|\breview[i]?[n]?[g]?\s+sop[s]?\b|\brevis[e]?[i]?[g]?\s+sop[s]?\b|\bdevelop[i]?[n]?[g]?\s+sop[s]?\b|\bassess[i]?[n]?[g]?sop[s]?\b|\bsop[s]?\b\s+update\w*|\bsop[s]?\b\s+review\w*|\bsop[s]?\b\s+revis\w*|\bsop[s]?\b\s+develop\w*|\bsop[s]?\b\s+assess\w*|\bupdat[e]?[i]?[n]?[g]?\s+\w+\s+procedure\w*|\breview[i]?[n]?[g]?\s+\w+\s+procedure\w*|\brevis[e]?[i]?[g]?\s+\w+\s+procedure\w*|\bdevelop[i]?[n]?[g]?\s+\w+\s+procedure\w*|\bassess[i]?[n]?[g]?\s+\w+\s+procedure\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'update procedure instruction'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for demonstrate trustworthiness
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdemonstrat[e]?[i]?[n]?[g]?\s+trust\w*|\bshow[i]?[n]?[g]?\s+trust\w*|\bestablish[i]?[n]?[g]?\s+trust\w*|\bestablish[i]?[n]?[g]?\s+credibility\w*|\bdeliver\s+discretion\w*|\bmaintain\s+trust\w*|\bconfidential\s+information\w*|\bconfidential\s+document\w*|\bshow[i]?[n]?[g]?\s+credibility\w*|\bdisplay[i]?[n]?[g]?\s+credibility\w*|\bdisplay[i]?[n]?[g]?\s+trust\w*|\bdevelop[i]?[n]?[g]?\s+trust\w*|\bdevelop[i]?[n]?[g]?\s+credibility\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'demonstrate trustworthiness'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use computer-aided software engineering tools and systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoftware\s+engineer\w*|\bsoftware\s+develop\w*|\bsoftware\s+draw\w*|\bcomputer\s+aid[e]?[d]?\s+software\s+engineer\w*|\bcomputer\s+-\s+aid[e]?[d]?\s+software\s+engineer\w*|\bcomputer\s+aid[e]?[d]?\s+engineer\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\barchitect\w*|\bplan\w*|\bprogrammer\w*|\bprogramming\w*|\bdeveloper\w*|\bcoding\w*|\bcode\w*|\btool\w*|\bsoftware\w*', text)
        match_3 = o(r'(?<!business )case(?! study)(?! studies)\w*', text)
        match_4 = o(r'\bsoftware\s+engineer\w*|\bsoftware\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+software\w*|\bsoftware\s+design\w*|\bprogrammer\w*|\bprogramming\w*|\bdeveloper\w*|\bcoding\w*|\bcod[e]?[i]?[n]?[g]?\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use computer-aided software engineering tools and systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for draw sketches
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduce\s+sketch\w*|\bdesign\s+sketch\w*|\b2d\s+draw\w*|\b2\s+-\s+draw\w*|\bhand\s+sketch\w*', text)
        match_2 = o(r'\bdraw\b|\bdrawing\b', text)
        match_3 = o(r'\bsketch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'draw sketches'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage storage operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstorage\s+management\w*|\bmanage\s+storage\w*|\bstorage\s+activity\w*|\bplan\s+storage\w*|\bstorage\s+plan\w*|\bstorage\s+operation\w*|\boperation[s]?\s+storage\w*|\bstorage\s+coordinat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage storage operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform pressure testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpressure\s+test\w*|\btest[i]?[n]?[g]?\s+pressure\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform pressure testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform torque testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btorque\s+test\w*|\btest[i]?[n]?[g]?\s+torque\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform torque testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage backlogs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badress\s+backlog\w*|\bcontrol\s+backlog\w*|\bdeal\s+backlog\w*|\bensure\s+backlog\w*|\bhandle\s+backlog\w*|\bmanage\s+backlog\w*|\bregulate\s+backlog\w*|\bbacklog[s]?\s+manage\w*|\bbacklog[s]?\s+report\w*|\breport\s+backlog\w*|\bassess\s+backlog\w*|\bbacklog[s]?\s+assess\w*|\banalyse\s+backlog\w*|\banalyze\s+backlog\w*|\bbacklog[s]?\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage backlogs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide leadership
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprovide\s+leadership\w*|\bprovide\s+authority\w*|\bleadership\s+experience\w*|\bdemonstrate\s+leadership\w*', text)
        match_2 = o(r'\bleadership\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide leadership'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide information on hydrogen
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrogen\s+system\w*', text)
        match_2 = o(r'\bhydrogen\w*|\bh2\b', text)
        match_3 = o(r'\bengineer\w*|\bspecialist\w*|\bsale\w*|\bselling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide information on hydrogen'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fix programming bugs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfix\s+bug\w*|\bbug\s+fix\w*|\bfix\s+code\w*|\bcode\s+fix\w*|\bbug\s+maintenance\w*|\bbug\s+feature\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'fix programming bugs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare and submit deviation and concession request
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeviation\s+request\w*|\bdeviation\s+waiver\w*|\bconcession\s+deviation\w*|\bconcession\s+request\w*|\bconcession\s+waiver\w*|\bdeviation\s+document\w*|\bconcession\s+document\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare and submit deviation and concession request'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and maintain electrical systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectrical\s+system\w*|\belectronic\s+system\w*', text)
        match_2 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\binstall\w*|\bmaintenance\w*|\bmaintain\w*|\bcondition\s+monitor\w*|\breliability\s+analy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and maintain electrical systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for execute integration testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegration\s+test\w*|\bintegrating\s+test\w*|\btest[i]?[n]?[g]?\s+integration\w*', text)
        match_2 = o(r'\bintegration\s+system\w*|\bintegrate\s+system\w*|\bsystem\s+integrate\w*|\bintegrating\s+system\w*', text)
        match_3 = o(r'\btest\w*|\bcheck\w*|\bmanage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'execute integration testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate hydraulic pump
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydraulic\b', text)
        match_2 = o(r'\bpump\b|\bpumping\b', text)
        match_3 = o(r'\bcontrol\s+system\w*', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate hydraulic pump'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform tubing-conveyed perforating
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btubing\s+conveying\s+perforat\w*|\btubing\s+conveyed\s+perforat\w*|\btubing\s+-\s+convey[e]?[i]?[n]?[g]?\s+perforat\w*', text)
        match_2 = o(r'\btcp\b', text)
        match_3 = o(r'\bwell\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform tubing-conveyed perforating'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create assembly instructions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bestablish\s+assembly[i]?[n]?[g]?\s+instruction\w*|\bdevelop\s+assembly[i]?[n]?[g]?\s+instruction\w*|\bcreate\s+assembly[i]?[n]?[g]?\s+instruction\w*|\belaborate\s+assembly[i]?[n]?[g]?\s+instruction\w*|\bwrite\s+down\s+assembly[i]?[n]?[g]?\s+instruction\w*|\belaborate\s+assembly[i]?[n]?[g]?\s+instruction\w*|\bformulate\s+assembly[i]?[n]?[g]?\s+instruction\w*|\bdefine\s+assembly[i]?[n]?[g]?\s+instruction\w*', text)
        match_2 = o(r'\bassembl\w*', text)
        match_3 = o(r'\bestablish\s+instruction\w*|\bdevelop\s+instruction\w*|\bcreate\s+instruction\w*|\belaborate\s+instruction\w*|\bwrite\s+down\s+instruction\w*|\belaborate\s+instruction\w*|\bformulate\s+instruction\w*|\bdefine\s+instruction\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create assembly instructions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oversee assembly operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassembl[y]?[e]?\s+check\w*|\bassembl[y]?[e]?\s+review(?! deliver)\w*|\breview\s+assembl\w*|\bcheck\s+assembly\w*|\boversee\s+assembly\w*|\bassembl[y]?[e]?\s+oversight\w*|\bregulate\s+assembl\w*|\bsupervise\s+assembl\w*|\bmanage\s+assembl\w*|test\s+assembl\w*|assembl[y]?[e]?\s+test\w*', text)
        match_2 = o(r'\bassembl\w*', text)
        match_3 = o(r'\binspect\w*|\bmanage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'oversee assembly operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply assembly techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdisassembly\w*|\bperform[i]?[n]?[g]?\s+assembly\w*|\badopt[i]?[n]?[g]?\s+assembly\w*|\bapply[i]?[n]?[g]?\s+assembly\w*|\bassembl[y]?[e]?\s+technique\w*|\bassembl[e]?[y]?\s+product\w*|\bproduct\s+assembl\w*|\bassembl[e]?[y]?\s+procedure\w*|\belectornic\s+assembl\w*|\belectrical\s+assembl\w*|\bmechanical\s+assembl\w*|\binstallation\s+technician\w*|\bassembl[y]?[e]?\s+machine\w*|\build\s+machine\w*|\bassembl[y]?[e]?\s+equipment\w*|\bmachine\s+assembl\w*|\bmachine\s+build\w*|\bassembl[y]?[e]?\s+\w+\s+machine\w*|\bbuild\s+\w+\s+machine\w*', text)
        match_2 = o(r'\bassembl\w*', text)
        match_3 = o(r'\brepair\w*|\bmaintenance\w*|\bfabrication\w*|\bworkshop\w*|\btechnician\w*|\bxray\b|\bray\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply assembly techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate seismic or navigation equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseismic\s+equipment\w*|\bobn\b|\bseismic\s+tool\w*|\bgnss\b|\b2d\s+seismic\w*|\b3d\s+seismic\w*|\bdownlink\b|\buplink\b', text)
        match_2 = o(r'\bsingle\s+beam\w*|\bsinglebeam\w*|\bmulti\s+beam\w*|\bmultibeam\w*', text)
        match_3 = o(r'\bsounder\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate seismic or navigation equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse seismic data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseismic\s+interpret\w*|\bseismic\s+analy\w*|\banalyse\s+seismic\w*|\banalyze\s+seismic\w*|\banalysis\s+seismic\w*|\binterpret\s+seismic\w*|\bmanag[e]?[i]?[n]?[g]?\s+seismic\w*|\bseismic\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+seismic\w*', text)
        match_2 = o(r'\bseismic\s+data\b|\bseismic\s+datum\w*|\bdata\s+seismic\w*|\bdatum\s+seismic\w*', text)
        match_3 = o(r'\banaly\w*|\bassess\w*|\binterpret\w*|\bcollect\w*|\bmonitor\w*|\bsurvey\w*', text)
        match_4 = o(r'\bseismic\w*', text)
        match_5 = o(r'\brecord\s+datum\b|\breport\s+datum\b|\brecord\s+data\b|\bcollect\s+data\b|\bcollect\s+datum\w*|\bdatum\s+collect\w*|\bdata\s+collect\w*|\bmonitor\s+datum\w*|\bmonitor\s+data\b|\bdatum\s+monitor\w*|\bdata\s+monitor\w*|\breporting\s+datum\w*|\brecording\s+datum\w*|\bcollect\s+analyse\w*|\bdatum\s+acquisition\w*|\bdatum\s+process\w*|\bdata\s+acquisition\w*|\bdata\s+process\w*|\bsurvey\s+datum\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse seismic data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for interpret geophysical data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeophysic[s]?[a]?[l]?\s+data\b|\bgeophysic[s]?[a]?[l]?\s+datum\w*|\bgeophysic[a]?[l]?\s+analy\w*|\banalyse\s+geophysic\w*|\banalyze\s+geophysic\w*|\banalysis\s+geophysic\w*|\binterpret\s+geophysic\w*|\bgeophysic\s+interpret\w*|\bmanag[e]?[i]?[n]?[g]?\s+geophysic\w*|\bgeophysic[a]?[l]?\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+geophysic\w*', text)
        match_2 = o(r'\bgeophysic[a]?[l]?\s+data\w*|\bgeophysic[a]?[l]?\s+datum\w*|\bdatum\s+geophysic\w*|\bdata\s+geophisic\w*', text)
        match_3 = o(r'\banaly\w*|\bassess\w*|\binterpret\w*|\bcollect\w*|\bmonitor\w*|\bsurvey\w*', text)
        match_4 = o(r'\bgeophysic\w*', text)
        match_5 = o(r'\brecord\s+datum\b|\breport\s+datum\b|\brecord\s+data\b|\bcollect\s+data\b|\bcollect\s+datum\w*|\bdatum\s+collect\w*|\bdata\s+collect\w*|\bmonitor\s+datum\w*|\bmonitor\s+data\b|\bdatum\s+monitor\w*|\bdata\s+monitor\w*|\breporting\s+datum\w*|\brecording\s+datum\w*|\bcollect\s+analyse\w*|\bdatum\s+acquisition\w*|\bdatum\s+process\w*|\bdata\s+acquisition\w*|\bdata\s+process\w*|\bsurvey\s+datum\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'interpret geophysical data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage geospatial data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeospatial\s+interpret\w*|\bgeospatial\s+analy\w*|\banalyse\s+geospatial\w*|\banalyze\s+geospatial\w*|\banalysis\s+geospatial\w*|\binterpret\s+geospatial\w*|\bmanag[e]?[i]?[n]?[g]?\s+geospatial\w*|\bgeospatial\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+gospatial\w*', text)
        match_2 = o(r'\bgeospatial\s+data\b|\bgeospatial\s+datum\w*|\bdata\s+geospatial\w*|\bdatum\s+geospatial\w*|\bspatial\s+data\b|\bspatial\s+datum\w*|\bdatum\s+spatial\w*|\bdata\s+spatial\w*', text)
        match_3 = o(r'\banaly\w*|\bassess\w*|\binterpret\w*|\bcollect\w*|\bmonitor\w*|\bsurvey\w*|\bgeodesy\w*|\bgeodetic\w*|\bgeomatic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage geospatial data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for collect and analyse geological data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeologic[s]?[a]?[l]?\s+interpret\b|\bgeologic[s]?[a]?[l]?\s+analy\w*|\banalyse\s+geolog\w*|\banalyze\s+geolog\w*|\banalysis\s+geolog\w*|\binterpret\s+geology\w*|\blog\s+core\w*|\blogging\s+core\w*|\bgeology\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+geology\w*|\bmap[p]?[i]?[n]?[g]?\s+geology\w*|\bmanage[e]?[i]?[n]?[g]?\s+geolog\w*', text)
        match_2 = o(r'\bgeology\s+data\b|\bgeology\s+datum\w*|\bgeological\s+data\b|\bgeological\s+datum\w*|\bdata\s+geolog\w*|\bdatum\s+geolog\w*|\bfieldwork\s+geolog\w*', text)
        match_3 = o(r'\banaly\w*|\bassess\w*|\binterpret\w*|\bcollect\w*|\bmonitor\w*|\bsurvey\w*|\bgather\w*|\bmeasure\w*', text)
        match_4 = o(r'\bgeology\w*|\bgeological\w*|\bgeologist\w*', text)
        match_5 = o(r'\brecord\s+datum\b|\breport\s+datum\b|\brecord\s+data\b|\bcollect\s+data\b|\bcollect\s+datum\w*|\bdatum\s+collect\w*|\bdata\s+collect\w*|\bmonitor\s+datum\w*|\bmonitor\s+data\b|\bdatum\s+monitor\w*|\bdata\s+monitor\w*|\breporting\s+datum\w*|\brecording\s+datum\w*|\bcollect\s+analyse\w*|\bdatum\s+acquisition\w*|\bdatum\s+process\w*|\bdata\s+acquisition\w*|\bdata\s+process\w*|\bsurvey\s+datum\w*|\bgather[i]?[n]?[g]?\s+data\b|\bgather[i]?[n]?[g]?\s+datum\w*|\bdata\s+gather\w*|\bdatum\s+gather\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'collect and analyse geological data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform electromagnetic geophysical masurements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectromagnetic\s+device\w*|\belectromagnetic\s+measure\w*|\belectromagnetic\s+tool\w*|\belectromagnetic\s+equipment\w*', text)
        match_2 = o(r'\bgeophysic\w*', text)
        match_3 = o(r'\belectromagnetic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform electromagnetic geophysical masurements'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop and assist in geophysical surveys
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeophysic[s]?[a]?[l]?\w*', text)
        match_2 = o(r'\bsurvey\w*', text)
        match_3 = o(r'\bdatum\w*|\bdata\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop and assist in geophysical surveys'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assist hydrographic surveys
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrography\w*|\bhydrographic\w*', text)
        match_2 = o(r'\bsurvey\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assist hydrographic surveys'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on geophysical procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeophysic[s]?[a]?[l]?\s+support\w*|\bgeophysic[s]?[a]?[l]?\s+advice\w*|\bgeophysic[s]?[a]?[l]?\s+advisor\w*|\bgeophysic[s]?[a]?[l]?\s+adviser\w*|\bgeophysic[s]?[a]?[l]?\s+service\w*|\badvise\s+geophysic\w*|\bsupport\s+geophysic\w*|\bhelp\s+geophysic\w*|\bgeophysic[s]?[a]?[l]?\s+help\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on geophysical procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use geographic information systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeograph[y]?[i]?[c]?\s+information\s+system\w*', text)
        match_2 = o(r'\bgis\b', text)
        match_3 = o(r'\bfield\w*|\bsite\w*|\bdatum\w*|\banaly\w*|\bassess\w*|\binterpret\w*|\bgeographic\w*|\bsystem\w*|\bmapping\w*|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use geographic information systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create gis report
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgis\s+report\w*|\breport\s+gis\b', text)
        match_2 = o(r'\bgis\b|\bgeograph[y]?[i]?[c]?\s+information\s+system\w*', text)
        match_3 = o(r'\breport\w*|\bdocumentation\w*|\bpaperwork\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create gis report'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create non conformance reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bncr\b|\bnon\s+conformance\s+report\w*|\bnon\s+-\s+conformance\s+report\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create non conformance reports'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for park vehicles in depot
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdepot\s+foreman\w*|\bcoal\s+depot\w*', text)
        match_2 = o(r'\bdepot\w*', text)
        match_3 = o(r'\bdriver\b|\bvehicle\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'park vehicles in depot'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assemble electromechanical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassembl\w*', text)
        match_2 = o(r'\belectromechanical\w*|\belectro\s+mechanical\w*|\belectro\s+-\s+mechanical\w*', text)
        match_3 = o(r'\bdevice\w*|\btool\w*|\bequipment\w*|\bmachine\w*|\bsystem\w*|\bfabricat\w*|\bmanufactur\w*|\bproduction\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assemble electromechanical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design electromechanical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectromechanical\w*|\belectro\s+mechanical\w*|\belectro\s+-\s+mechanical\w*', text)
        match_2 = o(r'\bcreate\w*|\bdesign\b|\bdesigner\b|\bdesigning\b|\bplan\w*|\bconstruct\w*', text)
        match_3 = o(r'\bdevice\w*|\btool\w*|\bequipment\w*|\bmachine\w*|\bsystem\w*|\bhardware\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design electromechanical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design mechanical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+mechanical\s+equipment\w*|\bfabricate\s+mechanical\s+equipment\w*|\bmechanical\s+equipment\s+fabricat\w*|\bmechanical\s+equipment\s+design\w*|\bcreat[e]?[i]?[n]?[g]?\s+mechanical\s+equipment\w*|\bdesign[i]?[n]?[g]?\s+mechanical\s+part[s]?\w*|\bfabricate\s+mechanical\s+part[s]?\w*|\bmechanical\s+part[s]?\s+fabricat\w*|\bmechanical\s+part[s]?\s+design\w*|\bcreat[e]?[i]?[n]?[g]?\s+mechanical\s+part[s]?\w*|\bdesign[i]?[n]?[g]?\s+mechanical\s+tool[s]?\w*|\bfabricate\s+mechanical\s+tool[s]?\w*|\bmechanical\s+tool[s]?\s+fabricat\w*|\bmechanical\s+tool[s]?\s+design\w*|\bcreat[e]?[i]?[n]?[g]?\s+mechanical\s+tool[s]?\w*', text)
        match_2 = o(r'\bmechanical\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bconstruct\w*', text)
        match_4 = o(r'\bdevice\w*|\btool\w*|\bequipment\w*|\bmachine\w*|\bhardware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design mechanical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test electrical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btest[i]?[n]?[g]?\s+equipment\w*|\bequipment\s+test\w*|\bexperience\s+test[i]?[n]?[g]?\w*|\bcheck[i]?[n]?[g]?\s+euipment\w*|\bequimpent\s+check\w*|\btest[i]?[n]?[g]?\s+procedure\w*|\bexperience\s+\w+\s+test[i]?[n]?[g]?\b', text)
        match_2 = o(r'\belectric\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test electrical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test mechanical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btest[i]?[n]?[g]?\s+equipment\w*|\bxperience\s+test[i]?[n]?[g]?\w*|\bequipment\s+test\w*|\bcheck[i]?[n]?[g]?\s+equipment\w*|\bequimpent\s+check\w*|\btest[i]?[n]?[g]?\s+procedure\w*|\bexperience\s+\w+\s+test[i]?[n]?[g]?\b', text)
        match_2 = o(r'\bmechanical\w*|\bmachinery\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test mechanical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read engineering drawings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bengineer[i]?[n]?[g]?\s+draw\w*|\bpid[s]?\s+draw\w*|\bpid[s]?\s+diagram\w*|\bpfd[s]?\s+draw\w*|\bpfd[s]?\s+diagram\w*|\bpfs\s+draw\w*|\bpfs\s+diagram\w*|\bpefs\s+draw\w*|\bpefs\s+diagram\w*|\bpdb[s]?\s+draw\w*|\bpdb[s]?\s+diagram\w*|\bpid[s]?\s+line\s+diagram\w*|\bprocess\s+flow\s+diagram\w*|\bprocess\s+block\s+diagram\w*|\bpid[s]?\s+\w+\s+draw\w*|\bpid[s]?\s+\w+\s+diagram\w*|\bpfd[s]?\s+\w+\s+draw\w*|\bpfd[s]?\s+\w+\s+diagram\w*|\bpfs\s+\w+\s+draw\w*|\bpfs\s+\w+\s+diagram\w*|\bpefs\s+\w+\s+draw\w*|\bpefs\s+\w+\s+diagram\w*|\bpdb[s]?\s+\w+\s+draw\w*|\bpdb[s]?\s+\w+\s+diagram\w*', text)
        match_2 = o(r'\bpid[s]?\b|\bpefs\b', text)
        match_3 = o(r'\bpfd[s]?\b|\bpfs\b', text)
        match_4 = o(r'\bcad\w*|\bengineer\w*|\bdesign\w*|\bdrawing\w*|\bdocument\w*|\breport\w*|\bdiagram\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read engineering drawings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for read assemble drawing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassembl[y]?[i]?[n]?[g]?\s+draw\w*|\bassembl[y]?[e]?\s+draw\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'read assemble drawing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain test equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcheck[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+equipment\w*|\bgaug[e]?[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+equipment\w*|\bmaintain[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+equipment\w*|\bcheck[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+equipment\w*|\bgaug[e]?[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+equipment\w*|\bmaintain[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+equipment\w*|\btest[i]?[n]?[g]?\s+equipment\s+check\w*|\bcheck[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+device\w*|\bgaug[e]?[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+device\w*|\bmaintain[i]?[n]?[g]?\s+measure[e]?[i]?[n]?[g]?\s+device\w*|\bcheck[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+device\w*|\bgaug[e]?[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+device\w*|\bmaintain[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+device\w*|\btest[i]?[n]?[g]?\s+equipment\s+maintain\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain test equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend computer numerical control lathe machine
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcnc\b|\bcam\b|\bcomputer\s+numeric[a]?[l]?\s+control\w*', text)
        match_2 = o(r'\blathe\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend computer numerical control lathe machine'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate computer numerical control machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcnc\b|\bcam\b|\bcomputer\s+numeric[a]?[l]?\s+control\w*', text)
        match_2 = o(r'\bmachine\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate computer numerical control machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare and monitor drilling fluid
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrill[i]?[n]?[g]?\s+fluid\w*|\bdrill[i]?[n]?[g]?\s+mud\b|well\s+fluid\w*|\bfluid\s+intervention\s+system\w*|\bchoke\s+manage\w*', text)
        match_2 = o(r'\bcontrol\s+fluid\w*|\bfluid\s+property\w*', text)
        match_3 = o(r'\bdrill\w*|\brig\b|\bwell\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare and monitor drilling fluid'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend hoses
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhose\b|\bhoses\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend hoses'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use object oriented programming
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bobject\s+-\s+orient\w*|\bobject\s+orient\w*|\bobject-orient\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use object oriented programming'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use skype
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bskype\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use skype'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor valves
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvalve\w*', text)
        match_2 = o(r'\bmonitor[i]?[n]?[g]?\w*', text)
        match_3 = o(r'\bmaster\s+valve\w*|\bmonitor[i]?[n]?[g]?\s+valve\w*|\bcontrol[l]?[i]?[n]?[g]?\s+valve\w*|\bcheck[i]?[n]?[g]?\s+valve\w*|\badjust[i]?[n]?[g]?\s+valve\w*|\bvalve\s+monitor\w*|\bvalve\s+control\w*|\bvalve\s+check\w*|\bvalve\s+maintenance\w*|\binspect[i]?[o]?[n]?\s+valve\w*|\bvalve\s+inspect\w*|\bmanage[i]?[n]?[g]?\s+valve\w*|\bvalve\s+adjust\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor valves'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and perform wireline operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwireline\w*', text)
        match_2 = o(r'\boperat\w*|\bmaintain\w*|\bmaintenance\w*|\binspect\w*|\bdiagnos\w*|\binterention\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and perform wireline operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and perform slickline operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bslickline\w*', text)
        match_2 = o(r'\boperat\w*|\bmaintain\w*|\bmaintenance\w*|\binspect\w*|\bdiagnos\w*|\bintervention\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and perform slickline operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monior movement of goods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomponent[s]?\s+move\w*|\bmove\s+component\w*|\bmovement\s+item\w*|\bmovement\s+component\w*|\bmovement\s+good\w*|\bgood[s]?\s+move\w*|\bmov[e]?[i]?[n]?[g]?\s+good\w*|\bmov[e]?[i]?[n]?[g]?\s+item\w*|\bitem[s]?\s+move\w*|\brelocat[e]?[i]?[n]?[g]?\s+item\w*|\brelocat[e]?[i]?[n]?[g]?\s+good\w*|\brelocat[e]?[i]?[n]?[g]?\s+component\w*|\bitem[s]?\s+relocat\w*|\bgood[s]?\s+relocat\w*|\bcomponent[s]?\s+relocat\w*', text)
        match_2 = o(r'\bensure\w*|\bcompliance\w*|\brequire\w*|\boversee\w*|\bsupervise\w*|\bmonitor\w*|\bquality\w*|\bassurance\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monior movement of goods'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage and support tender processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btendering\b|\btender\s+fund\w*|\btender\s+manage\w*|\btender\s+experience\w*|\bensure\s+tender\w*|\btender\s+query\w*|\breview\s+tender\w*|\bprepar[e]?[i]?[n]?[g]?\s+tender\w*|\bpreparation\s+tender\w*|\bprepar[e]?[i]?[n]?[g]?\s+bid[s]?\b|\bpreparation\s+bid[s]?\b|\bmanag[e]?[i]?[n]?[g]?\s+tender\w*|\bassist[i]?[n]?[g]?\s+tender\w*|\bbid\s+tender\w*|\btender\s+process\w*|\btender\s+submission\w*|\btender\s+design\w*|\bdesign\s+tender\w*|\bsubmit\s+tender\w*|\btender\s+phase\w*|\btender\s+stage\w*|\baccount\s+tender\w*|\brais[e]?[i]?[n]?[g]?\s+tender\w*|\bdevelop\s+tender\w*|\btender\s+develop\w*|\btender\s+strategy\w*|\bsupplier[s]?\s+tender\w*|\bvendor[s]?\s+tender\w*|\bcontractor[s]?\s+tender\w*|\bclient[s]?\s+tender\w*|\bcustomer[s]?\s+tender\w*|\bstudy[i]?[n]?[g]?\s+tender\w*|\brequest\s+for\s+tender\w*|\binvitation\s+tender\w*|\bassess[i]?[n]?[g]?\s+tender\w*|\btender\s+assess\w*|\bevaluat[e]?[i]?[n]?[g]?\s+tender\w*|\btender\s+evaluat\w*|\bdraft[i]?[n]?[g]?\s+tender\w*|\btender[i]?[n]?[g]?\s+prepar\w*|\bmarketing\s+tender\w*|\bsupport[i]?[n]?[g]?\s+tender\w*|\bprovid[e]?[i]?[n]?[g]?\s+input[s]?\s+tender\w*|\bprepar[e]?[i]?[n]?[g]?\s+\w+\s+tender\w*|\bpreparation\s+\w+\s+tender\w*|\btender[i]?[n]?[g]?\s+\w+\s+project\w*|\bensure\s+\w+\s+tender\w*', text)
        match_2 = o(r'\brqt\b|\bitt\b|\btender[i]?[n]?[g]?\s+team\w*', text)
        match_3 = o(r'\bproposal\w*|\btender\w*|\brfp\b|\brequest\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage and support tender processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare invitation to bid proposal
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binvitation\s+bid[i]?[n]?[g]?\w*|\bbid[i]?[n]?[g]?\s+invitation\w*', text)
        match_2 = o(r'\bitb\b', text)
        match_3 = o(r'\brequest\s+proposal\w*|\btender\w*|\brqt\b|\bitt\b|\bproposal\w*|\brequest\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare invitation to bid proposal'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform cementing procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcementing\w*|\blay\s+cement\w*|\bdishcharge\s+cement\w*|\bcement\s+dischar\w*|\bcement\s+mixer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform cementing procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and build remotely operated systems and equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brov\s+workshop\w*|\brov\s+technician\w*|\brov\s+support\w*|\brov\s+system\w*|\brov\s+project\w*|\brov\s+service\w*|\brov\s+equipment\w*|\bmaintain\s+rov\w*|\brov\s+maintenace\w*', text)
        match_2 = o(r'\brov\b', text)
        match_3 = o(r'\binspect\w*|\bmaintain\w*|\bmaintenance\w*|\bdiagnos\w*|\brepair\w*|\bfix\b|\bfixing\w*|\bassemble\w*|\bbuild\w*|\bbuilt\w*|\bmanufactur\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and build remotely operated systems and equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate rov equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperat[e]?[i]?[n]?[g]?\s+rov\b|\brov\s+operat\w*|\brov\s+pilot\w*|\bpilot\s+rov\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate rov equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare technical design document
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+design\s+document\w*|\btechnical\s+design\s+paperwork\w*|\btechnical\s+design\s+information\w*', text)
        match_2 = o(r'\btdd\b', text)
        match_3 = o(r'\bdocument\w*|\breport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare technical design document'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform data modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+model\w*|\bdatum\s+model\w*|\bmodel\s+data\w*|\bmodel\s+datum\w*|\bmodel\s+\w+\s+datum\w*', text)
        match_2 = o(r'\bscientific\s+model\w*|\bnumeric[a]?[l]?\s+model\w*|\bmathematic[s]?[a]?[l]?\s+model\w*', text)
        match_3 = o(r'\bdata\b|\bdatum\b|\bresearch\w*|\bexperiment\w*|\banaly\w*|\bcreate\w*|\bexperience\w*|\bunderstand\w*|\bfamiliar\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform data modelling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for brainstorm ideas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbrainstorm\w*|\bshare\s+idea\w*|\bdiscuss\s+idea\w*|\bdevelop\s+idea\w*|\bresearch\s+idea\w*|\bcontribute\s+discuss\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'brainstorm ideas'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform machining operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmachining\w*|\bgriding\w*|\bblanking\w*|\bpunching\w*', text)
        match_2 = o(r'\bmanufactur\w*|\bmachine\w*|\bworkshop\w*|\bmechanical\w*|\bindustr\w*|\boperation\w*|\btask\w*|\bperform\w*|\bresponsibl\w*|\bconduct\w*|\boperate\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform machining operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for trade in jewellery
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjeweller\w*', text)
        match_2 = o(r'\bsale\w*|\btrade\w*|\bselling\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'trade in jewellery'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean jewellery pieces
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean\s+jeweller\w*|\bjewellery\s+clean\w*|\bclean\s+ring\w*|\bclean\s+earring\w*|\bclean\s+necklace\w*|\bclean\s+gems\w*|\bclean\s+diamond\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean jewellery pieces'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adjusting and fixing jewellery
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brepair\s+jeweller\w*|\bjewellery\s+repair\w*|\bfix\s+jeweller\w*|\bfixing\s+jeweller\w*|\bmend\s+jeweller\w*|\brepairing\s+jeweller\w*|\bmending\s+jeweller\w*|\badjust\s+jeweller\w*|\bjewellery\s+adjust\w*|\badjusting\s+jewellery\w*|\bremodel\s+jeweller\w*|\brework\s+jeweller\w*|\bremodelling\s+jeweller\w*|\breworking\s+jeweller\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'adjusting and fixing jewellery'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate till and card machine
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperate\s+till\w*|\buse\s+till\w*|\bcard\s+machine\w*|\btend\s+till\w*|\brun\s+till\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate till and card machine'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for think and act proactively
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpro\s?[-]?\s?actively\w*|\bthink[i]?[n]?[g]?\s+lateral\w*|\bthink[i]?[n]?[g]?\s+positive\w*|\bthink[i]?[n]?[g]?\s+active\w*|\bthink[i]?[n]?[g]?\s+ahead\w*', text)
        match_2 = o(r'pro\s?[-]?\s?activ\w*', text)
        match_3 = o(r'\bthink\w*|\bact\w*|\bbehave\w*|\battitude\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'think and act proactively'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on environmental and social principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment[a]?[l]?\s+social\s+risk\w*|\benvironment[a]?[l]?\s+social\s+impact\w*', text)
        match_2 = o(r'\bconsult\w*|\badvise\w*|\badvice\w*|\badvisor\w*', text)
        match_3 = o(r'\bes\s+discipline\w*|\bes\s+lead\w*|\bes\s+advisor\w*|\bes\s+advice\w*|\bes\s+manage\w*|\brces\s+discipline\w*|\brces\s+lead\w*|\brces\s+advisor\w*|\brces\s+advice\w*|\brces\s+manage\w*', text)
        match_4 = o(r'\benvironment\w*|\bsustainab\w*|\bprovide\w*|\bcommunicat\w*|\btalk\w*|\bdisucss\w*|\bpresent\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on environmental and social principles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for recognise and manage corrosion
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsign[s]?\s+corrosion\w*|\bsign[s]?\s+metal\s+oxid\w*|\bsign[s]?\s+rust\w*|\bcorrosion\s+manage\w*|\brust\s+manage\w*|\bcorrosion\s+risk\w*|\brust\s+risk\w*|\bcorrosion\s+threat\w*|\brust\s+threat\w*|\bcorrosion\s+inspect\w*|\brust\s+inspect\w*|\bcorrosion\s+enineer\w*|\brust\s+engineer\w*|\bcorrosion\s+monitor\w*|\brust\s+monitor\w*|\bmanag[e]?[i]?[n]?[g]?\s+corrosion\w*|\bmonitor[i]?[n]?[g]?\s+corrosion\w*|\binspect[i]?[n]?[g]?\s+corrosion\w*|\bdiagnos[e]?[i]?[n]?[g]?\s+corrosion\w*|\bmanag[e]?[i]?[n]?[g]?\s+rust\w*|\bmonitor[i]?[n]?[g]?\s+rust\w*|\binspect[i]?[n]?[g]?\s+rust\w*|\bdiagnos[e]?[i]?[n]?[g]?\s+rust\w*|\bprevent[i]?[n]?[g]?\s+corrossion\w*|\bprevent[i]?[n]?[g]?\s+rust\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'recognise and manage corrosion'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop preventive maintenance procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpreventive\s+maintenance\w*|\bpredictive\s+maintenance\w*|\bpdm\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop preventive maintenance procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design control systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+control\s+system\w*|\bconfigur[e]?[i]?[n]?[g]?\s+control\s+system\w*|\bcontrol\s+system[s]?\s+configur\w*|\bcreat[e]?[i]?[n]?[g]?\s+control\s+system\w*|\bdefine\s+control\s+system\w*|\bdevelop[i]?[n]?[g]?\s+control\s+system\w*|\bplan[n]?[i]?[n]?[g]?\s+control\s+system\w*|\bprepar[e]?[i]?[n]?[g]?\s+control\s+system\w*|\bcontrol\s+system[s]?\s+design\w*|\bcontrol\s+system[s]?\s+creat\w*|\bcontrol\s+system[s]?\s+develop\w*|\bcontrol\s+system[s]?\s+plann\w*|\bcontrol\s+system[s]?\s+prepar\w*|\bcontrol\s+system\s+\w+\s+design\w*', text)
        match_2 = o(r'\bcontrol\s+system\w*|\biacs\b|\bics\b|\blcp\b|\bmcc\b|\bmotor\s+control\s+panel\w*|\blocal\s+control\s+panel\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bdevelop\w*|\bcreate\w*|\bplan\w*|\bwire\w*|\bconnect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design control systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for exert expenditure control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcapex\w*|\bcapital\s+expenditure\w*|\bexpenditure\s+control\w*|\bmonitor\s+expenditure\w*|\bcontrol\s+expenditure\w*|\bexpenditure\s+monitor\w*|\bexpenditure[s]?\s+spend\w*|\bmanag[e]?[i]?[n]?[g]?\s+expenditure\w*|\bexpenditure[s]?\s+credit\w*|\brdec\b', text)
        match_2 = o(r'\bexpenditure\w*', text)
        match_3 = o(r'\bcost\w*|\bbudget\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'exert expenditure control'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operational expense control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bopex\b|\boperating\s+expense\w*|\boperational\s+expense\w*|\bday\s+day\s+expense\w*|\bday\s+-\s+day\s+expense\w*|\boperational\s+budget\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operational expense control'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and repair rotating equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brotat[e]?[i]?[n]?[g]?\s+equipment\s+engineer\w*', text)
        match_2 = o(r'\brotating\s+equipment\w*|\bree\b|\brotate\s+equipment\w*', text)
        match_3 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\bmaintenance\w*|\bmaintain\w*|\bdiagnos\w*|\bcondition\s+monitor\w*|\breliability\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and repair rotating equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan evaluation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanage\s+evaluation\w*|\bevaluation\s+manage\w*|\bplan\s+evaluation\w*|\bevaluation\s+plan\w*|\bevaulation\s+method\w*|\bmethod[s]?\s+evaulat\w*|\bmethodology\s+evaluat\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan evaluation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for combine multiple fields of knowledge
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmultiple\s+disciplin\w*|\bmultiple\s+field\w*|\bmultidisciplin\w*|\bnumerous\s+field\w*|\bmulti\s+disciplin\w*|\bmulti\s?[-]?\s?disciplin\w*|\bmultitude\s+disciplin\w*|\bnumerous\s+disciplin\w*|\bmultitude\s+\w+\s+disciplin\w*|\bnumerous\s+\w+\s+disciplin\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'combine multiple fields of knowledge'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply crisis management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrisis\s+manage\w*|\bcrisis\s+\w+\s+manage\w*|\bdisaster\s+manage\w*|\bdisaster\s+\w+\s+manage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply crisis management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect vessel
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binspect[i]?[n]?[g]?\s+vessel\w*|\bvessel\s+theory\w*|\binspection\s+vessel\w*|\bcheck[i]?[n]?[g]?\s+vessel\w*|\bvessel[s]?\s+check\w*|\bvessel[s]?\s+inspect\w*|\bvessel[s]?\s+visit\w*|\bvessel\s+compliance\w*|\bvessel\s+asses\w*|\bassesment\s+vessel\w*|\bassess\s+vessel\w*|\bvdt\b|\bvessel\s+\w+\s+supervis\w*|\bsupervise\s+vessel\w*', text)
        match_2 = o(r'\bvessel\w*', text)
        match_3 = o(r'\bhs\b|\bhealth\s+safety\w*|\bhsse\b|\bhsec\b|\baudit\w*|\brisk\s+assess\w*|\bassess\s+risk\w*|\bsuperintendent\w*|\bhs\b|\bhses\b', text)
        match_4 = o(r'\baccounting\w*|\baccountant\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect vessel'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage vessel fleet
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmana[e]?[i]?[g]?\s+vessel\w*|\bmanag[e]?[i]?[g]?\s+fleet\w*|\bmaintain[i]?[n]?[g]?\s+fleet\w*|\bmaintain[i]?[n]?[g]?\s+vessel\w*|\bvessel\s+manage\w*|\bvessel\s+maintenance\w*|\bvessel\s+maintain\w*|\bfleet\s+maintain\w*|\bfleet\s+maintenance\w*|\bfleet\s+manage\w*|\bvessel\s+assurance\w*|\bmanagement\s+vessel\w*|\bmanagement\s+fleet\w*|\bmana[e]?[i]?[g]?\s+\w+\s+vessel\w*|\bmanag[e]?[i]?[g]?\s+\w+\s+fleet\w*|\bmaintain[i]?[n]?[g]?\s+\w+\s+fleet\w*|\bmaintain[i]?[n]?[g]?\s+\w+\s+vessel\w*', text)
        match_2 = o(r'\bdpa\b', text)
        match_3 = o(r'\bvessel\w*|\bmarine\w*|\bsubsea\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage vessel fleet'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for support vessel manoeuveres
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvessel\s+manoeuvr\w*|\bfleet\s+manoeuvr\w*|\bship\s+manoeuvr\w*|\bmanoeuvr[e]?[i]?[n]?[g]?\s+vessel\w*|\bmanoeuvr[e]?[i]?[n]?[g]?\s+ship\w*|manoeuvr[e]?[i]?[n]?[g]?\s+fleet\w*', text)
        match_2 = o(r'\bberthing\w*|\bmooring\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2:
            matched_fragments.extend([match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'support vessel manoeuveres'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and use vessel equipment and systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvessel\s+equipment\w*|\bequipment\s+vessel\w*|\bfleet\s+equipment\w*|\bequipment\s+fleet\w*|\bequipment\s+onboard\w*|\bonboard\s+equipment\w*|\bvessel\s+system\w*|\bfleet\s+system\w*|\banchoring\s+wire\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and use vessel equipment and systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify skill gaps
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcompetency\s+gap\w*|\bcompetency\s+\w+\s+gap\w*|\bskill[s]?\s+gap\w*|\bskill[s]?\s+assessment\w*|\bskill\s+shortage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify skill gaps'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for organise provide and evaluate training
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+train\w*|\benhance\s+train[i]?[n]?[g]?\s+program\w*|\bestablish\s+train[i]?[n]?[g]?\s+curriculum\w*|\bestablish\s+training\s+program\w*|\benhance\s+training\s+curriculum\w*|\btrain\s+employee\w*|\btrain\s+staff\w*|\bcoordinate\s+training\w*|\bconduct\s+train\w*|\bco\s+-\s+ordinate\s+training\w*|\bordinate\s+training\w*|\btraining\s+co\s+-\s+ordinat\w*|\btraining\s+ordinat\w*|\bdepartment\s+train\w*|\btraining\s+manage\w*|\bassist\s+train\w*|\btraining\s+competence\s+officer\w*|\btrain[i]?[n]?[g]?\s+complain\w*|(?<!class )\btrain[i]?[n]?[g]?\s+support\w*|\bschedlue\s+train\w*|\btrain[i]?[n]?[g]?\s+schedule\w*|\bdesign\s+train\w*|\btrain[i]?[n]?[g]?\s+design\w*|\btrain[i]?[n]?[g]?\s+coordinat\w*|\btrain[i]?[n]?[g]?\s+co\s+-\s+ordinat\w*|\btrain[i]?[n]?[g]?\s+ordinat\w*|\bplan\s+train\w*|\btran[i]?[n]?[g]?\s+plan\w*|\bdeliver[i]?[n]?[g]?\s+train\w*|\bprovid[e]?[i]?[n]?[g]?\s+train\w*|\btrain[i]?[n]?[g]?\s+provision\w*|\btrain[i]?[n]?[g]?\s+delivery\w*|\btrain[i]?[n]?[g]?\s+deliver\w*|organiz[e]?[i]?[n]?[g]?\s+train\w*|\borganis[e]?[i]?[n]?[g]?\s+train\w*|\btrain[i]?[n]?[g]?\s+session\w*|\bconduct\s+\w+\s+train\w*|\bassist\s+\w+\s+train\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+train\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+\w+\s+train\w*|\bdeliver[i]?[n]?[g]?\s+\w+\s+train\w*|\bprovid[e]?[i]?[n]?[g]?\s+\w+\s+train\w*', text)
        match_2 = o(r'\bapprentice\w*|\bapprenticeship\w*', text)
        if match_1 and not match_2:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'organise provide and evaluate training'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform fundrising activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfundraising\w*|\bfundraise\w*|\braise\s+fund\w*|\bfund[s]?\s+rais[e]?[i]?[n]?[g]?\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform fundrising activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use accounting software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\s+system\w*|\baccounting\s+software\w*|\bbookkeep[i]?[n]?[g]?\s+software\w*|\bbook-keep[i]?[n]?[g]?\s+software\w*|\bbook\s+-\s+keep[i]?[n]?[g]?\s+software\w*|\bbookkeep[i]?[n]?[g]?\s+system\w*|\bbook-keep[i]?[n]?[g]?\s+system\w*|\bbook\s+-\s+keep[i]?[n]?[g]?\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use accounting software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle petty cash
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhandl[e]?[i]?[n]?[g]?\s+cash(?! flow)\w*|\bmanag[e]?[i]?[n]?[g]?\s+cash(?! flow)\w*|\bpetty\s+cash\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle petty cash'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply grammar and spelling rules
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgrammar\w*|\bspelling\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply grammar and spelling rules'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for issue sales invoices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsale[s]?\s+invoice\w*|\bpurchase\s+invoice\w*|\bcost[s]?\s+invoice\w*|\bpayment\s+invoice\w*|\bprocess[i]?[n]?[g]?\s+invoice\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'issue sales invoices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage manufacturing facilities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanage\s+facilities\w*', text)
        match_2 = o(r'\bplant\b|\bmanufactur\w*|\bindustrial\w*|\bmechanical\w*|\bengineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage manufacturing facilities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide technical accounting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprovide\s+technical\s+accounting\w*|\bequire\s+technical\s+accounting\w*|\btechnical\s+accounting\s+knowledge\w*|\btechnical\s+accounting\s+support\w*|\btechnical\s+accounting\s+expertise\w*|\btechnical\s+accounting\s+experience\w*|\bexperience\s+technical\s+accounting\w*|\bexpertise\s+technical\s+accounting\w*|\btechnical\s+accounting\s+advice\w*|\badvice\s+technical\s+accounting\w*|\badvise\s+technical\s+accounting\w*|\btechnical\s+accounting\s+advis\w*|\btechnical\s+accounting\s+analy\w*|\breview\s+technical\s+accounting\w*', text)
        match_2 = o(r'\btechnical\s+accounting\w*', text)
        match_3 = o(r'\bsenior\s+accountant\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide technical accounting'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse financial performance of a company
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinanc[e]?[i]?[a]?[l]?\s+report\s+analy\w*|\banalyse\s+financ[e]?[i]?[a]?[l]?\s+report\w*|\banalyze\s+financ[e]?[i]?[a]?[l]?\s+report\w*|\banalysis\s+financ[e]?[i]?[a]?[l]?\s+report\w*|\bfinanc[e]?[i]?[a]?[l]?\s+performance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse financial performance of a company'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure compliance with accounting conventions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\s+compliance\w*|\baccounting\s+regulat\w*|\baccounting\s+legislation\w*|\baccounting\s+law\w*|\baccounting\s+guideline\w*|\baccounting\s+convention\w*|\bcompliance\s+accounting\w*|\baccounting\s+survey\w*|\baccountancy\s+survey\w*', text)
        match_2 = o(r'\baccounting\w*|\bbookkeep\w*|\bbook-keep\w*|\bbook\s+-\s+keep\w*|\baccountant\w*', text)
        match_3 = o(r'\bacca\b|\bacma\b|\bcima\b|\bcpa\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure compliance with accounting conventions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow the statutory obligations and maintain statutory records
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatutory\s+book\w*|\bstatutory\s+accounting\w*|\bstatutory\s+accountant\w*|\bstatutory\s+report\w*|\bstatutory\s+obligation\w*|\bstatutory\s+requirement\w*|\bstatutory\s+prepare\w*|\bprepare\s+statutory\w*|\bstatutory\s+account\w*|\bstatutory\s+audit\w*|\border\s+statutory\w*|\bstatutory\s+order\b|\bstatutory\s+rules\w*|\bstatutory\s+process\w*|\bcompliance\s+statutory\w*|\bsubmit\s+statutory\w*|\bstatutory\s+comply\w*|\bcomply\s+statutory\w*|\bsubmission\s+statutory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow the statutory obligations and maintain statutory records'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply unfied communciations practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bunified\s+communicat\w*|\bcommunication\s+techniques\w*|\btechnique[s]?\s+communicat\w*|\btelecommunication[s]?\s+equipment\w*|\bcommunication[s]?\s+equipment\w*|\bequipment\s+communicat\w*|\btelecomunication[s]?\s+device\w*|\bcommunication[s]?\s+device\w*|\bcommunication[s]?\s+science\w*|\bcommunication[s]?\s+stud[y]?[i]?\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply unfied communciations practices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle cash flow
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+cash\s+flow\w*|\bhandl[e]?[i]?[n]?[g]?\s+cash\s+flow\w*|\bcash\s+flow\s+forecast\w*|\bforecast[i]?[n]?[g]?\s+cash\s+flow\w*|\bcash\s+flow\s+project\w*|\bdeal[i]?[n]?[g]?\s+cash\s+flow\w*|\bupdat[e]?[i]?[n]?[g]?\s+cash\s+flow\w*|\bmanag[e]?[i]?[n]?[g]?\s+cash\s+flow\w*|\boversee\s+cash\s+flow\w*|\bmonitor[i]?[n]?[g]?\s+cash\s+flow\w*|\bcash\s+flow\s+manage\w*|\bcash\s+flow\s+overs\w*|\bcash\s+flow\s+monitr\w*|\bcash\s+flow\s+understand\w*|\bupdat[e]?[i]?[n]?[g]?\s+\w+\s+cash\s+flow\w*', text)
        match_2 = o(r'\bcash\s+flow\w*', text)
        match_3 = o(r'\bcost[s]?\s+control\w*|\bcost\s+risk\w*|\bcontrol[l]?[i]?[n]?[g]?\s+cost\w*|\baccounting\w*|\bbookkeep\w*|\bbook-keep\w*|\bbook\s+-\s+keep\w*|\baccountant\w*|\bbudget\b|\btreasury\w*|\bbank[i]?[n]?[g]?\b|\bpl\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle cash flow'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use rope access techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brope\s+access\w*|\brope\s+work\w*|\birata\s+rope\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use rope access techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform non-destructive testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnon\s+-\s+destructive\s+test\w*|\bdestructive\s+test\w*|\bndt\b|\bnon\s+-\s+destructive\s+examin\w*|\bdestructive\s+examin\w*|\bnon\s+-\s+destructive\s+equipment\w*|\bdestructive\s+equipment\w*|\bnon\s+-\s+destructive\s+tool\w*|\bdestructive\s+tool\w*|\bnon\s+-\s+destructive\s+method\w*|\bnde\b', text)
        match_2 = o(r'\bnon\s+-\s+destructive\w*|\bnon\s+destructive\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform non-destructive testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform welding inspection
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelding\s+code\w*|\bweld[i]?[n]?[g]?\s+monitor\w*|\bmonitor\s+weld\w*|\bquality\s+welding\w*|\bwelding\s+quality\w*|\bwelding\s+inspect\w*|\binspect[i]?[o]?[n]?\s+weld\w*|\bweldgin\s+engineer\w*', text)
        match_2 = o(r'\bwelding\w*', text)
        match_3 = o(r'\binspect\w*|\bdiagnose\w*|\bndt\b|\bnon\s+-\s+destructive\w*|\bintegrity\w*|\bvalidator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform welding inspection'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply latest sport science findings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsport\s+science\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply latest sport science findings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coach young people
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcoach\w*|\btrain\b|\btraining\b|\binstruct\b|\binstructing\b|\bteach\b|\bteaching\b|\beducate\w*|\bsupervis\w*|\bmanag\w*', text)
        match_2 = o(r'\byoung\s+people\w*|\bchildren\w*|\byoungster\w*|\byoung\s+player\w*|\byoung\s+footballer\w*', text)
        match_3 = o(r'\bapprentice\w*|\bapprenticeship\w*', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coach young people'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform or support quality audits process
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquality\s+audit\w*|\bquality\s+\w+\s+audit\w*', text)
        match_2 = o(r'\bquality\w*|\bqc\b|\bqa\b|\bassurance\w*', text)
        match_3 = o(r'\baudit\w*', text)
        match_4 = o(r'\bsupport\w*|\bmanage\w*|\bmainain\w*|\bensure\w*|\bparameter\w*|\braport\w*|\breport\w*|\bdocument\w*|\bpaper\w*|\bperform\w*|\bconduct\w*|\bexperience\w*|\bfamiliar\w*|\bknowledge\w*|\bhelp\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform or support quality audits process'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply arc welding techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barc\s+welding\w*|\barc\s+weld\w*|\bphosporate\s+coat\w*|\bbrazing\w*|\braze\w*', text)
        match_2 = o(r'\bweld\w*', text)
        match_3 = o(r'\barc\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply arc welding techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and check diving equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdiv[e]?[i]?[n]?[g]?\s+equipment\w*|\bdrysuit\w*|\bequipment\s+div[e]?[i]?[n]?[g]?\w*|\bwetsuit\w*|\bscuba\w*', text)
        match_2 = o(r'\bmaintain\w*|\bmaintenance\w*|\binspect\w*|\bdiagnose\w*|\bsuperintendent\w*|\breview\w*|\bexamin\w*|\bcheck\w*|\brepair\w*|\bfix\b|\bfixing\b|\bquality\w*|\banaly\w*|\bassess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and check diving equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for draft bill of materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbill\s+of\s+material\w*', text)
        match_2 = o(r'\bbom\b', text)
        match_3 = o(r'\bproduction\w*|\bmaterial\w*|\bcomponent\w*|\bmanufacture\w*|\bengineer\w*|\bcreate\w*|\bwrite\w*|\bproduce\w*|\bwriting\w*|\bcreating\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'draft bill of materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect material
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binspect[i]?[n]?[g]?\s+material\w*|\bmaterial\s+inspect\w*|\btest[i]?[n]?[g]?\s+material\w*|\bassess[i]?[n]?[g]?\s+material\w*|\bmaterial\s+assess\w*|\bcheck[i]?[n]?[g]?\s+material\w*|\bmaterial\s+check\w*|\bevaluat[e]?[i]?[n]?[g]?\s+material\w*|\bmaterial\s+performance\w*|\bselect[i]?[n]?[g]?\s+material\w*|\bmaterial\s+selection\w*|\bmaterial[s]?\s+control\w*|\bperformance\s+\w+\s+material\w*|\btest[i]?[n]?[g]?\s+\w+\s+material\w*|\binspect[i]?[n]?[g]?\s+\w+\s+material\w*|\bassess[i]?[n]?[g]?\s+\w+\s+material\w*|\bcheck[i]?[n]?[g]?\s+\w+\s+material\w*|\bevaluat[e]?[i]?[n]?[g]?\s+\w+\s+material\w*', text)
        match_2 = o(r'\bredundant\s+material\w*', text)
        match_3 = o(r'\bidentify\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect material'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmrp\b|\bmaterial[s]?\s+requirement\s+plan\w*|\bmaterial[s]?\s+resource[s]?\b|\bresource[s]?\s+material[s]\b|\bmaterial[s]?\s+requirement[s]?\s+plan\w*', text)
        match_2 = o(r'\bmaterial[s]?\s+process\w*|\bbill\s+of\s+material[s]?\b', text)
        match_3 = o(r'\bbom\b', text)
        match_4 = o(r'\bmaterial[s]\b', text)
        match_5 = o(r'\berp\w*|enterprise\s+resource[s]?\s+plan\w*|\bmanage\w*|\blogistic\w*|\bexperience\w*|\bknowledge\w*|\bunderstand\w*|\bfamiliar\w*|\bplan\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_5:
            matched_fragments.extend([match_2, match_5])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate the ship or vessel crew
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeck\s+foreman\w*|\bmanag[e]?[i]?[n]?[g]?\s+deck\s+crew\w*|\bmanag[e]?[i]?[n]?[g]?\s+vessel\s+crew\w*|\bmanag[e]?[i]?[n]?[g]?\s+crew\w*|\bcrew[i]?[n]?[g]?\s+manage\w*|\bcrew[i]?[n]?[g]?\s+administ\w*|\bcrew[i]?[n]?[g]?\s+coordinat\w*|\bcrew[i]?[n]?[g]?\s+ordinat\w*|\bcrew[i]?[n]?[g]?\s+co\s+-\s+ordinat\w*|\bordinate\s+crew\w*|\bcoordinate\s+crew\w*|\bco\s+-\s+ordinate\s+crew\w*|\bcrew\s+administrat\w*|\badministrator\s+crew\w*|\badminister\s+crew\w*|\boffshore\s+crewing\w*', text)
        match_2 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+people\w*|\bpeople\s+management\w*|\bcoordinate\s+people\w*|\bordinate\s+people\w*|\bco\s+-\s+ordinate\s+people\w*|\bsupervise\s+people\w*|\bpeople\s+supervis\w*|\bmanag[e]?[i]?[n]?[g]?\s+duty\w*|\bmanag[e]?[i]?[n]?[g]?\s+duties\w*|\bduty\s+manage\w*|assign\s+duties\w*|\bassign\s+role\w*|\bassign\s+activity\w*|\bassign\s+duty\w*|\bduty\s+assign\w*|\bstore\s+manager\w*|\bshop\s+manager\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+staff\w*|\bmanagement\s+\w+\s+staff\w*|assign\s+\w+\s+duties\w*|\bassign\s+\w+\s+role\w*|\bassign\s+\w+\s+activity\w*|\bassign\s+\w+\s+duty\w*|\bdeal\s+personnel\w*|\bdeck\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+deck\w*|\bvessel\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+vessel\w*|\bcoordinat[e]?[i]?[o]?[n]?\s+deck\w*|\bordinat[e]?[i]?[o]?[n]?\s+deck\w*|\bcoordinat[e]?[i]?[o]?[n]?\s+vessel\w*|\bordinat[e]?[i]?[o]?[n]?\s+vessel\w*', text)
        match_3 = o(r'\bship\w*|\bvessel\w*|\bdeck\w*|\bcrew\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate the ship or vessel crew'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage service requests
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bservice\s+request\w*|\bsuppliers[s]?\s+request\w*|\bvendor[s]?\s+request\w*|\bcustomer[s]?\s+request\w*|\bclient[s]?\s+request\w*|\bcontractor[s]?\s+request\w*|\bsuppliers[s]?\s+\w+\s+request\w*|\bvendor[s]?\s+\w+\s+request\w*|\bcustomer[s]?\s+\w+\s+request\w*|\bclient[s]?\s+\w+\s+request\w*|\bcontractor[s]?\s+\w+\s+request\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage service requests'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sell tickets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bticket\s+office\w*', text)
        match_2 = o(r'\bticket\w*', text)
        match_3 = o(r'\bmatch\w*|\bfootball\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'sell tickets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assemble manufactured pipeline parts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpipeline\s+fabricat\w*|\bpipe\s+fabricat\w*|\bfabricate\s+pipe\w*|\bpipeline\s+production\w*|\bpipeline\s+part\w*|\bpipe\s+construct\w*|\bpipeline\s+construct\w*|\bmanufactur[e]?[i]?[n]?[g]?\s+pipe\w*|\bconstruct[i]?[n]?[g]?\s+pipe\w*|\bfield\s+joint\w*|\bpipeline\s+consolidat\w*|\bconsolidat[e]?[i]?[o]?[n]?\s+pipeline\w*|\bfabricate\s+spoolbase\w*|\bspoolbase\s+fabricat\w*', text)
        match_2 = o(r'\bspoolbase\w*|\bpmm\b|\breel\s+[-]?\s+lay\w*', text)
        match_3 = o(r'\bengineer\w*|\bcoordinat\w*|\bordinat\w*|\bpipeline\w*|\bassembly\w*|\bmanufactur\w*|\bconstruct\w*|\bvessel\w*|\bfabricat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assemble manufactured pipeline parts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for promote health and safety
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpromot[e]?[i]?[n]?[g]?\s+health\s+safety\w*|\bpromot[e]?[i]?[n]?[g]?\s+hs\b|\bpromot[e]?[i]?[n]?[g]?\s+hse\b|\bpromot[e]?[i]?[n]?[g]?\s+hsse\b|\bpromot[e]?[i]?[n]?[g]?\s+hsea\b|\bpromot[e]?[i]?[n]?[g]?\s+hses\b|\bpromot[e]?[i]?[n]?[g]?\s+health\w*|\bstrengthen\s+hsse\b|\breinforce\s+hsse\b|\bstrengthen\s+\w+\s+hsse\b|\breinforce\s+\w+\s+hsse\b|\bstrengthen\s+health\b|\breinforce\s+health\b|\bstrengthen\s+\w+\s+health\b|\breinforce\s+\w+\s+health\b|\bstrengthen\s+safety\b|\breinforce\s+safety\b|\bstrengthen\s+\w+\s+safety\b|\breinforce\s+\w+\s+safety\b|\bstrengthen\s+hse\b|\breinforce\s+hse\b|\bstrengthen\s+\w+\s+hse\b|\breinforce\s+\w+\s+hse\b|\bstrengthen\s+hsea\b|\breinforce\s+hsea\b|\bstrengthen\s+\w+\s+hsea\b|\breinforce\s+\w+\s+hsea\b|\bstrengthen\s+hses\b|\breinforce\s+hses\b|\bstrengthen\s+\w+\s+hses\b|\breinforce\s+\w+\s+hses\b|\bstrengthen\s+hs\b|\breinforce\s+hs\b|\bstrengthen\s+\w+\s+hs\b|\breinforce\s+\w+\s+hs\b|\bhealth\s+promot[e]?[i]?[n]?[g]?\b|\bhealth\s+promotion\w*|\bhealth\s+safety\s+consult\w*|\bhealth\s+safety\s+advice\w*|\bhealth\s+safety\s+advis\w*|\badvertis[e]?[i]?[n]?[g]?\s+health\s+safety\w*|\badvertis[e]?[i]?[n]?[g]?\s+hs\b|\badvertis[e]?[i]?[n]?[g]?\s+hse\b|\badvertis[e]?[i]?[n]?[g]?\s+hsse\b|\badvertis[e]?[i]?[n]?[g]?\s+hsea\b|\badvertis[e]?[i]?[n]?[g]?\s+hses\b|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+health\s+safety\w*|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+hs\b|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+hses\b|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+hse\b|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+hsse\b|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+hsea\b|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+health\s+safety\w*|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+hs\b|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+hses\b|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+hse\b|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+hsse\b|\badvertis[e]?[i]?[n]?[g]?\s+\w+\s+hsea\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'promote health and safety'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage company accounts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badminister\s+account\w*|\bmanag[e]?[i]?[n]?[g]?\s+account\w*|\bmaintain[i]?[n]?[g]?\s+account\w*|\baccount[s]?\s+manage\w*|\boversee[i]?[n]?[g]?\s+account[s]?\w*|\badminister\s+financial\s+record\w*|\bmanag[e]?[i]?[n]?[g]?\s+financial\s+record\w*|\bmaintain[i]?[n]?[g]?\s+financial\s+record\w*|\boversee[i]?[n]?[g]?\s+finanical\s+record\w*|\bfinancial\s+record[s]?\s+overs\w*|\baccount[s]?\s+overs\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+account\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage company accounts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage operational budgets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperat[e]?[i]?[n]?[g]?\s+budget\w*|\boperational\s+budget\w*|\boperation[s]?\s+budget\w*|\bops\s+budget\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage operational budgets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for establish rapport with people
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brapport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'establish rapport with people'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for acquire manufacturing components for production
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btooling\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'acquire manufacturing components for production'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prevent pipeline deterioration
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpipeline\s+maintenance\w*|\bmaintain\s+pipeline\w*|\bdiagnose\s+pipeline\w*|\bpipeline\s+diagnos\w*|\bmonitor\s+pipeline\w*|\bpipeline\s+maintain\w*|\brepair\s+pipeline\w*|\bfix\s+pipeline\w*|\bpipeline\s+repair\w*|\bpigging\b', text)
        match_2 = o(r'\bsign[s]?\s+corrosion\w*|\bsign[s]?\s+metal\s+oxid\w*|\bsign[s]?\s+rust\w*|\bcorrosion\s+manage\w*|\brust\s+manage\w*|\bcorrosion\s+risk\w*|\brust\s+risk\w*|\bcorrosion\s+threat\w*|\brust\s+threat\w*|\bcorrosion\s+inspect\w*|\brust\s+inspect\w*|\bcorrosion\s+enineer\w*|\brust\s+engineer\w*|\bcorrosion\s+monitor\w*|\brust\s+monitor\w*|\bmanag[e]?[i]?[n]?[g]?\s+corrosion\w*|\bmonitor[i]?[n]?[g]?\s+corrosion\w*|\binspect[i]?[n]?[g]?\s+corrosion\w*|\bdiagnos[e]?[i]?[n]?[g]?\s+corrosion\w*|\bmanag[e]?[i]?[n]?[g]?\s+rust\w*|\bmonitor[i]?[n]?[g]?\s+rust\w*|\binspect[i]?[n]?[g]?\s+rust\w*|\bdiagnos[e]?[i]?[n]?[g]?\s+rust\w*|\bprevent[i]?[n]?[g]?\s+corrossion\w*|\bprevent[i]?[n]?[g]?\s+rust\w*|\bcra\b|\bili\b|\bin\s+line\s+inspect\w*|\bline\s+inspect\w*|\bin\s+-\s+line\s+inspect\w*', text)
        match_3 = o(r'(?<!sale )(?<!great )\bpipe\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prevent pipeline deterioration'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and use sensor equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsurvey\s+sensor\w*|\bsensor\s+equipment\w*|\bsurvey\s+sensor\w*|\bacquisition\s+sensor\w*|\bmcc\s+plc\b|\brov\s+survey\w*|\bauv\s+survey\w*', text)
        match_2 = o(r'\bsensor\w*', text)
        match_3 = o(r'\bmaintenance\w*|\bmaintain\w*|\bmonitor\w*|\binspect\w*|\bdiagnos\w*|\btroubleshoot\w*|\binstal\w*|\bequipment\w*|\btool\w*|\bexperience\w*|\bfamiliar\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and use sensor equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use survey online software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoftware\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+software\w*|\bonline\s+survey\w*|\bsurvey\s+online\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use survey online software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect and maintain wind turbines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwindfarm\s+experience\w*|\bwind\s?[-]?\s?farm\s+experience\w*|\bexperience\s+windfarm\b|\bexperience\s+wind\s?[-]?\s?farm\b', text)
        match_2 = o(r'\boffshore\s+wind\w*|\bwind\s+turbine\w*', text)
        match_3 = o(r'\brepair\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\binstal\w*|\bassembl\w*|\bdiagnos\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect and maintain wind turbines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage purchasing activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+buyer\w*|\bpurchas[e]?[i]?[n]?[g]?\s+activity\w*|\bpurchas[e]?[i]?[n]?[g]?\s+support\w*|\bpurchas[e]?[i]?[n]?[g]?\s+order\w*|\bpurchas[e]?[i]?[n]?[g]?\s+skill\w*|\bpurchas[e]?[i]?[n]?[g]?\s+ability\w*|\bskill\s+purchas\w*|\bability\s+purchas\w*|\bmanag[e]?[i]?[n]?[g]?\s+purchas\w*|\bpurchas[e]?[i]?[n]?[g]?\s+manage\w*|\bcoordinate\s+purchas\w*|\bordinate\s+purchas\w*|\bco\s+-\s+ordinate\s+purchas\w*|\bpurchas[e]?[i]?[n]?[g]?\s+coordinat\w*|\bpurchas[e]?[i]?[n]?[g]?\s+co\s+-\s+ordinat\w*|\bpurchas[e]?[i]?[n]?[g]?\s+ordinat\w*|\bcontrol[i]?[n]?[g]?\s+purchas\w*|\bpurchas[e]?[i]?[n]?[g]?\s+control\w*|\bpurchas[e]?[i]?[n]?[g]?\s+system\w*', text)
        match_2 = o(r'\bpurchasing\b', text)
        match_3 = o(r'\blogistic[s]?\b|\binventory\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage purchasing activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect construction site
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclerk\s+work[s]?\b', text)
        match_2 = o(r'\bconstruction\w*|\bbuilt\w*|\build\w*|\bstructur\w*', text)
        match_3 = o(r'\b(?<!company )(?<!operational )(?<!companys )\bsite[s]?\b', text)
        match_4 = o(r'\bmonitor\w*|\bvisit\w*|\basses\w*|\breview\w*|\bensur[e]?[i]?[n]?[g]?\w*|\binspect\w*|\bconduct\w*|\bperform\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect construction site'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform accounts recivables analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+\baccount[s]?\s+receivable\w*|\baccount[s]?\s+receivable[s]\s+manage\w*|\bsupervise\s+\baccount[s]?\s+receivable\w*|\baccount[s]?\s+receivable[s]?\s+supervis\w*|\banalyze\s+\baccount[s]?\s+receivable\w*|\baccount[s]?\s+receivable[s]?\s+assist\w*', text)
        match_2 = o(r'\baccount[s]?\s+receivable\w*', text)
        match_3 = o(r'\banaly\w*|\bmonitor\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform accounts recivables analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform construction survey
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconstruction\s+survey\w*|\bsurvey\s+construction\w*|\bbuild[i]?[n]?[g]?\s+survey\w*|\bbuilt\s+survey\w*|\bsurvey\s+position\w*|\bposition[i]?[n]?[g]?\s+survey\w*|\bconstruction\s+w+\s+survey\w*', text)
        match_2 = o(r'\bconstruction\w*|\bbuilt\w*|\bbuild\w*|\bstructural\w*', text)
        match_3 = o(r'\bsite\s+survey\w*|\bsite\s+investigat\w*|\bsite\s+assessment\w*', text)
        match_4 = o(r'\bsurvey\w*|\bsite\w*', text)
        match_5 = o(r'\bgeo\s+data\w*|\bgeo\s+datum\w*|\bgeo\s+-\s+datum\w*|\bgeo\s+-\s+data\w*|\bgeodesy\w*|\bgeodetic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5:
            matched_fragments.extend([match_2, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform construction survey'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply statistical analysis techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistic[a]?[l]?\s+analy\w*|\banalysis\s+statistic\w*|\banalyse\s+statistic\w*|\banalyze\s+statistic\w*', text)
        match_2 = o(r'(?!national )\bstatistic\w*', text)
        match_3 = o(r'\bdata\b|\bdatum\w*|\bevaluation\w*|\bpattern\w*|\btrend\w*|\banaly\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply statistical analysis techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle and interpret photohgraphic data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphorographyic\s+datum\w*|\bphotographic\s+data\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle and interpret photohgraphic data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform video editing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvideo\s+edit\w*|\bedit[i]?[n]?[g]\s+video\w*', text)
        match_2 = o(r'\bvideo\w*', text)
        match_3 = o(r'\bphoto\s+edit\w*|\bedit[i]?[n]?[g]?\s+photo\w*|\bphotography\s+edit\w*|\bedit\s+image\w*|\bimage[s]?\s+edit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform video editing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform image editing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphoto[g]?[r]?[a]?[p]?[h]?[y]?\s+edit\w*|\bedit[i]?[n]?[g]\s+photo\w*|\bimage[s]?\s+edit\w*|\bedit\s+image\w*', text)
        match_2 = o(r'\bphoto\w*', text)
        match_3 = o(r'\bvideo\s+edit\w*|\bedit[i]?[n]?[g]?\s+video\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform image editing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for collect sea or water samples
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcollect\s+water\s+sample\w*|\bcollect[i]?[n]?[g]?\s+sea\s+sample\w*|\btake[i]?[n]?[g]?\s+water\s+sample\w*', text)
        match_2 = o(r'\bseabed\w*|\breef\w*|\bintertidal\w*', text)
        match_3 = o(r'\bsample\w*|\bcollect\w*|\bmarine\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'collect sea or water samples'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for measure pollution
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpollution\w*|\btoxic\s+release\w*|\btoxic\s+waste\w*', text)
        match_2 = o(r'\bmeasur\w*|\banaly\w*|\basses\w*|\bmonitor\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'measure pollution'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prevent sea and marine pollution
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsea\s+pollution\w*|\bwater\s+pollution\w*|\bpollution\s+prevent[i]?[o]?[n]?\s+sea\w*|\bpollution\s+sea\w*|\bpollution\s+water\w*|\bmarine\s+pollution\w*|\bpollution\s+marine\w*|\bmaritime\s+pollution\w*|\bpollution\s+maritime\w*', text)
        match_2 = o(r'\bprevent\w*|\bmitigat\w*|\bremediat\w*', text)
        match_3 = o(r'\bpollution\w*|\bmarpol\b', text)
        match_4 = o(r'\bprevent\w*|\bmitigat\w*|\bremediat\w*', text)
        match_5 = o(r'\bsea\w*|\bmarine\w*|\bmaritime\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prevent sea and marine pollution'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for interpret 3d data and plans
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b3d\b', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*|\bplan\b|\bplans\b|\bsurvey\w*|\bdesign\w*|\bsoftware\w*', text)
        match_3 = o(r'\bmeasure\w*|\banaly\w*|\bassess\w*|\binterpret\w*|\bmonitor\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'interpret 3d data and plans'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate 3d graphic software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b3d\s+software\w*|\b3d\s+program\w*|\bsoftware\s+3d\w*|\b3d\s+software\w*|\b3d\s+program\w*|\bgraphic\s+3d\w*|\b3d\s+computer\s+graphic\w*|\b3d\s+graphic\w*|\bus[e]?[i]?[n]?[g]?\s+\w+\s+3d\w*', text)
        match_2 = o(r'\b3d\b', text)
        match_3 = o(r'\bsoftware\w*', text)
        match_4 = o(r'\bdeveloper\w*|\bprogramming\w*|\bfinite\s+element\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\buse\w*|\bunderstand\w*|\butilise\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate 3d graphic software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for implement biodiversity action plans
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiodiversity\s+plan\w*|\bplan[n]?[i]?[n]?[g]?\s+biodiversity\w*|\bmanag[e]?[i]?[n]?[g]?\s+biodiversity\w*|\bbiodiversity\s+manage\w*', text)
        match_2 = o(r'\bbiodivesirty\w*', text)
        match_3 = o(r'\bstrategy\w*|\bplan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'implement biodiversity action plans'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for safeguard biodiversity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiodiversity\s+net\s+gain\w*|\bdefra\s+metric\w*', text)
        match_2 = o(r'\bbiodivers\w*', text)
        match_3 = o(r'\bbng\b|\bdefra\w*', text)
        match_4 = o(r'\bbiodiversity\w*', text)
        match_5 = o(r'\bimpact\w*|\bmitigat\w*|\bsafeguard\w*|\bsustain\w*|\bprotect\w*|\bpreserve\w*|\bconserve\w*|\badvice\w*|\badvise\w*|\bmanag\w*|\boffset\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'safeguard biodiversity'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct or assist in ecological surveys
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becology\w*|\becologist\w*|\becological\w*', text)
        match_2 = o(r'\bsurvey\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct or assist in ecological surveys'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform power simulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brms\b|\bemt\b|\bemtp\b|\bemtdc\w*|\bpscad\b|\betap\b|\bsimulation\w*', text)
        match_2 = o(r'\bpower\w*|\belectronic\w*|\belectric\w*', text)
        match_3 = o(r'\bsystem\w*|\bsoftware\w*|\bcomputer\s+tool\w*|\bprogramming\w*|\bsimulation\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform power simulations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage geotehnical staff
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+geotehnic\w*|\bsupervis[e]?[i]?[n]?[g]?\s+geotechnical\w*|\bsupervision\s+geotechnic\w*|\bmanagement\s+geotechnic\w*', text)
        match_2 = o(r'\bgeotechnic\w*', text)
        match_3 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+people\w*|\bpeople\s+management\w*|\bcoordinate\s+people\w*|\bordinate\s+people\w*|\bco\s+-\s+ordinate\s+people\w*|\bsupervise\s+people\w*|\bpeople\s+supervis\w*|\bmanag[e]?[i]?[n]?[g]?\s+duty\w*|\bmanag[e]?[i]?[n]?[g]?\s+duties\w*|\bduty\s+manage\w*|assign\s+duties\w*|\bassign\s+role\w*|\bassign\s+activity\w*|\bassign\s+duty\w*|\bduty\s+assign\w*|\bstore\s+manager\w*|\bshop\s+manager\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+staff\w*|\bmanagement\s+\w+\s+staff\w*|assign\s+\w+\s+duties\w*|\bassign\s+\w+\s+role\w*|\bassign\s+\w+\s+activity\w*|\bassign\s+\w+\s+duty\w*|\bdeal\s+personnel\w*|\bmanag[e]?[i]?[n]?[g]?\s+team\w*|\bteam\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+colleague\w*|\bcolleague\s+manage\w*|\bcoordinate\s+staff\w*|\bco\s+-\s+ordinate\s+staff\w*|\bordinate\s+staff\w*|\bcoordinate\s+team\w*|\bordinate\s+team\w*|\bco\s+-\s+ordinate\s+team\w*|\bteam\s+co\s+-\s+ordinat\w*|\bteam\s+coordinat\w*|\bteam\s+ordinat\w*|\bmanag[e]?[i]?[n]?[g]?\s+employee\w*|\bemployee\s+management\w*|\bcoordiante\s+employee\w*|\bmanag[e]?[i]?[n]?[g]?\s+cross\s+-\s+functional\s+team\w*|\bcoordinate\s+\w+\s+staff\w*|\bordinate\s+\w+\s+staff\w*|\bcoordinate\s+\w+\s+team\w*|\bordinate\s+\w+\s+team\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+team\w*|\bmanagement\s+\w+\s+team\w*|\bsupervise\s+staff\w*|\bsupervise\s+team\w*|\bteam\s+supervis\w*|\bstaff\s+supervis\w*|\bsupervise\s+employee\w*|\bemployee\s+supervis\w*', text)
        match_4 = o(r'\bsupervisor\b|\bsupervision\b|\bsupervise\b', text)
        match_5 = o(r'\bteam\b', text)
        match_6 = o(r'\bleader\w*|\bcoach\w*', text)
        match_7 = o(r'\bmanagement\w*', text)
        match_8 = o(r'\bleadership\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5 and match_6:
            matched_fragments.extend([match_2, match_4, match_5, match_6])
        elif match_2 and match_7 and match_8:
            matched_fragments.extend([match_2, match_7, match_8])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage geotehnical staff'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor geotechnical structures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeotechnic[a]?[l]?\s+assess\w*|\bgeotechnic[a]?[l]?\s+investigat\w*|\bgeotehnic[a]?[l]?\s+analy\w*', text)
        match_2 = o(r'\bgeotechnic\w*', text)
        match_3 = o(r'\bdatum\w*|\bdata\b|\bengineer\w*', text)
        match_4 = o(r'\bmonitor\w*|\bassess\w*|\banaly\w*|\binvestigat\w*|\bplann\w*', text)
        match_5 = o(r'\brail\b|\broad\b|\bstructure\b|\bairport\w*|\brailway\w*|\bbuild\w*|\bconstruction\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4 and match_5:
            matched_fragments.extend([match_2, match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor geotechnical structures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for promote sustainable transport
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsustainable\s+transport\w*', text)
        match_2 = o(r'\badvice\w*|\badvise\w*|\bconsult\w*|\bcoordinat\w*|\bco\s+-\s+ordinat\w*|\bordinat\w*|\bpromote\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'promote sustainable transport'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design transportation systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransport[a]?[t]?[i]?[o]?[n]?\s+modelling\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+modeller\w*|\bmodel\s+transport\w*|\bplan[n]?[i]?[n]?[g]?\s+transport\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+plann\w*|\bhighway[s]?\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+highway\w*|\bhighway[s]?\s+design\w*|\bole\s+engineer\w*|\bole\s+design\w*', text)
        match_2 = o(r'\bsustainable\s+transport\w*|\btransportation\s+system\w*|\btransport\s+project\w*|\btransport\s+engineer\w*|\btransportation\s+engineer\w*|\bengineer\s+transport\w*|\bengineering\s+transport\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+plan\b|\broad\s+design\w*|\bdesign[i]?[n]?[g]?\s+road\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bconstruction\w*', text)
        match_4 = o(r'\brail\b|\brailway\w*|\bbridge\w*|\broad\b|\bairport\w*|\bhighway\w*|\btransportation\s+business\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design transportation systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage gas transmission systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransmission\s+system\w*|\btransmission\s+network\w*|\btransportation\s+network\w*|\btransportation\s+system\w*|\bdistibution\s+system\w*|\bdistribution\s+network\w*', text)
        match_2 = o(r'\bgas\b|\bhydrogen\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage gas transmission systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage electricity transmission systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransmission\s+system\w*|\btransmission\s+network\w*|\btransportaton\s+network\w*|\btransportation\s+system\w*|\bdistibution\s+system\w*|\bdistribution\s+network\w*|\bnetwork\s+plan\w*|\bnetwork\s+develop\w*|\bpowerfactory\b|\bemtdc\b|\bemtp\b|\bpscad\b|\behv\s+transmission\w*|\bhv\s+transmission\w*|\bole\s+engineer\w*|\bole\s+design\w*|\boverhead\s+line\s+equipment\s+design\w*|\boverhead\s+line\s+equipment\s+engineer\w*|\bnetwork\s+design\w*', text)
        match_2 = o(r'\belectric\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage electricity transmission systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify risk of flooding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brisk\s+flood\w*|\bflood[i]?[n]?[g]?\s+risk\w*|\bflood[i]?[n]?[g]?\s+mitigat\w*', text)
        match_2 = o(r'\bflood[i]?[n]?[g]?\b', text)
        match_3 = o(r'\bfra\b', text)
        match_4 = o(r'\bflood[i]?[n]?[g]?\b', text)
        match_5 = o(r'\brisk\w*', text)
        match_6 = o(r'\bmanage\w*|\bassess\w*|\bcontrol\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify risk of flooding'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for execute feasibility study of hydrogen
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrogen\w*|\bh2\b', text)
        match_2 = o(r'\bevaluation\s+study\w*|\bsurvey\w*|\bfeasibility\s+study\w*|\bresearch\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'execute feasibility study of hydrogen'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform hydrodynamic calculations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrodynamic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform hydrodynamic calculations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for planning ability
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!enterprise resource )\bplan\w*', text)
        match_2 = o(r'\bskill\w*|\bability\w*|\bexperience\w*|\bknowledge\w*|\bable\w*|\bcreate\w*|\bdevelop\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'planning ability'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate gas turbines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+turbine\w*|\bturbine\s+gas\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate gas turbines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for undertake employee screening
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpeople\s+screening\w*|\bemployee\s+screeining\w*|\bstaff\s+screening\w*|\bengagement\s+screening\w*|\bscreening\s+employe\w*|\bscreening\s+staff\w*|\bscreening\s+people\w*', text)
        match_2 = o(r'\bhr\b|\bhuman\s+resource\w*', text)
        match_3 = o(r'\bscreening\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'undertake employee screening'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach special needs student
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btutor\w*|\bteach\w*|\bclassroom\w*|\beducation\s+support\w*|\beducation\s+coordinat\w*', text)
        match_2 = o(r'\bspecial\s+need\w*|\bspecial\s+-\s+need\w*|\bspecial-need\b|\bdisadvantage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach special needs student'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach esol language or literacy class
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besol\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach esol language or literacy class'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design cables and cable systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcable\s+design\w*|\bcable\s+engineer\w*|\bcable\s+calculation\w*|\bhv\s+cable\w*|\bdesign[i]?[n]?[g]?\s+cable\s+route\w*|\bhv\s+grid\w*|\bgrid\s+connect\w*|\behv\s+cable\w*|\bcable\s+system\w*|\bcable\s+install\w*|\binstal[l]?[a]?[t]?[i]?[o]?[n]?\s+cable\w*|\bdesign[i]?[n]?[g]\s+\w+\s+cable\w*|\bhv\s+design\w*|\bhigh\s+voltage\s+design\w*|\bhvdc\s+system\w*|\bhv\s+system\w*|\bhvdc\s+\w+\s+system\w*|\bhv\s+\w+\s+system\w*', text)
        match_2 = o(r'\betap\b|\binterconnector\w*|\bpowerfactory\w*', text)
        match_3 = o(r'\bcable\s+system\w*|\bsubmarine\s+cable\w*|\bunderground\s+cable\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design cables and cable systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use thermal analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bthermal\s+control\w*|\bthermal\s+rating\w*|\bthermal\s+analy\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use thermal analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor social impact
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsocial\s+impact\w*|\bequality\s+impact\w*|\bsocial\s+value[s]?\s+strategy\w*', text)
        match_2 = o(r'\bassess\w*|\bmanag[e]?[i]?[n]?[g]?\w*|\bmonitor\w*|\bmeasur[e]?[i]?[n]?[g]?\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor social impact'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess and investigate contamination
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontamination\s+test\w*|\bcontamination\s+check\w*|\btest[i]?[n]?[g]?\s+conataminat\w*|\bcheck[i]?[n]?[g]?\s+contaminat\w*', text)
        match_2 = o(r'\bcontaminat\w*', text)
        match_3 = o(r'\bmeasur[e]?[i]?[n]?[g]?\w*|\bassess\w*|\binvestigat\w*|\banaly\w*|\binvestigat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess and investigate contamination'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct land survey
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bland\s+assessment\w*|\bland\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+land\w*|\bassess\s+land\w*|\bland\s+investigat\w*|\binvestigat[e]?[i]?[o]?[n]?\s+land\w*', text)
        match_2 = o(r'\blss\b', text)
        match_3 = o(r'\bcad\b|\bmathcad\w*|\bautocad\w*|\bacad\b|\bcreo\b|\bacad\b', text)
        match_4 = o(r'\bland\b', text)
        match_5 = o(r'\bsite\s+survey\w*|\bsite\s+ivestigation\w*|\bsite\s+assessment\w*', text)
        match_6 = o(r'\bsurvey\w*', text)
        match_7 = o(r'\benvironment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        elif match_4 and match_6 and match_7:
            matched_fragments.extend([match_4, match_6, match_7])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct land survey'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use hydraulic modelling for water and wastewater management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydraulic[s]?\s+model\w*|\bmodel[l]?[i]?[n]?[g]?\s+hydraulic\w*|\bhydraulic\s+design\w*', text)
        match_2 = o(r'\bhydrolog[y]?[i]?[c]?[a]?[l]?\s+model\w*|\bmodel[l]?[i]?[n]?[g]?\s+hydrolog\w*|\bhydrology\s+design\w*', text)
        match_3 = o(r'\bhydraulic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use hydraulic modelling for water and wastewater management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use hydrology modelling for water and wastewater management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrolog[y]?[i]?[c]?[a]?[l]?\s+model\w*|\bmodel[l]?[i]?[n]?[g]?\s+hydrolog\w*|\bhydrology\s+design\w*', text)
        match_2 = o(r'\bhydraulic[s]?\s+model\w*|\bmodel[l]?[i]?[n]?[g]?\s+hydraulic\w*|\bhydraulic\s+design\w*', text)
        match_3 = o(r'\bhydrology\w*|\bhydrologic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use hydrology modelling for water and wastewater management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle mobile phone data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphone\s+datum\w*|\btelephone\s+datum\w*|\bphone\s+data\b|\bphone\s+datum\w*|\bdatum\s+phone\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle mobile phone data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on heating systems energy efficiency
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badvice\s+energy\s+efficiency\w*', text)
        match_2 = o(r'\bhouse\b|\bhouseholder\b|\bhousehold\b', text)
        match_3 = o(r'\benergy\s+efficiency\w*|\benergy\s+sav[e]?[i]?[n]?[g]?\w*|\bsav[e]?[i]?[n]?[g]?\s+energy\w*|\breduce\s+energy\w*', text)
        match_4 = o(r'\bcommunicate\s+customer\w*|\bcommunicate\s+client\w*|\bcustomer\s+communicate\w*|\bcustomer[s]?\s+assist\w*|\bassist[a]?[n]?[c]?[e]?\s+customer\w*|\bclient[s]?\s+assist\w*|\bassist[a]?[n]?[c]?[e]?\s+client\w*|\bcustomer[s]?\s+service\w*|\bcustomer[s]?\s+advice\w*|\bclient[s]?\s+advice\w*|\badvise\s+customer\w*|\badvise\s+client\w*|\bclient[s]?\s+services\b|\bconsult\s+customer\w*|\bconsult\s+client\w*|\bcustomer\s+-\s+service\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on heating systems energy efficiency'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage conservation of natural and cultural heritage
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnatural\s+heritage\w*|\bcultural\s+heritage\w*', text)
        match_2 = o(r'\bheritage\w*', text)
        match_3 = o(r'\bwildlife\w*|\bhabitat\w*|\bconservation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage conservation of natural and cultural heritage'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop website wireframe
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwebsite\s+wireframe\w*|\bweb\s+wireframe\w*|\bwireframe\s+web\b|\bwireframe\s+website\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop website wireframe'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop and manage website
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwebsite\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+website\w*|\bweb\s+develop\w*|\bdevelop\s+web\b|\bmaintain\s+website\w*|\bmaintain\s+web\b|\bwebsite\s+maintenance\w*|\bweb\s+maintenace\w*|\bmanag[e]?[i]?[n]?[g]?\s+web\b|\bmanag[e]?[i]?[n]?[g]?\s+website\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop and manage website'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for execute feasibility study
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfeasibility\s+study\w*|\bconstraint[s]?\s+report\w*', text)
        match_2 = o(r'\banaly\w*|\bassess\w*|\bdatum\w*|\bdata\b|\bevaluat\w*|\bresearch\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'execute feasibility study'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for think creatively
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bthink[i]?[n]?[g]?\s+creativ\w*|\bcreative\s+think\w*|\banticipate\s+need\b|\bcreative\s+mindset\w*|\bbrainstorm\b|\bexperiment\b|\bfind\s+new\s+idea\b|\binnovate\b|\binnovative\s+think\b|\bcreative\s+practitioner\w*|\bcreative\s+practitice\w*|\bseek\s+out\s+question\b|\bshow\s+original\b|\bthink\s+creative\b|\bcreative\s+way\b|\bthinking\s+creative\b|\buse\s+imagination\b|\bvisualise\s+completed\s+project\b|\bvisualise\s+completed\s+work\b|\bwork\s+concept\w*', text)
        match_2 = o(r'\bdesign\b\|\bdesigner\b', text)
        match_3 = o(r'\baspect\w*|\belement\w*|\bteam\w*|\bcommunicat\w*|\bcreate\w*|\bdeliver\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'think creatively'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create landscape design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blandscap[e]?[i]?[n]?[g]?\s+design\w*|\blandscap[e]?[i]?[n]?[g]?\s+architect\w*|\blandscap[e]?[i]?[n]?[g]?\s+plann\w*|\bplan[n]?[i]?[n]?[g]?\s+landscap\w*|\bdesign[i]?[n]?[g]?\s+landscap\w*|\bcreat[e]?[i]?[n]?[g]?\s+landscape\w*', text)
        match_2 = o(r'(?<!media )(?<!medium )(?<!economic )\blandscape\w*|\blandscaping\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bdraw\w*|\bdraughting\w*|architect\w*|\bconstruction\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create landscape design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage landscape design projects
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!media )(?<!medium )\blandscape\w*|\blandscaping\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bdraw\w*|\bdraughting\w*|architect\w*|\bconstruction\w*', text)
        match_3 = o(r'manage\w*|\bsupervis\w*|\boversee\w*|\boversight\w*|\bplan\w*|\bstrategy\w*|ordinate\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage landscape design projects'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct soil sample tests
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoil\s+sampl[e]?[i]?[n]?[g]?\w*|\bpeat\s+probe\w*', text)
        match_2 = o(r'\bsoil\w*|\brock\w*', text)
        match_3 = o(r'\bpeat\b|\benvironment\w*', text)
        match_4 = o(r'\bsample\w*|\bsampling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct soil sample tests'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for engage with stakeholders
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bengag[e]?[i]?[n]?[g]?\s+stakeholder\w*|\bengag[e]?[m]?[e]?[n]?[t]?\s+\w+\s+stakeholder\w*|\bstakeholder[s]?\s+relationship\w*|\bstakeholder\s+manage\w*|\bengag[e]?[m]?[e]?[n]?[t]?\s+\w+\s+stakeholder\w*|\bengag[e]?[i]?[n]?[g]?\s+\w+\s+stakeholder\w*|\brelationship\s+stakeholder\w*|\bwork[i]?[n]?[g}?\s+stakeholder\w*|\binternal\s+external\s+stakeholder\w*|\bexternal\s+internal\s+stakeholder\w*|\bact\s+business\s+partner\w*|\bwork[i]?[n]?[g]?\s+\w+\s+stakeholder\w*|\brelationship\s+\w+\s+stakeholder\w*|\bwork\s+\w+\s+\w+\s+stakeholder\w*|\bengag[e]?[m]?[e]?[n]?[t]?\s+\w+\s+\w+\s+stakeholder\w*|\bengag[e]?[i]?[n]?[g]?\s+\w+\s+\w+\s+stakeholder\w*', text)
        match_2 = o(r'\bconsult\w*|\badvis\w*|\badvice\w*|\brelation\w*|\bsupport\w*', text)
        match_3 = o(r'\bengag\w*|\bliais\w*|\bnegotiat\w*', text)
        match_4 = o(r'\bstakeholder\w*', text)
        match_5 = o(r'\binternal\s+stakeholder\w*', text)
        match_6 = o(r'\bexternal\s+stakeholder\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        elif match_5 and match_6:
            matched_fragments.extend([match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'engage with stakeholders'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor compliance with licensing agreements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blicens[e]?[i]?[n]?[g]?\s+regime\w*|\blicens[e]?[i]?[n]?[g]?\s+act\w*|\blicens[e]?[i]?[n]?[g]?\s+law[s]?\b|\blicens[e]?[i]?[n]?[g]?\s+legislation\w*|\blicens[e]?[i]?[n]?[g]?\s+guideline\w*|\blicens[e]?[i]?[n]?[g]?\s+regulation\w*|\blicens[e]?[i]?[n]?[g]?\s+policy\w*|\blicens[e]?[i]?[n]?[g]?\s+policies\w*|\bconsult[i]?[n]?[g]?\s+licens[e]?[i]?[n]?[g]?\w*|\blicens[e]?[i]?[n]?[g]?\s+compliance\w*|\bcompliance\s+licens[e]?[i]?[n]?[g]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor compliance with licensing agreements'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage licensing and consenting agreements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconstent\s+manage\w*|\bdevelop[m]?[e]?[n]?[t]?\s+consent\w*|\bconsenting\s+licensing\w*|\bconsent\s+premission\w*|\bdevelop\s+consent\w*', text)
        match_2 = o(r'\blicensing\w*|\bconsent\w*', text)
        match_3 = o(r'\bmanage\w*|\bconsult\w*', text)
        match_4 = o(r'\bnia\b|\bnic\b', text)
        match_5 = o(r'\benergy\w*|\belectricity\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage licensing and consenting agreements'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply 3D imaging techniques in construction
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b3d\w*', text)
        match_2 = o(r'\bimage\w*|\bscan\w*|\bimaging\w*', text)
        match_3 = o(r'\bconstruction\w*|\bbuild\w*|\bbuilt\w*|\bstrucutre\w*', text)
        match_4 = o(r'\bdimensional\s+survey\w*|\blasser\s+scan[n]?[i]?[n]?[g]?\s+technician\w*|\bdimensional\s+control\s+survey\w*', text)
        match_5 = o(r'\btotal\s+station\w*|\bdgps\b|\bmbes\b|\beiva\b|\brtk\b', text)
        match_6 = o(r'\blaser\b|\bexperience\w*|\buse\w*|\butilise\w*|\bknowledge\w*|\bunderstand\w*|\bfamiliar\w*', text)
        match_7 = o(r'\bdimensional\s+control\w*|\blaser\s+scan\w*', text)
        match_8 = o(r'\bsurvey\w*|dimensional\w*|\bland\s+survey\w*|\bconstruction\w*|\bperform\w*|\bconduct\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        elif match_4 and match_8:
            matched_fragments.extend([match_4, match_8])
        elif match_5 and match_6 and match_8:
            matched_fragments.extend([match_5, match_6, match_8])
        elif match_7 and match_8:
            matched_fragments.extend([match_7, match_8])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply 3D imaging techniques in construction'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for estimate cost of building materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+bid\w*|\bconstruction\s+bid\w*', text)
        match_2 = o(r'\bmanage\w*|\bestimat\w*|\bcontrol\w*|\bplan\w*|\bprepar\w*|\breport\w*|\bmanaging\w*', text)
        match_3 = o(r'\budget\w*|\bcost\w*', text)
        match_4 = o(r'\bconstruction\w*|\bbuild\w*|\bbuilt\w*|\bstrucutre\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'estimate cost of building materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assist in bridge inspection
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbridge\w*', text)
        match_2 = o(r'\binspect\w*|\bquality\w*|\bstructural\w*|\btest\w*|\bassess\w*|\bquality\w*|\banaly\w*|\bcheck\w*|\bstrucutre\s+analy\w*|\bensure\w*|\binspect\w*|\bintegrit\w*|\bstructural\s+engineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assist in bridge inspection'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform metallurgical structural analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmetal\b|\bmetallurgical\b|\bsteel\b|\bjacket\s+structure\w*', text)
        match_2 = o(r'\bstructural\w*|\btest\w*|\bassess\w*|\bquality\w*|\banaly\w*|\bcheck\w*|\bstrucutre\s+analy\w*|\bensure\w*|\binspect\w*|\bintegrit\w*|\bstructural\s+engineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform metallurgical structural analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for check construction compliance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconstruction\s+compliance\w*', text)
        match_2 = o(r'\bstructural\s+design\w*|\bstructural\s+analysis\w*|\bstructural\s+engineer\w*', text)
        match_3 = o(r'\bconstruction\w*|\bbuild\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'check construction compliance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow up pipeline integrity management priorities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'bpipeline\s+integrity\w*|\bintegrity\s+pipe\w*', text)
        match_2 = o(r'\bintegrity\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+integrity\w*|\bintegrity\s+engineer\w*|\bengineer\s+integrit\w*|\bstructural\s+integrity\w*|\bdesign\s+integrity\w*|\basset\s+integrity\w*|\btmij\b|\bmij\b', text)
        match_3 = o(r'\bpipeline\w*|\bpipe\b|\bpipefit\w*|\bpipelay\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow up pipeline integrity management priorities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for reinforce concrete
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breinforc[e]?[i]?[n]?[g]?\s+concrete\w*|\bconcrete\s+strength\w*|\bstrengthen[i]?[n]?[g]?\s+concrete\w*|\bre\s+-\s+inforc[e]?[i]?[n]?[g]?\s+concrete\w*|\benforce\s+concrete\w*|\brc\s+concrete\w*|\brc\s+stealwork\w*|\brc\s+detailing\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'reinforce concrete'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test concrete
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btest[i]?[n]?[g]?\s+concrete\w*|\bconcrete\s+test\w*|\bcheck[i]?[n]?[g]?\s+concrete\w*|\bconcrete\s+check\w*|\bconcrete\s+hardness\w*', text)
        match_2 = o(r'\bconcrete\w*', text)
        match_3 = o(r'\bstructural\s+engineer\w*|\bstructural\s+integrity\w*|\bstrucutral\s+analy\w*', text)
        match_4 = o(r'\bassess\w*|\bquality\w*|\binspect\w*', text)
        match_5 = o(r'\b3d\s+model\w*|\b3\s+-\s+model\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and not match_5:
            matched_fragments.extend([match_2, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test concrete'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design electrical wiring diagrams
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsingle\s+line\s+diagram\w*|\bblock\s+cable\s+diagram\w*|\belectrical\s+installation\w*|\belectrical\s+wiring\w*|\bcircuit\s+diagram\w*|\bcircuit\s+blueprint\w*|\bcircuit\s+schema\w*|\bdiagram\s+circuit\w*|\belectric[a]?[l]?\s+diagram\w*|\belectric[a]?[l]?\s+schemat\w*|\belectronic\s+diagram\w*|\belectronic\s+schemat\w*|\belementary\s+diagram\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\binstall\w*', text)
        match_3 = o(r'\belectrical\w*|\bcable\b|\bwiring\w*|\belectric\w*|\bcircuit\b', text)
        match_4 = o(r'\bdiagram\b', text)
        match_5 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\binstall\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design electrical wiring diagrams'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for draft or read functional design specifications
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign\s+specification\w*|\bfds\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'draft or read functional design specifications'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for draft or read user requirements specification
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\buser\s+requirement[s]?\s+specification\w*|\brequirement\s+specification\w*|\burs\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'draft or read user requirements specification'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for write specifications
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwrit[e]?[i]?[n]?[g]?\s+specification\w*|\bspecification\s+writ[e]?[i]?[n]?[g]?\b|\bdevelop\s+specification\w*|\bproduct\s+specification\w*|\bservice\s+specification\w*|\bspecification\s+document\w*|writ[e]?[i]?[n]?[g]?\s+\w+\s+specification\w*|\bdevelop\s+\w+\s+specification\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'write specifications'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for write batch record documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbatch\s+record\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'write batch record documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design data center
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+centre\w*|\bdatum\s+centre\w*|\bdata\s+center\w*|\bdatum\s+center\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bconstruct\w*|\barchitect\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design data center'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design drainage well systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrainage\s+design\w*|\bdrainage\s+plan\w*|\bdrainage\s+strategy\w*|\bdesign[i]?[n]?[g]?\s+drainage\w*|\bdrainage\s+model\w*|\bmodel[l]?[i]?[n]?[g]?\s+drainage\w*', text)
        match_2 = o(r'\bdrainage\w*|\bportable\s+foul\b|\bfoul\s+system\w*|\bfoul\s+water\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b', text)
        match_4 = o(r'\bmicrodrainage\w*|\bmicro\s+drainage\w*', text)
        match_5 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design drainage well systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install and maintain drainage well systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassess[i]?[n]?[g]?\s+drainage\w*|\bdrainage\s+assess\w*', text)
        match_2 = o(r'\bdrainage\w*', text)
        match_3 = o(r'\binstall\w*|\bmaintain\w*|\bmaintenance\w*|\brepair\w*|\binfrastrucutre\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install and maintain drainage well systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for liaise with local authorities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blocal\s+authority\w*|\bregional\s+authority\w*', text)
        match_2 = o(r'\bliais\w*|\bconsult\w*|\badvise\w*|\badvice\w*|\badvisor\w*|\badviser\w*|\bcommunicat\w*|\bnegotiat\w*|\bcontact\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'liaise with local authorities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design water infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+water\s+infrastructure\w*|\bdesign[i]?[n]?[g]?\s+wastewater\s+infrastructure\w*|\bdesign[i]?[n]?[g]?\s+waste\s+water\s+infrastructure\w*|\bdesign[i]?[n]?[g]?\s+water\s+wastewater\s+infrastructure\w*|\bdesign[i]?[n]?[g]?\s+wastewater\s+water\s+infrastructure\w*|\bwater\s+infrastructure\s+design\w*|\bwastewater\s+infrastrcutre\s+design\w*|\bwaste\s+water\s+infrastructure\s+design\w*|\bdesign[i]?[n]?[g]?\s+construction\s+water\w*|\bconstruction\s+design[i]?[n]?[g]?\s+water\w*', text)
        match_2 = o(r'\bwater\s+infrastructure\w*|\bwastewater\s+infrastructure\w*|\bwaste\s+water\s+infrastructure\w*', text)
        match_3 = o(r'\bstorage\s+tank\w*|\bpump[i]?[n]?[g]?\s+system\w*|\bpump[i]?[n]?[g]?\s+station\w*|\bpipe\s+network\w*|\bpiping\w*|\bpipe[s]?\b', text)
        match_4 = o(r'\bwater\w*|\bwaste\s?water\w*', text)
        match_5 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b', text)
        match_6 = o(r'\bengineer\b|\btechnician\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_5 and match_6:
            matched_fragments.extend([match_2, match_5, match_6])
        elif match_3 and match_4 and match_5 and match_6:
            matched_fragments.extend([match_3, match_4, match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design water infrastructure'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design water networks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+network\w*|\bwaste\s+water\s+network\w*|\bwastewater\s+network\w*|water\s+treatment\s+project\w*|water\s+distribution\w*|water\s+transportation\w*|\btransportation\s+water\w*|\btransportation\s+waste\w+water\w*|\btransportation\s+wastewater\w*|\bdistribution\s+water\w*|\bdistribution\s+waste\w+water\w*|\bdistribution\s+wastewater\w*|water\s+distribution\w*', text)
        match_2 = o(r'\bcreate\w*|\bdesign\b|\bdesigner\b|\bdesigning\b|\bplan\w*|\bconstruct\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design water networks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for set up water pump
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+pump\w*|\bpump\s+equipment\w*|\bpump\s+pump\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'set up water pump'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate radio equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperat[e]?[i]?[n]?[g]?\s+radio\w*|\bradio\s+operat\w*|\bradio\s+equipment\w*|\bradio\s+device\w*', text)
        match_2 = o(r'\bcaa\b|\bvhf\b|\blf\b|\bmf\b|\bhf\b', text)
        match_3 = o(r'\bradio\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate radio equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform tool box talks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btool\s+box\s+talk\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform tool box talks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for undertake procedures to meet helicopter flight requirements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhelicopter\s+fuel\w*|\bhelicopter\s+ueling\w*|\bhelicopter\s+landing\s+officer\w*|\bhelicopter\s+flight\s+procedure\w*|\bprocedure\s+helicopter\w*|\bhelicopter\s+procedure\w*', text)
        match_2 = o(r'\bhelicopter\w*', text)
        match_3 = o(r'\blanding\w*|\bland\b|\bflight\b|\blift\s+off\w*|\blift-off\w*|\blift\s+-\s+off\w*|\bliftoff\w*', text)
        match_4 = o(r'\bprocedure\w*|\bguideline\w*|\bflight\s+requirement\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'undertake procedures to meet helicopter flight requirements'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for direct rigging equipment operators
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeck\s+foreman\w*|\bdirect[i]?[n]?[g]?\s+rigging\w*|\bdirect[i]?[n]?[g]?\s+lifting\w*|\bguid[e]?[i]?[n]?[g]?\s+rigging\w*|\bguid[e]?[i]?[n]?[g]?\s+lifting\w*', text)
        match_2 = o(r'\blifting\w*|\brigging\w*', text)
        match_3 = o(r'\bguide\w*|\bguidance\w*|\bsupport\w*|\bdirect\b|\bdirecting\b', text)
        match_4 = o(r'\bequipment\w*|\btool\w*|\bcrane\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'direct rigging equipment operators'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for determine cargo loading sequence
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeck\s+foreman\w*', text)
        match_2 = o(r'\boffload\w*|\bbackload\w*', text)
        match_3 = o(r'\bcargo\w*|\boffload\w*|\bbackload\w*', text)
        match_4 = o(r'\bload[i]?[n]?[g]?\s+sequence\w*|\bload[i]?[n]?[g]?\s+plan\w*|\bload[i]?[n]?[g]?\s+strateg\w*|\befficient\s+load[i]?[n]?[g]?\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'determine cargo loading sequence'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage air quality
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bair\s+test\w*|\btest[i]?[n]?[g]?\s+air\w*|\bsampl[e]?[i]?[n]?[g]?\s+air\w*|\bair\s+sampl[e]?[i]?[n]?[g]?\w*', text)
        match_2 = o(r'\bair\s+quality\w*|\bquality\s+air\w*', text)
        match_3 = o(r'\bmanage\w*|\btest\w*|\bassess\w*|\bmonitor\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage air quality'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create digital content
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+content\s+creator\w*|\bcreat[e]?[i]?[n]?[g]?\s+digital\s+content\w*', text)
        match_2 = o(r'\bdigital\s+content\w*|\bdigital\s+production\w*', text)
        match_3 = o(r'\bvlog\b|\bvlogging\b|\bvideo\b|\banimation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create digital content'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for meet expectations of target audience
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btarget\s+audience\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'meet expectations of target audience'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop animations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+animation\w*|\bdevelop[i]?[n]?[g]?\s+animation\w*', text)
        match_2 = o(r'\bdigital\s+content\w*|\bdigital\s+production\w*', text)
        match_3 = o(r'\banimation\w*|\banimated\s+model\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop animations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform surge analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsurge\s+analy\w*|\banalyse\s+surge\w*|\banalysis\s+surge\w*|\banalyze\s+surge\w*|\bsurge\s+engineer\w*', text)
        match_2 = o(r'\bsurge\w*', text)
        match_3 = o(r'\bhydraulic\w*|\bpipe\b|\bpipeline\w*|\bwater\w*|\bwastewater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform surge analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use common data environment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcommon\s+data\s+environment\w*|\bcommon\s+datum\s+environment\w*', text)
        match_2 = o(r'\bcde[s]?\b', text)
        match_3 = o(r'\bbim\b|\bbuild[i]?[n]?[g]?\s+information\s+model\w*|\bcad\b|\bcadd\b|\bautocad\b|\bconstruction\w*|\barchitect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use common data environment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage medical records
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhealth\s+technican\w*|\bmedical\s+record\w*|\bmedical\s+data\b|\bmedical\s+datum\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage medical records'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for review patient medical data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breview[i]?[n]?[g]?\s+patient\w*|\bpatient\s+review\w*|\bassess[i]?[n]?[g]?\s+patient\w*|\bmedical\s+datum\w*|\bmedical\s+information\w*|\bmedical\s+data\b|\bhealth\s+technician\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'review patient medical data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply teaching strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bteaching\s+strategy\w*|\bassist[i]?[g]?[g]?\s+teach\w*|\bteach[i]?[n]?[g]?\s+session\w*|\bteach[i]?[n]?[g]?\s+assist\w*|\bteach[i]?[n]?[g]?\s+people\w*|\bteach[i]?[n]?[g]?\s+student\w*|\bteach[i]?[n]?[g]?\s+pupil\w*|\bsupervise\s+student\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply teaching strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calculate safety integrity levels
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsil\b|\bsafe[t]?[y]?\s+integrity\s+level\w*', text)
        match_2 = o(r'\bhazard\w*|\brisk\w*|\bsafety\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calculate safety integrity levels'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use safety instrumented systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsis\b|\bsafe[t]?[y]?\s+instrument[e]?[d]?\s+system\w*|\bsafe[t]?[y]?\s+instrument[a]?[t]?[i]?[o]?[n]?\s+system\w*', text)
        match_2 = o(r'\bhazard\w*|\brisk\b|\bsafety\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use safety instrumented systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for determine fire risk
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfire\s+risk\w*|\brisk\s+fire\w*|\bseverity\s+fire\w*|\bfire\s+severity\w*|\bmodel[l]?[i]?[n]?[g]?\s+fire\w*|\bfire\s+model\w*|\bprobability\s+fire\w*|\bfire\s+probability\w*|\bfire\s+control\w*|\bcontrol[l]?[i]?[n]?[g]?\s+fire\w*|\bpredict[i]?[n]?[g]?\s+\w+\s+fire\w*|\bmodel[l]?[i]?[n]?[g]?\s+\w+\s+fire\w*', text)
        match_2 = o(r'\bfire\b', text)
        match_3 = o(r'\brisk\s+assess\w*|\bassess[i]?[n]?[g]?\s+risk\w*|\bhazop\w*|\bhazid\w*|\brisk\s+control\w*|\bcontrol[l]?[i]?[n]?[g]?\s+risk\w*|\bhazard\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'determine fire risk'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use manual draughting techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdraughting\b', text)
        match_2 = o(r'\bmanual\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use manual draughting techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create prototype for user experience design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bux\s+design\w*|\buser\s+experience\s+design\w*', text)
        match_2 = o(r'\bux\b|\buser\s+experience\w*', text)
        match_3 = o(r'\bprogramming\w*|\bcode\b|\bdeveloper\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create prototype for user experience design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design lighting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blighting\s+material\w*|\blighting\s+system\w*|\bdesign[i]?[n]?[g]?\s+light\w*|\blighting\s+design\w*|\bdialux\b', text)
        match_2 = o(r'\blighting\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\barchitect\w*', text)
        match_4 = o(r'\belectric\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design lighting'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design quality assurance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+assurance\w*|\bassurance\s+design\w*|\bqa\s+design\w*|\bdesign[i]?[n]?[g]?\s+quality\s+assurance\w*|\bdesing[i]?[n]?[g]?\s+qa\b|\bengineer[i]?[n]?[g]?\s+assurance\w*|\bengineer[i]?[n]?[g]\s+quality\s+assurance\w*|\bengineer[i]?[n]?[g]?\s+qa\b|\bassurance\s+engineer\w*|\bqa\s+engineer\w*|\bproduct\s+reliability\w*|\bproduct\s+safety\w*|\bdesign\s+verification\w*|\bverify[i]?[n]?[g]?\s+design\w*|\bdesign\s+validat\w*|\bvalidat[e]?[i]?[n]?[g]?\s+design\w*|\bproduct\s+develop[m]?[e]?[n]?[t]?\s+quality\w*|\bproduct\s+integrity\s+assurance\w*|\bdesign[i]?[n]?[g]?\s+process\s+assurance\w*|\bdesign[i]?[n]?[g]?\s+\w+\s+assurance\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design quality assurance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform factory acceptance test
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfat\b', text)
        match_2 = o(r'\bcommission\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform factory acceptance test'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform site acceptance test
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsite\sacceptance\s+test\w*', text)
        match_2 = o(r'\bsat\b', text)
        match_3 = o(r'\bcommission\w*|\bsite\b|\bquality\w*|\btest\w*|\bperform\w*|\bconduct\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform site acceptance test'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop digital strategy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+strategy\w*|\bdigital\s+workflow\w*|\bdigital\s+consultan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop digital strategy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform end to end testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcypress\b|\bjest\b|\be2e\b|\bend2end\b|\bend\s+2\s+end\w*|\bend\s?-2\s?-\s?end\w*', text)
        match_2 = o(r'\bsupply\s+chain\w*', text)
        if match_1 and not match_2:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform end to end testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor disposal of radioactive substances
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnuclear\s+material\w*|\bradioactive\s+substance\w*|\bnuclear\s+substance\w*|\bnuclear\s+material\w*|\bnuclear\s+fuel\w*', text)
        match_2 = o(r'\bradioactive\w*|\bnuclear\w*', text)
        match_3 = o(r'\bwaste\w*|\bresidue\w*|\bcontaminant\w*|\bfallout\w*|\bbproduct\w*|\bdebri\w*', text)
        match_4 = o(r'\bdisposal\w*|\bdispose\b|\bwaste\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+waste\w*|\bremove\w*|\bremoval\w*', text)
        if match_1 and match_4:
            matched_fragments.extend([match_1, match_4])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor disposal of radioactive substances'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop technology strategy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnology\s+strategy\w*|\btechnological\s+strategy\w*|\bstrategy\s+technolog\w*|(?<!information )\btechnology\s+manage\w*|\btechnological\s+manage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop technology strategy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage engineering project
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+engineer\s+project\w*|\bcoordinat[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+project\w*|\bordinat[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+project\w*|\bmonitor[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+project\w*|\boversee\s+engineer[i]?[n]?[g]?\s+project\w*|\bsupervis[e]?[i]?[n]?[g]?\s+engineer[i]?[n]?[g]?\s+project\w*|\bengineer[i]?[n]?[g]?\s+project\s+manage\w*|\bengineer[i]?[n]?[g]?\s+project\s+supervis\w*|\bengineer[i]?[n]?[g]?\s+project\s+oversight\w*|\bengineer[i]?[n]?[g]?\s+project\s+oversee\w*|\bengineer[i]?[n]?[g]?\s+project\s+manage\w*|\bengineer[i]?[n]?[g]?\s+project\s+coordinat\w*|\bengineer[i]?[n]?[g]?\s+project\s+co\s+-\s+ordinat\w*|\bengineer[i]?[n]?[g]?\s+project\s+co-ordinat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage engineering project'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for employ habitat survey techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhabitat\w*|\bhab\s+survey\w*', text)
        match_2 = o(r'\bsurvey\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'employ habitat survey techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage acquatic habitat
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarine\s+habitat\w*|\bfreshwater\s+habitat\w*|\bwater\s+habitat\w*|\bsea\s+habitat\w*|\bocean\s+habitat\w*|\bacquatic\s+habitat\w*', text)
        match_2 = o(r'\bmanage\w*|\btend\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage acquatic habitat'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for negoatiate land acqusition (land referencing)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnegotiat[e]?[i]?[o]?[n]?\s+land\w*|\bland\s+negotiat\w*|\blandowner\s+negotiat\w*|negotiat[e]?[i]?[o]?[n]?\s+lease\w*|\blease\s+negotiat\w*|\bland\s+erencing\w*|\bland\s+reference\w*|\bland\s+acquisition\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'negoatiate land acqusition (land referencing)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor workload
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonitor[i]?[n]?[g]?\s+workload\w*|\bworkload\s+monitor\w*|\bmanag[e]?[i]?[n]?[g]?\s+workload\w*|\bworkload\s+manage\w*|\bassess[i]?[n]?[g]?\s+workload\w*|\bworkload\s+assess\w*|\bcontrol[l]?[i]?[n]?[g]?\s+workload\w*|\bworkload\s+control\w*|\bmanag[e]?[i]?[n]?[g]?\s+volume\s+work\w*|\bmanag[e]?[i]?[n]?[g]?\s+work\s+volume\w*|\bassess[i]?[n]?[g]?\s+work\s+volume\w*|\bassess[i]?[n]?[g]?\s+volume\s+work\w*|\bcontrol[l]?[i]?[n]?[g]?\s+work\s+volume\w*|\bcontrol[l]?[i]?[n]?[g]?\s+volume\s+work\w*|\bmonitor[i]?[n]?[g]?\s+work\s+volume\w*|\b[i]?[n]?[g]?\s+volume\s+work\w*|\bmonitor[i]?[n]?[g]?\s+load\s+work\w*|\bmonitor[i]?[n]?[g]?\s+work\s+load\w*|\bmanag[e]?[i]?[n]?[g]?\s+work\s+load\w*|\bmanag[e]?[i]?[n]?[g]?\s+load\s+work\w*|\bassess[i]?[n]?[g]?\s+work\s+load\w*|\bassess[i]?[n]?[g]?\s+load\s+work\w*|\bcontrol[l]?[i]?[n]?[g]?\s+work\s+load\w*|\bcontrol[l]?[i]?[n]?[g]?\s+load\s+work\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor workload'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage contract disputes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontract[u]?[a]?[l]?\s+dispute\w*', text)
        match_2 = o(r'\bmatter[s]?\s+dispute\w*|\bdispute\s+matter\w*|\bdispute\s+between\w*|\bdispute\s+resolution\w*|\bmanag[e]?[i]?[n]?[g]?\s+dispute\w*|\bresolv[e]?[i]?[n]?[g]?\s+dispute\w*|\bmatter[s]?\s+\s+\w+\s+dispute\w*|\bdispute\s+\w+\s+matter\w*|\bdispute\s+\w+\s+between\w*|\bdispute\s+\w+\s+resolution\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+dispute\w*|\bresolv[e]?[i]?[n]?[g]?\s+\w+\s+dispute\w*', text)
        match_3 = o(r'\bcontract\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage contract disputes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct energy audit
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+audit\w*|\baudit\s+energy\w*|\bassess[i]?[n]?[g]?\s+energy\w*|\banalysis\s+energy\w*|\banalys[e]?[i]?[n]?[g]?\s+energy\w*|\banalyz[e]?[i]?[n]?[g]?\s+enegy\w*|\benergy\s+efficiency\s+assess\w*|\benergy\s+efficiency\s+monitor\w*|\benergy\s+efficiency\s+analy\w*|\benegry\s+efficiency\s+audit\w*|\benergy\s+consumption\s+analy\w*|\benergy\s+consumption\s+assess\w*|\benergy\s+consumption\s+audit\w*|\bload\s+flow\s+fault\s+study\w*|\bload\s+flow\s+study\w*|\benergy\s+consumption\s+monitor\w*|\benergy\s+performance\s+audit\w*|\benergy\s+performance\s+assess\w*|\benergy\s+performance\s+analy\w*|\benergy\s+performance\s+monitor\w*|\benergy\s+\w+\s+audit\w*|\benergy\s+\w+\s+analy\w*|\benergy\s+\w+\s+assess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct energy audit'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan facilities management policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfacilities\s+management\w*', text)
        match_2 = o(r'\bstrategy\w*', text)
        match_3 = o(r'\bfacilities\w*', text)
        match_4 = o(r'\bfm\s+consultan\w*|\bfm\s+advice\w*|\bfm\s+advisor\w*|\bfm\s+adviser\w*|\bfm\s+performance\w*|\bfm\s+benchmark\w*|\bfm\s+policies\w*|\bfm\s+policy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan facilities management policies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform ground maintenance activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgardening\w*|\bground\s+maintenance\w*|\blandscap[e]?[i]?[n]?[g]?\s+maintenance\w*|\bmaintain\s+ground\w*', text)
        match_2 = o(r'\bgardening\w*', text)
        match_3 = o(r'\bmaintenance\w*|\bmaintain\w*|\bactivity\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform ground maintenance activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use and maintain gardening equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgardening\s+tool\w*|\bgardening\s+equipment\w*|\bequipment\s+garden\w*|\bgarden\s+tool\w*|\btool[s]?\s+garden\w*|\bland\s+mower\w*|\bstrimmer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use and maintain gardening equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle mail
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmail\s+activity\w*|\bhandl[e]?[i]?[n]?[g]?\s+letter\w*|\bcollect[i]?[n]?[g]?\s+letter\w*|\bcollect[i]?[n]?[g]?\s+post\b|\baudio\s+letter\w*|\bletter\s+label\w*|\bcollect[i]?[n]?[g]?\s+mail\w*|\bcollect[i]?[n]?[g]?\s+parcel\w*|\bhandl[e]?[i]?[n]?[g]?\s+parcel\w*', text)
        match_2 = o(r'\bletter\w*|\bparcel\w*|\bmail\b', text)
        match_3 = o(r'\boffice\s+administrat\w*|\boffice\s+admin\w*|\badministrative\s+dut[y]?[i]?[e]?[s]?\b|\badministration\s+dut[y]?[i]?[e]?[s]?\b|\bgeneral\s+admin\w*|\bgeneral\s+administrat\w*|\boffice\s+clerk\w*|\bclerk\s+activit\w*|\bfile\s+file[s]?\b|\bfiling\s+file[s]?\w*|\bfile\s+document\w*|\badministrative\s+support\w*|\badministrative\s+assistant\w*|\badmin\s+assist\w*|\bassist\s+office\w*|\boffice\s+assist\w*|\bcommercial\s+administrator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle mail'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage cost of quality
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcost[s]?\s+quality\w*|\bquality\s+cost\w*|\bprevention\s+cost\w*|\bpreventive\s+cost\w*|\bcost[s]?\s+prevent\w*|\bappraisal\s+cost\w*|\bappraisal\s+engineer\w*|\bcost[s]?\s+appraisal\w*|\binternal\s+failure\s+cost\w*|\bcost[s]?\s+internal\s+failure\w*|\bexternal\s+failure\s+cost\w*|\bcost\s+external\s+failure\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage cost of quality'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct erosion control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfcerm\b|\berosion\s+process\w*|\berosion\s+control\w*|\berosion\s+manage\w*|\berosion\s+assess\w*|\berosion\s+damage\w*|\berossion\s+monitor\w*|\bcontrol[l]?[i]?[n]?[g]?\s+erosion\w*|\bmanag[e]?[i]?[n]?[g]?\s+erosion\w*|\bassess[i]?[n]?[g]?\s+erosion\w*|\bmonitor[i]?[n]?[g]?\s+erosion\w*|\berosion\s+risk\w*|\bflood\s+coastal\s+erosion\s+risk\s+management\w*', text)
        match_2 = o(r'\berosion\w*', text)
        match_3 = o(r'\bwater\w*|\benvironment\w*|\bmarine\w*|\bsea\w*|\bocean\b|\blake\b|\briver\b', text)
        match_4 = o(r'\bmanage\w*|\bassess\w*|\banaly\w*|\bmonitor\w*|\brisk\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct erosion control'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct sediment control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeomorpholog\w*|\bsediment\s+control\w*|\bsediment\s+manage\w*|\bsediment\s+assess\w*|\bsediment\s+process\w*|\bsediment\s+monitor\w*|\bmanag[e]?[i]?[n]?[g]?\s+sediment\w*|\bcontrol[l]?[i]?[n]?[g]?\s+sediment\w*|\bmonitor[i]?[n]?[g]?\s+sediment\w*|\bassess[i]?[n]?[g]?\s+sediment\w*|\bsediment\s+transport\w*|\btransport[i]?[n]?[g]?\s+sediment\w*', text)
        match_2 = o(r'\bsediment\w*', text)
        match_3 = o(r'\bmarine\w*|\bmaritime\w*|\bcoastal\w*|\bsea\b|\bocean\b|\blake\b|\briver\b', text)
        match_4 = o(r'\bmanage\w*|\banaly\w*|\bassess\w*|\bmonitor\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct sediment control'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform seabed mapping
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseabed\s+map\w*|\bmap[p]?[i]?[n]?[g]?\s+seabed\w*', text)
        match_2 = o(r'\bseabed\w*', text)
        match_3 = o(r'\btopograph\w*|\bgeomorpholog\w*|\btotal\s+station\w*|\blaser\w*|\bdgps\w*|\bpositioning\w*|\brtk\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform seabed mapping'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow health and safety procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhealth\s+safety\w*|\bfit\s+safety\w*|\bfitness\s+safety\w*|\bhealthy\s+safe\w*|\bsafe\s+health\w*|\bsafe\s+manner\w*|\bsafe\s+working\s+practice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow health and safety procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design signalling systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsignal[l]?[i]?[n]?[g]?\s+design\w*|\bsignal[l]?[i]?[n]?[g]?\s+engineer\w*|\bsignalling\s+design\w*|\bsignalling\s+survey\w*|\bsignalling\s+\w+\s+survey\w*', text)
        match_2 = o(r'\bsignal\w*|\bassess\w*|\bdesign\w*|\bdevelop\w*', text)
        match_3 = o(r'\birse\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design signalling systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for purchase supplies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpurchas[e]?[i]?[n]?[g]?\s+supply\w*|\bsupply\s+purchase\w*|\bpurchas[e]?[i]?[n]?[g]?\s+material\w*|\bmaterial[s]?\s+purchase\w*|\bpurchas[e]?[i]?[n]?[g]?\s+good\w*|\bgood[s]?\s+purchase\w*|\bpurchas[e]?[i]?[n]?[g]?\s+supplies\w*|\bsupplie[s]?\s+purchase\w*|\bacquir[e]?[i]?[n]?[g]?\s+supplies\w*|\bacquir[e]?[i]?[n]?[g]?\s+supply\w*|\bbuy[i]?[n]?[g]?\s+supply\w*|\bsupply\s+buy\w*|\bbuy[i]?[n]?[g]?\s+supplies\w*|\bsupplie[s]?\s+buy\w*|\bgain[i]?[n]?[g]?\s+supplies\w*|\bbuy[i]?[n]?[g]?\s+supply\w*', text)
        match_2 = o(r'\bpurchaser\w*', text)
        match_3 = o(r'\bsupply\w*|\bsupplier\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'purchase supplies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle incidents
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhandl[e]?[i]?[n]?[g]?\s+incident\w*|\bincident\s+response\w*|\bdeal[i]?[n]?[g]?\s+incident\w*|\bresponse\s+incident\w*|\bmanag[e]?[i]?[n]?[g]?\s+incident\w*|\bincident\s+manage\w*|\bcontrol[l]?[i]?[n]?[g]?\s+incident\w*|\bincident\s+control\w*|\binvestigat[e]?[i]?[n]?[g]?\s+incident\w*|\bincident\s+investigat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle incidents'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform ground investigation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bground\s+investigat\w*|\bground\s+survey\w*|\binvestigat[e]?[i]?[n]?[g]?\s+ground\w*|\bsurvey[i]?[n]?[g]?\s+ground\w*', text)
        match_2 = o(r'\bground\w*|\bsoil\w*|\brock\w*', text)
        match_3 = o(r'\bsite\s+investigation\w*|\bsite\s+assess\w*|\bsite\s+survey\w*', text)
        match_4 = o(r'\bsurvey\w*', text)
        match_5 = o(r'\benvironment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5:
            matched_fragments.extend([match_2, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform ground investigation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and prepare ground
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgroundwork\w*|\bprepare\s+ground\w*|\bclear\s+ground\w*|\bground\s+prepar\w*|\blandscape\s+maintenance\w*|\bmaintain[i]?[n]?[g]?\s+landscape\w*|\bmaintain[i]?[n]?[g]?\s+ground\w*|\bground\s+engineer\w*|\bground\s+investigat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and prepare ground'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design metal components
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmetal\s+component\w*|\bmetal\s+tool\w*|\bmetal\s+equipment\w*|\bmetal\s+part\w*|\bmetallurgical\s+component\w*|\bmetallurgical\s+tool\w*|\bmetallurgical\s+equipment\w*|\bmetallurgical\s+part\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bcad\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design metal components'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use cmm machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcmm\s+machine\w*|\bmachine\s+cmm\b|\bfaro\s+arm\b', text)
        match_2 = o(r'\bmeasure\w*|\bmeasuring\w*|\bmachine\w*', text)
        match_3 = o(r'\bcmm\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use cmm machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare and use technical documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+document\w*|\btechnical\s+specification\w*|\btechnical\s+file\w*|\btechnical\s+paperwork\w*', text)
        match_2 = o(r'\bproduct\s+life\s+cycle\w*|\bproject\s+life\s+cycle\w*|\bproduct\s+\w+\s+life\s+cycle\w*|\bproduct\s+\w+\s+life\s+cycle\w*|\burs\b|\bfds\b|\bhds\b|\bsds\b', text)
        match_3 = o(r'\bdocument\w*|\bpaperwork\w*|\breport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare and use technical documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design and model hardware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign[i]?[n]?[g]?\s+hardware\w*|\bmodel[l]?[i]?[n]?[g]?\s+hardware\w*|\bsimulat[e]?[i]?[n]?[g]?\s+hardware\w*|\bhardware\s+simulat\w*|\bhardware\s+engineer\w*|\bhadrware\s+design\w*|\bhardware\s+model\w*|\bhardware\s+specify\w*|\bspecify[i]?[n]?[g]?\s+hardware\w*|\bhandover\s+\w+\s+hardware\w*|\bspecify[i]?[n]?[g]?\s+\w+\s+hardware\w*|\bhardware\s+\w+\s+engineer\w*|\bdesign[i]?[n]?[g]?\s+\w+\s+hardware\w*|\bmodel[l]?[i]?[n]?[g]?\s+\w+\s+hardware\w*|\bsimulat[e]?[i]?[n]?[g]?\s+\w+\s+hardware\w*', text)
        match_2 = o(r'\bhardware\w*', text)
        match_3 = o(r'\bengineer\w*|\bpid\b|\bpfd\b|\bcad\b', text)
        match_4 = o(r'\bdesign\b|\bdesigning\b|\bdesigner\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design and model hardware'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain logbooks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blogbook[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain logbooks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide care
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcare\s+assistant\w*|\bprovid[e]?[i]?[n]?[g]?\s+care\w*|\bprovid[e]?[i]?[n]?[g]?\s+\w+\s+care\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide care'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for counsel clients
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstrategic\s+counsel\w*', text)
        match_2 = o(r'\bcounsel\b|\bcounseling\w*', text)
        match_3 = o(r'\bclient\w*|\bcustomer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'counsel clients'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for negotiate sales contracts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontract\s+negotiation\w*|\bnegotiate\s+commercial\s+agreement\w*|\bnegotiate\s+price\w*|\bcommercial\s+negotiat\w*', text)
        match_2 = o(r'\bnegotiation\b|\bnegotiate\b', text)
        match_3 = o(r'\bsale\w*|\bcontract\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'negotiate sales contracts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for negotiation skill
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnegotiation\s+skill\w*|\bnegotiating\s+skill\w*|\bability\s+negotiate\w*|\bnegotiate\s+effectively\w*|\bperform\s+negotiation\w*|\bnegotiate\s+customer\w*|\bnegotiate\s+client\w*|\bnegotiate\s+term\w*|\bnegotiate\s+condition\w*|\bnegotiate\s+claim\w*', text)
        match_2 = o(r'\bnegotiat\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'negotiation skill'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide legal support
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blegal\s+advice\w*|\blegal\s+support\w*|\blegal\s+help\w*|\blegal\s+expertise\w*|\blegal\s+guidance\w*|\bsupport\s+legal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide legal support'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sell technical or scientific products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+sale\w*|\bclinical\s+specialist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'sell technical or scientific products'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor delivery of products or services
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonitor\s+delivery\w*|\bmonitor\s+shipment\w*|\bsupervise\s+delivery\w*|\bsupervise\s+shipment\w*|\bmanag[e]?[i]?[n]?[g]?\s+delivery\w*|\bmanag[e]?[i]?[n]?[g]?\s+shipment\w*|(?<!project )delivery\s+manage\w*|\bshipment\s+manage\w*|\bdelivery\s+monito\w*|\bshipment\s+monito\w*|\bdelivery\s+supervis\w*|\bshipment\s+supervis\w*|\btimely\s+delivery\w*|\btimely\s+shipment\w*|\bdelivery\s+time\w*|\bshipment\s+time\w*|\bschedule\s+delivery\w*|\bschedule\s+shipment\w*|\bdelivery\s+schedul\w*|\bshipment\s+schedul\w*|\bassist\s+delivery(?! project| strategy| plan| commercial| contract)\w*|\bassist\s+shipment\w*|\barrange\s+delivery\w*|\barrange\s+shipment\w*|\barrange\s+pickup\w*|\bdelivery\s+assist\w*|\bshipment\s+assist\w*|\btransit\s+process\w*|\bshipment\s+process\w*|\bdelivery\s+document\w*|\bshipping\s+document\w*|\btransit\s+document\w*|\bdelivery\s+process\w*|\bdelivery\s+engineer\b', text)
        match_2 = o(r'\btransport[a]?[t]?[i]?[o]?[n]?\s+plan\w*|\bmanag[e]?[i]?[n]?[g]?\s+transport\w*|\borganiz[e]?[i]?[n]?[g]?\s+load\b|\borganis[e]?[i]?[n]?[g]?\s+load\b', text)
        match_3 = o(r'\bshipment\w*|\blogistic\w*|\bpacking\w*|\bdelivery\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor delivery of products or services'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for revise legal documents
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blegal\b', text)
        match_2 = o(r'\bdocument\w*', text)
        match_3 = o(r'\brewiew\w*|\bprepare\w*|\bdraft\w*|\bwrite\w*|\bhandle\w*|\bmanage\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'revise legal documents'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure compliance with policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bregulatory\s+compliance\w*|\bregulatory\s+guideline\w*|\bregulatory\s+guidance\w*|\bregulatory\s+oversight\w*|\bregulatory\s+requirement\w*|\bregulatory\s+framework\w*|\bcompliance\s+level\w*|\bmeet\s+compliance\w*|\bsatisfy\s+regulatory\w*|\bmanag[e]?[i]?[n]?[g]?\s+compliance\w*|ensur[e]?[i]?[n]?[g]?\s+compliance\w*|\bcompliance\s+manage\w*|\bsupervise\s+compliance\w*|\bcompliance\s+supervi\w*|\blegislative\s+compliance\w*|\blegislation\s+complaince\w*|\bcompliance\s+regulat\w*|\bquality\s+compliance\w*|\bregulatory\s+obligation\w*|\bensur[e]?[i]?[n]?[g]?\s+relevant\s+policy\w*|\bensur[e]?[i]?[n]?[g]?\s+procedure[s]?\s+follow\w*|\bensur[e]?[i]?[n]?[g]?\s+polic[y]?[i]?[e]?[s]?\s+follow\w*|\bline\s+company\s+policy\w*|\bline\s+company\s+policies\b|\brelevant\s+policy\s+procedure\w*|\bcompany\s+policy\s+procedure\w*|\bpolicy\s+procedure\s+process\w*|\badher[e]?[i]?[n]?[g]?\s+policy\w*|\bstandard[s]?\s+comply\w*|\bstandard[s]?\s+compliance\w*|\baccordance\s+policy\w*|\bfull\s+compliance\w*|\bensur[e]?[i]?[n]?[g]?\s+complain\w*|\baccording\s+policy\w*|\bregulatory\s+failure\w*|\blegislative\s+requirement\w*|\blegislative\s+guideline\w*|\bdriv[e]?[i]?[n]?[g]?\s+compliance\w*|\bpolicy\s+standard\w*|\bmaintenance\s+compliance\w*|\bmanag[e]?[i]?[n]?[g]?\s+regulatory\s+inspection\w*|\bcomply\s+\w+\s+policy\w*|\bcomply\s+\w+\s+policies\w*|\bcomply\s+\w+\s+legislation\w*|\bcomply\s+\w+\s+regulat\w*|\bcomply\s+\w+\s+procedure\w*|\bcompliance\s+\w+\s+policy\w*|\bcompliance\s+w+\s+policies\w*|\bcompliance\s+\w+\s+legislation\w*|\bcompliance\s+\w+\s+regulat\w*|\bcompliance\s+\w+\s+procedure\w*|\baccordance\s+\w+\s+policy\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+complian\w*|\baccording\s+\w+\s+policy\badhere\s+\biso\b|\badhere\s+\bfda\b|\badherence\s+\biso\b|\badherence\s+\bfda\b|\biso\b\s+adhre\w*|\bfda\b\s+adhere\w*|\bfda\b\s+compliance\w*|\biso\b\s+compliance\w*|\bethics\s+compliance\w*|\bfpca\b|\bcorruption\s+act\w*|\bpolicy\s+process\w*|\bmaintain\s+compliance\w*|\bapplicable\s+certification\w*|\bapplicable\s+legislation\w*|\bproject[s]?\s+policy\w*|\bproject[s]?\s+policies\w*|\bregulatory\s+specification\w*|\blead\s+assessor\w*|\bcompany\s+policy\w*|\bcompany\s+policies\w*|\bfirm\s+policy\w*|\bfirm\s+policies\w*|\bpolic[y]?[i]?[e]?[s]?\s+constraint\w*|\bensure\s+certificat\w*|\bapplicable\s+\w+\s+legislation\w*|\bapplicable\s+\w+\s+certification\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+procedure[s]?\s+follow\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+polic[y]?[i]?[e]?[s]?\s+follow\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure compliance with policies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply company policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpolicy\s+procedure\w*|\bcompany\s+policy\w*|\badhere\s+policy\w*|\bpolicy\s+adher\w*|\bcompany\s+phliosophy\w*|\bfirm\s+policy\w*|\bfirm\s+procedure\w*|\bcompany\s+procedure\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply company policies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure customs compliance and arrange customs inspections
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexperience\s+custom[s]?\b|\bcustom[s]?\s+experience\w*|\bcustom[s]?\s+clearance\w*|\bcustom[s]?\s+compliance\w*|\bcustom[s]?\s+inspection\w*|\bcustom[s]?\s+operation\w*|\bcustom[s]?\s+procedure\w*|\bletter\s+custom[s]?\b|\btariff[s]?\s+code\w*|\btrad[e]?[i]?[n]?[g]?\s+trariff\w*|\bcustom[s]?\s+document\w*|\bcustom[s]?\s+paperwork\w*|\bend\s?[-]?\s?use\s+paperwork\w*|\bend\s?[-]?\s?use\s+document\w*', text)
        match_2 = o(r'\bcustoms\b', text)
        match_3 = o(r'\blogistic\w*|\bshipment\w*|\btransport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure customs compliance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maximise sales revenues
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bincrease\s+sale\w*', text)
        match_2 = o(r'\bsale\s+maximization\w*|\bmaximize\s+sale\w*|\bsales\s+maximization\w*|\bmaximises\s+sale\w*|\bmaximise\s+sale\w*|\bsale\s+maximisation\w*|\bmaximise\s+earning\w*|\bmaximize\s+earning\w*|\bearning\s+maximi\w*|\bearnings\s+maximi\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2:
            matched_fragments.extend([match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maximise sales revenues'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for meet sales targets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmeet\s+sale\s+target\w*|\bmeet\s+sales\s+target\w*|\breach\s+sale\s+target\w*|\breach\s+sales\s+target\w*|\bachieve\s+sale\s+target\w*|\bachieve\s+sales\s+target\w*|\bmeet\s+sale\s+goal\w*|\bmeet\s+sales\s+goal\w*|\breach\s+sale\s+goal\w*|\breach\s+sales\s+goal\w*|\bachieve\s+sale\s+goal\w*|\bachieve\s+sales\s+goal\w*|\bmeet\s+target\s+sale\w*|\breach\s+target\s+sale\w*|\bachieve\s+target\s+sale\w*', text)
        match_2 = o(r'\bachieve\s+quatterly\s+target\w*|\bachieve\s+annual\s+target\w*|\bachieve\s+quaterlly\s+annual\s+target\w*', text)
        match_3 = o(r'\bsale\w*|\bselling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'meet sales targets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan events
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplan\w*|\borganise\w*|\bconduct\w*|\borganize\w*', text)
        match_2 = o(r'\bevent\w*|\bseminar\w*|\bshow\w*|\broadshow\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan events'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate events
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bevent\b\s+\bbudget\w*|\bevent[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+event\w*|\bcoordinate\b\s+\bevent\w*|\bco\s+-\s+ordinate\s+event\w*|\bordinate\s+event\w*|\bevent\s+coordinat\w*|\bevent\s+co\s+-\s+ordinat\w*|\bevent\s+ordinat\w*|\bordinate\s+\w+\s+event\w*|\bcoordinate\s+\w+\s+event\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate events'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data analysis automation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalytic\s+\bautomation\w*', text)
        match_2 = o(r'\bautomation\w*|\bautomate\w*', text)
        match_3 = o(r'\bdata\s+analy\w*|\bdatum\b\s+analy\w*|\bdatum\s+interpretation\w*|\bdata\s+interpretation\w*|\banalyse\s+data\w*|\banalyse\s+datum\w*|\banalyze\s+data\b|\banalyze\s+datum\w*|\binterpret\s+data\w*|\binterpret\s+datum\w*|\banalysis\s+manager\w*|\banalytics\s+manager\w*|\btechnical\s+analy\w*', text)
        match_4 = o(r'\bperform[i]?[n]?[g]?\s+analy\w*|\bconduct[i]?[n]?[g]?\s+analy\w*|\bcarry\s+analy\w*', text)
        match_5 = o(r'\bdatum\w*|\bdata\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5:
            matched_fragments.extend([match_2, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'data analysis automation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform data analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+analy\w*|\bdatum\b\s+analy\w*|\bdata\s+science\w*|\bdatum\s+science\w*|\bdatum\s+interpretation\w*|\bdata\s+interpretation\w*|\banalyse\s+data\w*|\banalyse\s+datum\w*|\banalyze\s+data\b|\banalyze\s+datum\w*|\binterpret\s+data\w*|\binterpret\s+datum\w*|\banalysis\s+manager\w*|\banalytics\s+manager\w*|\btechnical\s+analy\w*|\banalyse\s+\w+\s+datum\w*|\banalysis\s+\w+\s+datum\w*|\banalyze\s+\w+\s+data\b|\banalyse\s+\w+\s+data\b|\banalysis\s+\w+\s+data\b|\banalyze\s+\w+\s+data\b|\binterpret\s+\w+\s+datum\w*', text)
        match_2 = o(r'\bperform[i]?[n]?[g]?\s+analy\w*|\bconduct[i]?[n]?[g]?\s+analy\w*|\bcarry\s+analy\w*|\banalyse\s+outcome\w*|\banalyze\s+outcome\w*|\binterpret\s+outcome\w*', text)
        match_3 = o(r'\bdata\b|\bdatum\w*|\bdataset\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform data analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for think proactively to secure sales
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badvice\b\s+customer\s+\bproactive\w*|\bproactive[l]?[y]?\s+sale\w*|\bconvince\s+customer\w*|\bconvince\s+client\w*|\bsell\s+proactively\w*|\bproactive[l]?[y]?\s+sell\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'think proactively to secure sales'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for stay up to date with social media
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsocial\b\s+medium\b|\bsocial\b\s+media/b\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'stay up to date with social media'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan social media marketing campaigns
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsocial\b\s+medium\b|\bsocial\b\s+media/b\w*', text)
        match_2 = o(r'\bmarketing\b', text)
        match_3 = o(r'\bsocial\b', text)
        match_4 = o(r'\bmedium\b|\bmedia\b', text)
        match_5 = o(r'\bmarketing\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan social media marketing campaigns'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan messaging services campaign
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bservice\s+messaging\s+campaign\w*|\bmessaging\s+service\s+campaign\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan messaging services campaign'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan telemarketing campaign
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btelemarketing\s+campaign\w*|\btelemarketing\s+plan\w*|\bplan[n]?[i]?[n]?[g]?\s+telemarketing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan telemarketing campaign'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan and promote climate change marketing campaigns
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclimate\s+change\w*', text)
        match_2 = o(r'\bcarbon\w*|\bemission\w*|\bclimate\b', text)
        match_3 = o(r'\bsustianab\w*|\benvironment\w*|\bfootprint\w*', text)
        match_4 = o(r'\bmarketing\s+campaign\w*|\bmarketing\s+plan\w*|\bplan[n]?[i]?[n]?[g]\s+marketing\w*', text)
        if match_1 and match_4:
            matched_fragments.extend([match_1, match_4])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan and promote climate change marketing campaigns'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage budgets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbudget\s+compliance\b|\bmanag[e]?[i]?[n]?[g]?\s+\bbudget\w*|\bbudget+\bmanagement\b|\bbudget\s+control\w*|\bbudget\s+calculat\w*|\bensure\s+budget\w*|\bforecast\s+budget\w*|\bcontrol\s+budget\w*|\boperat[e]?[i]?[n]?[g]?\s+budget\w*|\boversee\s+budget\w*|\bbudget\s+oversight\w*|\bmeet[i]?[n]?[g]?\s+budget\w*|\bovesight\s+budget\w*|\bdetermine\s+financial\s+requirement\w*|\bmonitor[i]?[n]?[g]?\s+budget\w*|\bbudget\s+monitor\w*|\bprepar[e]?[a]?[t]?[i]?[o]?[n]?\s+budget\w*|\ballocate\s+budget\w*|\bbudget\s+allocation\w*|\bbudgetary\s+allocation\w*|\bbudgeting\w*|\bquantity\s+survey\w*|\bcommercial\s+manage\w*|\btimescale\s+budget\w*|\bbudget\s+estimate\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+budget\w*|\bmonitor[i]?[n]?[g]?\s+\w+\s+budget\w*|\breport[i]?[n]?[g]?\s+\w+\s+budget\w*|\bcontrol\s+\w+\s+budget\w*|\bpreparation\s+\w+\s+budget\w*|\bprepare\s+\w+\s+budget\w*|\bmeet[i]?[n]?[g]?\s+\w+\s+budget\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+budget\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage budgets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for finish project within the budget
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagree\s+budget\w*|\bagree\s+upon\s+budget\w*|\bwithin\s+budget\w*|\bmeet[i]?[n]?[g]?\s+budget\w*|\bwithin\s+\w+\s+budget\w*|\bunder\s+budget\w*|\bunder\s+\w+\s+budget\w*|\bmeet[i]?[n]?[g]?\s+\w+\s+budget\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'finish project within the budget'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create marketing budget
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcreat[e]?[i]?[n]?[g]?\s+marketing\s+budget\w*|\bdesign[i]?[n]?[g]?\s+makreting\s+budget\w*|\bdevelop[i]?[n]?[g]?\s+marketing\s+budget\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create marketing budget'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain a central project repository and document project progress
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+document\w*|\bdocument\s+progress\w*|\bdocument\s+project\s+progress\w*|\bproject\s+repository\w*|\bproject\s+repo\b|\bproject\s+progress\w*|\bhandover\s+pid\w*|\bproject\s+initiation\s+document\w*|\bmonitor\s+progress\w*|\breport[i]?[n]?[g]?\s+progress\w*|\bprogress\s+monitor\w*|\bprogress\s+performance\w*|\bperformance\s+status\s+report\w*', text)
        match_2 = o(r'\bproject\s+manager\b|\bproject\s+management\b', text)
        match_3 = o(r'\bprogress\w*|\bstatus\s+report\w*|\brepository\w*|\brepo\b|\breport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain a central project repository and document project progress'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect project regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+regulation\w*', text)
        match_2 = o(r'\bproject\s+manager\w*|\bproject\s+manage\w*|\bproject\s+controller\w*', text)
        match_3 = o(r'\bguidance\w*|\badvise\w*|\badvice\w*|\badvisor\w*|\bconsult\w*', text)
        match_4 = o(r'\bproject[s]?\b', text)
        match_5 = o(r'\bregulation\w*|\blegislation\w*|\bguideline\w*|(?<!grow )(?<!video )\bpolicy\w*|\bcompliance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_5:
            matched_fragments.extend([match_2, match_5])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect project regulations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for research new products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevelop\s+new\s+product\w*|\blaunch\s+product\w*|\blaunch\s+new\s+product\w*|\bdevelop\s+product\w*|\bresearch\s+new\s+product\w*|\bbdm\b|\bbusiness\s+development\s+manage\w*', text)
        match_2 = o(r'\bnpd\b|\bmpis\b|\bnpi\b', text)
        match_3 = o(r'\bresearch\w*|\bproject\w*|\bproduct\b|\bdevelopemnt\w*|\brepair\w*|\bmaintain\w*|\bprocess\w*|\bintroduction\w*|\bplan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'research new products'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for research new ideas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevelop[m]?[e]?[n]?[t]?\s+new\s+idea\w*|\bdevelop[m]?[e]?[n]?[t]?\s+new\s+concept\w*|\bdeveloping\s+new\s+idea\w*|\bdeveloping\s+new\s+concept\w*|\bdevelop\s+innovative\s+idea\w*|\bdevelop\s+new\s+research\w*|\bbring\s+new\s+idea\w*|\bbring\s+innovative\s+idea\w*|\bdevelop\s+idea\w*|\bbring\s+idea\w*|\bresearch\s+new\s+idea\w*|\bresearch\s+innovative\s+idea\w*|\bbdm\b|\bdusiness\s+development\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+improvement\w*|\bimprovement\s+recommendation\w*|\bresearch\s+capability\w*|\bresearch\s+validate\w*|\brd\s+project\w*|\bresearch\s+development\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'research new ideas'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor competition
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonitor\s+competition\w*|\bkeep\s+track\s+competition\w*|\bresearch\s+competition\w*|\bcompetitor\s+activity\w*|\bmonitor\s+competitor\w*|\bknowledge\s+competitor\w*|\bknowledge\s+competition\w*|\bkeep\s+track\s+competitor\w*|\bresearch\s+competitor\w*|\bmonitor\s+online\s+competitor\w*|\bmonitor\s+online\s+competition\w*|\bsupervise\s+competition\w*|\bsupervise\s+competitor\w*|\bcompetitor\s+activity\w*|\bcompetition\s+activity\w*|\bcompetitive\s+landscape\w*|\bcompetition\s+landscape\w*|\bcompetition\s+analy\w*|\bcompetitor\s+analy\w*|\banalse\s+competitor\w*|\banalyse\s+competitor\w*|\banalye\s+competition\w*|\banalyze\s+competitor\w*|\bcompetitive\s+information\w*|\binformation\s+competitor\w*|\bcompetitor\s+information\w*|\bcompetitor\s+datum\w*|\bcompetition\s+datum\w*|\bcompetitor\s+data\w*|\bcompetition\s+data\w*|\bdata\s+competitor\w*|\bdata\s+competition\w*|\bdatum\s+competitor\w*|\bdatum\s+competition\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor competition'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for utilize project management skills
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+\bproject\w*|\bproject\b\s+\bmanagement\w*|\bmanagement\s+project\w*|\bscope\s+definition[s]?\b|\bproject\s+manager\b|\bproject\s+engineer\w*|\bproject\s+controller\w*|\boversee[i]?[n]?[g]?\s+project\w*|\bsite\s+lead\w*|\bprepare\s+project\s+plan\w*|\bproject\s?[/]?\s?engineer[i]?[n]?[g]?\s+manage\w*|\blead[i]?[n]?[g]?\s+project\w*|\bmanag[e]?[i]?[n]?[g]?\s+multiple\s+project\w*|\bproject\s+coordinat\w*|\bcoordinat[e]?[i]?[n]?[g]?\s+project\w*|\bordinat[e]?[i]?[n]?[g]?\s+project\w*|\bproject\s+co\s+-\s+ordinat\w*|\bproject\s+ordinat\w*|\bproject\s+oordinat\w*|\bproject\s+planning\w*|\bproject\s+plan\s+control\w*|\bproject\s+delivery\s+strategy\w*|\bproject\s+-\s+co\s+ordination\w*|\bmanag[e]?[i]?[n]?[g]?\s+multi\s+-\s+disciplinary\s+project\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+project\w*|\blead[i]?[n]?[g]?\s+\w+\s+project\w*|\boversee[i]?[n]?[g]?\s+\w+\s+project\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'utilize project management skills'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adhere to agile project management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagile\s+\bproject\s+\bmanagement\b|\bmanag[e]?[i]?[n]?[g]?\s+agile\w*', text)
        match_2 = o(r'\bproject\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+project\w*', text)
        match_3 = o(r'\bagile\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'adhere to agile project management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan product management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduct\s+\bowner\w*|\bplan\s+product\s+management\w*|\bimplement\s+product\s+management\w*|\bproduct\s+introduction\w*', text)
        match_2 = o(r'\bimplementation\s+plan\w*|\bimplement\s+solution\w*|\bplan\s+implement\w*', text)
        match_3 = o(r'\bproduct\w*|\bstrategy\w*|\bservice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan product management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for implement strategic planning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstrategic\s+plan\w*|\bplan[n]?[i]?[n]?[g]?\s+strategy\w*|\bstrategic\s+think\w*|\bstrategic\s+guidance\w*|\bstrategic\s+advice\w*|\bstrategic\s+analys\w*|\banalysis\s+manager\w*|\banalytics\s+manager\w*|\bproject\s+strateg\w*|\bgrowth\s+strateg\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'implement strategic planning'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for project commissioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+commission\w*|\bcommissioning\s+project\w*|\bcommission\s+project\w*|\bproject\s+engineer\w*|\bproject\s+delivery\w*|\bcommission[i]?[n]?[g]?\s+plan\w*', text)
        match_2 = o(r'\bproject\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+project\w*|\bproject\s+engineer\w*', text)
        match_3 = o(r'\bquality\s+control\w*|\bqc\b|\bcommission\w*|\bquality\s+assurance\b|\bmanag[e]?[i]?[n]?[g]?\s+quality\w*|\bassess\s+quality\w*|\bquality\s+assess\w*|\bensure\s+quality\w*|\bconstruction\s+commission\w*|\bproject\s+multi\s+disciplin[e]?[a]?[r]?[y]?\s+commission\w*|\bproject\s+multi\s+-\s+disciplin[e]?[a]?[r]?[y]?\s+commission\w*|\bproject\s+multidisciplin[e]?[a]?[r]?[y]?\s+commission\w*|\bconstruction\s+\w+\s+commission\w*', text)
        match_4 = o(r'\bknowledge\s+commissioning\w*|\bday\s+-\s+-\s+day\s+commissioning\w*|\bcommission[i]?[n]?[g]?\s+procedure\w*|\bcommission[i]?[n]?[g]?\s+manager\w*|\bcomission[i]?[n]?[g]?\s+activity\w*|\bcommission[i]?[n]?[g]?\s+process\w*|\bcommission[i]?[n]?[g]?\s+engineer\w*|\bcommission[i]?[n]?[g]?\s+support\w*|\bsupport[i]?[n]?[g]?\s+commission\w*', text)
        match_5 = o(r'\bproject\w*|\bplan\w*|\bstrategy\w*|\bensure\w*|\bexperience\w*|\bknowledge\w*|\bunderstand\w*|\bfamiliar\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'project commissioning'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform quality assurance of medical devices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqc\b|\bquality\b\s+assurance\b|\bquality\b\s+control\b', text)
        match_2 = o(r'\bdevice\w*|\bequipment\b', text)
        match_3 = o(r'\bmedical\w*|\bbiomedical\b|\bbiotech\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform quality assurance of medical devices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage quality
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquality\b\s+\bcontrol\w*|\bqc\b|\bquality\b\s+assurance\b|\bquality\s+requirement\w*|\bmanag[e]?[i]?[n]?[g]?\s+quality\w*|\bquality\s+engineer\w*|\bassess[i]?[n]?[g]?\s+quality\w*|\bquality\s+assess\w*|\bquality\s+test\w*|\btest[i]?[n]?[g]?\s+quality\w*|\bensur[e]?[i]?[n]?[g]?\s+quality\w*|\bquality\s+manage\w*|\bquality\s+parameter\w*|\bquality\s+standard\w*|\bproject\s+quality\w*|\bquality\s+plan\w*|\bmonitor[i]?[n]?[g]?\s+quality\w*|\bquality\s+monitor\w*|\bquality\s+plan\w*|\bquality\s+check\w*|\bquality\s+safety\s+requirement\w*|\bquality\s+\w+\s+test\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+quality\s+issue\w*', text)
        match_2 = o(r'\bqa\b', text)
        match_3 = o(r'\bquality\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage quality'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor production line
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonitor[i]?[n]?[g]?\s+production\w*|\binsepct\s+production\w*|\bproduction\s+monitor\w*|\bproduction\s+inspect\w*|\bmanag[e]?[i]?[n]?[g]?\s+production\w*|\bproduction\s+manage\w*|\boversee\s+production\w*|\bsupervise\s+production\w*|\bcheck[i]?[n]?[g]?\s+production\w*|\bproduction\s+check\w*|\bproduction\s+supervisor\w*|\bproduction\s+operation[s]?\s+technician\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor production line'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure safety in the production area
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsafety\s+\bguidance\b|\bensure\s+safety\w*|\bensure\s+safe\b|\bsafety\s+assurance\w*|\bhealth\s+safety\w*|\boperation[s]?\s+technician\w*|\bsafety\s+engineer\w*|\bmanag[e]?[i]?[n]?[g]?\s+\bhealth\s+\bsafety\w*|\bprovide\s+safe\w*|\bsafety\s+brief\w*|\bsafe[t]?[y]?\s+manage\w*|\bensure\s+safe\w*|\bsafety\s+analy\w*|\bsafety\s+engineer\w*|\bhse\s+performance\w*|\bensure\s+\bhealth\s+safety\w*|\bhealth\s+safety\s+guideline\w*|\bhealth\s+safety\s+standard\w*|\bhealth\s+safety\s+measure\w*|\bhealth\s+safety\s+regulation\w*|\bhealth\s+safety\s+legislation\w*|\bhealth\s+safety\s+compliance\w*|\bsafe\s+working\s+environment\w*|\bsafe\s+working\s+condition\w*|\bsafety\s+procedure\w*|\bsafety\s+policy\w*|\bsafety\s+precaution\w*|\bsafety\s+require\w*|\boperation[s]?\s+technician\w*|\bindustrial\s+hygienist\w*|\bindustry\s+hygienist\w*|\bhealth\s+safety\s+advice\w*|\bhealth\s+safety\s+advis\w*|\bhealth\s+safety\s+consult\w*|\bhse\s+officer\w*|\bhse\s+suppor\w*|\bprovide\s+hse\b|\bdesign\s+hse\b|\bhse\s+standard\w*|\bhse\s+review\w*|\bhsec\s+officer\w*|\bhsec\s+suppor\w*|\bhsec\s+advisor\w*|\bhsec\s+advice\w*|\bprovide\s+hsec\b|\bhsec\s+standard\w*|\bhsec\s+review\w*|\breview\s+hsec\b|\bhsec\s+require\w*|\bhseq\s+issue\w*|\bhs\s+advisor\w*|\bhs\s+advice\w*|\bhs\s+review\w*|\bhsse\s+officer\w*|\bhs\s+review\w*|\breview\s+hs\b|\bhs\s+support\w*|\bhs\s+compliance\w*|\bprovide\s+hs\b|\bhs\s+standard\w*|\bhs\s+require\w*|\bhs\s+issue\w*|\bhsse\s+suppor\w*|\bprovide\s+hsse\b|\bhsse\s+standard\w*|\bhsse\s+review\w*|\bhsse\s+require\w*|\bhsse\s+issue\w*|\breview\s+hsse\b|\bhsea\s+suppor\w*|\bprovide\s+hsea\b|\bhsea\s+standard\w*|\bhsea\s+review\w*|\bhsea\s+require\w*|\bhsea\s+issue\w*|\breview\s+hsea\b|\bhealth\s+safety\s+[y]?[i]?[e]?[s]?\w*|\bhs\s+[y]?[i]?[e]?[s]?\w*|\bhsse\s+polic[y]?[i]?[e]?[s]?\w*|\bhse\s+polic[y]?[i]?[e]?[s]?\w*|\bhsea\s+polic[y]?[i]?[e]?[s]?\w*', text)
        match_2 = o(r'\bmanufactur\w*|(?!project )\bproduction\w*|\bfabricat\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure safety in the production area'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage health and safety
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+\bhealth\s+\bsafety\w*|\bhse\s+performance\w*|\bensur[e]?[i]?[n]?[g]?\s+health\s+safety\w*|\bhealth\s+safety\s+guideline\w*|\bhealth\s+safety\s+standard\w*|\bhealth\s+safety\s+measure\w*|\bhealth\s+safety\s+regulation\w*|\bhealth\s+safety\s+legislation\w*|\bhealth\s+safety\s+compliance\w*|\bsafe\s+work[i]?[n]?[g]?\s+environment\w*|\bsafe\s+work[i]?[n]?[g]?\s+condition\w*|\bsafety\s+procedure\w*|\bsafety\s+policy\w*|\bsafety\s+precaution\w*|\bsafety\s+require\w*|\bindustrial\s+hygienist\w*|\bindustry\s+hygienist\w*|\bhealth\s+safety\s+advice\w*|\bhealth\s+safety\s+advis\w*|\bhealth\s+safety\s+consult\w*|\bhse\s+officer\w*|\bhse\s+suppor\w*|\bprovide\s+hse\b|\bdesign[i]?[n]?[g]?\s+hse\b|\bhse\s+standard\w*|\bhse\s+review\w*|\bhsec\s+officer\w*|\bhsec\s+suppor\w*|\bhsec\s+advisor\w*|\bhsec\s+adviser\w*|\bhsec\s+consult\w*|\bhsec\s+advice\w*|\bprovide\s+hsec\b|\bhsec\s+standard\w*|\bhsec\s+review\w*|\breview\s+hsec\b|\bhsec\s+require\w*|\bhseq\s+issue\w*|\bhs\s+advisor\w*|\bhs\s+advice\w*|\bhs\s+adviser\w*|\bhs\s+consult\w*|\bhs\s+standard\w*|\bhs\s+review\w*|\bhsse\s+officer\w*|\bhs\s+review\w*|\breview[i]?[n]?[g]?\s+hs\b|\bhs\s+support\w*|\bhs\s+compliance\w*|\bhses\s+compliance\w*|\bprovid[e]?[i]?[n]?[g]?\s+hs\b|\bhs\s+require\w*|\bhs\s+issue\w*|\bhsse\s+suppor\w*|\bprovid[e]?[i]?[n]?\s+hsse\b|\bhsse\s+standard\w*|\bhsse\s+review\w*|\bhsse\s+require\w*|\badvise\s+hsse\b|\badvice\s+hsse\w*|\bhsse\s+advice\w*|\bhsse\s+advis\w*|\bhsse\s+consult\w*|\bhsse\s+issue\w*|\breview\s+hsse\b|\bhsea\s+suppor\w*|\bprovide\s+hsea\b|\bhsea\s+standard\w*|\bhsea\s+review\w*|\bhsea\s+require\w*|\bhsea\s+issue\w*|\breview\s+hsea\b|\badvice\s+hsea\b|\badvise\s+hsea\w*|\bhsea\s+advis\w*|\bhsea\s+advice\w*|\bhsea\s+constul\w*|\bhealth\s+safety\s+[y]?[i]?[e]?[s]?\w*|\bhs\s+[y]?[i]?[e]?[s]?\w*|\bhsse\s+polic[y]?[i]?[e]?[s]?\w*|\bhse\s+polic[y]?[i]?[e]?[s]?\w*|\bhsea\s+polic[y]?[i]?[e]?[s]?\w*|\bensure\s+\w+\s+health\s+safe\w*', text)
        match_2 = o(r'\boperation[s]?\s+technician\w*|\bsafety\s+assessment\w*|\bsafety\s+manage\w*|\bsafety\s+control\w*|\bmanag[e]?[i]?[n]?[g]?\s+safety\w*|\bprovide\s+safe\w*|\bensure\s+safe\w*|\bsafety\s+brief\w*|\bsafe[t]?[y]?\s+manage\w*|\bensure\s+safe\w*|\bsafety\s+analy\w*|\bsafety\s+engineer\w*', text)
        match_3 = o(r'\bhealth\s+safety\w*|\bhs\b|\bhseq\b|\bhse\b|\bhsec\b|\bhsse\b|\bhesa\b|\bhses\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage health and safety'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cost management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcost[s]?\s+manage\w*|\bcost[s]?\s+\bsaving\w*|\bcost[s]?\s+efficiency\w*|\bsave\s+cost\w*|\boptimize\s+cost\w*|\boptimise\s+cost\w*|\bestimate\s+cost\w*|\bcost[s]?\s+optimis\w*|\bcost[s]?\s+optimiz\w*|\bcost[s]?\s+save\w*|\bcost[s]?\s+report\w*|\breport[i]?[n]?[g]?\s+cost\w*|\bcost[s]?\s+-\s+save\w*|\bcost[s]?\s+analy\w*|\banalyze\s+cost\w*|\banalyse\s+cost\w*|\bmanag[e]?[i]?[n]?[g]?\s+cost\w*|\boversee\s+cost\w*|\bcost[s]?\s+overs\w*|\bschedule\s+cost\w*|\bcost\s+control\w*|\bcontrol\s+cost\w*|\bcost[s]?\s+plan\w*|\bplan[n]?[i]?[n]?[g]?\s+cost\w*|\bcost[s]?\s+schedule\w*|\bcost[s]?\s+analy\w*|\bcost[s]?\s+estimate\w*|\bcost[s]?\s+model\w*|\bcost[s]?\s+intelligence\w*|\bcost[s]?\s+proposal\w*|\bfee[s]?\s+proposal\w*|\bcvp\s+process\w*|\bcost\s?[-]?\s?volume\s?[-]?\s?profit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'cost management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply change management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchange\s+\bmanagement\b|\bplan\s+change\w*|\bmanagement\s+change\w*|\bmanag[e]?[i]?[n]?[g]?\s+change\w*|\bchange\s+strateg\w*|\bchange\s+control\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply change management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adapt to change
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchange\s+environment\w*|\badapt[i]?[n]?[g]?\s+quick\w*|\badaptation\s+skill\w*|\badaptation\s+ability\w*|\badapt[i]?[n]?[g]?\s+change\w*|adapt[s]?\s+change\w*|\badjust[i]?[n]?[g]?\s+change\w*|\badjust[i]?[n]?[g]?\s+quick\w*|\breponisve\s+change\w*|\bwilling[n]?[e]?[s]?[s]?\s+change\w*|\bopen[n]?[e]?[s]?[s]?\s+change\w*|\bchang[e]?[i]?[n]?[g]?\s+work\s+environment\w*|\badapt[i]?[n]?[g]?\s+\w+\s+change\w*', text)
        match_2 = o(r'\badaptability\w*|\badapting\b|\badaptation\w*|\badapt\w*', text)
        match_3 = o(r'\bflexibility\w*|\bflexible\b|\bflexibly\w*|\bability\w*|\bchange\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'adapt to change'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for intercompany netting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintercompany\b\s+\bnetting\b|\bnetting\b\s+\bpayment\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'intercompany netting'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for process payments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcredit\s+card\s+transaction\w*|\bpayment\s+request\w*|\baccept\s+payment\w*|\bprocess\s+payment\w*|\bprocessing\s+payment\w*|\baccepting\s+payment\w*|\bhandl[e]?[i]?[n]?[g]?\s+payment\w*|\bpayment\s+settlement\w*|\bprocess[i]?[n]?[g]?\s+\w+\s+payment\w*|\bhandl[e]?[i]?[n]?[g]?\s+\w+\s+payment\w*|\baccept[i]?[n]?[g]?\s+\w+\s+payment\w*', text)
        match_2 = o(r'\bensure\s+smooth\s+day\b', text)
        match_3 = o(r'\bcard\s+transaction\w*|\bcard\s+payment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'process payments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for convert currency
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexchange\s+rate\b', text)
        match_2 = o(r'\bcurrency\w*|\bnote\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'convert currency'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform or support reconciliation processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breconcile\b|\breconciliation[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform or support reconciliation processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply credit risk policy and prepare credit offers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcredit\s+limit\w*|\bcredit\s+controller\b|\bcredit\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+credit\w*', text)
        match_2 = o(r'\bcredit\b', text)
        match_3 = o(r'\baccount\w*|\boffer\w*', text)
        match_4 = o(r'\bcalculat\w*|\baccounting\w*|\brisk\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply credit risk policy and prepare credit offers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design firmware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfirmware\b', text)
        match_2 = o(r'\bdesign\b|\bcreate\b|\bdevelop\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design firmware'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for program firmware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfirmware\b', text)
        match_2 = o(r'\bcode\w*|\bprogram\w*|\bcoding\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'program firmware'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for upgrade and install firmware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bupgrade\s+firmware\w*|\bupdate\s+firmware\w*|\bupgrade\s+software\w*|\bupdate\s+software\w*|\bsoftware\s+upgrade\w*|\bsoftware\s+update\w*|\bfirmware\s+upgrade\w*|\bfirmware\s+update\w*|\binstallation\s+package\w*|\binstall[i]?[n]?[g]?\s+package\w*|\binstall[i]?[n]?[g]?\s+software\w*|\binstallation\s+software\w*|\binstall[i]?[n]?[g]?\s+firmware\w*|\binstallation\s+firmware\w*|\bsoftware\s+install\w*|\bfirmware\s+install\w*', text)
        match_2 = o(r'\bsoftware\w*|\bfirmware\w*', text)
        match_3 = o(r'\bhardware\w*', text)
        match_4 = o(r'\bupdate\w*|\bupgrade\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'upgrade and install firmware'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for primers manufacturing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprimer[s]\s+manufactur\w*|\bmanufactur[i]?[n]?[g]?\s+primer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'primers manufacturing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for evaluate and analyse genetic data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdna\b\s+sequencing\b|\bgenetic\s+analy\w*|\bgene\s+edit\w*|\bgenetic\s+evaluation\w*', text)
        match_2 = o(r'\bgenetic\s+datum\w*|\bgenetic\s+data\w*|\bgenome\s+data\w*|\bgenome\s+datum\w*', text)
        match_3 = o(r'\btest\w*|\bresearch\w*|\banaly\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'evaluate and analyse genetic data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform oligonucleotide synthesis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boligonucleotide\s+\bsynthesis\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform oligonucleotide synthesis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare presentation material
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprepare\s+\bpresentation\w*|\bpresentation\s+skill\w*|\bdeliver\s+presentation\w*|\bability\s+present\w*|\bpresent\s+idea\w*|\bpresent\s+datum\w*|\bpresent\s+data\b|\bpresent\s+material\w*|\bgive\s+presentation\w*|\bmak[e]?[i]?[n]?[g]?\s+presentation\w*|\bprovide\s+presentation\w*|\bconduct\s+presentation\w*|\bdevelop\s+presentation\w*|\breport\s+presentation\w*|\bpresent[i]?[n]?[g]?\s+result\w*|\bpresent[i]?[n]?[g]?\s+\w+\s+result\w*|\bmark[e]?[i]?[n]?[g]?\s+\w+\s+presentation\w*', text)
        match_2 = o(r'\bpresentation\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*|\breview\w*|\bdevelop\w*', text)
        match_4 = o(r'\bpresent\b', text)
        match_5 = o(r'\bposter\w*|\bpublication\w*|\bresult\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare presentation material'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for collect debt
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcollection\s+\bdebt\w*|\bdebt\s+\bcollect\w*|\bcollect\b\s+\bdebt\w*|\bcredit\s+collect\w*|\bfield\s+debt\s+solution\w*|\bresolve\s+debt\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'collect debt'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for make clinical decisions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+expertise\w*|\bclinical\s+decision\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'make clinical decisions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lead clinical pharmacology studies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpharmacology\s+study\s+supervis\w*|\bpharmacology\s+study\s+manage\w*|\blead\s+clinical\s+pharmacology\w*|\bmanag[e]?[i]?[n]?[g]?\s+pharmacology\s+study\w*|\bsupervise\s+pharmacology\s+study\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'lead clinical pharmacology studies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform scientific research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bresearch\s+scientist\w*|\bclinical\s+research\s+fellow\w*|\bclinical\s+research\s+associate\w*|\bscientific\s+research\w*|\bscientific\s+clinical\s+research\w*|\bdisciplinary\s+reserach\w*|\bdiscipline\s+research\w*|\bresearch\s+skill\w*|\bresearch\s+\w+\s+skill\w*|\bresearch\s+experience\w*', text)
        match_2 = o(r'\bconduct\s+research\w*|\bperform\s+research\w*|\bconduct\s+\w+\s+research\w*|\breserach[e]?[r]?\s+role\w*|\bundertake\s+research\w*|\bundertake\s+\w+\s+research\w*|\bconduct\s+\w+\s+research\w*|\bperform\w+\s+\w+research\w*|\bresearch\s+experience\w*|\bperform\s+\w+\s+research\w*', text)
        match_3 = o(r'\bvitro\b|\bvivo\b|\btoxicology\w*|science\w*|\bpharmacology\w*|\blab\w*|\blaboratory\w*|\bscientific\w*|\binvestigation\w*|\bquantitative\w*|\bgenetic\w*|\bquantum\w*|\bmolecular\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform scientific research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow clinical guidelines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+research\w*|\bclinical\s+study\w*', text)
        match_2 = o(r'\bregulation\w*|\bguideline\w*|\bcompliance\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow clinical guidelines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assist in clinical trials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+research\s+associate\w*|\bclinical\s+trial\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assist in clinical trials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage clinical environments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+trial\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+clinical\s+trial\w*|\bclinical\s+director\b|\bclinical\s+trial\s+supervis\w*|\bclinical\s+trial\s+director\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage clinical environments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform clinical coding procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+administration\w*|\bclinical\s+coding\w*|\bclinical\s+coder\b|\bdiagnostic\s+coder\b|\bdiagnostic\s+coding\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform clinical coding procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor loan portfolio
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdebt\w*|\bloan\w*', text)
        match_2 = o(r'\bportfolio\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor loan portfolio'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdocument\s+control\w*|document\s+management\w*|document\s+schedule\w*|manag[e]?[i]?[n]?[g]?\s+document\w*|schedule\s+document\w*|maintain\s+document\w*|document\s+procedure\w*|procedure[s]?\s+document\w*|prepar[e]?[a]?[t]?[i]?[o]?[n]?\s+document\w*|provid[e]?[i]?[n]?[g]?\s+document\w*|control[l]?[i]?[n]?[g]?\s+document\w*|updat[e]?[i]?[n]?[g]?\s+document\w*|upload[i]?[n]?[g]?\s+document\w*|index[i]?[n]?[g]?\s+document\w*|\breview[i]?[n]?[g]?\s+document\w*|handl[e]?[i]?[n]?[g]?\s+document\w*|complete\s+document\w*|document[s]?\s+control\w*|fill[i]?[n]?[g]?\s+document\w*|documentation\s+control\w*|paperwork\s+control\w*|manag[e]?[i]?[n]?[g]?\s+paperwork\w*|paperwork\s+management\w*|handl[e]?[i]?[n]?[g]?\s+paperwork\w*|ensure\s+document\w*|ensure\s+paperwork\w*|documentation\s+compliance\w*|document\s+compliance\w*|management\s+document\w*|inspect\s+document\w*|inspect\s+paperwork\w*|master\s+document\w*|document[s]?[a]?[t]?[i]?[o]?[n]?\s+inspect\w*|paperwork\s+inspect\w*|qc\s+document\w*|qa\s+document\w*|qc\s+paperwork\w*|qa\s+paperwork\w*|project\s+document\w*|schedule\s+document\w*|tender[i]?[n]?[g]?\s+document\w*|supplier[s]?\s+document\w*|payment\s+document\w*|payment\s+paperwork\w*|document[a]?[t]?[i]?[o]?[n]?\s+payment\w*|writ[e]?[i]?[n]?[g]?\s+document\w*|document[a]?[t]?[i]?[o]?[n]?\s+project\w*|relevant\s+paperwork\w*|administration\s+record\w*|document[a]?[t]?[i]?[o]?[n]?\s+activity\w*|\bgood[s]?\s+receipt\w*|\bequipment\s+receipt\w*|scope\s+work\s+document\w*|completing\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|handl[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|maintain\s+\w+\s+document\w*|maintain\s+\w+\s+paperwork\w*|complete\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+paperwork\w*|handl[e]?[i]?[n]?[g]?\s+\w+\s+paperwork\w*|ensur[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|ensur[e]?[i]?[n]?[g]?\s+\w+\s+paperwork\w*|project\s+\w+\s+document\w*|writ[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|review[i]?[n]?[g]?\s+document\w*|prepar[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|upload[i]?[n]?[g]?\s+\w+\s+document\w*|updat[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|index[i]?[n]?[g]?\s+\w+\s+document\w*|provid[e]?[i]?[n]?[g]?\s+w+\s+document\w*|provid[e]?[i]?[n]?[g]?\s+w+\s+paperwork\w*|maintain[i]?[n]?[g]?\s+\w+\s+document\w*|maintain[i]?[n]?[g]?\s+\w+\s+paperwork\w*|review[i]?[n]?[g]?\s+\w+\s+document\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for proofread text
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproof\s+read\w*|\bproofread\w*|\bproof\s+-\s+read\w*', text)
        match_2 = o(r'\btext\b|\bdocument\w*|\bfile\w*|\bpaper\w*|\braport\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'proofread text'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform product testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinished\s+good\s+testing\w*|\bproduct\s+test\w*|\btest[i]?[n]?[g]?\s+product\w*', text)
        match_2 = o(r'\btest\w*', text)
        match_3 = o(r'\bfinished\w*', text)
        match_4 = o(r'\bgood\w*|\bproduct\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform product testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop product design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevelop[i]?[n]?[g]?\s+product\s+design\w*|\bproduc[e]?[i]?[n]?[g]?\s+product\s+design\w*|\blaunch[i]?[n]?[g]?\s+product\s+design\w*|\bevaluat[e]?[i]?[n]?[g]?\s+product\s+design\w*|\bestablish[i]?[n]?[g]?\s+product\s+design\w*|\bassess[i]?[n]?[g]?\s+product\s+design\w*|\bdevelop[i]?[n]?[g]?\s+\w+\s+product\s+design\w*|\bproduc[e]?[i]?[n]?[g]?\s+\w+\s+product\s+design\w*|\blaunch[i]?[n]?[g]?\s+\w+\s+product\s+design\w*|\bevaluat[e]?[i]?[n]?[g]?\s+\w+\s+product\s+design\w*|\bestablish[i]?[n]?[g]?\s+\w+\s+product\s+design\w*|\bassess[i]?[n]?[g]?\s+\w+\s+product\s+design\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop product design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend automatic sewing machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsewing\s+machine\w*|\bautomatic\s+sewing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend automatic sewing machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use manual sewing techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseweing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use manual sewing techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for infusion technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binfusion\s+technology\w*|\binfusion\s+feed\w*|\binfustion\s+technique\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'infusion technologies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drive large goods vehicles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btonne\s+driver\w*|\btruck\s+driver\w*|\bdrive\s+hgv\w*|\bdrive\s+\w+\s+hgv\w*', text)
        match_2 = o(r'\bhgv\b|\bhgvs\b|\bc1\b', text)
        match_3 = o(r'\bgood\s+vehicle\w*|\bplant\s+auxiliary\w*|\blicense\w*|\bcertificate\w*|\bfleet\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'drive large goods vehicles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drive vehicles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bperson\s+driver\w*|\bpersonal\s+driving\w*|\bdriving\s+license\w*|\bdriving\s+licence\w*|\bfull\s+driver[s]?\b|\bdrive\s+license\w*|\bdrive\s+license\w*|\bdriver[s]?\s+license\w*|\bability\s+drive\w*|\bdrive\s+vehicle\w*|\bdriving\s+vehicle\w*|\brequire\s+drive\w*|\brequire\s+driving\w*|\bdrive\s+require\w*|\bdriving\s+require\w*|drive\s+car\b|\bdriving\s+car\b|\bhandl[e]?[i]?[n]?[g]?\s+vehicle\w*|\bhandl[e]?[i]?[n]?[g]?\s+car\b|\boperat[e]?[i]?[n]?[g]?\s+vehicle\w*|\boperat[e]?[i]?[n]?[g]?\s+car\b|\bcpc\s+card\w*|\bcat\s+c\s+license\w*|\bfull\s+\w+\s+driver[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'drive vehicles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply customer engagement strategy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer[s]?\s+engage\w*|\bclient[s]?\s+engage\w*|\bengage\s+customer\w*|\bcustomer[s]?\s+insight\w*|\bclient[s]?\s+insight\w*|\bengage\s+client\w*|\bcustomer\s+onboard\w*|\bengage\s+stakeholder\w*|\bstakeholder[s]?\s+engage\w*|\bcustomer[s]?\s+care\s+solution\w*|\bcustomer[s]?\s+care\s+strategy\w*|\bclient[s]?\s+care\s+solution\w*|\bclient[s]?\s+care\s+strategy\w*|\bcustomer\s+sucess\s+manage\w*', text)
        match_2 = o(r'\bcustomer[s]?\s+strategy\w*|\bclient[?]\s+strategy\w*|\bcustomer[s]?\s+care\w*|\bcustomer\s+sucess\w*', text)
        match_3 = o(r'\bmanagement\w*|\bsale\w*|\bmarketing\w*|\bensure\w*|\bmanage\w*', text)
        match_4 = o(r'\bengagement\s+consultation\w*', text)
        match_5 = o(r'\bcustomer\w*|\bclient\w*|\bstakeholder\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply customer engagement strategy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use customer relationship management software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrm\b|\bcustomer[s]?\s+relatio[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+system\w*|\bcustomer[s]?\s+relation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+software\w*|\bclient[s]?\s+relation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+software\w*|\bclient[s]?\s+relation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+system\w*|\bstakeholder[s]?\s+relation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+software\w*|\bstakeholder[s]?\s+relation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+system\w*', text)
        match_2 = o(r'\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+stakeholder\w*|\bcustomer\s+relationship\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+stakeholder\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+customer\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+client\w*|\benhanc[e]?[i]?[n]?[g]?\s+relationship\s+client\w*|\bnurture\s+\w+\s+relationship\w*|\bliais[e]?[o]?[n]?\s+customer\w*|\bliais[e]?[o]?[n]?\s+client\w*|\bliais[e]?[o]?[n]?\s+stakeholder\w*|\bliais[e]?[o]?[n]?\s+\w+\s+customer\w*|\bliais[e]?[o]?[n]?\s+\w+\s+client\w*|\bliais[e]?[o]?[n]?\s+\w+\s+stakeholder\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+stakeholder\w*|\bcustomer[s]?\s+relationship\w*|\bclient[s]?\s+relationship\w*|\bstakeholder[s]?\s+relationship\w*|\bcustomer[s]?\s+manage\w*|\bclient[s]?\s+manage\w*|\bstakeholder[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+customer\w*|\bmanag[e]?[i]?[n]?[g]?\s+client\w*|\bmanag[e]?[i]?[n]?[g]?\s+stakeholder\w*', text)
        match_3 = o(r'\brelation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+software\w*|\brelation[s]?[h]?[i]?[p]?\s+manage[m]?[e]?[n]?[t]?\s+system\w*|\bmanage[m]?[e]?[n]?[t]?\s+software\w*|\bmanage[m]?[e]?[n]?[t]?\s+system\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use customer relationship management software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess physical conditions of clients and staff
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boccupational\s+health\s+technician\w*', text)
        match_2 = o(r'\bhealth\s+screen\w*|\bscreen[i]?[n]?[g]?\s+health\w*|\bhealth\s+condition\w*|\bcondition\s+health\w*|\bphysical\s+condition\w*|\bhealth\s+technician\w*', text)
        match_3 = o(r'\bclient\w*|\bpatient\w*|\bcustomer\w*|\bstaff\b|\bemployee\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess physical conditions of clients and staff'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for show commitment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcommitment\s+skill\w*|\bwillingness\s+learn\b|\bdemonstrate\s+commitment\w*|\bwillingness\s+perform\w*|\bwillingness\s+work\w*|\bdemonstrate\s+enthusiasm\w*|\bdemonstrate\s+motivation\w*|\benjoy\s+work\w*|\bshow\s+passion\w*|\blearn\s+quick\w*|\bquick\s?[-]?\s?learn\w*|\bfast\s?[-]?\s?learn\w*', text)
        match_2 = o(r'(?<!train )(?<!training )(?<!customer )(?<!company )\bcommitment\b(?! career|  career)\w*|(?<!train )(?<!training )(?<!customer )(?<!company )(?<!companys )\bcommit\b(?! career|  career)\w*', text)
        match_3 = o(r'\bpresentation\w*|\benthusiasm\w*|\borganisational\w*|\borganizational\w*|\bcollaborat\w*|\bnegotiat\w*|\banaly\w*|\binterpresonal\w*|\binter\s+personal\w*|\bproactive\w*', text)
        match_4 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'show commitment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assist in/perform blood sampling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphlebotomist\w*|\btake\s+blood\s+sample\w*|\bassist\s+blood\s+sample\w*|\bperform\s+blood\s+sample\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assist in/perform blood sampling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for troubleshoot and prepare troubleshooting documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btroubleshoot\w*|\btrouble\s+-\s+shoot\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'troubleshoot and prepare troubleshooting documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use specialized equipment (electron microscope)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectron\s+microscope\w*|\belectron\s+microscopy\w*|\bcascadion\w*', text)
        match_2 = o(r'\bsem\b', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|\bmicroscop\w*|\bmachine\w*|\bequipment\w*|\bimage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use specialized equipment (electron microscope)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use radioisotopes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bradioisotope\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use radioisotopes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use microtome
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrotome\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use microtome'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for work in biosafty cabinets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiomedical\s+safety\s+cabinet\w*|\bbiosafety\s+cabinet\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'work in biosafty cabinets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use specialized equipment (field emmision scanning microscope)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfield\s+emmission\s+scan\w*|\bfield\s+emmission\s+microscop\w*', text)
        match_2 = o(r'\bfei\b', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|\bmicroscop\w*|\bmachine\w*|\bequipment\w*|\bimage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use specialized equipment (field emmision scanning microscope)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform fitting-out works
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binterior\s+finish\s+work\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform fitting-out works'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for paint decorative designs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdecorator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'paint decorative designs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for recruit personnel or employees
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brecruitment\s+consultant\w*|\brecruit[i]?[n]?[g]?\s+consultant\w*|\brecruitment\s+experience\w*|\brecruiting\s+experience\w*|\bimprov[e]?[i]?[n]?[g]?\s+recruitment\w*|\bhir[e]?[i]?[n]?[g]?\s+process\w*|\bhir[e]?[i]?[n]?[g]?\s+personnel\w*|\bhir[e]?[i]?[n]?[g]?\s+manage\w*|\bpersonnel\s+hir[e]?[i]?[n]?[g]?\w*|\brecruit[i]?[n]?[g]?\s+personnel\w*|\bemployee[s]?\s+recruit\w*|\brecruit[i]?[n]?[g]?\s+employee\w*|\brecruit[i]?[n]?[g]?\s+staff\w*|\binternal\s+recruiter\w*|\brecruitment\s+manager\w*|\bassist\s+recruitment\w*|\binternal\s+recruitment\w*|\bcandidate[s]?\s+pipeline\w*|\bhir[e]?[i]?[n]?[g]?\s+\w+\s+personnel\w*|\brecruit[i]?[n]?[g]?\s+\w+\s+employee\w*|\brecruit[i]?[n]?[g]?\s+\w+\s+staff\w*|\bimprov[e]?[i]?[n]?[g]?\s+\w+\s+recruitment\w*', text)
        match_2 = o(r'\bresourcing\w*', text)
        match_3 = o(r'\brecruitment\w*|\bpersonnel\w*|\bemployees\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'recruit personnel or employees'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for postgraduate student recruitment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmsro\b|\bstudent\s+recritment\w*|\brecruit\s+\w+\s+student\w*', text)
        match_2 = o(r'\bpg\b|\bpostgraduate\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'postgraduate student recruitment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage time and meet commitments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpunctuality\b|\btime\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+time\w*|\btime\s+constraint\w*|\btime\s+pressure\w*|\bmulti\s?[-]?\s?task\w*|\bself\s+manage\w*|\bunder\s+pressure\w*|\bestablish[e]?[d]?\s+timeline\w*|\bexecute\s+time\w*|\bhandl[e]?[i]?[n]?[g]?\s+pressure\w*|\btime\s+budget\w*|\bbudget\s+schedule\w*|\btight\s+schedule\w*|\bschedule\s+budget\w*|\bschedule\s+aware\w*|\bmeet\s+target\w*|\bachieve\s+target\w*|\bdelivery\s+schedule\w*|\bproduction\s+target\w*|\btime\s+manner\w*|\btime\s?[-]?\s?manage\w*|\btimely\s+manner\w*|\bensure\s+timely\w*|\bmanag[e]?[i]?[n]?[g]?\s+portfolio\s+work\w*|\bensure\s+objective\w*|\bresponsible\s+multiple\s+task\w*|\bcompletion\s+task\w*|\bcomplete\s+task\w*|\btask\s+ownership\b|\bachieve\s+project\s+goal\w*|\btime\s+delivery\s+requirement\w*|\bcomplete\s+\w+\s+task\w*|\bcompletion\s+\w+\s+task\w*|\bhandl[e]?[i]?[n]?[g]?\s+\w+\s+task\w*|\badher[e]?[i]?[n]?[g]?\s+\w+\s+timeline\w*|\bfinish\s+agree\s+time\w*|\bwork\s+agree\s+time|\bcomplete\s+agree\s+time|\bflexible\s+work[i]?[n]?[g]?\s+pattern\w*|\bflexible\s+work[i]?[n]?[g]?\s+schedule\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage time and meet commitments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prioritise tasks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprioritize\s+task\w*|\bprioritization\s+skill\w*|\bprioritize\s+work\w*|\bprioritise\s+work\w*|\bprioritise\s+project\w*|\bprioritise\s+task\w*|\bprioritisation\s+skill\w*|\bprioritisation\s+ability\w*|\bprioritization\s+ability\w*|ability\s+prioritize\w*|\bability\s+prioritise\w*|\bprioritising\s+task\w*|\bprioritizing\s+task\w*|\bprioritising\s+ability\w*|\bschedule\s+prioriti\w*|\bprioritize\s+schedule\w*|\bprioritise\s+schedule\w*|\bprioritizing\s+schedule\w*|\bpriotitizing\s+schedule\w*|\bprioritizing\s+ability\w*|\bpriotitize\s+effectively\w*|\bprioritise\s+effectively\w*|\bplan[n]?[i]?[n]?[g]?\s+task\w*|\btaks[s]?\s+plan\w*|\bidentify\s+priority\w*|\bmanag[e]?[i]?[n]?[g]?\s+priorit\w*|\bsnag\s+list\w*|\bprioritise\s+\w+\s+task\w*|\bprioritize\s+\w+\s+task\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+priorit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prioritise tasks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for meet deadlines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badhere\s+deadline\b|\bmeet[i]?[n]?[g]?\s+deadline\w*|\btight\s+deadline\w*|\bstrict\s+deadline\w*|\bunder\s+deadline\w*|\bfollow\s+deadline\w*|\bwithin\s+timeline\w*|\bschedule\s+requirement\w*|\bstick\s+deadline\w*|\bachieve\s+deadline\w*|\bensure\s+deadline\w*|\bwork\s+deadline\w*|\bdeadline\s+project\w*|\bcomplete\s+deadline\w*|\bproject\s+deadline\w*|\bchallenging\s+deadline\w*|\bmeet[i]?[n]?[g]?\s+timeline\w*|\bmeet\s+\w+\s+deadline\w*|\breact\s+\w+\s+deadline\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'meet deadlines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for procure time sheet approval
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bensure\s+timesheet\s+correcly\w*|\btime\s+sheet\s+approval\w*|\btimesheet\s+approval\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'procure time sheet approval'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gather and manage feedback from employees
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemployee\w*|\bstaff\w*|\bworker\w*', text)
        match_2 = o(r'\bfeedback\w*|\bsuggestion\w*', text)
        match_3 = o(r'\bgather\w*|\bcolect\w*|\bmanage\w*|\bget\b|\btalk\w*|\blisten\w*|\bincorporate\w*|\bact\w*|\bencourage\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'gather and manage feedback from employees'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for give constructive feedback
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprovide\s+feedback\w*|\bgive\s+feedback\w*|\bprovide\s+performance\s+feedback\w*', text)
        match_2 = o(r'\bprovide\s+constructive\w*|\bgive\s+constructive\w*', text)
        match_3 = o(r'\bfeedback\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'give constructive feedback'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage performance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassess[i]?[n]?[g]?\s+perfromance\w*|\bassay\s+performance\w*|\bmanag[e]?[i]?[n]?[g]?\s+performace\w*|\bperformance\s+metric\w*|\bmonitor[i]?[n]?[g]?\s+performance\w*|\bmeet\s+performance\w*|\bperformance\s+manage\w*|\bkpi\b|\bdevelop\s+poc\b|\bpoc\s+review\w*|\bpocs\b|\bkpis\b|\bkey\s+performance\s+indicator\w*|\bevaluation\s+report\w*|\bbusiness\s+performance\w*|\bcompany\s+performance\w*|\bimprov[e]?[i]?[n]?[g]?\s+performance\w*|\bcapacity\s+metric\w*|\boptimize\s+performance\w*|\boptimise\s+performance\w*|\boperation[s]?\s+technician\w*|\bunit\s+performance\w*|\bsupervise\s+performance\w*|\bsupervision\s+performance\w*|\breport[i]?[n]?[g]?\s+performance\w*|\bimporov[e]?[i]?[n]?[g]?\s+\w+\s+performance\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+performance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage performance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for measure team performance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bperformance\w*', text)
        match_2 = o(r'\bteam\w*|\bstaff\w*|\bemployee\w*|\bwork\w*|\bmember\w*', text)
        match_3 = o(r'\bmeasure\w*|\bmanage\w*|\bmeasuring\w*|\bmanaging\w*|\bcheck\w*|\bensure\w*|\btake\b|\bensure\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'measure team performance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage feedback
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfeedback\w*|\bsuggestion\w*', text)
        match_2 = o(r'\bincorporate\w*|\bmanag[e]?[i]?[n]?[g]?\w*|\bgather\w*|\bcustomer\w*|\bclient\w*|\blisten\w*|\bcollect\w*|\benable\w*|\bstakeholder\w*|\bconsumer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage feedback'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for participate in investigator meetings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binvestigator\s+meeting\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'participate in investigator meetings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for participate in meetings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak\s+meeting\w*|\bmeeting\s+participate\w*|\bparticipate\s+meeting\w*|\bteleconferencing\w*|\bconference\s+call\w*|\bmeeting\s+attend\w*|\battend[i]?[n]?[g]?\s+meeting\w*|\bmanagement\s+meeting\w*|\bcustomer[s]?\s+meet\w*|\bclient[s]?\s+meet\w*|\bstakeholder[s]?\s+meet\w*|\bteam\s+meet\w*|\bchapter\s+meeting\w*|\bclient[s]?\s+\w+\s+meet\w*|\bstakeholder[s]?\s+\w+\s+meet\w*|\bcustomer[s]?\s+\w+\s+meet\w*|\bparticipate\s+\w+\s+meeting\w*', text)
        match_2 = o(r'\bface\b', text)
        match_3 = o(r'\bmeet\w*', text)
        match_4 = o(r'\battend[i]?[n]?[g]?\b', text)
        match_5 = o(r'\bmeeting[s]\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif len(match_2) >= 2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'participate in meetings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for organise meetings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmeeting\s+schedul\w*|\bprepare\s+meeting\w*|\bdeliver[y]?[i]?[n]?[g]?\s+meeting\w*|\bmeeting\s+prepare\w*|(?<!global )\bmeeting\s+plann\w*|\bplan[n]?[i]?[g]?\s+meeting\w*|\barrang[e]?[i]?[n]?[g]?\s+meeting\w*|\bbook[i]?[n]?[g]?\s+meeting\w*|\bchair\s+meeting\w*|\borganise\s+meeting\w*|\borganize\s+meeting\w*|\borganisation\s+meeting\w*|\bmanag[e]?[i]?[n]?[g]?\s+meeting\w*|\bmeeting\s+management\b|\bmeeting\s+coordinat\w*|\bmeeting\s+co\s+-\s+ordinat\w*|\bmeeting\s+ordinat\w*|\bcoordinate\s+meeting\w*|\bco\s+-\s+ordinate\s+meeting\w*|\bordinate\s+meeting\w*|\borganise\s+\w+\s+meeting\w*|\borganize\s+\w+\s+meeting\w*|\bchair\s+\w+\s+meeting\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+meeting\w*|\bordinate\s+\w+\s+meeting\w*|\bplan[n]?[i]?[n]?[g]?\s+\w+\s+meeting\w*|\barrang[e]?[i]?[n]?[g]?\s+\w+\s+meeting\w*|\blead\s+\w+\s+meeting\w*|\bdeliver[y]?[i]?[n]?[g]?\s+\w+\s+meeting\w*|\bdeliver[y]?[i]?[n]?[g]?\s+\w+\s+\w+\s+meeting\w*', text)
        match_2 = o(r'\bbriefing\w*', text)
        match_3 = o(r'\bcustomer\w*|\bclient\w*|\bstakeholder\w*|\bteam\s+member\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'organise meetings'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coach employees
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcoach\s+staff\w*|\bcoach\s+employee\w*|\b(?<!sport  )(?<!sport )(?<!football )(?<!football  )coaching\b|\bday\s+day\s+coach\w*|\bsupport\s+coach[i]?[n]?[g]?\s+process\w*|\bsupport[i]?[n]?[g]?\s+junior\s+staff\w*|\bsupport[i]?[n]?[g]?\s+junior\s+member\w*', text)
        match_2 = o(r'\bcoach\w*', text)
        match_3 = o(r'\bmentor\w*|\bleader\w*', text)
        match_4 = o(r'\bjunior\s+staff\w*|\bjunior\s+member\w*|\bjunior\s+team\s+member\w*', text)
        match_5 = o(r'\bcoach\w*|\bmentor\w*|\bleader\w*|\btrain\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coach employees'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for motivate others
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmotivat[e]?[i]?[n]?[g]?\s+staff\w*|\bmotivat[e]?[i]?[n]?[g]?\s+employee\w*|\bmotivat[e]?[i]?[n]?[g]?\s+others\w*|\bmotivat[e]?[i]?[n]?[g]?\s+team\w*|\bmotivat[e]?[i]?[n]?[g]?\s+people\w*|\bmotivating\w*|\binfluenc[e]?[i]?[n]?[g]?\s+people\*|\b\binfluenc[e]?[i]?[n]?[g]?\s+other\w*|\binfluenc[e]?[i]?[n]?[g]?\s+employee\w*|\binfluenc[e]?[i]?[n]?[g]?\s+team\w*|\binfluenc[e]?[i]?[n]?[g]?\s+staff\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'motivate other'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage payroll
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+payroll\w*|\bpayroll\s+administrator\b|\bpay\s+administrator\w*|\bpay\s+processing\w*|\badministration\s+payroll\w*|\badminister\s+payroll\w*|\bpayroll\s+manage\w*|\bpayroll\s+processing\w*|\bprocessing\s+pay\b|\bprocessing\s+payroll\w*|\bprocess\s+pay\b|\bprocess\s+payroll\w*|\bproduce\s+payroll\w*|\bpayroll\s+production\w*|\bpayroll\s+update\w*|\bpayroll\s+activity\w*', text)
        match_2 = o(r'\boffice\s+manage\w*|\bhr\b|\bhuman\s+resource\w*', text)
        match_3 = o(r'\bpayroll\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage payroll'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage contracts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprepare\s+contract\w*|\bpreparation\s+contract\w*|\bdevelop[i]?[n]?[g]?\s+contract\w*|\bmanag[e]?[i]?[n]?[g]?\s+contract\w*|\bdeveloping\s+contract\w*|\bpreparing\s+contract\w*|\bcontract[s]?\s+manage\w*|\bdraft[i]?[n]?[g]?\s+contract\w*|\bcontract[s]?\s+strateg\w*|\bcontracting\s+strateg\w*|\bprepare\s+contract\w*|\bcontract\s+specialist\w*|\badministrate\s+contract\w*|\bcontract[s]?\s+review\w*|\breview\s+contract\w*|\bnec\s+contract\w*|\bnec\s+option\s+contract\w*|\bdraft[i]?[n]?[g]?\s+\w+\s+contract\w*|\bnec\s+option\s+\w+\s+contract\w*', text)
        match_2 = o(r'\bquantity\s+survey\w*|\bcommercial\s+manage\w*', text)
        match_3 = o(r'\bcontract\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage contracts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use cad software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcad\b|\bcadd\b|\bautocad\b', text)
        match_2 = o(r'\bdesign\w*|\bplan\w*|\barchitect\w*|\bdraughting\w*|\bsoftware\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use cad software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use 3d tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b3d\s+modelling\w*|\b3\s+-\s+modelling\w*|\b3d\s+developer\w*|\b3d\s+visualisation\w*|\b3d\s+software\w*|\b3d\s+tool\w*|\b3d\s+cad\b|\b3d\s+\w+\s+tool\w*|\bus[e]?[i]?[n]?[g]?\s+3d\b|\bus[e]?[i]?[n]?[g]\s+\w+\s+3d\b', text)
        match_2 = o(r'\b3d\b|\b3d\s+-\s+model\w*', text)
        match_3 = o(r'\bcad\b|\bmathcad\w*|\bautocad\w*|\bacad\b|\bcreo\b|\bacad\b|\bmicrostation\w*|\brevit\b|\baecosim\b|\bcads\b|\bbentley\b|\bdesign\w*|\bcreate\w*|\bcreating\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use 3d tools'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create 3d holograms
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bholographic\s+\b3d\b', text)
        match_2 = o(r'\bhologra\w*', text)
        match_3 = o(r'\b3d\b|\bdesign\w*|\bcreate\w*|\bmodel\w*|\bcreating\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create 3d holograms'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct ICT code review
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcode\s+review\w*|\btest[i]?[n]?[g]?\s+code\w*|\bcode\s+test\w*|\bcode\s+review\w*|\bassess\s+code\w*|\bcode\s+assess\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct ICT code review'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for translate requirements into visual design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvisual\s+design\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'translate requirements into visual design'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for communication skill
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcommunication\s+skill\w*|\bcommunicator\w*|\bcommunication\s+people\s+skill\w*|\bgood\s+communication\w*|\buse\s+communication\w*|\bstrong\s+communicat\w*|\bcommunicate\s+people\w*|\bcommunication\s+strong\w*|\bclear\s+communicat\w*|\bcommunicate\s+clearly\w*|\beffectively\s+communicat\w*|\bcommunicate\s+effectively\w*|\befficient\s+communication\w*|\bcommunciate\s+efficient\w*|\boral\scommunication\w*|\bwrite\s+communication\w*|\bmanag[e]?[i]?[n]?[g]?\s+communication\w*|\bsupervise\s+communication\w*|\bcoordinate\s+communicat\w*|\bordinate\s+communicat\w*|\bco\s+-\s+ordinate\s+communicat\w*', text)
        match_2 = o(r'\bcommunication\w*|\bcommunicate\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'communication skill'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for organisational skill
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhighly\s+organise\w*|\borganisation\s+skill\w*|\borganisational\s+skill\w*|\borganizational\s+skill\w*|\borganization\s+skill\w*|\bbe\s+organized\w*|\bbe\s+organised\w*|\borganize\s+people\w*|\borganise\s+people\w*|\borganize\s+other\w*|\borganise\s+other\w*', text)
        match_2 = o(r'(?<!join )(?<!global )(?<!grow )(?<!part )\borganisational\w*|(?<!join )(?<!global )(?<!grow )(?<!part )\borganizational\w*|(?<!join )(?<!global )(?<!grow )(?<!part )\borganization\w*|(?<!join )(?<!global )(?<!grow )(?<!part )\borganisation\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'organisational skill'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer skill
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputer\s+skill\w*|\bcomputer\s+competence\w*|\bexcellent\s+it\b|\bit\s+support\w*|\bit\s+skill\w*|\bpc\s+skill\b|\bit\s+skill\w*|\bit\s+computer\w*|\bit\s+litera\w*|\bit\s+competence\w*|\bit\s+competen\w*|\bit\s+knowledge\w*|\bfamiliar\s+it\b|\bit\s+package\w*|\bit\s+experience\w*|\bit\s+support\w*|\bexperience\s+it\b|\bknowledge\s+it\b|\bdigital\s+skill\w*|\bprogramming\s+skill\w*|\buse\s+computer\w*|\bcomputer\s+knowledge\w*|\bcomputer\s+literate\w*|\bus[e]?[i]?[n]?[g]?\s+pc\b|\bconfident\s+technology\w*|\boperat[e]?[i]?[n]?[g]?\s+pc\b|\boperat[e]?[i]?[n]?[g]?\s+computer\w*|\busing\s+computer\w*|\bcomputer\s+literate\w*|\bskill\s+pc\b|\bcomputer\s+literacy\w*|\bcomputerized\w*|\bcomputerised\w*|\bprogramming\s+experience\w*|\bproficient\s+computer\w*|\bproficiency\s+computer\w*|\bexperience\s+programming\w*|\bproficient\s+skill\s+pc\b|\bdigital\s+\w+\s+skill\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'computer skill'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for communicate and collaborate with others
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcollaboration\s+ability\w*|\bcollaborate\s+other\w*|\bcommunicate\s+across\w*|\bcommunication\s+across\w*|\brelationship\s+build[i]?[n]?[g]?\w*|\bcollaborate\s+across\w*|\bcollaboration\s+across\w*|\bnetwork[i]?[n]?[g]?\s+skill\w*|\binfluencing\s+skill\w*|\bcollaborative\s+environment\w*|\bcollaborate\s+internal\w*|\bcollaborate\s+externall\w*|\beffective[l]?[y]?\s+collaborat\w*|\bcollaborative\s+approach\w*|\bcollaborative\s+relationship\w*|\bwork\s+collabora\w*|\bliaise\s+with\w*|\bliaising\s+with\w*|\bliaison\s+with\w*|\bmaintain\s+close\s+communicat\w*|\bfaciliate\s+close\s+communicat\w*|\bmaintain\s+close\s+collaborat\w*|\bfaciliate\s+close\s+collaborat\w*|\bliaise\s+third\s+party\w*|\bcollaborat[e]?[i]?[o]?[n]?\s+climate\w*|\bclimate\s+collaborat\w*', text)
        match_2 = o(r'\bcollaborate\b|\bcollaboration\b|\bsynergy\w*|\bintegrat\w*', text)
        match_3 = o(r'\bteam\b|teamwork\b|\bopportunit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'communicate and collaborate with others'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for work as a team
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bteam\s?[-]?\s?work\w*|teamwork\b|\bwork[i]?[n]?[g]?\s+team\w*|\bteam\s+play\w*|\bwork[i]?[n]?[g]?\s+closely\s+team\w*|\bpart\s+team\w*|\bstrong\s+team\w*|\bbig\s+team\w*|\bsmall\s+team\w*|\bsupport\s+colleagues\w*|\bwithin\s+team\w*|\bengag[e]?[i]?[n]?[g]?\s+team\w*|\bsupport\s+team\w*|\bteam\s+environment\w*|\bpart\s+\w+\s+team\w*|\bsmall\s+\w+\s+team\w*|\bwork[i]?[n]?[g]?\s+\w+\s+\w+\s+team\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'work as a team'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oversee all travel arrangements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+travel\w*|\borganise\s+travel\w*|\bsupervise\s+travel\w*|\bcoordinate\s+travel\w*|\bco\s+-\s+ordinate\s+travel\w*|\bordinate\s+travel\w*|\btravel\s+co\s+-\s+ordinat\w*|\btravel\s+ordinat\w*|\borganize\s+travel\w*|\boversee\s+travel\w*|\barrange\s+travel\w*|\btravel\s+arrangement\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'oversee all travel arrangements'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for credentialing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcredentialing\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'credentialing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate welding equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\b|\bweld\s+metal\w*|\bwelding\s+metal\w*|\bwelding\s+equipment\w*|\bjoin\s+metal\w*|\bjoining\s+metal\w*|\bfuse\s+metal\w*|\bmetal\s+weld\w*|\bweld\s+equipment\w*|\bweld\s+tool\w*|\bweld\s+machine\w*|\bwelding\s+equipment\w*|\bwelding\s+tool\w*|\bwelding\s+machine\w*|\bweld\s+engineer\w*|\bwelding\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate welding equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform thermite welding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\w*|\bwelding\b|\bweld\b', text)
        match_2 = o(r'\bthermite\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform thermite welding'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform metal active gas welding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\w*|\bwelding\b|\bweld\b', text)
        match_2 = o(r'(?<!oil )\bgas\b(?! oil\w*|oil)\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform metal active gas welding'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for weld underwater
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\w*|\bwelding\b|\bweld\b', text)
        match_2 = o(r'\bunderwater\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'weld underwater'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plastic welding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\w*|\bwelding\b|\bweld\b', text)
        match_2 = o(r'\bplastic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plastic welding'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for organise and maintain workshop space
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bworkshop\s+space\w*|\bworkshop\s+manage\w*|\bworkshop\s+technician\w*|\bmanag[e]?[i]?[n]?[g]?\s+workshop\w*|\bsupervise\s+workshop\w*|\bworkshop\s+supervis\w*|\borganise\s+workshop\w*|\bmaintain\s+workshop\w*|\bworkshop\s+organization\w*|\bworkshop\s+organisation\w*|\bworkshop\s+maintenance\w*|\bmaintain\s+\w+\s+workshop\w*', text)
        match_2 = o(r'\bworkshop\w*', text)
        match_3 = o(r'\bhousekeep\w*|\bhouse\s?[-]?\s?keep\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'organise and maintain workshop space'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform wood works
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwoodwork\w*|\bwood\s+work\w*|\brepair\s+wood\w*|\bfix\s+wood\w*', text)
        match_2 = o(r'\bjoiner\w*', text)
        match_3 = o(r'\bwood\b|\bwoodwork\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform wood works'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform metal joining
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjoin\w*', text)
        match_2 = o(r'\bmetal\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform metal joining'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor gas leachete
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'gas\s+leachate\w*', text)
        match_2 = o(r'\bmonitor\w*|\bmaintai\w*|\bmanag\w*|\bfix\w*|\brepair\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor gas leachete'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise waste disposal
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwaste\s+assessor\w*', text)
        match_2 = o(r'\bwaste\s+disposal\w*|\bwaste\s+transfer\s+disposal\w*|\bredundant\s+material\w*|\bdisposal\s+waste\w*', text)
        match_3 = o(r'\bmanage\w*|\bsupervis\w*|\bmonitor\w*|\bmanagining\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise waste disposal'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle helpdesk problems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhelp\s+desk\b|\bhelpdesk\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle helpdesk problems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct cleaning in place
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcaretaker\w*|\bkeep\s+\w+\s+clean\w*|\barea\s+clean\w*|\bclean\s+store\w*|\bclean[i]?[n]?[g]?\s+area\w*|\barea\s+\w+\s+clean\w*|\bconduct\s+clean\w*', text)
        match_2 = o(r'\bcleaning\w*', text)
        match_3 = o(r'\bwarehouse\w*|\broom\w*|\barea\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct cleaning in place'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oversee and maintain cleaning equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcleaning\s+equipment\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'oversee and maintain cleaning equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate dishwashing machine
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bglasswasher\b|\bdishwasher\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate dishwashing machine'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean furniture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcleaning\b', text)
        match_2 = o(r'\bfurniture\w*', text)
        match_3 = o(r'\bclean[i]?[n]?[g]\s+furniture\w*|\bdust[i]?[n]?[g]?\s+furniture\w*|\bclean[i]?[n]?[g]\s+\w+\s+furniture\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean furniture'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean surfaces
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean\s+floor\w*|\bclean\s+surface\w*|\bclean\s+\w+\s+floor\w*|\bclean\s+\w+\s+surface\w*', text)
        match_2 = o(r'\bclean\b', text)
        match_3 = o(r'\bsurface\w*|\bfloor\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean surfaces'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain pricing database
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpricing\s+database\w*|\bpricing\s+datum\w*|\bprice\s+datum\w*|\bpricing\s+data\w*|\bprice\s+data\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain pricing database'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform pricing operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduct\s+pricing\w*|\bpricing\s+analyst\w*', text)
        match_2 = o(r'\bpricing\w*', text)
        match_3 = o(r'\banaly\w*|\bproposal\w*|\btender\w*|\bprocurement\w*|\boperation\w*|\bmanage\w*|\bperform\w*|\bconduct\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform pricing operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for comprehend financial business terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+acumen\w*|\bfinancial\s+knowledge\w*|\bfinancial\s+expertise\w*|\bbusiness\s+acumen\w*|\bbusiness\s+knolwedge\w*|\bbusiness\s+expertise\w*|\bcommercial\s+acumen\w*|\bfinnancial\s+acumen\w*|\bcommercial\s+process\w*|\bcommercial\s+activity\w*|\bbusiness\s+develop\w*|\bfinancial\s+litera\w*|\bfinance\s+litera\w*|\bunderstand[i]?[n]?[g]?\s+business\w*', text)
        match_2 = o(r'\bbusiness\w*', text)
        match_3 = o(r'\bfinance\w*|\bfinancial\w*|\bcommercial\w*|\bknowledg\w*|\bexpertise\w*|\bexperience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'comprehend financial business terminology'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calculate return on investment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breturn\s+investment\w*', text)
        match_2 = o(r'\broi\b', text)
        match_3 = o(r'\bmarketing\w*|\binvest\w*|\bfinance\w*|\baccounting\w*|\bbookkeep\w*|\bbook-keep\w*|\bbook\s+-\s+keep\w*|\baccountant\w*|\bcaluclate\w*|\bestimate\w*|\bmanage\w*|\bensure\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calculate return on investment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate counterbalance forklift
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcounterbalance\s+license\w*', text)
        match_2 = o(r'\bcounterbalance\w*', text)
        match_3 = o(r'\bforklift\w*|\bfork\s+lift\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate counterbalance forklift'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate forklift
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'forklift\w*|\bfork\s+lift\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate forklift'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for store warehouse goods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforklift\s+operation\w*|\bstoreman\w*', text)
        match_2 = o(r'\bforklift\w*|\bpick[i]?[n]?[g]?\b|\bpack[i]?[n]?[g]?\b|\bstore\s+good\w*|\bstorage\w*|\bstore\s+\w+\s+good\w*', text)
        match_3 = o(r'\bwarehouse\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'store warehouse goods'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform loading and unloading operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bunload\s+item\w*|\bload\s+item\w*', text)
        match_2 = o(r'\bforklift\w*|\bwarehouse\w*|\bunload\w*|\bload\w*', text)
        match_3 = o(r'\bdelivery\s+vehicle\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform loading and unloading operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate warehouse record systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpackaging\s+record\w*|\bstoreman\b|\bwarehouse\s+record\s+system\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate warehouse record systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain company records
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brecord\s+keeping\w*|\bmaintain\s+record\w*|\bkeep\s+record\w*|\binformation\s+record\w*|\becord\s+information\w*|\bmonitor\s+record\w*|\brecord\s+monitor\w*|\bmanag[e]?[i]?[n]?[g]?\s+record\w*|\bcomputerized\s+record\w*|\bapprove\s+quality\s+record\w*|\breview[i]?[n]?[g]?\s+quality\s+record\w*|\breview[i]?[n]?[g]?\s+\w+\s+quality\s+record\w*|\bmaintain\s+\w+\s+record\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+record\w*', text)
        match_2 = o(r'\bretention\s+schedul\w*', text)
        match_3 = o(r'\brecords\b|\brecord\b|\bdocument\b|\bdocumentation\b|\bdocuments\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain company records'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform warehouse operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeneral\s+warehouse\s+operation\w*|\bwarehouse\s+operation\w*|\bstoreman\b|\bpalletizing\w*|\bpalletize\b|\bwarehouse\s+operative\w*|\bwarehousing\s+operative\w*|\bwarehous[e]?[i]?[n]?[g]?\s+activity\w*|\bwarehous[e]?[i]?[n]?[g]?\s+practice\w*|\bwarehous[e]?[i]?[n]?[g]?\s+experience\w*|\bwarehous[e]?[i]?[n]?[g]?\s+service\w*|\bwarehouse\s+activit\w*|\bwarehouse\s+worker\w*', text)
        match_2 = o(r'\bpick\w*|\bpack\w*|\bload\b|\bunload\b', text)
        match_3 = o(r'\bwarehouse\w*|\blift\w*|\boperation\w*|\bperform\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform warehouse operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage logistics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+logistic\w*|\blogistic\s+manage\w*|\blogistic[s]?\s+coordinat\w*|\blogistic[s]?\s+ordinat\w*|\blogistic[s]?\s+oordinat\w*|\blogistic[s]?\s+co\s?[-]?\s?ordinat\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+logistic\w*|\bmanage\s+warehous\w*|\blogistic[s]?\s+strategy\w*|\blogistic[s]?\s+system\w*|\blogistic[s]?\s+specialist\w*|\bmanage\s+\w+\s+warehous\w*|\blogistic[s]?\s+service\w*|\bcontrol[l]?[i]?[n]?[g]?\s+logistic\w*|\bmaterial\s+movement\s+ticket\w*|\bcontrol[l]?[i]?[n]?[g]?\s+\w+\s+logistic\w*', text)
        match_2 = o(r'\bshipping\s+logistic\w*|\blogistic\s+pipeline\w*|\blogistic\s+warehous\w*|\bwarehousing\b', text)
        match_3 = o(r'\badministrator\w*|\bmanager\w*|\blogistic\w*|\bmmt[s]?\w*', text)
        match_4 = o(r'\bshipping\w*|\bwarehous\w*|\bdocument\w*|\bmanag[e]?[i]?[n]?[g]?\b|\bmanagement\b|\bcontrol\w*|\bpolic\w*', text)
        match_5 = o(r'\blogistic\w*|\bcoi\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage logistics'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clean warehouse
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwarehouse\s+clean\w*', text)
        match_2 = o(r'\bwarehouse\b', text)
        match_3 = o(r'\bclean\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'clean warehouse'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for move objects
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blift\w*|\bmove\w*|\bpush\w*', text)
        match_2 = o(r'\bpush\w*|\blift\w*|\bmove\w*', text)
        match_3 = o(r'\bwarehouse\w*|\bobject\w*|\bgood[s]?\b', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'move objects'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure correct goods labelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blabel\s+dispatch\w*|\bdispatch\s+label\w*|\blabelling\w*|\blabeling\b|\bcreate\s+label\w*|\blabel\s+good\w*|\bensure\s+label\w*|\blabel\s+product\w*|\bproduct\s+label\w*|\bstick\s+label\w*', text)
        match_2 = o(r'\blabel\w*', text)
        match_3 = o(r'\bwarehouse\w*|\bgood[s]?\b|\bproduct\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure correct goods labelling'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for deliver sales pitch and demonstrate motivation for sales
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsale[s]?\s+pitch\w*|\binvestor\s+pitch\w*|\bpitch[i]?[n]?[g]?\s+sale\w*|\bpitch[i]?[n]?[g]?\s+invest\w*', text)
        match_2 = o(r'\bsale\w*', text)
        match_3 = o(r'\bonsite(?! train)(?! access)(?! parking)(?! fitness)\w*|\bon\s+site(?! train)(?! access)(?! parking)(?! fitness)\w*', text)
        match_4 = o(r'\bcustomer\w*|client\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'deliver sales pitch and demonstrate motivation for sales'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage warehouse distribution operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistribution\s+warehouse\w*|\bstoreman\b|\bwarehouse\s+distribution\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage warehouse distribution operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform accounting operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\b|\bsatuatory\s+report\w*|\bbookkeep\w*|\baccountant\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform accounting operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise accounting operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\b|\bbookkeep\w*', text)
        match_2 = o(r'\bsupervise\w*|\bsupervision\w*|\boversee\w*|\boversight\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise accounting operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform forensic accounting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\b|\bbookkeep\w*|\baccountant\b', text)
        match_2 = o(r'\bforensic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform forensic accounting'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform cost accounting activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcost\s+accountant\w*|\bcost\s+accounting\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform cost accounting activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare financial auditing reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\b|\bbookkeep\w*', text)
        match_2 = o(r'\baudit\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare financial auditing reports'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control financial resources
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontrol\s+financial\s+resources\w*|\bfinancial\s+control\s+procedure\w*|\bfinancial\s+control\s+report\w*|\bfinancial\s+controller\w*|\bcontrol\s+financial\s+procedures\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'control financial resources'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare request for proposal
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconsult\s+\brfp\b|\brfp\b\s+consult\w*|\brequest\s+proposal\s+bid\w*|\bbid\s+contract\w*', text)
        match_2 = o(r'\brfp\b', text)
        match_3 = o(r'\bprocurement\b|\bproject\b|\bproduct\b|\bproposal\w*|\btender\b|\bbid\b|\bprepare\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare request for proposal'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for produce requests for quoatations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconsult\s+\brfq\b|\brfq\b\s+consult\w*', text)
        match_2 = o(r'\brfq\b', text)
        match_3 = o(r'\bprocurement\b|\bproject\b|\brequest\w*|\bitt\b|\bitb\b|\brqt\b|\bproduct\b|\bproposal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'produce requests for quoatations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for arrange audit
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplan\s+audit\b|\barrange\s+audit\w*|\borganise\s+audit\w*|\bmanag[e]?[i]?[n]?[g]?\s+audit\w*|\bprepare\s+audit\w*|\borganize\s+audit\w*|\bperform\s+audit\w*|\bstrategic\s+review\s+audit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'arrange audit'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage the general ledger
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpurchase\s+ledger\b|\bgeneral\s+ledger\b', text)
        match_2 = o(r'\bgeneral\s+ledger\w*|\bgl\b', text)
        match_3 = o(r'\bfinance\w*|\baccounting\w*|\bbookkeep\w*|\bbook-keep\w*|\bbook\s+-\s+keep\w*|\bmaintain\w*|\bmanage\w*|\bmanagining\w*|\boversee\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage the general ledger'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply numeracy skills
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnumercay\s+skill\b|\bnumerical\s+skill\b|\bengineering\s+calculation\w*|\bdegree\s+numeracy\w*|\bdegree\s+numerical\w*|\bnumerical\s+abiliy\w*|\bnumerical\s+\w+\s+skill\w*|\bnumeracy\s+\w+\s+skill\w*', text)
        match_2 = o(r'\bnumeracy\w*|\bnumerical\w*', text)
        match_3 = o(r'\bcalculat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply numeracy skills'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calculate tax and prepare tax return forms
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btax\s+return\w*|\bvat\s+return\w*|\btax\s+report\w*|\breport[i]?[n]?[g]?\s+tax\w*|\btax\s+return\w*|\btax\s+manager\w*|\bmanag[e]?[i]?[n]?[g]?\s+tax\w*|\bmanag[e]?[i]?[n]?[g]?\s+tax\w*', text)
        match_2 = o(r'\btax\w*|\bvat\b|\btaxation\w*|\btaxes\b', text)
        match_3 = o(r'\baccounting\b|\bbookkeep\w*|\bdocument\w*|\breturn\w*|\bform\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calculate tax and prepare tax return forms'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform balance sheet operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbalance\s+sheet\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform balance sheet operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advise on tax planning and policy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btax\s+plan\w*|\bplan\s+tax\w*|\btax\s+strategy\w*|\btaxation\s+strategy\w*|\btax\s+advice\w*|\badvise\s+tax\b|\badvise\s+taxation\w*', text)
        match_2 = o(r'\btax\w*|\btaxation\w*|\btaxes\b', text)
        match_3 = o(r'\bcorporat\w*|\bpolicy\w*|\bpolicies\w*|\badvice\w*|\bconsult\w*|\bguidance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advise on tax planning and policy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for support managers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badvise\s+manager\w*|\badvise\s+supervisor\w*|\badvice\s+manager\w*|\badvice\s+supervisor\w*|\bassist[i]?[n]?[g]?\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bhelp[i]?[n]?[g]?\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bsupport[i]?[n]?[g]?\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bmanage[r]?[m]?[e]?[n]?[t]?\s+assistance|\bmanage[r]?[m]?[e]?[n]?[t]?\s+support\w*|\bassist[i]?[n]?[g]?\s+supervisor\w*|\bhelp[i]?[n]?[g]?\s+supervisor\w*|\bsupport[i]?[n]?[g]?\s+supervisor\w*|\bsupervisor[y]?\s+assistance|\bsupervisor[y]?\s+support\w*|\badvise\s+\w+\s+manager\w*|\badvise\s+\w+\s+supervisor\w*|\badvice\s+\w+\s+manager\w*|\badvice\s+\w+\s+supervisor\w*|\bassist[i]?[n]?[g]?\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bhelp[i]?[n]?[g]?\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bsupport[i]?[n]?[g]?\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bassist[i]?[n]?[g]?\s+\w+\s+supervisor\w*|\bhelp[i]?[n]?[g]?\s+\w+\s+supervisor\w*|\bsupport[i]?[n]?[g]?\s+\w+\s+supervisor\w*|\bassist[i]?[n]?[g]?\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bhelp[i]?[n]?[g]?\s+\w+\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bsupport[i]?[n]?[g]?\s+\w+\s+\w+\s+manage[r]?[m]?[e]?[n]?[t]?\b|\bassist[i]?[n]?[g]?\s+\w+\s+\w+\s+supervisor\w*|\bhelp[i]?[n]?[g]?\s+\w+\s+\w+\s+supervisor\w*|\bsupport[i]?[n]?[g]?\s+\w+\s+\w+\s+supervisor\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'support managers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform accounts payable analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+\baccount[s]?\s+payable\w*|\baccount[s]?\s+payable[s]?\s+manage\w*|\bsupervise\s+\baccount[s]?\s+payable\w*|\baccount[s]?\s+payable[s]?\s+supervis\w*|\banalyze\s+\baccount[s]?\s+payable\w*|\baccount[s]?\s+payable[s]?\s+assist\w*', text)
        match_2 = o(r'\baccount[s]?\s+payable\w*', text)
        match_3 = o(r'\banaly\w*|\bmonitor\w*|\bmanage\w*|\bsupport\w*|\bmanaging\w*|\boversee\w*|\boversight\w*|\bstatement\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform accounts payable analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor banking activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbanking\s+analys\w*|\banalys[e]?[i]?[s]?\s+bank\w*|\bmonitor[i]?[n]?[g]?\s+bank\w*|\binspect\s+bank\w*|\bbank\s+inspect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor banking activities'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for disseminate information on tax legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btax\s+\blegislation\w*|\btax\s+\blaw\w*|\btax\s+regulation\w*|\btax\s+guideline\w*|\bhmrc\s+procedure\w*', text)
        match_2 = o(r'\btax\w*', text)
        match_3 = o(r'\blaw\w*|\bregulation\w*|\blegislation\w*|\blitigation\w*|\bpolicy\w*|\bpolicies\w*|\bguideline\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'disseminate information on tax legislation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct report to report
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\br2r\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct report to report'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform marketing management operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btactical\s+marketing\w*|\bmarketing\s+manager\w*|\bmanag[e]?[i]?[n]?[g]?\s+marketing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform marketing management operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for collaborate in the development of marketing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarketing\s+communication\w*|\bmarketing\s+collaboration\w*|\bcollaborate\s+marketing\w*|\bcommunicate\s+marketing\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'collaborate in the development of marketing strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prospect new regional contracts and expand store regional presence
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bregional\s+marketing\w*|\bregional\s+contract\w*|\bmanag[e]?[i]?[n]?[g]?\s+regional\w*|\bregional\s+management\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prospect new regional contracts and expand store regional presence'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan marketing strategy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarketing\s+plan\w*|\bmarket\s+plan\w*|\bplan[n][i]?[n]?[g]?\s+marketing\w*|\bmarketing\s+strategy\w*|\bmarket\s+strategy\w*|\bstrategy\s+marketing\w*|\bstrategic\s+marketing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan marketing strategy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for draw up marketing and sales plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsale[s]?\s+plan\s+coordinat\w*|\bsale[s]?\s+plan\s+co\s+-\s+ordinat\w*|\bsale[s]?\s+plan\s+ordinat\w*|\bcoordinate\s+sale[s]?\s+plan\w*|\bco\s+-\s+ordinate\s+sale[s]?\s+plan\w*|\bordinate\s+sale[s]?\s+plan\w*|\bsale[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+sale\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'draw up marketing and sales plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct strategic sourcing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsourcing\s+strategy\w*|\bstrategy\s+sourcing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct strategic sourcing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct mobile marketing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmobile\s+marketing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct mobile marketing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for execute email marketing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemail\s+marketing\w*|\bemail\s+strategy\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'execute email marketing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan digital marketing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+marketing\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan digital marketing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify talent
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btalent\s+acquisition(?! system)\w*|\btalent\s+sourcer\b|\btalent\s+pipeline\w*|\btalent\s+pool\w*', text)
        match_2 = o(r'\bta\b', text)
        match_3 = o(r'\brecruiting\b|\brecruit\w*|\bsourcing\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify talent'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop creative ideas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcreativity\w*|\binnovative\s+idea\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop creative ideas'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for execute marketing plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexecution\s+marketing\w*|\bmarketing\s+execution\w*|\bexecute\s+marketing\s+plan\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'execute marketing plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan or assist in developing marketing campaigns
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'marketing\s+campaign\w*|\bcampaign\s+marketing\w*|\bcampaign\s+officer\w*', text)
        match_2 = o(r'\bmarketing\b', text)
        match_3 = o(r'\bcampaign\w*|\bstrategy\w*', text)
        match_4 = o(r'\bdevelop\w*|\bmanage\w*|\bmanaging\w*|\bassist\w*|\bplan\w*|\bcreat\w*|\bdesign\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan or assist in developing marketing campaigns'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct ecological research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becology\b|\becologist\b|\becological\w*', text)
        match_2 = o(r'\bresearch\w*|\bstudy\w*|\bscientist\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct ecological research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform market research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarket\s+research\w*|\bmarket\s+analy\w*|\banalyze\s+market\w*|\banalyse\s+makret\w*|\bassess\s+market\w*|\bmarket\s+assess\w*|\binvestigate\s+market\w*|\bmarket\s+investigat\w*|\bproposal\s+specialist\w*|\bservicable\s+addressable\s+market\w*|\btotal\s+addressable\s+market\w*|\btotal\s+availvable\s+market\w*|\bmarket\s+share\s+analy\w*|\banalyse\s+market\s+share\w*|\bassess\s+market\s+share\w*|\banalyze\s+market\s+share\w*|\banalysis\s+market\s+share\w*|\binvestigat[i]?[o]?[n]?\s+market\s+share\w*', text)
        match_2 = o(r'\btarget\s+market\w*|\btam\b|\bsas\b', text)
        match_3 = o(r'\bmarketing\b|\bcampaign\b|\bsale[s]?\s+manager\w*|\bperform\w*|\bconduct\w*|\banaly\w*|\bdemand\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform market research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for think analytically
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalytical\s+skill\w*|\banalytical\s+experience\w*|\banalytical\s+thinking\w*|\bthink\s+analytically\w*|\bstructural\s+analy\w*|\bthink\s+logical\w*|\blogical\s+think\w*|\banalyse\s+product\w*|\bdata\s?[-]?\s?driven\s+approach\w*|\banalytic[a]?[l]?\s+technique\w*|\banalysis\s+technique\w*|\banalytical\s+test\w*|\bevaluat[e]?[i]?[n]?[g]?\s+result\w*|\bassess[i]?[n]?[g]?\s+result\w*|\banaly[s]?[i]?[n]?[g]?\s+result\w*|\bevaluat[e]?[i]?[n]?[g]?\s+outcome\w*|\bassess[i]?[n]?[g]?\s+outcome\w*|\banaly[s]?[i]?[n]?[g]?\s+outcome\w*|\bresult[s]?\s+analy\w*|\bresult[s]?\s+assess\w*|\bresult[s]?\s+evaluat\w*|\boutcome[s]?\s+analy\w*|\boutcome[s]?\s+assess\w*|\boutcome[s]?\s+evaluat\w*', text)
        match_2 = o(r'\banalytic[s]?(?! drive)(?! challange)\w*|\banalytical(?! drive)(?! challange)\w*', text)
        match_3 = o(r'\bskill\w*|\bability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'think analytically'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare financial projections and assess other project metrics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalyse\s+project\w*|\bfinance\s+projection\w*|\bfinancial\s+projection\w*|\bassess[i]?[n]?[g]?\s+project\w*|\bproject\s+performance\w*|\bperformance\s+project\w*|\bproject\s+metric\w*|\bproject\s+analy\w*|\banalyze\s+project\w*|\bevaluat[e]?[i]?[n]?[g]?\s+project\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare financial projections and assess other project metrics'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop project schedule
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+scheduler\w*|\bprovid[e]?[i]?[n]?[g]?\s+schedule\w*|\bproject\s+scheduling\w*|\bproject\s+coordinat\w*|\bproject\s+ordinat\w*|\bproject\s+co\s+-\s+ordinat\w*|\bcoordinate\s+project\w*|\bco\s+-\s+ordinate\s+project\w*|\boordinate\s+project\w*|\bdevelop\s+project\s+schedule\w*|\bmanag[e]?[i]?[n]?[g]?\s+project\s+schedule\w*|\bdesign\s+project\s+scedule\w*|\bcreate\s+project\s+schedule\w*|\btest[i]?[n]?[g]?\s+schedule\w*|\bdevelop[m]?[e]?[n]?[t]?\s+project\s+timeline\w*|\bdevelop[m]?[e]?[n]?[t]?\s+project\s+schedule\w*|\bprovid[e]?[i]?[n]?[g]?\s+\w+\s+schedule\w*|\bprovid[e]?[i]?[n]?[g]?\s+\w+\s+project\s+schedule\w*', text)
        match_2 = o(r'\bplan\w*|\bdevelop\w*|\bcreat\w*|\bmanage\w*|\bmanaging\w*', text)
        match_3 = o(r'\bschedule\w*', text)
        match_4 = o(r'\bproject\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop project schedule'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage sales and distribution channels
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchannel\s+management\w*|\bsale[s]?\s+channel\w*|\bdistribution\s+channel\w*|\bmarketing\s+channel\w*|\btrade\s+channel\w*|\bsales\s+instrument\w*', text)
        match_2 = o(r'\bexport\b|\bdistribut\w*', text)
        match_3 = o(r'\bsale\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage sales and distribution channels'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage business plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+plan\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage business plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop online sales business plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bonline\s+business\s+plan\w*', text)
        match_2 = o(r'\bsale\w*|\bselling\w*|\bmarketing\w*|\bdevelop\w*|\bcreat\w*|\bmanage\w*|\bmanaging\w*|\bdesign\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop online sales business plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform customer service operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcommunicate\s+customer\w*|\bcommunicate\s+client\w*|\bcustomer\s+communicate\w*|\binspire\s+customer\w*|\bdevot[e]?[i]?[o]?[n]?\s+customer\w*|\binspire\s+client\w*|\bdevot[e]?[i]?[o]?[n]?\s+client\w*|\bclient\s+face\w*|\bcustomer\s+face\w*|\bmeet[i]?[n]?[g]?\s+client\w*|\bmeet[i]?[n]?[g]?\s+customer\w*|\bcustomer[s]?\s+service\w*|\bclient[s]?\s+services\b|\bconsult\s+customer\w*|\bconsult\s+client\w*|\bdeal\s+customer\w*|\bhandl[e]?[i]?[n]?[g]?\s+customer\w*|\bexperience\s+customer\s+service\w*|\bcustomer\s+service\s+experience\w*|\bdeal\s+client\w*|\bdeal\s+customer\w*|\bline\s+technical\s+support\w*|\bcustomer\s+appointment\w*|\bappointment\s+customer\w*|\bclient\s+appointment\w*|\bappointment\s+client\w*|\bassist\s+customer\w*|\bassist\s+client\w*|\bcustomer[s]\s+assist\w*|\bclient[s]\s+assist\w*|\binternal\s+customer\w*|\binternal\s+client\w*|\bexternal\s+customer\w*|\bexternal\s+client\w*|\bensure\s+client\w*|\bensure\s+customer\w*|\bsupport[i]?[n]?[g]?\s+customer\w*|\bsupport[i]?[n]?[g]?\s+client\w*|\bcustomer[s]?\s+support\w*|\bclient[s]?\s+support\w*|\binteract[i]?[n]?[g]?\s+customer\w*|\binteract[i]?[o]?[n]?\s+customer\w*|\binteract[i]?[n]?[g]?\s+client\w*|\binteract[i]?[o]?[n]?\s+client\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform customer service operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for moderate a discussion
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmoderat[e]?[i]?[n]?[g]?\s+discussion\w*|\bguid[e]?[i]?[n]?[g]?\s+discussion\w*|\blead[i]?[n]?[g]?\s+discussion\w*|\boversee[i]?[n]?[g]?\s+discussion\w*|\bmoderat[e]?[i]?[n]?[g]?\s+workshop\w*|\bmoderat[e]?[i]?[n]?[g]?\s+event\w*|\bmoderat[e]?[i]?[n]?[g]?\s+conference\w*|\bdiscussion\s+moderat\w*|\bdiscussion\s+oversight\w*|\bdiscussion\s+guidance\w*|\bmoderat[e]?[i]?[n]?[g]?\s+\w+\s+discussion\w*|\blead[i]?[n]?[g]?\s+\w+\s+discussion\w*|\boversee[i]?[n]?[g]?\s+\w+\s+disucssion\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'moderate a discussion'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage customer experience
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer[s]?\s+experience\w*|\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+stakeholder\w*|\bcustomer\s+requirement\w*|\bcustomer[s]?\s+satisfaction\w*|\bclient[s]?\s+satisfaction\w*|\bcustomer[s]?\s+expectation\w*|\bclient[s]?\s+expectation\w*|\bstakeholder[s]?\s+expectation\w*|\bsatisfy\s+customer\w*|\bsatisfy\s+client\w*|\bliais[e]?[o]?[n]?\s+customer\w*|\bliais[e]?[o]?[n]?\s+client\w*|\bliais[e]?[o]?[n]?\s+stakeholder\w*|\bliais[e]?[o]?[n]?\s+\w+\s+customer\w*|\bliais[e]?[o]?[n]?\s+\w+\s+client\w*|\bliais[e]?[o]?[n]?\s+\w+\s+stakeholder\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+stakeholder\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage customer experience'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for communicate by telephone
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\buse\s+phone\w*|\buse\s+telephone\w*|\bdeal[i]?[n]?[g]?\s+telephone\w*|\bdeal[i]?[n]?[g]?\s+phone\w*|\binbound\s+call\w*|\bhandl[e]?[i]?[n]?[g]?\s+telephone\w*|\btelephone\s+call\w*|\bphone\s+outreach\w*|\bcommunicate\s+phone\w*|\bcommunicate\s+telephone\w*|\breciev[e]?[i]?[n]?[g]?\s+call\w*|\bdeal[i]?[n]?[g]?\s+phone\w*|\bhandl[e]?[i]?[n]?[g]?\s+phone\w*|\bphone\s+manner\w*|\btelephone\s+manner\w*|\btak[e]?[i]?[n]?[g]?\s+phone\w*|\btak[e]?[i]?[n]?[g]?\s+telephone\w*|\banswer[i]?[n]?[g]?\s+phone\w*|\banswer[i]?[n]?[g]?\s+telephone\w*|\bprocess[i]?[n]?[g]?\s+telephone\w*|\breciev[e]?[i]?[n]?[g]?\s+telephone\w*|\breciev[e]?[i]?[n]?[g]?\s+\w+\s+telephone\w*|\breciev[e]?[i]?[n]?[g]?\s+\w+\s+call\w*|\bphone\s+\w+\s+outreach\w*|\banwser[i]?[n]?[g]?\s+\w+\s+call\w*|\binbbound\s+\w+\s+call\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'communicate by telephone'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for type on electronic devices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btype\s+computer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'type on electronic devices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use internet chat
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blive\s+chat\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use internet chat'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prospect new customers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer\s+acquisition\w*|\bclient\s+acquisition\w*|\bacquire\s+customer\w*|\bacquire\s+client\w*|\bexpand\s+customer\w*|\bexpand\s+client\w*|\bprospect\s+customer\w*|\bprospect\s+client\w*|\bprospective\s+customer\w*|\bprospective\s+clients\w*|\bdevelop\s+\w+\s+contract\w*', text)
        match_2 = o(r'\bnew\b\s+customer\w*|\bnew\b\s+client\w*|\bnew\s+contract\w*', text)
        match_3 = o(r'\bsale\w*|\bmarketing\w*|\bprospect\w*|\bacquisition\w*|\bqcquire\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prospect new customers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse cell cultures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcell\s+culture\w*|\bcell\s+analy\w*|\bcell\s+sort\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse cell cultures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse sales operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banaly[s]?[e]?[i]?[s]?\s+sale[s]?\s+operation\w*|\banaly[z]?[e]?\s+sale[s]?\s+operation\w*', text)
        match_2 = o(r'\bsales\s+operations\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2:
            matched_fragments.extend([match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse sales operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse scientific data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bevaluate\s+study\s+datum\b|\breview\s+study\s+datum\w*|\breview\s+study\s+data\b|\bevaluation\s+study\s+datum\b|\bevaluate\s+scientific\s+datum\b|\bevaluation\s+scientific\s+data\b|\bscientific\s+evaluation\w*|\bevaluate\s+study\s+data\b|\bevaluation\s+study\s+data\b|\banalytical\s+scientist\w*|\breview\s+scientific\s+paper\w*|\bscientific\s+report\w*', text)
        match_2 = o(r'\bscientific\s+study\w*|\bscientific\s+datum\w*', text)
        match_3 = o(r'\banaly\w*|\breview\w*|\bwork\w*|\beperience\w*|\breport\w*|\bconduct\w*|\bprepare\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse scientific data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse images
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banaly\w*|\bassess\w*|\binvestigate\w*', text)
        match_2 = o(r'\bimage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse images'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse ultrasound images
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banaly\w*|\bassess\w*', text)
        match_2 = o(r'\bcrl\b|\bultrasoud\b', text)
        match_3 = o(r'\bimage\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse ultrasound images'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse microscopy images
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicroscope\w*|\bmicro\b|\bnano\w*|\bpicometer\w*', text)
        match_2 = o(r'\bimage\w*|\bscale\w*', text)
        match_3 = o(r'\bimage\w*', text)
        match_4 = o(r'\banaly\w*|\bassess\w*|\binvestigate\w*|\bresearch\w*', text)
        match_5 = o(r'\bpathology\w*|\bmicroscopic\w*|\bscale\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse microscopy images'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse environmental data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment\w*', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*', text)
        match_3 = o(r'\w*|\banaly\w*|\bassess\w*|\bmonitor\w*|\bmanage\w*|\binvestigate\w*|\bresearch\w*|\bsurvey\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse environmental data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for advice and monitor nature conservation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwildlife\s+conservation\w*|\bnature\s+conservation\w*|\benvironment[a]?[l]?\s+conservation\w*|\benvironment[a]?[l]?\s+prevention\w*|\benvironment[a]?[l]?\s+protection\w*|\bspecie[s]?\s+protect\w*|\bprotect[i]?[n]?[g]?\s+specie\w*|\bprotect\s+animal\w*|\banimal\s+protect\w*', text)
        match_2 = o(r'\bconserve\w*|\bconservation\w*|\brestore\w*|\brestoration\w*', text)
        match_3 = o(r'\bwildlife\w*|\bhabitat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'advice and monitor nature conservation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for educate the public about wildlife
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwildlife\w*|\bnature\s+conservation\w*', text)
        match_2 = o(r'\bteach\w*|\beducate\w*|\blecture\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'educate the public about wildlife'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop environmental remediation strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremediation\w*|\bremedial\w*|\bconservat\w*', text)
        match_2 = o(r'\bplan\w*|\bstrateg\w*|\bproject\w*', text)
        match_3 = o(r'\benvironment\w*|\becolog\w*|\bhabitat\w*|\bwildlife\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop environmental remediation strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for promote environmental awareness
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpromote\s+environment\w*|\benvironment[a]?[l]?\s+awareness\w*|\benvironment[a]?[l]?\s+educat\w*|\beco\s+-\s+school\w*|\bpromot[e]?[i]?[n]?[g]?\s+environment\w*|\bpromot[e]?[i]?[n]?[g]?\s+\w+\s+environment\w*', text)
        match_2 = o(r'\benvironment\w*|\bwildlife\w*|\bhabitat\w*', text)
        match_3 = o(r'\bconservation\w*|\bawareness\w*', text)
        match_4 = o(r'\bteach[i]?[n]?[g]?\w*|\beducat[e]?[i]?[n]?[g]?\w*|\blectur[e]?[i]?[n]?[g]?\w*|\bpromot[e]?[i]?[n]?[g]?\w*|\btutor\w*|\bteach\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'promote environmental awareness'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide advice to scientists
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+support(?! group)\w*|\bscientific\s+guidance\w*|\bscientific\s+oversight\w*|\bassist\s+scientist\w*|\bassistant\s+scientist\w*', text)
        match_2 = o(r'\badvice\w*|\bguidance\w*|\bsupport\w*|\bassist\w*|\bconsult\w*', text)
        match_3 = o(r'\bscientist\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide advice to scientists'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for interact with others
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binteract[i]?[n]?[g]?\s+customer[s]?\b|\binteract[i]?[n]?[g]?\s+employee[s]?\w*|\binteract[i]?[n]?[g]?\s+manager\w*|\binteract[i]?[n]?[g]?\s+supervisor\w*|\binteract[i]?[n]?[g]?\s+leader\w*|\bliaise\s+other[s]?\b|\bliaise\s+supervisor\w*|\bliaise\s+manager\w*|\bliaise\s+employee[s]?\b|\bliaise\s+customer[s]?\b|\bliaise\s+client[s]?\b|\bliaise\s+leader\w*|\binteract[i]?[n]?[g]?\s+other[s]?\w*|\bengag[e]?[i]?[n]?[g]?\s+other[s]?\b|\bengag[e]?[i]?[n]?[g]?\s+client[s]?\b|\bengag[e]?[i]?[n]?[g]?\s+customer[s]?\b|\bengag[e]?[i]?[n]?[g]?\s+employee[s]?\b|\bengag[e]?[i]?[n]?[g]?\s+leader\w*|\bengag[e]?[i]?[n]?[g]?\s+manager\w*|\bengag[e]?[i]?[n]?[g]?\s+supervisor\w*|\bengag[e]?[i]?[n]?[g]?\s+other[s]?\b|\bnetwork[i]?[n]?[g]?\s+skill\w*|\binfluencing\s+skill\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'interact with others'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide mentorship
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'mentoring\w*|\bmentor\s+staff\w*|\bmentor\s+employee\w*|\bmentor\s+other\w*|\bemployee\s+mentorship\w*|\bstaff\s+mentorship\w*|\bmentor[i]?[n]?[g]?\s+\w+\s+staff\w*|\bmentor[i]?[n]?[g]?\s+\w+\s+employee\w*', text)
        match_2 = o(r'(?<!receive )\bmentor\w*', text)
        match_3 = o(r'\bcoach\w*|\bleader\w*|\bjunior\s+staff\w*|\bjunior\s+employee\w*|\bless\s+experience[d]?\s+employee\w*', text)
        match_4 = o(r'\bcompany\s+provide\s+mentorship\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide mentorship'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for promote company values
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapply\s+company\s+value\w*|\bpromote\s+company\s+value\w*|\benforce\s+company\s+value\w*|\bimplement\s+company\s+value\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'promote company values'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare validate and measure raw materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassistant\s+technician\s+formulation\w*|\bformulation\s+laboratory\s+technician\w*|\bformulation\s+lab\s+technician\w*', text)
        match_2 = o(r'\braw\s+material(?! bill)\b', text)
        match_3 = o(r'\bformulation\b|\btest\w*|\bprepar\w*|\bmeasur\w*|\bvalidat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare validate and measure raw materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test raw materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\braw\s+material\s+test\w*|\btest[i]?[n]?[g]?\s+raw\s+material\w*', text)
        match_2 = o(r'\btest\w*|\bassess\w*|\binvestigate\w*|\binspect\w*', text)
        match_3 = o(r'\braw\s+material(?! bill)\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test raw materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for check quality of raw materials
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcompliance\s+raw\s+material\w*|\bmaterial\s+compliance\w*', text)
        match_2 = o(r'\braw\s+material\w*', text)
        match_3 = o(r'\bquality\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'check quality of raw materials'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage resources
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+resources\w*|\bresource[s]?\s+management\w*|\bdirect[i]?[n]?[g]\s+resource[s]?\w*|\borganize\s+resource\w*|\bidentify\s+resource\w*|\bmanag[e]?[i]?[n]?[g]?\s+material\w*|\bresource[s]?\s+schedul\w*|\bresourcing\s+plan\w*|\bresourcing\s+strategy\w*|\bresourcing\s+schedul\w*|\bmaterial\s+manage\w*|(?<!enterprise )\bresource[s]?\s+plan\w*|\bresource[s]?\s+schedule\w*|\bmonitor[i]?[n]?[g]?\s+resource\w*|\bresource[s]?\s+availab\w*|\bensure\s+necessary\s+resource\w*|\bensure\s+essential\s+resource\w*|\bensure\s+crucial\s+resource\w*|\bensure\s+necessary\s+material\w*|\bensure\s+crucial\s+material\w*|\bensure\s+essential\s+material\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+resources\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage resources'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare liquid media
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliquid\s+medium\s+formulation\w*|\bliquid\s+medium\s+preparation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare liquid media'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform stem cell transplantation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpluripotent\s+cell\b|\bstem\s+cell\b', text)
        match_2 = o(r'\bbone\s+marrow\w*|\btransplant\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform stem cell transplantation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform molecular tests
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmolecular\w*', text)
        match_2 = o(r'\btest\w*|\bassay\w*|\bsample\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform molecular tests'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform immunoassay
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunoassay\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform immunoassay'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for administer immunotherapy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+immunotherapy\w*', text)
        match_2 = o(r'\bimmunotherapy\b', text)
        match_3 = o(r'\badminister\w*|\bperform\w*|\bconduct\w*|\bpatient\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'administer immunotherapy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage acute oncology patients
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpet\b|\bcancer\w*|\bpatient\w*|\bcase\b', text)
        match_2 = o(r'\bmetastatic\w*|\bmetastasis\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage acute oncology patients'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide anti-cancer medical treatment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'immunotherapy\w*|\btreatment\w*|\bpatient\w*', text)
        match_2 = o(r'\bcancer\w*|\boncology\w*', text)
        match_3 = o(r'\bmanage\w*|\bcare\w*|\bprovide\w*|\bmanaging\w*|\btreat\b|\bproviding\w*|\bhandle\w*|\bcaring\b', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide anti-cancer medical treatment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for antibody conjugation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bantibody\s+conjugation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'antibody conjugation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform immunodiagnostics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunodiagnostic\w*', text)
        match_2 = o(r'\btest\w*|\bperform\w*|\bconduct\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform immunodiagnostics'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform biomedical tests
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiomedical\s+test\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform biomedical tests'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform bioanalysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbioanalyst\w*|\bbioanalytical\w*|\bbioanalysis\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform bioanalysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform laboratory operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+environment\w*|\blab\b\s+environment\w*|\blaboratory\s+test\w*|\blaboratory\s+experience\w*|\borganise\s+laboratory\w*|\borganise\s+\blab\b|\blab\b\s+experience\w*|\blab\s+experiment\w*|\blaboratory\s+experiment\w*|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+lab\b|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+laboratory\w*|\blaboratory\s+sampling\w*|\blab\b\s+test\w*|\blab\b\s+sampling\w*|\blaboratory\s+datum\s+assess\w*|\blab\s+datum\s+assess\bwork[i]?[n]?[g]?\s+laboratory\w*|\bwork[i]?[n]?[g]?\s+lab\b|\blaboratory\s+analy\w*|\blab\s+analy\w*|\blaboratory\s+duty\w*|\blab\s+duty\w*|\blaboratory\s+assist\w*|\blab\s+assist\w*|\bexperience\s+laboratory\w*|\bexperience\s+lab\b|\blaboratory\s+experience\w*|\blab\s+experience\w*|\blab\s+maintenance\w*|\blab\s+upkeep\w*|\blab\s+technician\w*|\blaboratory\s+maintenance\w*|\blaboratory\s+upkeep\w*|\blaboratory\s+technician\w*|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+lab\b|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+laboratory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform laboratory operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for work in a clean room
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclean\s?room\s+experience\w*|\bclean\s?room\s+knowledge\w*|\bclean\s?room\s+suit\w*|\bclean\s?room\s+environment\w*|\bclean\s?room\s+setting\w*|\bwork[i]?[n]?[g]?\s+clean\s?room\w*|\bclean\s?room\s+operation\w*|\bexperience\s+clean\s?room\w*|\bknowledge\s+clean\s?room\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'work in a clean room'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply safety procedures in laboratory
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+safety\w*|\blaboratory\s+guidelin\w*|\blab\b\s+safety\w*|\blab\b\s+guideline\w*|\bsafety\s+laboratory\w*|\bsafety\s+\blab\b|\bsafe\s+laboratory\s+environment\w*|\bsafe\s+\blab\b\s+environment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply safety procedures in laboratory'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform laboratory testing on samples of animals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstudy\w*|\bresearch\w*|\btest\w*|\bassay\w*', text)
        match_2 = o(r'\banimal\w*|\bmammal\w*|\bbird\w*', text)
        match_3 = o(r'\bsample\w*', text)
        match_4 = o(r'\breplace\s+animal\s+clinical\s+trial\w*', text)
        if match_1 and match_2 and match_3 and not match_4:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform laboratory testing on samples of animals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage inhalation chambers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binhalation\s+technician\w*|\binhalation\s+science\w*|\binhalation\s+chamber\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage inhalation chambers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for make independent operating decisions and work independently
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwork[i]?[n]?[g]?\s+independent\w*|\bindependent[l]?[y]?\s+work\w*|\bwork[i]?[n]?[g]?\s+independent\w*|\bminimum\s+supervis\w*|\bminimal\s+unsupervis\w*|\bown\s+initiative\w*|\bminimal\s+supervis\w*|\bminimum\s+oversight\w*|\bminimal\s+oversight\w*|\bself\s+start[e]?[r]?\b|\bself\s+-\s+start[e]?[r]?\w*|\bself\s+motivate\w*|\bself\s+-\s+motivate\w*|\bself\s+-\s+manage\w*|\bself\s?[-]?\s?direct\w*|\bwork[i]?[n]?[g]?\s+alone\w*|\bwork[i]?[n]?[g]?\s+autonom\w*|\bautonomous[l]?[y]?\s+work\w*|\bwork[i]?[n]?[g]?\s+independent\w*|\bthink\s+independent\w*|\bindependent[l]?[y]?\s+think\w*|\bindependent\s+plann\w*|\bact\s+independent\w*|\bindependent[l]?[y]?\s+perform\w*|\bwork[i]?[n]?[g]?\s+\w+\s+independent\w*|\bwork[i]?[n]?[g]?\s+\w+\s+limited\s+supervis\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'make independent operating decisions and work independently'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for make decisions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmake\s+decision(?<!employment )(?<!hire )(?<!hiring )\w*|\bdecision\s+make\w*|\bmake\s+choice\w*|\bmaking\s+choice\w*|\bmaking\s+decision\w*|\btake\s+decision\w*|\btaking\s+decision\w*|\bpragmatic\s+solve\w*|\bbe\s+decisive\w*|\boptioneer\w*|\bdecision\s+make\w*|\bdecision\s?[-]?\s?make\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'make decisions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for attend to detail
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'attention\s+detail\w*|\beye\s+detail\w*|\bdetail\s+orient\w*|\battend\s+\s+detail\w*|\battend\s+accuracy\w*|\bability\s+accurate\w*|\bcreate\s+accurat[e]?[l]?[y]?\w*|\battend\s+precision\w*|\bdeal\s+detail\w*|\bmanag[e]?[i]?[n]?[g]?\s+detail\w*|\bsort\s+detail\w*|\bwork\s+precis\w*|\bwork\s+careful\w*|\beye\s+detail\s+quality\w*|\battending\s+\s+detail\w*|\battending\s+accuracy\w*|\battending\s+precision\w*|\bdealing\s+detail\w*|\bsorting\s+detail\w*|\bworking\s+precis\w*|\bworking\s+careful\w*|\bdive\s+detail\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'attend to detail'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use ilustration techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bilustration\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use ilustration techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage field operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfield\s+operations\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+field\s+operation\w*|\bmanag[e]?[i]?[n]?[g]?\s+field\s+work\w*|\borganise\s+field\s+work\w*|\borganize\s+field\s+work\w*|\borganise\s+field\s+operation\w*|\borganize\s+field\s+operation\w*|\bsupervise\s+field\s+work\w*|\bsupervise\s+field\s+operation\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+field\s+work\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+field\s+study\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+field\s+operation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage field operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform backups
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbackup\w*', text)
        match_2 = o(r'\bdata\b|\bdatum\w*|\bdatabase\w*|\bsystem\w*|\bnetwork\w*|\brecovery\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform backups'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse network configuration and performance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconfigur\w*', text)
        match_2 = o(r'\bnetwork\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse network configuration and performance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply endpoint detection and response
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bapply\w*|\bdeploy\w*|\bmanage\w*|\bmanaging\w*|\bconfigur\w*|\bset\w*', text)
        match_2 = o(r'\bedr\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply endpoint detection and response'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply extended detection and response
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bapply\w*|\bdeploy\w*|\bmanage\w*|\bmanaging\w*|\bconfigur\w*|\bset\w*', text)
        match_2 = o(r'\bxdr\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply extended detection and response'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop information security strategy or policy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b', text)
        match_2 = o(r'\bstrategy\w*|\bstrategic\s+plan\w*|\bprocedure\w*|\bpolicy\w*', text)
        match_3 = o(r'\bcreate\w*|\bdevelop\w*|\badvise\w*|\bmanage\w*|\bmanaging\w*|\bcreating\w*|\bdesign\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop information security strategy or policy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure information security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure information security'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply information security policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bisa\b', text)
        match_2 = o(r'\bpolicy\w*|\bprocedure\w*|\bpolicies\b|\bguideline\w*|\brule\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply information security policies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain database security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bsecure\w*|\bprotect\w*', text)
        match_2 = o(r'\bdatabase\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain database security'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage system security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnetwork\s+security\b|\bsystem\s+security\w*|\bsecurity\s+system\w*|\bsecurity\s+network\w*|\bics\s+security\w*|\biacs\s+security\w*', text)
        match_2 = o(r'\bsecurity\s+engineer\w*|\bsecurity\s+specialist\w*', text)
        match_3 = o(r'\bnetwork\w*|\bsystem\w*|\bics\b|\biacs\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage system security'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ensure email security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bensure\w*|\bdevelop\w*', text)
        match_2 = o(r'\bmail\s+security\w*|\bemail\s+security\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'ensure email security'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform database operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatabase\w*|\bdatum\s+query\w*|\bdata\s+query\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform database operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate relational database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brdbms\b|\brelational\s+database\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate relational database management system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use electronic case report form
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becrf\b', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*|\breport\w*|\buse\w*|\bperform\w*|\boperation\w*|\bhandle\w*|\bmanage\w*|\bhandling\w*|\bmanaging\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use electronic case report form'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use electronic batch records
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectronic\s+batch\s+record\w*', text)
        match_2 = o(r'\bebr\b', text)
        match_3 = o(r'\bdata\w*|\bdatum\w*|\breport\w*|\buse\w*|\bperform\w*|\boperation\w*|\bhandle\w*|\bmanage\w*|\bhandling\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use electronic batch records'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for remote diagnostics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremote\s+diagnostic\s+\bservice\w*', text)
        match_2 = o(r'\bremote\s+diagnostic\w*|\bremote\s+maintenance\w*', text)
        match_3 = o(r'\bsecurity\w*|\bcybersecurity\w*|\bcomputer\w*\w*|\service\w*|\bclient\w*|\bcustomer\w*|\bnetwork\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'remote diagnostics'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for publish academic research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpublish\s+academic\s+research\w*|\bresearch\s+proposal\w*|\bpublish\s+academic\s+study\w*|\bconduct\s+academic\s+research\w*|\bconduct\s+academic\s+study\w*|\bperform\s+academic\s+study\w*|\bacademic\s+researcher\b|\blead\s+adademic\s+research\w*|\bpublish\s+\w+\s+journal\w*|\bpublish\s+academic\s+\w+\s+research\w*|\bconduct\s+academic\s+\w+\s+research\w*|\blead\s+academic\s+\w+\s+research\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'publish academic research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for present publishing plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstudy\s+publisher\b|\breport\s+publisher\b|\bpublishing\w*', text)
        match_2 = o(r'\bplan\b|\bstrategy\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'present publishing plan'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply for research funding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfund[i]?[n]?[g]?\s+proposal\w*|\bfund[i]?[n]?[g]?\s+application\w*|\bresearch\s+funding\w*|\breport\s+publishing\b|\bresearch\s+grant\w*|\bgrant\s+application\w*|\bresearch\s+bid\w*|\bresearch\s+\w+\s+bid\w*', text)
        match_2 = o(r'\bapply\w*|\bprepare\w*|\bmanage\w*|\bmanaigng\w*|\bdevelop\w*|\bcreate\w*|\bcreating\w*|\bgrow\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply for research funding'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage research data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bresearch\s+datum\w*|\bresearch\s+\w+\s+datum\w*', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*', text)
        match_3 = o(r'\bresearch\w*', text)
        match_4 = o(r'\blab\w*|\blaboratory\w*|\bclinical\w*|(?<!fisher )\bscientific(?! challenge)\w*|\bmanage\w*|\bassess\w*|\bmanaging\w*|\banaly\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage research data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for record test or survey data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b(?!track )record[i]?[n]?[g]?\w*|\bpiq\b|\breport[i]?[n]?[g]?\w*|\bcollect\w*|\bgather\w*|\bcollat[e]?[i]?[n]?[g]?\w*|\bcaptur[e]?[i]?[n]?[g]?\w*', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*', text)
        match_3 = o(r'\btest\b|\bsurvey\b|\bresearch\w*|\bstudy\b|\btechnical\s+report\w*|\banalysis\b|\bresearch\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'record test or survey data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse and gather experimental data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexperiment[a]?[l]?\s+work\w*|\bexperiment[a]?[l]?\s+data\b|\bexperiment[a]?[l]?\s+datum\w*|\blab\s+experiment\w*|\blaboratory\s+experiment\w*', text)
        match_2 = o(r'\blab\s+data\b|\blab\s+datum\w*|\blaboratory\s+datum\w*|\blaboratory\s+data\b', text)
        match_3 = o(r'\bstudy\w*|\bassess\w*|\bresearch\w*|\banaly\w*', text)
        match_4 = o(r'\bexperiment\w*', text)
        match_5 = o(r'\banalyse\s+result\w*|\bresult\s+analy\w*|\bdatum\s+analy\w*|\banalyse\s+datum\w*|\banalyze\s+datum\w*|\bdata\s+analy\w*|\banalyse\s+data\w*|\banalyze\s+data\w*|\banalysis\s+data\w*|\banalysis\s+datum\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse and gather experimental data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for process and manage data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+handl\w*|\bdatum\s+handl\w*|\bdata\s+management\w*|\bdatum\s+transfer\w*|\btransfer\s+datum\w*|\bdata\s+transfer\w*|\btransfer\s+data\b|\bmanag[e]?[i]?[n]?[g]?\s+datum\w*|\btechnology\s+transfer\w*|\bcollate\s+data\b|\bcollate\s+datum\w*|\bmanag[e]?[i]?[n]?[g]?\s+data\b|\bdatum\b\s+management\w*|(?<!criminal record )\bdatum\s+process\w*|(?<!criminal record )\bdata\s+process\w*|\bprocesssing\s+datum\w*|\bprocessing\s+data\b|\bprocess\s+datum\w*|\bprocess\s+data\b|\bdata\s+manager\w*|\bdatum\b\s+manager\w*|\bdata\s+integrity\w*|\bdatum\s+integrity\w*|\badminister\s+data\w*|\badminister\s+datum\w*|\bdatum\s+resource[s]?\s+manage\w*|\bdata\s+resource[s]?\s+manage\w*|\bdata\s+lifecycle\w*|\bdata\s+life\s+cycle\w*|\bdata\s+life\s+-\s+cycle\w*|\bdatum\s+lifecycle\w*|\bdatum\s+life\s+cycle\w*|\bdatum\s+life\s+-\s+cycle\w*|\bprocess[i]?[n]?[g]?\s+\w+\s+data\b|\bprocess[i]?[n]?[g]?\s+\w+\s+datum\w*|\bprepar[e]?[a]?[t]?[i]?[o]?[n]?\s+datum\w*|\bprepar[e]?[a]?[t]?[i]?[o]?[n]?\s+data\w*|\bdata\s+prepar\w*|\bdatum\s+prepar\w*|\breview[i]?[n]?[g]?\s+data\b|\breview[i]?[n]?[g]?\s+datum\w*|\bprocess[i]?[n]?[g]?\s+\w+\s+data\w*|\bprocess[i]?[n]?[g]?\s+\w+\s+datum\w*|\bdatum\s+\w+\s+process\w*|\bdata\s+\w+\s+process\w*|\bmanag[e]?[i]?[n]?[g]?\s+\s+\s+datum\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+data\w*|\bcollate\s+\w+\s+data\b|\bcollate\s+\w+\s+datum\w*', text)
        match_2 = o(r'\betl\b|\bextract\s+transform\s+load\w*', text)
        match_3 = o(r'\bdatum\w*|\bdata\b|\bpipeline\w*', text)
        match_4 = o(r'\bdatum\w*|\bdata\b', text)
        match_5 = o(r'\bcollect\w*|\bgather\w*|\b(?<!track )(?<!track  )(?<!track   )record\w*|\bcapture\w*|\bprovide\w*|\bmanage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'process and manage data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse big data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbig\s+data\b|\bbig\s+datum\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse big data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform clerical duties
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsecretarial\b|\bcrerical\b|\bdata\s+entry\w*|\bdatum\s+entry\w*', text)
        match_2 = o(r'\badministration\w*|\badministrative\w*|\boffice\w*|\bresponsib\w*|\bduty\w*|\badmin\b|\bdocument\w*|\bdocumentation\w*|\breport[i]?[n]?[g]?\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform clerical duties'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain library
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blibrary\b|\blibrarian\b', text)
        match_2 = o(r'\badministration\w*|\badministrator\w*|\borganise\w*|\bcatalogu[e]?[i]?[n]?[g]?\w*|\bmanage\w*|\bmaintain\w*|\bmanaging\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain library'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage quantitative data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquantitative\w*', text)
        match_2 = o(r'\bresearch\w*|\bstudy\w*|\bdata\w*|\bdatum\w*|\banaly\w*|\bassess\w*|\binterpret\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage quantitative data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage qualitative data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqualitative\w*', text)
        match_2 = o(r'\bresearch\w*|\bstudy\w*|\bdata\w*|\bdatum\w*|\banaly\w*|\bassess\w*|\binterpret\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage qualitative data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct quantitative research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquantitative\s+research\w*|\bquantitative\s+study\b', text)
        match_2 = o(r'\bqualitative\w*', text)
        match_3 = o(r'quantitative\w*', text)
        match_4 = o(r'\bresearch\w*|\bstudy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct quantitative research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct qualitative research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqualitative\s+research\w*|\bqualitative\s+study\w*', text)
        match_2 = o(r'\bqualitative\w*', text)
        match_3 = o(r'quantitative\w*', text)
        match_4 = o(r'\bresearch\w*|\bstudy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct qualitative research'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse ecological data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becology\w*|\becological\b|\becologist\w*', text)
        match_2 = o(r'\bconsultant\b|\bconsulting\b|\bresearch\w*|\bscientist\w*|\bdata\w*|\bdatum\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse ecological data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage customer accounts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccount[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+account[s]?\w*|\bassign\s+account[s]?\w*|\baccount[s]?\s+assignment\w*|\bcustomer[s]?\s+account[s]?\w*|\bclient[s]\s+account[s]?\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+account\w*', text)
        match_2 = o(r'\bcustomer\w*|\bclient\w*|\bstakeholder\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage customer accounts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create/adhere to asset management plan or strategy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basset\s+management\s+plan\w*|\basset\s+management\s+strateg[y]?[i]?[e]?[s]?\b|\basset\s+strateg[y]?[i]?[e]?[s]?\b', text)
        match_2 = o(r'\bfinance\w*|\basset\w*|\bbusiness\w*|\baccounting\w*|\bmanage\w*|\badhere\w*|\bcreate\w*|\bdesign\w*|\bdevelop\w*|\bcreating\w*|\bmanaging\w*', text)
        match_3 = o(r'\bamp\b', text)
        match_4 = o(r'\basset\w*|\bfinance\w*|\baccounting\w*|\bbookkeep\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create/adhere to asset management plan or strategy'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage transfer pricing documentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransfer\s+pricing\w*', text)
        match_2 = o(r'\bdocument\w*|\bpaperwork\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage transfer pricing documentation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform financial analysis on price strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+analys\w*', text)
        match_2 = o(r'\bprice\w*|\bpricing\w*', text)
        match_3 = o(r'\bstrategy\w*|\bplan\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform financial analysis on price strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oprate fax and write fax cover sheets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfax\s+write\w*|\bfax\s+machine\w*|\boperate\s+fax\b|\buse\s+fax\w*|\bsend\s+fax\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'oprate fax and write fax cover sheets'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage web orders
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bweb\s+order\w*|\bonline\s+order\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage web orders'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse supply chain strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupply\s+plan\w*|\bsupply\s+chain\s+strategy\w*|\bplan[n]?[i]?[n]?[g]?\s+supply\s+chain\w*|\bsupply\s+chain\s+plan\w*|\banalys[e]?[i]?[s]?\s+supply\s+chain\w*|\bsupply\s+chain\s+analy\w*|\banalyze\s+supply\s+chain\w*|\bsupply\s+chain\s+\w+\s+plan\w*|\bsupply\s+chain\s+\w+\s+analy\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse supply chain strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse supply chain trends
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupply\s+chain\s+trend\w*', text)
        match_2 = o(r'\bsupply\s+chain\w*', text)
        match_3 = o(r'\btrend\w*', text)
        match_4 = o(r'\banaly\w*|\btrack\w*|\bcheck\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse supply chain trends'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for implement strategic management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanagement\s+strategy\w*|\bstrategic\s+management\w*|\bstrategic\s+decision\s+making\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'implement strategic management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lead a team
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+lead\w*|\b(?<!industry )(?<!industry\s-\s)(?<!industry-)(?<!market )lead[i]?[n]?[g]?\s+team\w*|\blead[i]?[n]?[g]?[g]?\s+project\w*|\bteam\s+lead\w*|\btake\s+lead\w*|\bleadership\s+role\w*|\blead[i]?[n]?[g]?\s+\w+\s+team\w*|\blead[i]?[n]?[g]?\s+\w+\s+\w+\s+team\w*', text)
        match_2 = o(r'\bteam\b', text)
        match_3 = o(r'\blead\w*', text)
        match_4 = o(r'\bliaise\w*|\bliaison\w*|\bcollaborat\w*|\bguide\w*|\bdiary\w*|\bmeeting\w*|\bcalendar\w*|\bschedule\w*|\battend\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'lead a team'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for represent company
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brepresent[i]?[n]?[g]?\s+company\w*|\brepresent[i]?[n]?[g]?\s+firm\w*|\bcompany\s+represent\w*|\bfirm\s+represent\w*|\bact\s+ambassador\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'represent company'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for represent team
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brepresent[i]?[n]?[g]?\s+team\w*|\bteam\s+represent\w*|\brepresent\s+department\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'represent team'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for represent customers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brepresent[i]?[n]?[g]?\s+customer\w*|\bcustomer[s]?\s+represent\w*|\bclient[s]?\s+represent\w*|\brepresent[i]?[n]?[g]?\s+client\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'represent customers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage a team
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+team\w*|\bteam\s+management\w*|\bteam\s+structure\w*|\b(?<!self - )(?<!self-)(?<!self- )(?<!self )direct[i]?[n]?[g]?\s+team\w*|\bmanag[e]?[i]?[n]?[g]?\s+colleague\w*|\bcolleague\s+manage\w*|\bcoordinate\s+staff\w*|\bco\s+-\s+ordinate\s+staff\w*|\bordinate\s+staff\w*|\bcoordinate\s+team\w*|\bordinate\s+team\w*|\bco\s+-\s+ordinate\s+team\w*|\bteam\s+co\s+-\s+ordinat\w*|\bteam\s+coordinat\w*|\bteam\s+ordinat\w*|\bmanag[e]?[i]?[n]?[g]?\s+employee\w*|\bemployee\s+management\w*|\bcoordiante\s+employee\w*|\bmanag[e]?[i]?[n]?[g]?\s+cross\s+-\s+functional\s+team\w*|\bcoordinate\s+\w+\s+staff\w*|\bordinate\s+\w+\s+staff\w*|\bcoordinate\s+\w+\s+team\w*|\bordinate\s+\w+\s+team\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+team\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage a team'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage people
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+people\w*|\bpeople\s+management(?! system)(?! software)\w*|\bcoordinate\s+people\w*|\bordinate\s+people\w*|\bco\s+-\s+ordinate\s+people\w*|\bsupervise\s+people\w*|\bpeople\s+supervis\w*|\bmanag[e]?[i]?[n]?[g]?\s+duty\w*|\bmanag[e]?[i]?[n]?[g]?\s+duties\w*|\bduty\s+manage\w*|assign\s+duties\w*|\bassign\s+role\w*|\bassign\s+activity\w*|\bstore\s+manager\w*|\bshop\s+manager\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+staff\w*|\bmanagement\s+\w+\s+staff\w*|assign\s+\w+\s+duties\w*|\bassign\s+\w+\s+role\w*|\bassign\s+\w+\s+activity\w*|\bassign\s+\w+\s+duty\w*|\bdeal\s+personnel\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage people'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise staff
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupervise\s+staff\w*|\bsupervise\s+team\w*|\bteam\s+supervis\w*|\bstaff\s+supervis\w*|\bsupervise\s+employee\w*|\bemployee\s+supervis\w*|\bshift\s+leader\w*|\boversee\s+work\w*', text)
        match_2 = o(r'\bsupervisor\b|\bsupervision\b|\bsupervise\b', text)
        match_3 = o(r'\bteam\b', text)
        match_4 = o(r'\bcoach\w*|\bcoach\b|(?<!market )(?<!industry )(?<!world )\bleader\b(?! field)\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise staff'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate communication within a team
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+coordinator\b|\bproject\s+coordination\b|\bproject\s+co\s+-\s+ordinator\w*|\bproject\s+ordinator\b|\bproject\s+co\s+-\s+ordination\w*|\bproject\s+ordination\w*|\bcoordinate\s+team\w*|\bordinate\s+team\w*|\bco\s+-\s+ordinate\s+team\w*|\bteam\s+co\s+-\s+ordinat\w*|\bteam\s+coordinat\w*|\bteam\s+ordinat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate communication within a team'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for coordinate engineering teams
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcoordinate\w*|\bcoordination\w*|\bmanage\w*|\blead\w*|\bsupervise\w*', text)
        match_2 = o(r'\bteam\w*', text)
        match_3 = o(r'\bengineering\b(?! process)\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'coordinate engineering teams'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct field work
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'field\s+survey\w*|\bfield\s+work\w*|\bfieldwork\w*|\bwork\s+field\w*|\bsite\s+operation\w*|\bsite\s+lead\w*|\bsite\s+work\b|\bsite\s+visit(?! app)\w*|\bvisit\s+site\w*|\bfield\s+repair\w*|\bfield\s+engineer\w*|\bcustomer[s]?\s+site\w*|\bclient[s]?\s+site\w*|\bmarine\s+site\w*|\bservice\s+field\w*|\binstallation\s+field\w*|\binstall\s+field\w*|\bconduct\s+field\s+work\w*|\binformation\s+from\s+field\w*|\bfield\s+information\w*|\bvisit[i]?[n]?[g]?\s+operational\s+site\w*|\bconducting\s+field\s+work\w*|\bundertake\s+field\s+work\w*|\bundertaking\s+field\s+work\w*|\bcarry\s+out\s+field\s+work\w*|\bcarrying\s+out\s+field\s+work\w*|\bcarry\s+field\s+work\w*|\bcarring\s+field\s+work\w*|\bwork\s+outdoor\w*|\bfield\s+service\w*|\bfield\s+technician\w*|\boutdoor[s]?\s+work\w*|\bspend\s+outdoor[s]?\w*|\bfield\s+\w+\s+work\w*|\boutdoor[s]?\s+\w+\s+site\w*|\bvisit[i]?[n]?[g]?\s+(?!company \w*|companys \w*|web )\w+\s+\bsite[s]?\b|\bsite\s+\w+\s+visit\w*', text)
        match_2 = o(r'\boutdoor[s]?\b|\bfield\s+work\w*', text)
        match_3 = o(r'\bwater\s+infrastructure\w*|\bwater\s+supply\w*|\bpipeline\w*|\bpipework\w*|\bwater\s+treatment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct field work'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gather data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatum\w*|\bdata\b', text)
        match_2 = o(r'\bcollect\w*|\bgather\w*|\b(?<!track )(?<!track  )(?<!track   )record\w*|\bcapture\w*|\bcollat\w*|\bextract\w*|\bscrape\w*|\bregister\w*|\badminister\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'gather data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calibrate optical instruments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspectromet\w*|\boptical\s+equipment\w*|\boptical\s+instrument\w*|\boptical\s+tool\w*', text)
        match_2 = o(r'\bcalibrat\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calibrate optical instruments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage multiple patients simultaneously
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpatient\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+patient\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+patient\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage multiple patients simultaneously'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for diagnose heart conditions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcardiac\s+rhythm\s+manage\w*', text)
        match_2 = o(r'\bheart\w*|\bcardiact\w*', text)
        match_3 = o(r'\bdiagnos\w*|\bpatient\w*|\bcare\b|\bmanage\w*|\bmanaging\w*|\btest\w*|\bhealth\s+screen\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'diagnose heart conditions'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage or support human resources
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhr\b|\bhuman\s+resource\w*', text)
        match_2 = o(r'manage\w*|\bmanaging\w*|\bsupport\w*|\bguidance\w*|\bconsult\w*|\badvise\w*|\badvice\w*|\boversee\w*|\boersight\w*|\bpersonnnel\s+opeartion\w*|\boperations\s+administrator\w*|\bsafeguarding\w*|\bpersonnel\s+resources\w*|\boperation[s]?\s+personnel\w*', text)
        match_3 = o(r'\bhr\b\s+query\w*|\bhr\b\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+personnel\w*|\bpersonnel\s+manage\w*|\bpersonnel\s+supervis\w*|\bhuman\s+resource[s]?\s+management\w*|\boptimize\s+hr\b|\bhr\b\s+guidance\w*|\bhr\b\s+support\w*|\bhr\s+admin\w*|\bhuman\s+resource[s]?\s+admin\w*|\bhuman\s+resource[s]?\s+support\w*|\bhuman\s+resource[s]?\s+guidance\w*|\bhuman\s+resource[s]?\s+support\w*|\bsupport\s+hr\b|\bhuman\s+resource[s]?\s+admin\w*|\bpersonnel\s+admin\w*|\bpersonnel\s+oversight\w*|\bsupport\s+human\s+resource\w*|\boptimise\s+hr\b|\boptimize\s+human\s+resource\w*|\boptimise\s+human\s+resource\w*|\bhuman\s+resource[s]?\s+oversight\w*|bhr\s+supervis\w*|\bhr\s+oversight\w*|\bhuman\s+resource[s]?\s+supervis\w*|\bstaff[i]?[n]?[g]?\s+requirement\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage or support human resources'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify necessary human resources
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhr\b\s+business\s+support\w*|\bhr\b\s+analys\w*|\bhuman\s+resource[s]?\s+analy\w*|\banalyse\s+hr\b|\banalyse\s+human\s+resource\w*|\banalyze\s+human\s+resource\w*|\bhuman\s+resource[s]?\s+business\s+support\w*|\bhuman\s+resource[s]?\s+business\s+support\w*|\bidentify\s+human\s+resource\w*|\bidentify\s+hr\b|\bsuitable\s+hr\b|\bsuitable\s+human\s+resource\w*|\bhr\b\s+strateg\w*|\bhuman\s+resource[s]?\s+strateg\w*|\bhuman\s+resource[s]?\s+strateg\w*|\bhuman\s+resource[s]?\s+advisor\w*|\bhuman\s+resource[s]?\s+adviser\w*|\bhuman\s+resource[s]?\s+advisor\w*|\bhr\b\s+advisor\w*|\bhr\s+adviser\w*|\bhr\s+advice\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify necessary human resources'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct genome research and analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsynthetic\s+promoter\w*|\bngs\s+technology\w*|\bngs\s+datum\w*', text)
        match_2 = o(r'\bgenome\w*|\bgenomics\w*', text)
        match_3 = o(r'\bresearch\w*|\btest\w*|\banaly\w*|\bsurvey\w*|\bstudy\w*', text)
        match_4 = o(r'\bperform\w*|\bconduct\w*|\bdevelop\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct genome research and analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for work in cold environments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcold\s+storage\w*|\bcold\s?[-]?\s?room\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'work in cold environments'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assemble electromechanical systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectromechanic\w*', text)
        match_2 = o(r'\bassembl\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assemble electromechanical systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for design electromechanical systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesign\w*|\bcreate\w*', text)
        match_2 = o(r'\belectromechanic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'design electromechanical systems'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assemble printed circuit boards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpcba\b', text)
        match_2 = o(r'\bassembl\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assemble printed circuit boards'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for solve problems and identify issues
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsolve\s+problem\w*|\bthink\s+critical\w*|\bcritical\s+think\w*|\bsolving\s+problem\w*|\bpractical\s+solution\w*|\brecognize\s+remedy\w*|\brecognise\s+remedy\w*|\bdetermine\s+bottleneck\w*|\bfind[i]?[n]?[g]?\s+solution\w*|\bresolv[e]?[i]?[n]?[g]?\s+problem\w*|\bproblem\s+solving\w*|\bproblem\s+solve\w*|\bfault\s+report\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+issue\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+fault\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+hazard\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+problem\w*|\bfault\s+identif\w*|\bissue\s+identif\w*|\bhazad\s+identif\w*|\bproblem\s+identify\w*|\bassess\s+problem\w*|\bidentification\s+issue\w*|\bidentify\s+issue\w*|\bcomplex\s+problem\w*|\bresolve\s+problem\w*|\bfind\s+issue\w*|\bfind\s+defect\w*|\bdefect[s]?\s+find\w*|\bdefect[s]?\s+identif\w*|\bissue[s]?\s+find\w*|\bfind\s+fault\w*|\bfault[s]?\s+find\w*|\bfind\s+problem\w*|\bproblem[s]?\s+find\w*|\bprovide\s+issue\s+update\w*|\bissue\s+update\s+project\w*|\brectify\s+issue\w*|\brecify\s+problem\w*|\bproblem\s?[-]?\s?solve\w*|\bproblem\s?[-]?\s?solving\w*|\bidentif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+\w+\s+issue\w*|\bfind[i]?[n]?[g]?\s+\w+\s+solution\w*|\bpractical\s+\w+\s+solution\w*|\bidentification\s+\w+\s+issue\w*|\breport\s+issue\w*|\breport\s+damage\w*|\broot\s+cause\w*|\brcfa\b|\bissue\s+resolut\w*|\bfault\s+study\w*|\bfault\s+studies\w*|\bfault\s+find\w*|\bresolution\s+issue\w*|\bissue\s+resolution\w*|\bidentify[i]?[n]?[g]?\s+\w+\s+issue\w*|\bidentify[i]?[n]?[g]?\s+\w+\s+problem\w*|\bidentification\s+\w+\s+problem\w*|\bidentification\s+\w+\s+issue\w*|\bsolv[e]?[i]?[n]?[g]?\s+\w+\s+problem\w*|\bresolv[e]?[i]?[n]?[g]?\s+\w+\s+problem\w*|\bhighlight\s+\w+\s+issue\w*|\bdetermine\s+\w+\s+bottleneck\w*', text)
        match_2 = o(r'\bproblem\w*|\bissue\w*|\bbottleneck\w*|\broadblock\w*|\bobscatle\w*', text)
        match_3 = o(r'\breport\w*|\bidentif\w*|\bassess\w*|\bdefine\w*|\binvestigat\w*|\banaly\w*|\banticipat\w*|\belmiat\w*|\bresolv\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'solve problems and identify issues'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use microarray tool
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicroarray\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use microarray tool'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for produce semiconductor crystals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrystal\w*', text)
        match_2 = o(r'\bfpga\b|\blithography\w*|\bsemiconductor\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'produce semiconductor crystals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for imprint circuit design onto wafers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrystallization\w*|\bfpga\b|\blithography\w*|\bcircuit\w*', text)
        match_2 = o(r'\bwafer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'imprint circuit design onto wafers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct photolithography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphotolithograph\w*|\betching\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct photolithography'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prevent damage to utility infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'prevent[i]?[n]?[g]?\s+utility\s+damage\w*|avoid[i]?[n]?[g]?\s+utility\s+damage\w*|\bprevention\s+utility\s+damange\w*|\bprevention\s+damage\s+utility\w*|\bavoid[i]?[n]?[g]?\s+damage\s+utility\w*|\bprevent[i]?[n]?[g]?\s+damage\s+utility\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prevent damage to utility infrastructure'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use adobe acrobat
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badobe\s+acrobat\b', text)
        match_2 = o(r'\badobe\w*', text)
        match_3 = o(r'\bexperience\w*|\bfamiliar\w*|\buse\w*|\bsoftware\w*|\bmicrosoft\w*|\bms\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use adobe acrobat'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage standard enterprise resource planning system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benterprise\s+resource[s]?\s+plan\w*|\berp\s+deployment\w*|\bknowledge\s+erp[s]?\b|\berp[s]?\s+knowledge\w*|\bexperience\s+erp[s]?\b|\berp[s]?\b\s+experience\w*', text)
        match_2 = o(r'\berp\b|\bifs\b', text)
        match_3 = o(r'\bsoftware\w*|\bsystem\w*|\btool\w*|\bsap\b|\be1\b|\bmanage\w*|\buse\w*|\bexperience\w*|\bknowledge\w*|\bentry\w*|\bfamiliar\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage standard enterprise resource planning system'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use microsoft office programs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexcel\b', text)
        match_2 = o(r'\bword\b', text)
        match_3 = o(r'\bpowerpoint\w*|\bpower\s+point\w*|\bmicrosoft\w*|\bms\b', text)
        match_4 = o(r'\be1\b|\bmicrosoft\s+office\w*|\bms\b\s+office\w*|\bms\b\s+teams\w*|\bmicrosoft\s+package\w*|\bmicrosoft\s+base\s+package\w*|\bsharepoint\b|\bms\b\s+base\s+package\w*|\bmicrosoft\s+software\w*|\bms\s+software\w*|\bmicrosoft\s+word\w*|\bms\s+word\w*', text)
        match_5 = o(r'\bmicrosoft\w*|\bms\b', text)
        match_6 = o(r'\boutlook\b|\bit\s+skill\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        elif match_4:
            matched_fragments.extend([match_4])
        elif match_5 and match_6:
            matched_fragments.extend([match_5, match_6])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use microsoft office programs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use microsoft excel
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bexcel\b', text)
        match_2 = o(r'\bword\b|\bpowerpoint\w*|\bpower\s+point\w*|\bmicrosoft\w*|\bspreadsheet\w*|\bms\b|\bpivot\s+table\w*|\bproficient\w*|\badvanced\w*|\buse\w*|\bexperience\w*|\bknowledge\w*|\boperation\w*|\bpivot\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use microsoft excel'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for deliver visual presentation of data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvisualisation\b|\bvisualization\b|\bvisualise\b|\bvisualize\b|\bvisualization\w*', text)
        match_2 = o(r'\bdata\b|\bdatum\w*|\banaly\w*|\bresult\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'deliver visual presentation of data'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for make powerpoint presentations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpowerpoint\w*|\bpower\s+point\w*', text)
        match_2 = o(r'\bmicrosoft\w*|\bpresent\w*|\bms\b|\bexcel\b|\bword\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'make powerpoint presentations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use microsoft power bi tool
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpower\s+bi\b|\bpowerbi\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use microsoft power bi tool'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop statistical software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistic\w*|\bscientific\w*', text)
        match_2 = o(r'\bsoftware\w*|\btool\w*|\bprogram\b', text)
        match_3 = o(r'\bdevelop\w*|\bprogramm\w*|\bcode\w*|\bcoding\b|\bcreate\w*|\bdesign\w*|\bcreating\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop statistical software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for automate application deployment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautomate\s+deploy\w*|\bautomate\s+\w+\s+deploy\w*', text)
        match_2 = o(r'\bsoftware\w*|\bapp\b|\bapplication\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'automate application deployment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain computer hardware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhardware\b', text)
        match_2 = o(r'\bconfigur\w*|\bcomputer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain computer hardware'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use application programming software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapplication\s+programming\s+interface', text)
        match_2 = o(r'\brest\b|\bgraphql\b', text)
        match_3 = o(r'\bprogramming|\\bsoftware', text)
        match_4 = o(r'\bapi\b(?!/\w*| /\w*| \|| 6a)', text)
        match_5 = o(r'(?<!\\)\bapi\b', text)
        match_6 = o(r'(?<!/)\bapi\b', text)
        match_7 = o(r'(?<!/ )\bapi\b', text)
        match_8 = o(r'(?<!\\ )\bapi\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4:
            matched_fragments.extend([match_4])
        elif match_5:
            matched_fragments.extend([match_5])
        elif match_6:
            matched_fragments.extend([match_6])
        elif match_7:
            matched_fragments.extend([match_7])
        elif match_8:
            matched_fragments.extend([match_8])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use application programming software'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop with cloud services
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcloud\s+service\w*|\bcloud\s+infrastructure\b|\bcloud\s+architecture\w*|\bcloud\s+software\w*', text)
        match_2 = o(r'\bprogramming\w*|\bcode\w*|\bdevelop\w*|\bdesign\w*|\bcreate\w*|\butilise\w*|\butilize\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop with cloud services'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for automate cloud tasks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcloud\s+service\w*|\bcloud\s+infrastructure\b|\bcloud\s+architecture\w*|\bcloud\s+software\w*|\bcloud\s+task\w*', text)
        match_2 = o(r'\bautomate\b|\bautomation\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'automate cloud tasks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan migration to cloud
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcloud\w*', text)
        match_2 = o(r'\binteraction\w*|\bmigration\w*|\bintegration\w*|\bmigrate\w*|\bintegrate\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan migration to cloud'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct content quality assurance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontent\s+quality\w*|\bquality\s+content\w*|\bqca\b', text)
        match_2 = o(r'\bquality\s+control\w*|\bqc\b|\bqa\b|\bquality\s+assurance\w*|\bvalidate\s+quality\w*|\bvalidation\s+quality\w*', text)
        match_3 = o(r'\bcontent\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct content quality assurance'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for test first design procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btfd\b|\bfirst\s+design\s+procedure\w*', text)
        match_2 = o(r'\bquality\w*|\btest\w*|\bassess\w*|\bcheck\w*|\bevaluat\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'test first design procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage security equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsecurity\s+officer\w*', text)
        match_2 = o(r'\balarm\b', text)
        match_3 = o(r'\bsecurity\w*', text)
        match_4 = o(r'\bequipment\w*|\btool\w*|\bcamera\w*|\bcctv\w*|\balarm\w*', text)
        match_5 = o(r'\bmanage\w*|\bmaintain\w*|\bmanaging\w*|\boversee\w*|\bovesight\w*|\bmonitor\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4 and match_5:
            matched_fragments.extend([match_3, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage security equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform safety data analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatum\s+safety\w*|\bdata\s+safety\w*', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*', text)
        match_3 = o(r'\bsafety\w*|\bsafe\b', text)
        match_4 = o(r'\banaly\w*|\bassess\w*|\bcheck\w*|\binterpret\w*|\btest\w*|\bensure\w*|\bperform\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform safety data analysis'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform sample testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalys[e]?[i]?[n]?[g]?\s+sample\w*|\banalyz[e]?[i]?[n]?[g]?\s+sample\w*|\bexamin[e]?[i]?[n]?[g]?\s+sample\w*|\bexamination\s+sample\w*|\banalysis\s+sample\w*|\bsample\s+analy\w*|\btest[i]?[n]?[g]?\s+sample\w*|\bsample\s+test\w*|\bsample\s+process\w*|\bprocess\s+sample\w*|\bsample\s+collection\w*|\bcollect\s+sample\w*|\broutine\s+sampl\w*|\bsample\s+analy\w*|\bsampling\s+analy\w*|\bslide\s+production\w*|\bsample\s+prepar\w*|\bprepar[e]?[i]?[n]?[g]?\s+sample\w*|\bevaluat[e]?[i]?[o]?[n]?\s+sample\w*|\banalys[e]?[i]?[s]?\s+\w+\s+sample\w*|\bevaluat[e]?[i]?[o]?[n]?\s+\w+\s+sample\w*|\btest[i]?[n]?[g]?\s+\w+\s+sample\w*', text)
        match_2 = o(r'material\s+sample\w*|\banalyse\s+material\w*|\bassay\b|\bimmunoassay\b', text)
        match_3 = o(r'\btest\w*|\bevaluat\w*|\bcheck\b|analys\w*|\blaboratory\w*|\blab\b|\bresearch\w*|\binvestigat\w*', text)
        match_4 = o(r'\bhair\s+sample\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform sample testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prepare sample for testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprepar[e]?[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+sample\w*|\bprepar[e]?[i]?[n]?[g]?\s+sample\w*|\btak[e]?[i]?[n]?[g]?\s+sample\s+test\w*|\bsample\s+preparation\w*|\bsample\s+preapar[e]?[i]?[n]?[g]?\w*|\bmanufactur[e]?[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+sample\w*|\bmanufactur[e]?[i]?[n]?[g]?\s+sample\w*|\bsample\s+manufactur\w*|\bdevelop[i]?[n]?[g]?\s+test[i]?[n]?[g]?\s+sample\w*|\bsample\s+develop\w*', text)
        match_2 = o(r'\bsample\w*', text)
        match_3 = o(r'\bprepare\w*|\blabel\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'prepare sample for testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform conformance testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconformance\s+testing\w*', text)
        match_2 = o(r'(?<!non )\bconformance\w*', text)
        match_3 = o(r'\bquality\w*|\btest\w*|\bcheck\w*|\breview\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform conformance testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct impurity testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimpurity\w*', text)
        match_2 = o(r'\btest\w*|\bcheck\w*|\bassess\w*|\bquality\w*|\binvestigate\w*|\banaly\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct impurity testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for interpret statistical data and patterns
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistic\w*|\bstatistical\w*', text)
        match_2 = o(r'\bpattern\w*|\btrend\w*|\bdata\w*|\bdatum\w*|\binsight\w*', text)
        match_3 = o(r'\bevaluat\w*|\banaly\w*|\bassess\w*|\bcheck\w*|\binterpret\w*|\binvestigat\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'interpret statistical data and patterns'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply statistical programming
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistical\s+programming\w*|\bstatistical\s+coding\b|\bstata\b|\beview[s]?\b|\be-view[s]?\b|\be\s+-\s+view[s]?\b|\be\s+view[s]?\b|\bspss\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply statistical programming'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data extraction, transformation and loading tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+transformation\w*|\bdatum\s+transformation\w*|\bdata\s+extraction\w*|\bdatum\s+extraction\w*', text)
        match_2 = o(r'\bdtl\b', text)
        match_3 = o(r'\btool\w*|\bpipeline\w*|\bdata\w*|\bdatum\w*|\buse\w*|\bperform\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'data extraction, transformation and loading tools'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform search engine optimization
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bseo\b|\bsearch\s+engine\s+marketing\w*|\bsearch\s+engine\s+optimi\w*', text)
        match_2 = o(r'\bsem\b', text)
        match_3 = o(r'\bsale\b|\bselling\w*|\bmarketing\w*|\bsocial\s+media\w*|\bsocial\s+medium\w*|\bwebsite\w*|\bsoftware\w*|\bdeveloper\w*|\boptimise\w*|\boptimize\w*|\bassess\w*|\bcheck\w*|\binvestigate\w*|\butilise\w*|\butilize\w*|\bdeliver\w*|\bdesign\w*|\bdevelop\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform search engine optimization'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use scripting programming
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscript\w*', text)
        match_2 = o(r'\bcode\w*|\bprogram\w*|\bsoftware\w*|\bdevelop\w*|\btool\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use scripting programming'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor billing procedures and allocate bills
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbilling\s+procedure\w*|\ballocate\s+bill\w*|\bcollect\s+bill\w*|\bbilling[s]?\s+coordinat\w*|\bbilling[s]?\s+co\s+-\s+ordinate\w*|\bbilling[s]?\s+ordinate\w*|\bhybrid\s+bill\w*|\bbill\s+process\w*|\bprocess[i]?[n]?[g]?\s+bill\w*|\binvoice\s+review\w*|\breview[i]?[n]?[g]?\s+invoice\w*', text)
        match_2 = o(r'\baccount\b|\baccounts\b|\bcollect\w*|\ballocat\w*|\baccounting\w*|\bbookkeep\w*', text)
        match_3 = o(r'\bbill\b|\bbills\w*|\bbilling\w*|\binvoice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor billing procedures and allocate bills'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply medical imaging techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmedical\s+imaging\w*', text)
        match_2 = o(r'\bimaging\w*|\bimage\b', text)
        match_3 = o(r'\bbiology\w*|\bmedical\w*|\bcell\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply medical imaging techniques'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use x-ray machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bray\s+engineer\w*|\bxray\s+engineer\w*|\bradiography\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use x-ray machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform pre-treatment imaging
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcancer\w*|\btreatment\w*', text)
        match_2 = o(r'\bimaging\w*|\bscan\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform pre-treatment imaging'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform positron emission tomography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpet\b', text)
        match_2 = o(r'\bimaging\w*|\bscan\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform positron emission tomography'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend injection moulding machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binjection\s+stretch\w*', text)
        match_2 = o(r'\binjection\w*', text)
        match_3 = o(r'\bmould\w*', text)
        match_4 = o(r'\bmachine\w*|\bexperinece\w*|\bknowledge\w*|\bprocess\w*|\bprocedure\w*|\bperform\w*|\buse\w*|\btend\w*|\bhandle\w*|\butilise\w*|\bdevice\w*|\bequipment\w*|\btool\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend injection moulding machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tend blow moulding machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstretch\s+blow\b', text)
        match_2 = o(r'\bblow\w*', text)
        match_3 = o(r'\bmould\w*', text)
        match_4 = o(r'\bmachine\w*|\bexperinece\w*|\bknowledge\w*|\bprocess\w*|\bprocedure\w*|\bperform\w*|\buse\w*|\btend\w*|\bhandle\w*|\butilise\w*|\bdevice\w*|\bequipment\w*|\btool\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tend blow moulding machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create master models (moulds)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwax\b', text)
        match_2 = o(r'\bmould\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create master models (moulds)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use laboratory equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+equipment\w*|\blab\b\s+equipment\w*|\blaboratory\s+device\w*|\blab\b\s+device\w*|\blaboratory\s+machine\w*|\blab\b\s+machine\w*|\bwork[i]?[n]?[g]?\s+laboratory\w*|\bwork[i]?[n]?[g]?\s+lab\b|\blaboratory\s+analy\w*|\blab\s+analy\w*|\blaboratory\s+tool\w*|\blab\s+tool\w*|\blaboratory\s+instrument\w*|\blab\s+instrument\w*', text)
        match_2 = o(r'\blaboratory\w*|\blab\b|\bequipment\w*|\buse\w*|\btest\w*|\bresearch\w*|\bknowledge\w*|\bexperience\w*|\bwork\w*|\bstudy\w*|\bmachine\w*|\bdevice\w*|\binstrument\w*', text)
        match_3 = o(r'\bbeaker\w*|\berlenmeyer\s+flask\w*|\btest\s+tube\w*|\bgraduated\s+cylinder\w*|\bpipette\w*|\bburette\w*|\bmicroscope\w*|\bcentrifuge\w*|\bbalance\s+scale\w*|\bhot\s+plate\w*|\bthermometer\w*|\bph\s+meter\w*|\bspectrophotometer\w*|\bincubator\w*|\bautoclave\w*|\bfume\s+hood\w*|\bmagnetic\s+stirrer\w*|\bheating\s+mantle\w*|\bvortex\s+mixer\w*|\bwater\s+bath\w*|\bcapilary\s+machine\w*|\bdepth\s+filter\w*|\bmix\s+tank\w*', text)
        match_4 = o(r'\blaboratory\w*|\blab\w*', text)
        match_5 = o(r'\bresearch\w*|\bstudy\w*|\bexperiment\w*|\bequipment\w*|\btool\w*|\bmachine\w*|\bdevice\w*|\binstrument\w*', text)
        match_6 = o(r'\bresource\s+schedul\w*|\bresource\s+plan\w*|\bsale\w*|\bselling\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and not match_6:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use laboratory equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for calibrate laboratory equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcalibrate\w*|\bcalibration\w*', text)
        match_2 = o(r'\blab\b|\blaboratory\w*|\bray\b|\bxray\b', text)
        match_3 = o(r'\bequipment\w*|\btool\w*|\bmachine\w*|\bdevice\w*|\binstrument\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'calibrate laboratory equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analyse x-ray imagery
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bxray\b|\bray\b|\bedx\b', text)
        match_2 = o(r'\banaly\w*|\bimage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'analyse x-ray imagery'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for support veterinary diagnostic imaging preocedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvet\b|\bveterinary\w*|\banimal\w*', text)
        match_2 = o(r'\bimage\w*|\bimagining\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'support veterinary diagnostic imaging preocedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform machine maintenance and repair industrial equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brapair\s+machinery\w*|\bmachinery\s+maintenance\w*|\bmaintain\s+machine\w*|\bupgrade\s+exist[i]?[n]?[g]?\s+equipment\w*|\bupgrade\s+exist[i]?[n]?[g]?\s+machinery\w*|\bupgrade\s+exist[i]?[n]?[g]?\s+plant\s+equipment\w*|\bupgrade\s+exist[i]?[n]?[g]?\s+industr[y]?[i]?[a]?[l]?\s+equipment\w*|\boptimise\s+plant\s+equipment\w*|\boptimize\s+plant\s+equipment\w*|\boptimise\s+industr[y]?[i]?[a]?[l]?\s+equipment\w*|\boptimize\s+industr[y]?[i]?[a]?[l]?\s+equipment\w*|\bcontrol[l]?[i]?[n]?[g]?\s+instrument\w*|\binstrumentation\s+control\w*|\binstrument\s+control\w*|\binstrument\s+engineer\w*|\binstrumentation\s+engineer\w*|\binstrument\s+uptime\s+goal\w*|\bmaintain\s+\w+\s+machine\w*', text)
        match_2 = o(r'\brepair\b|\bfix\b|\bfixing\b|\bmaintenance\w*|\bdefect\s+elmination\w*|\belminate\s+defect\w*|\bcalibrate\b|\bcalibration\b', text)
        match_3 = o(r'\bmachinery\w*|\bequipment\w*|\binstrument\w*|\bci\s+engineer\w*|\bmachine\w*', text)
        match_4 = o(r'\bmanufactur\w*|\bplant\w*|\bindustr\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform machine maintenance and repair industrial equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and inspect electrical equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bequipment\w*|\bmachine\w*|\binstrument\w*|\binstallation\w*|\btool\w*|\bcontrol\s+system\w*|\bsystem[s]?\s+control\w*', text)
        match_2 = o(r'\belectric\w*|\belectronic\w*|\bpower\w*', text)
        match_3 = o(r'\brepair\w*|\binspect\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\breinstatement\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+fit\s+purpose\w*|\bcheck\w*|\bassess\w*|\btechnician\w*|\btest\w*|\binvestigat\w*|\bidentif\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and inspect electrical equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and inspect equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breliability\s+analy\w*|\breliability\s+improvement\w*|\breliability\s+process\w*|\breliability\s+engineer\w*|\brepair\w*|\bfix\b|\bfixing\b|(?<!database )maintenance\w*|\bmaintain\w*|\btechncian\w*|\bcheck\w*|\bassess\w*|\binvestigat\w*|\btest\w*|\bidentif\w*|\bfault\w*', text)
        match_2 = o(r'\bequipment\w*|\bdevice\w*|\binstrument\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and inspect equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect machines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdiagnose\s+fault\w*|\bfault[s]?\s+diagnos\w*|\binspect\s+machine\w*|\bfault[s]?\s+inspect\w*|\binspect\s+fault\w*|\bfault\s+machine\w*|\bmechanical\s+commission\w*|\btest[i]?[n]?[g]?\s+device\w*', text)
        match_2 = o(r'\bmainenance\b|\bmaintain\b|\brepair\w*|\binspect\w*|\bdiagnose\w*|\bfault\w*|\bassess\w*|\bcheck\w*|\btest\w*', text)
        match_3 = o(r'\bmachine\w*|\bmechanical\w*|\bplant\b|\bmanufactur\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect machines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inspect tidal stream generators
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btidal\w*|\bwave\w*', text)
        match_2 = o(r'\benegry\w*|\bpower\w*|\bgenerat\w*|\bmachine\w*|\bequipment\w*|\bdevice\w*|\bmotor\w*', text)
        match_3 = o(r'\bengineering\w*|\bquality\s+control\w*|\bqc\b|\bmainenance\b|\bmaintain\b|\brepair\w*|\binspect\w*|\bdiagnose\w*|\bfault\w*|\bassess\w*|\bcheck\w*|\btest\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'inspect tidal stream generators'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for install electrical and electronic equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectric\w*|\belectronic\w*', text)
        match_2 = o(r'\binstall\w*|\bfit\b|\bfitting\b', text)
        match_3 = o(r'\bequipment\w*|\bmachine\w*|\binstrument\w*|\binstallation\w*|\btool\w*|\bcontrol\s+system\w*|\bsystem[s]?\s+control\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'install electrical and electronic equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for comply with the electrician regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bedition\s+electric\w*|\bedition\s+regulation\w*|\belectric[a]?[l]?\s+regulation\w*|\belectric[a]?[l]?s+compliance\w*', text)
        match_2 = o(r'\belectric\w*', text)
        match_3 = o(r'\bregulation\w*|\bcompliance\w*|\bpolicy\w*|\bpolicies\w*|\blegislation\w*|\bcomply\w*|\badhere\w*|\badherence\w*|\bedition\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'comply with the electrician regulations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adjust manufacturing equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanufacturing\s+equipment\s+enigneer\w*|\bcalibrate\s+manufacturing\s+equipment\w*|\badjust\s+manufacturing\s+equipment\w*', text)
        match_2 = o(r'\breliability\w*|\breliable\w*|\bcalibrate\w*|\badjust\w*|\bfit\s+purpose\w*|\befficient\w*', text)
        match_3 = o(r'\bmachine\w*', text)
        match_4 = o(r'\binstrument\w*|\bdevice\w*|\bequipment\w*|\bmachine\w*|\bequipment\w*|\bdevice\w*', text)
        match_5 = o(r'\bengineer\w*|\bmanufactur\w*|\bfactory\w*|\bfabricat\w*|\bproduct\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5:
            matched_fragments.extend([match_2, match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'adjust manufacturing equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop scientific research protocols
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprotocol\w*|\bprocedure\w*', text)
        match_2 = o(r'\bdevelo\w*|\bcreate\w*|\bdesign\w*|\bformulat\w*|\bsuggest\w*|\bimporov\w*|\bassess\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|\breserach\w*|\bexperiment\w*|\bstudy\b|\bscientific\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop scientific research protocols'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply gmp
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgmp\b|\bgood\s+manufacturing\s+principle\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply gmp'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply risk management processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blopa\b|\brisk\s+management\w*|\brisk\s+analy\w*|\bmanag[e]?[i]?[n]?[g]?\s+risk\w*|\brisk\s+mitigat\w*|\bmitigate\s+risk\w*|\banticipat[e]?[i]?[o]?[n]?\s+risk\w*|\brisk\s+anticipat\w*|\bforecast\s+risk\w*|potential\s+risk\w*|\bpotential\s+threat\w*|\bremove\s+risk\w*|\bremove\s+threat\w*|\bminimize\s+risk\w*|\bcontrol\s+risk\w*|\brisk\s+control\w*|\bmitigate\s+hazard\w*|\brisk\s+preference\w*|\bcontrol\s+\w+\s+risk\w*|\bidentify\s+\w+\s+risk\w*|\bmitigate\s+\w+\s+risk\w*|\bremove\s+\w+\s+risk\w*|\bminimize\s+\w+\s+risk\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+risk\w*|\bforecast\s+\w+\s+risk\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply risk management processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess risk factors and draw up risk assessment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brisk\s+factor\w*|\bfactor[s]\s+risk\w*|\bhazop\b|\bhazid\b|\bsil\b|risk[s]?\s+assessment\w*|\bassess\s+risk\w*|\bassessment\s+risk\w*|\brbi\b|\balarp\b|\bealarp\b|\bcertified\s+risk\s+manage\w*|\baudit[i]?[n]?[g]?\s+risk\w*|\bensur[e]?[i]?[n]?[g]?\s+risk\w*|\baudit[i]?[n]?[g]?\s+\w+\s+risk\w*|\bensur[e]?[i]?[n]?[g]?\s+\w+\s+risk\w*', text)
        match_2 = o(r'\bcrm\b|\bfactor\w*|\bcause\w*', text)
        match_3 = o(r'\brisk\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess risk factors and draw up risk assessment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess supplier risks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupplier\s+risk\s+mitigation\w*|\bsupply\s+risk\s+mitigation\w*|\bsupply\s+risk\w*|\bsupplier[s]\s+risk\w*|\bvendor[s]\s+risk\w*|\brisk\s+vendor\w*|\brisk\s+profile\s+supplier\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess supplier risks'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for create incident reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bincident\s+report\w*|\breport\s+incident\w*|\breporting\s+incident\w*|\bincident\s+record\s+system\w*|\brecord\s+indicent\w*|\bincident\s+request\w*|\bincident\s+review\w*|\breview\s+incident\w*|\breport[i]?[n]?[g]?s+\w+\s+indicent\w*|\bdocument[a]?[t]?[i]?[o]?[n]?\s+incident\w*|\bincident\s+document\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'create incident reports'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow reporting procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatus\s+report\w*|\bwrit[e]?[i]?[n]?[g]?\s+report\w*|\bprepar[e]?[i]?[n]?[g]?\s+report\w*|\bgenerat[e]?[i]?[n]?[g]?\s+report\w*|\bproject\s+report\w*|\breport\s+document\w*|\bdocument\s+report\w*|\bdraft\s+report\w*|\breport[i]?[n]?[g]?\s+procedure\w*|\bcontrol[l]?[i]?[n]?[g]?\s+report\w*|\breport[i]?[n]?[g]?\s+control\w*|\bvisit\s+report\w*|\breport\s+provision\w*|\bminute\s+taking\w*|\broutine\s+report\w*|\bhr\s+report\w*|\bquality\s+report\w*|\breport\s+datum\w*|\bcreate\s+report\w*|\bdirect\s+report[i]?[n]?[g]?\b|\bwrit[e]?[i]?[n]?[g]?\s+report\w*|\bassessment\s+report\w*|\bprogress\s+report\w*|\breport\s+write\w*|\breport\s+data\w*|\bdata\s+report\w*|\bdatum\s+report\w*|\badministrative\s+report\w*|\bstatistical\s+report\w*|\breport[i]?[n]?[g]?\s+department\w*|\breport[i]?[n]?[g]?\s+standard\w*|\bdaily\s+report\w*|\bmonthly\s+report\w*|\byearly\s+report\w*|\bannual\s+report\w*|\breport[i]?[n]?[g]?\s+system\w*|\bscope\s+report\w*|\breport[i]?[n]?[g]?\s+scope\w*|\bcomputer\s+system\s+report\w*|\breport\s+internal\s+external\w*|\breport[i]?[n]?[g]?\s+control\w*|\bsurvey\s+report\w*|\breport[i]?[n]?[g]?\s+metric\w*|\binternal\s+report\w*|\bconstraint[s]?\s+report\w*|\bstandard\s+report\w*|\bboard\s+report\w*|\bwrit[e]?[i]?[n]?[g]?\s+\w+\s+report\w*|\bprepar[e]?[i]?[n]?[g]?\s+\w+\s+report\w*|\bwrit[e]?[i]?[n]?[g]?\s+\w+\s+report\w*|\bdevelop[i]?[n]?[g]\s+\w+\s+report\w*|\bprepar[e]?[i]?[n]?[g]?\s+monthly\s+\w+\s+report\w*|\bprepar[e]?[i]?[n]?[g]?\s+daily\s+\w+\s+report\w*|\bprogress\s+\w+\s+report\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow reporting procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain relationship with suppliers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupplier[s]?\s+relation\w*|\bsupplier\s+manage\w*|\bvendor\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+supplier[s]?\b|\bmanag[e]?[i]?[n]?[g]?\s+vendor[s]?\b|\bhandl[e]?[i]?[n]?[g]?\s+supplier\w*|\bmanag[e]?[i]?[n]?[g]?\s+distributor[s]?\b|\bmanag[e]?[i]?[n]?[g]?\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\b|\bvendor\s+representative\w*|\bliais[e]?[o]?[n]?\s+vendor\w*|\bliais[e]?[o]?[n]?\s+supplier\w*|\bevaluat[e]?[i]?[n]?[g]?\s+supplier\w*|\bevaluat[e]?[i]?[n]?[g]?\s+distributor\w*|\bevaluat[e]?[i]?[n]?[g]?\s+contractor\w*|\bcontractor[s]?\s+evaluat\w*|\bsupplier[s]?\s+evaluat\w*|\bdistributor[s]?\s+evaluat\w*|\b[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\s+manage\w*|\bdistributor\s+manage\w*|\bcontact[i]?[n]?[g]?\s+supplier\w*|\bcontact[i]?[n]?[g]?\s+vendor\w*|\bcontact[i]?[n]?[g]?\s+\b[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bliais[e]?[o]?[n]?\s+\b[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bvendor[s]?\s+support\w*|\bvendor[s]?\s+relation\w*|\bpoint\s+contractor\w*|\bpoint\s+vendor\w*|\bpoint\s+supplier\w*|\bpoint\s+[s]?[u]?[b]?\s?[-]?\s?contractor\w*|\b[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\s+support\w*|\b[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\s+relation\w*|\bdistributor[s]?\s+support\w*|\bdistributor[s]?\s+relation\w*|\bsupport[i]?[n]?[g]?\s+supplier\w*|\bsupport[i]?[n]?[g]?\s+vendor\w*|\bsupport[i]?[n]?[g]?\s+distributor\w*|\bdistributor[s]?\s+support\w*|\bsupport[i]?[n]?[g]?\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\binteract\s+vendor\w*|\binteract\s+supplier\w*|\binteract\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\binteract\s+distributor\w*|\boversee\s+distributor\w*|\boversee\s+vendor\w*|\boversight\s+suppl\w*|\boversight\s+vendor\w*|\boversight\s+distributor\w*|\bsupplier\s+oversight\w*|\bvendor\s+overshigt\w*|\bdistributor\s+oversight\w*|\bsupply\s+oversight\w*|\boversee\s+suppl\w*|\bsupplier\s+overse\w*|\brelationship\s+supplier\w*|\brelationship\s+vendor\w*|\brelationship\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bconsult[a]?[n]?[c]?[y]?\s+supplier\w*|\bconsult[a]?[n]?[c]?[y]?\s+vendor\w*|\bconsult[a]?[n]?[c]?[y]?\s+distributor\w*|\bvendor[s]\s+\s+consult\w*|\bsupplier[s]?\s+consult\w*|\bdistributor[s]?\s+consult\w*|\bkey\s+point\s+supplier\w*|\bkey\s+point\s+vendor\w*|\bkey\s+point\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\brelationship\s+management\s+supplier\w*|\brelationship\s+management\s+vendor\w*|\brelationship\s+management\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\brelationship\s+management\s+key\s+supplier\w*|\brelationship\s+management\s+key\s+vendor\w*|\brelationship\s+management\s+key\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bcontact[i]?[n]?[g]?\s+\w+\s+vendor\w*|\bcontact[i]?[n]?[g]?\s+\w+\s+supplier\w*|\bcontact[i]?[n]?[g]?\s+\w+\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\brelationship\s+\w+\s+supplier\w*|\brelationship\s+\w+\s+vendor\w*|\brelationship\s+\w+\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bconsult[a]?[n]?[c]?[y]?\s+\w+\s+vendor\w*|\bconsult[a]?[n]?[c]?[y]?\s+\w+\s+supplier\w*|\bconsult[a]?[n]?[c]?[y]?\s+\w+\s+[s]?[u]?[b]?\s?[-]?\s?contractor[s]?\w*|\bconsult[a]?[n]?[c]?[y]?\s+\w+\s+distributor\w*', text)
        match_2 = o(r'\bsupplier\w*', text)
        match_3 = o(r'\brelation\w*|\bcontract\w*', text)
        match_4 = o(r'\bmanage\b|\bmanaging\b|\bmanagement\b|\bmaintain\w*|\breview\w*|\bdevelop\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain relationship with suppliers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain relationship with customers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer[s]?\s+relation\w*|\brelationship\s+customer\w*|\brelationship\s+client\w*|\bclient[s]?\s+representative\w*|\bcustomer[s]?\s+represenative\w*|\brepresent[i]?[n]?[g]?\s+customer\w*|\brepresent[i]?[n]?[g]?\s+client\w*|\bcustomer[s]?\s+rapport\w*|\bclient[s]?\s+rapport\w*|\brapport\s+customer\w*|\brapport\s+client\w*|\bsme\s+relationship\w*|\brelationship\s+sme\b|\binterface\s+customer\w*|\binterface\s+client\w*|\bcustomer[s]?\s+interface\w*|\bclient[s]?\s+interface\s*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain relationship with customers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage inventory
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'manag[e]?[i]?[n]?[g]?\s+inventory\w*|\binventory\s+manage\w*|\binventory\s+transact\w*|\binventory\s+order\w*|\binventory\s+re\s?[-]?\s?order\w*|\bbuyer\s+planner\w*|\bplan\s+inventory\w*|\binventory\s+plan\w*|\binventory\s+optimal\w*|\binventory\s+control\w*|\boptimal\s+inventory\w*|\bmanag[e]?[i]?[n]?[g]?\s+stock\w*|\bstock\s+manage\w*|\boptimal\s+stock\w*|\bplan\s+stock\w*|\bstock\s+plan\w*|\bupdate\s+inventory\w*|\bcatalog[u]?[e]?\s+inventory\w*|\binventory\s+catalog\w*|\bmanag[e]?[i]?[n]?[g]?\s+stock\w*|\bstock\s+manage\w*|\bstock\s+check\w*|\bcheck[i]?[n]?[g]?\s+stock\w*|\bstock\s+adjustment\w*|\badjust[i]?[n]?[g]?\s+stock\w*|\bmaterial[s]?\s+manage\w*|\bmaterial\s+collation\w*|\bmaterial\s+movement\s+ticket\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+stock\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+inventory\w*', text)
        match_2 = o(r'\bschedul\w*|\bplan\w*|\bmanage\w*|\bcollation\w*|\bmanaging\w*|order\w*', text)
        match_3 = o(r'\binventory\w*|\bstock\w*|\bresource\w*|\brestock\w*|\bmmt[s]?\b|\bmaterial\w*', text)
        match_4 = o(r'\btraining\s+program\w*|\btrainer\w*|\btraining\s+plan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage inventory'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage stock rotation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstockperson\b|\bmanag[e]?[i]?[n]?[g]?\s+stock\b|\bstock[i]?[n]?[g]?\s+level\w*|\bstock\s+rotat[e]?[i]?[o]?[n]?\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage stock rotation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for purchase supplies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpurchase\s+supplies\b|\bgain\s+supplies\w*|\bpurchase\s+supply\w*|\bgain\s+supply\w*|\bpurchase\s+\w+\s+supplies\w*|\bpurcahse\s+\w+\s+supply\w*|\bgain\s+\w+\s+supplies\w*|\bgain\s+\w+\s+supply\w*', text)
        match_2 = o(r'\bsupplies\w*', text)
        match_3 = o(r'\bpurchase\w*|\barrange\w*|\bacquire\w*|\bgain\w*|\bpurchasing\w*|\bacquiring\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'purchase supplies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for issue purchase orders
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpurchase\s+order\w*|\bpo\s+number\w*', text)
        match_2 = o(r'\bprepar\w*|\bmanage\*|\bissu\w*|\bproduce\w*|\bpublish\w*|\bmake\w*|\bhandl\w*|\breview\w*|\bplace\w*|\brecord\w*|\btrack\w*', text)
        match_3 = o(r'\bpo\b', text)
        match_4 = o(r'\bprocurement\w*|\blogistic\w*|\binventory\w*|\bissue\w*|\bmanage\w*|\barrange\w*|\bhandle\w*|\bhandling\w*|\bmanaging\w*|\breview\w*|\bprepare\w*|\bpreparing\w*|\border\w*|\bsupply\w*|\bmake\w*|\breview\w*|\bplace\w*', text)
        match_5 = o(r'\bmeeting[s]?\b', text)
        if match_1 and match_2 and not match_5:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'issue purchase orders'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for forecast product demand
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdemand\s+plan\w*|\bdemand\s+forecast\w*|\bforecast\s+demand\w*|\banalyse\s+demand\w*|\bdemand\s+analys\w*|\bplan\s+demand\w*|\bsale\s+forecast\w*|\bforecast\s+sale\w*', text)
        match_2 = o(r'\bproduct\w*|\bservice\w*', text)
        match_3 = o(r'\bdemand\w*', text)
        match_4 = o(r'\bforecast\w*|\btrend\w*|\banaly\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'forecast product demand'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for determine production capacity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduction\w*|\bmanufacturing\w*|\bfabricat\w*', text)
        match_2 = o(r'\bcapacity\w*|\bmaximum\w*|\bscope\w*|\bnumber\w*|\bquantity\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'determine production capacity'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for schedule production
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bschedul[e]?[i]?[n]?[g]?\s+production\w*|\bplan[n]?[i]?[n]?[g]?\b\s+production\w*|\bproduction\s+planning\b|\bproduction\s+scheduling\w*|\bschedul[e]?[i]?[n]?[g]?\s+production\w*|\bschedul[e]?[i]?[n]?[g]?\s+\w+\s+production\w*|\bplan[n]?[i]?[n]?[g]?\s+\w+\s+production\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'schedule production'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for follow procedures to control substances hazardous to health
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcoshh\b|\bbiocoshh\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'follow procedures to control substances hazardous to health'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform agrochemical testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'agrochemical\w*', text)
        match_2 = o(r'\btest\w*|\bassess\w*|\bquality\w*|\bsafe\w*|\bqc\w*|\bqa\w*|\banaly\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform agrochemical testing'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monitor and adjust fermentation processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfermentation\w*', text)
        match_2 = o(r'\bmanufacture\w*|\bmanufacturing\w*|\bproduction\w*|\bplant\w*|\bprocess\w*|\badjust\w*|\bmonitor\w*|\bassess\w*|\btend\w*|\boversee\w*|\boversight\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'monitor and adjust fermentation processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply horticultural standards and practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhorticulture\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply horticultural standards and practices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle animals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\s+caretaker\w*|\banimal\s+technician\b|\bhusbandry\b|\banimal\s+care\w*|\banimal\s+record\w*|\bgrooming\b|\bcare\s+animal\w*|\bwork\s+animal\w*', text)
        match_2 = o(r'\banimal\s+health\w*|\banimal\s+safety\w*|\banimal\s+care\w*', text)
        match_3 = o(r'\bcare\w*|\bensure\w*|\btend\w*|\bassess\w*|\bmonitor\w*|\bmanage\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle animals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for handle poultry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\bcare\w*|\bgrooming\w*|\bsafety\w*|\bhealth\w*|\banimal\s+technician\b|\bhusbandry\b', text)
        match_2 = o(r'\bpoultry\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'handle poultry'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain animal accommodation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\blivestock\w*', text)
        match_2 = o(r'\bcage\w*|\baccomodation\w*|\bshelter\w*|\bkennel\w*|\bshed\w*|\bclean\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain animal accommodation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for assess management or environment of animals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\blivestock\w*', text)
        match_2 = o(r'\bmanagement\w*|\benvironment\w*', text)
        match_3 = o(r'\bassess\w*|\bquality\w*|\bevaluate\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'assess management or environment of animals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform autopsy on animals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnecropsy\b', text)
        match_2 = o(r'\bautopsy\w*', text)
        match_3 = o(r'\banimal\w*|\bperform\w*|\bconduct\w*|\bassit\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform autopsy on animals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach veterinary science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bveterinary\s+medicine\b|\bveterinary\s+education\w*|\bveterinary\s+science\w*', text)
        match_2 = o(r'\blecturer\b|\bteach\w*|\blecture\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach veterinary science'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide nursing care for hospitalised animals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bveterinary\s+nursing\w*', text)
        match_2 = o(r'\bnurse\w*|\bnursing\w*', text)
        match_3 = o(r'\banimal\w*', text)
        match_4 = o(r'\banimal\s+care\w*', text)
        match_5 = o(r'\bhospital\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide nursing care for hospitalised animals'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plan animal breeding programmes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\blivestock\w*', text)
        match_2 = o(r'\bbreed\w*|\bbreeding\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'plan animal breeding programmes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for instruct on animal care
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\blivestock\w*', text)
        match_2 = o(r'\bcare\w*|\bhandle\w*', text)
        match_3 = o(r'\binstructor\b|\binstruct\b|\bsupervis\w*|\bteach\w*|\btrain\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'instruct on animal care'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage anaimal welfare
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\w*|\blivestock\w*', text)
        match_2 = o(r'\bwelfare\w*|\bwell\s?[-]?\s?being\w*', text)
        match_3 = o(r'\bmanage\w*|\bcomply\w*|\bcompliance\w*|\bmanaging\w*|\bsupervi\w*|\bimplement\w*|\badhere\w*|\bensure\w*|\bensuring\w*|\badvice\w*|\bsupport\w*|\bimplement\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'manage anaimal welfare'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control livestock disease
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdisease\w*', text)
        match_2 = o(r'\bcontrol\w*|\bmanage\w*|\bmanaging\w*|\bhandle\w*|\bcheck\w*|\btend\w*|\bstudy\w*|\btest\w*', text)
        match_3 = o(r'\blivestock\w*|\bfarm\b|\banimal\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'control livestock disease'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain the farm
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaintain\s+farm\b|\bfarm\s+maintenance\w*|\bfarm\s+maintain\w*|\bmaintaining\s+farm\b|\bmanag[e]?[i]?[n]?[g]?\s+farm\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+farm\w*|\bmaintaining\s+\w+\s+farm\b|\bcrofter\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain the farm'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for provide advice to farmers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfarm\s+advisory\w*|\bfarm\s+advice\w*|\bfarmer\s+advisory\w*|\bguide\s+farm\w*|\badvice\s+farm\w*|\badvise\s+farm\w*', text)
        match_2 = o(r'\bprovide\s+advice\w*|\bprovide\s+guidance\w*|\bgive\s+guidance\w*|\bgive\s+advice\w*', text)
        match_3 = o(r'(?!wind )\bfarm\w*|\bcrofter\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'provide advice to farmers'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate dairy farms
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdairy\b', text)
        match_2 = o(r'\bfarm\w*|\bcrofter\b|\bcrofting\b|\boperate\w*|\bmanage\w*|\bmanaging\w*|\btend\w*|\bhandle\w*|\bsupport\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate dairy farms'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for research improvement of crop yields
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrop\s+technician\w*', text)
        match_2 = o(r'\bplant\w*|\byield\w*', text)
        match_3 = o(r'\bcrop[s]?\b', text)
        match_4 = o(r'bcrop[p]?[i]?[n]?[g]?\s+system\w*', text)
        match_5 = o(r'\bsciences\w*|\bresearch\w*|\bimprovement\w*|\befficiency\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'research improvement of crop yields'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for harvest and preserve crops
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrop\s+technician\w*', text)
        match_2 = o(r'\bharvest\w*|\bpreserv\w*|\bcollect\w*|\bgather\w*|\bconserving\w*|\bconserve\w*|\bpreserving\w*', text)
        match_3 = o(r'\bcrop\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'harvest and preserve crops'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise crop production and inspect fields
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrop\s+technician\w*|\binsepct[i]?[n]?[g]?\s+field\w*|\bcheck[i]?[n]?[g]?\s+field\w*|\bfield\s+insepct\w*|\binsepct[i]?[n]?[g]?\s+\w+\s+field\w*|\bfield\s+\w+\s+insepct\w*|\bcheck[i]?[n]?[g]?\s+\w+\s+field\w*', text)
        match_2 = o(r'\bfarm\w*|\bfield\w*|\bcrop\w*', text)
        match_3 = o(r'\bproduction\w*|\byield\w*', text)
        match_4 = o(r'\bsupervise\b|\bsupervision\b|\bsupervisor\b|\binspect\w*|\bmanage\w*|\bmanaging\w*|\boversee\w*|\boversight\w*|\bmonitor\w*|\bcheck\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise crop production and inspect fields'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for managae nutrient
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnutrient\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+nutrient\w*|\bcontrol[i]?[n]?[g]?s+nutrient\w*|\bnutrient\s+control\w*', text)
        match_2 = o(r'\bnutrient\w*', text)
        match_3 = o(r'\bcrop\w*|\bharvest\w*|\bfarm\w*|\bplant\w*|\bsoil\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'managae nutrient'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop forestry strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforester\w*|\bforestry\w*', text)
        match_2 = o(r'\bstrategy\w*|\bplan\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop forestry strategies'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain and operate forestry equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforester\w*|\bforestry\w*', text)
        match_2 = o(r'\bequipment\w*|\bmachinery\b|\bmachines\b|\btool\w*|\bdevice\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain and operate forestry equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate agricultural equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagriculture\w*|\bagricultural\w*', text)
        match_2 = o(r'\bequipment\w*|\btool\w*|\bdevice\w*|\bmachine\w*|\bmachinery\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate agricultural equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate farm equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfarm\w*', text)
        match_2 = o(r'\bequipment\w*|\btool\w*|\bdevice\w*|\bmachine\w*|\bmachinery\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate farm equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain farm machinery
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnician\w*|\brepare\w*|\brepair\w*|\bmaintain\w*|\bfix\b|\bmaintenance\w*|\btend\w*|\bfixing\w*', text)
        match_2 = o(r'\bfarm\w*', text)
        match_3 = o(r'\bequipment\w*|\btool\w*|\bdevice\w*|\bmachine\w*|\bmachinery\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain farm machinery'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate tractor
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btractor\w*', text)
        match_2 = o(r'\bdrive\w*|\boperate\w*|\bhandle\w*|\buse\w*|\busing\w*|\boperating\w*|\bdriving\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate tractor'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain agricultural machinery
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnician\w*|\brepair\w*|\brepare\w*|\bmaintain\w*|\bfix\b', text)
        match_2 = o(r'\bagriculture\w*|\bagricultural\w*', text)
        match_3 = o(r'\bequipment\w*|\btool\w*|\bdevice\w*|\bmachine\w*|\bmachinery\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain agricultural machinery'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate horticulture equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhorticultur\w*', text)
        match_2 = o(r'\bequipment\w*|\btool\w*|\bdevice\w*|\bmachine\w*|\bmachinery\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate horticulture equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for wear protective equipment against industrial noise
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnoise\s+level\b|\bindustrial\s+noise\w*', text)
        match_2 = o(r'\bconstruction\b|\bprotective\s+equipment\w*|\bproduction\w*|\bmanufactur\w*|\bppe\w*|\bprotect\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'wear protective equipment against industrial noise'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for promote food sustainability
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfood\s+sustainability\w*|\bsustainable\s+food\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'promote food sustainability'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform classroom management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclassroom\s+teaching\w*|\bclassroom\s+management\w*|\bclassroom\s+support\w*|\bclassroom\s+arrange\w*|\bcalssroom\s+organis\w*|\bmanag[e]?[i]?[n]?[g]?\s+classroom\w*|\borganis[e]?[i]?[n]?[g]?\s+classroom\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform classroom management'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform lectures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blecturer\b', text)
        match_2 = o(r'\bprofessor\bacademia\w*|\badcademic\w*|\bgive\w*|\bprovide\w*|\bpresent\w*|\bteach\w*|\bperform\w*', text)
        match_3 = o(r'\blecture\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform lectures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach engineering principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blecturer\s+engineering\w*|\bengineering\s+lecturer\w*|\bteach\s+engineering\w*|\bcoach\s+engineer\w*', text)
        match_2 = o(r'\bteach\w*|\btrain\w*|\badvice\w*|\bcoach\w*|\beducate\w*|\blecture\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        match_4 = o(r'\bprinciple\w*|\bknowledge\w*|\bexperience\w*|\bdiscipline\w*|\bstaff\w*|\bemployee\w*|\bworker\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach engineering principles'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach horticulture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhorticultur\w*', text)
        match_2 = o(r'\bteach\w*|\btrain\w*|\badvice\w*|\bcoach\w*|\beducate\w*|\blecture\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach horticulture'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teach rural animal health
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brural\w*', text)
        match_2 = o(r'\banimal\w*', text)
        match_3 = o(r'\bteach\w*|\btrain\w*|\badvice\w*|\bcoach\w*|\beducate\w*|\blecture\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'teach rural animal health'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct podiatry consultation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpodiatry\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct podiatry consultation'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conduct psychological assessment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpsychometrician\b|\bpsychometric\s+technique\w*|\bpsychological\s+asses\w*|\bpsychometric\s+analy\w*|\bpsychological\s+analy\w*|\bpsychometric\s+assess\w*', text)
        match_2 = o(r'\bpsycholog\w*|\bpsychiatr\w*', text)
        match_3 = o(r'\bmental\s+health\w*|\bpsychomet\w*|\bassess\w*|\banaly\w*|\bpatient\w*|\bhealth\s+screen\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'conduct psychological assessment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify and advise on mental health
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmental\s+health\w*', text)
        match_2 = o(r'\btherapy\b|\btherapist\b|\bcounsellor\w*|\bpsychiatrist\w*|\bpsychologist\w*|\badvice\w*|\bguidance\w*|\breccomend\w*|\bconsult\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify and advise on mental health'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for train staff in quality procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btrain\s+staff\w*|\bstaff\s+train\w*|\bpersonnel\s+train\w*|\bemployee[s]?\s+train\w*|\btraining\s+competence\s+officer\w*|\btraining\s+officer\w*', text)
        match_2 = o(r'\bquality\s+procedure\w*|\bquality\s+control\w*|\bqc\b|\bquality\s+assurance\w*|\bquality\s+manage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'train staff in quality procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for train staff in health and safety procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bteach\s+hs\b|\bteach\s+hsse\b|\bteach\s+hsec\b|\bteach\s+health\s+safety\w*|\bhs\b\s+teach\w*|\bhsse\b\s+teach\w*|\bhsec\b\s+teach\w*|\bhealth\s+safety\s+teach\w*|\btrain\s+hs\b|\btrain\s+hsse\b|\btrain\s+hsec\b|\btrain\s+health\s+safety\w*|\bhs\s+train\w*|\bhsse\s+train\w*|\bhsec\s+train\w*|\bhealth\s+safety\s+train\w*', text)
        match_2 = o(r'\btrain\s+staff\w*|\btrain\s+personnel\w*|\btrain\s+employee\w*|\bstaff\s+train\w*|\bpersonnel\s+train\w*|\bemployee[s]?\s+train\w*|\btraining\s+competence\s+officer\w*|\btraining\s+officer\w*|\bteach\w*|\bcoach\w*', text)
        match_3 = o(r'\bsafety\s+procedure\w*|\bhs\b|\bhsse\b|\bhsec\b|\bhealth\s+safety\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'train staff in health and safety procedures'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for train or support junior members of staff
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjunior\w*', text)
        match_2 = o(r'\bstaff\w*|\bmember\w*|\bemployee\w*|\bworker\w*|\bintenr\w*', text)
        match_3 = o(r'\bteach\w*|\btrain\w*|\bguide\w*|\bconsult\w*|\bcoach\w*|\bmentor\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'train or support junior members of staff'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use hand tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhand\s+tool\w*', text)
        match_2 = o(r'\buse\w*|\boperate\w*|\butilise\w*|\bhandle\w*|\butilize\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use hand tools'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for identify construction materials from blueprints
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bblueprint\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'identify construction materials from blueprints'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for listen actively
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blisten[i]?[n]?[g]?\s+skil\w*|\blisten[i]?[n]?[g]?\s+active\w*|\bability\s+listen\w*|active[l]?[y]?\s+listen\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'listen actively'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for operate material handling equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaterial\s+handl[e]?[i]?[n]?[g]?\s+equipment\w*|\bmaterial\s+handl[e]?[i]?[n]?[g]?\s+tool\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'operate material handling equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform procurement processes and assess procurement needs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocurement\s+sourcing\w*|\bprocurement\s+purchasing\w*|\bprocurement\s+analy\w*|\banalyse\s+procurement\w*|\banalyze\s+procurement\w*|\bprocurement\s+specialist\w*|\bprocurement\s+advice\w*|\bprocurement\s+consult\w*|\bprocurement\s+advisor\w*|\bprocurement\s+adviser\w*|\bperform\s+procurement\w*|\bprocurement\s+strateg\w*|\bmanag[e]?[i]?[n]?[g]?\s+procurement\w*|\bprocurement\s+manage\w*|\bpurchase\s+item\w*|\bpurchase\s+equipment\w*|\bproject\s+procurement\w*|\bcorporate\s+procurement\w*|\bprocurement\s+process\w*|\bdesign\s+procurement\w*|\bconstruction\s+procurement\w*|\bprocurement\s+construction\w*|\bcommodity\s+sourc\w*|\bcommodity\s+strategy\w*|\bprocurement\s+organi\w*|\borganize\s+procurement\w*|\borganise\s+procurement\w*|\bmanag[e]?[i]?[n]?[g]?\s+procurement\w*|\bprocurement\s+manage\w*|\bprocurement\s+need\w*|\bsupport[i]?[n]?[g]?\s+procurement\w*|\bfm\s+procurement\w*|\bmaterial\s+movement\s+ticket\w*|\bensure\s+\w+\s+procurement\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+procurement\w*|\banalyse\s+\w+\s+procurement\w*|\banalyze\s+\w+\s+procurement\w*|\borganise\s+\w+\s+procurement\w*|\borganize\s+\w+\s+procurement\w*|\bdevelop[i]?[n]?[g]?\s+\w+\s+procurement\w*|\bsupport[i]?[n]?[g]?\s+\w+\s+procurement\w*', text)
        match_2 = o(r'\bprocurement\w*|\btender\w*', text)
        match_3 = o(r'project\w*|\bcontract\b|\bcontracting\b', text)
        match_4 = o(r'\bprocurement\w*|\btender\w*|\bdocument\w*|\braport\w*', text)
        match_5 = o(r'\brqt\b|\bitt\b|\bitb\b|\brfq\b|\bpfi\b|\bppp[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform procurement processes and assess procurement needs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply good clinical practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bich\b|\bgcp\b|\bglp\b|\bgood\s+clinical\s+practice\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply good clinical practices'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apply clinical practices (non glp)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnon\s+glp\w*|\bnon\s+-\s+glp\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'apply clinical practices (non glp)'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supervise laboratory operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+laboratory\w*|\bmanag[e]?[i]?[n]?[g]?\s+lab\b|\bsupervis[e]?[i]?[n]?[g]?\s+laboratory\w*|\bsupervis[e]?[i]?[n]?[g]?\s+lab\b|\blaboratory\s+supervis\w*|\blab\s+supervis\w*|\blaboratory\s+manage\w*|\blab\s+manage\w*|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+lab\b|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+laboratory\w*|\bensur[e]?[i]?[n]?[g]?\s+laboratory\s+work\w*|\bensur[e]?[i]?[n]?[g]?\s+lab\b\s+work\w*|\bsupervis[e]?[i]?[n]?[g]?\s+\w+\s+lab\b|\bsupervis[i]?[n]?[g]?\s+\w+\s+laboratory\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+laboratory\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+lab\b|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+lab\b|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+laboratory\w*', text)
        match_2 = o(r'\boperation[s]?\s+management\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'supervise laboratory operations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use specialized equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalytical\s+equipment\w*|\banalytic\s+equipment\w*|\banalytics\s+equipment\w*|\banalysis\s+equipment\w*|\banalytical\s+device[s]?\b|\banalytic\s+device[s]?\b|\banalytics\s+device[s]?\b|\banalysis\s+device[s]?\b|\banalytical\s+machine[s]?\b|\banalytic\s+machine[s]?\b|\banalytics\s+machine[s]?\b|\banalysis\s+machine[s]?\b|\bspecialised\s+equipment\w*|\bspecialized\s+equipment\w*|\banalytical\s+equipment\w*|\banalysis\s+equipment\w*|\banalytic[s]?\s+equipment\w*|\bspecialised\s+tool[s]?\b|\bspecialized\s+tool[s]?\b|\bspecialised\s+device[s]?\b|\bspecialized\s+device[s]?\b|\bfield\s+device\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use specialized equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for develop pharmaceutical drugs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanufacture\s+new\s+drug\w*|\bmanufacture\s+drug\w*|\bdrug\s+manufacture\w*|\bdiscover\s+drug\w*|\bdrug\s+discovery\w*|\bdevelop\s+drug\w*|\bdevelop\s+\w+\s+drug\w*|\binvent\s+\w+\s+medicine\w*|\bdiscover\s+\w+\s+drug\w*|\binvent\s+\w\s+drug\w*|\bdiscover\s+\w+\s+medicine\w*|\bdrug\s+therapy\w*', text)
        match_2 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*|\bhelp\w*|\bassist\w*|\bscientist\w*|\bvivo\b|\bvitro\b|\btoxicology\b|\bwork\w*|\bperform\w*|\bconduct\w*|\btake\s+part\w*|\bmanage\w*|\bmanaging\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'develop pharmaceutical drugs'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality assurance for pharmaceutical products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpharmaceutical\s+quality\w*|\bquality\s+pharmaceutical(?! service)(?! company)\w*|\bdrug\s+quality\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'quality assurance for pharmaceutical products'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for perform drug investigations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrug[s]\b', text)
        match_2 = o(r'\bassay\w*|\bbioassay\w*|\bquality\s+control\w*|\bqc\b|\banaly\w*|\bresearch\w*|\btest\w*|\bdevelop\w*|\binvestigat\w*|\bcheck\w*|\beffect\w*|\bstudy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'perform drug investigations'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for evaluate scientific data concerning medicines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstudy\s+datum\b|\bscientific\s+datum\b|\bscientific\s+data\b|\bstudy\s+data\b', text)
        match_2 = o(r'\bevaluate\w*|\bstudy\w*|\binterpret\w*|\bassess\w*|\banaly\w*|\bmanage\w*|\bmanaging\w*|\bassay\w*', text)
        match_3 = o(r'\bpharmacokinetic\b|\bmedicine\b|\bdrug\b|\btrial\b|\bpharmaceutical\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'evaluate scientific data concerning medicines'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carry out flow cytometry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bflow\s+cytomet\w*', text)
        match_2 = o(r'\bsale\w*', text)
        if match_1 and not match_2:
            matched_fragments.extend([match_1])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'carry out flow cytometry'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for automate ppi processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bppi\b|\bpractical\s+process\s+improvement\w*', text)
        match_2 = o(r'\bautomate\w*|\bautomation\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'automate ppi processes'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for estimate damage
        matched_fragments = []
        match_1 = o(r'\bdamage\w*', text)
        match_2 = o(r'\bassess\w*|\bestimat\w*|\banaly\w*|\bcalculat\w*|\bcontrol\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'estimate damage'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintain availability of spare parts
        matched_fragments = []
        match_1 = o(r'\bspare[s]?\s+hold\w*', text)
        match_2 = o(r'\bspare[s]?\s+part\w*|\bspare[s]?\s+equipment|\bequipment\s+spare\w*|\bspare[s]?\s+stock\w*|\bspare[s]?\s+component\w*|\bsubstitute\s+part[s]?\b|\brepair\s+part[s]?\b|\breplacement\s+part[s]?\b', text)
        match_3 = o(r'\bmaintain\w*|\bmanage\b|\bmanagement\b|\bmanaging\b|\bupkeep\w*|\bensur\w*|\bavailab\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'maintain availability of spare parts'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tolerate stress
        matched_fragments = []
        match_1 = o(r'\bhandl[e]?[i]?[n]?[g]?\s+stress\w*|\babaid[e]?[i]?[n]?[g]?\s+stress\w*|\bendur[e]?[i]?[n]?[g]?\s+stress\w*|\bpermit\s+stress\w*|\btolerat[e]?[i]?[n]?[g]?\s+stress\w*|\bstress\s+handl\w*|\bstress\s+endur\w*|\bstress\s+tolera\w*|\bstress\s+resistan\w*|\bcop[e]?[i]?[n]?[g]?\s+stress\w*|\bcop[e]?[i]?[n]?[g]?\s+\w+\s+stress\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'tolerate stress'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for use barcode scanning equipment
        matched_fragments = []
        match_1 = o(r'\bbarcoding', text)
        match_2 = o(r'\bbarcode', text)
        match_3 = o(r'\bequipment\w*|\bscan\w*|\bprocedur\w*|\bperform\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            s = s + 1
            df.loc[index, 'skill_' + str(s)] = 'use barcode scanning equipment'
            df.loc[index, 'skill_' + str(s) + '_fragments'] = join_lists(matched_fragments)
        s = 0
    return df

def knowledge(df):#
    k = 0
    o = re.findall
    for index, row in tqdm(df.iterrows(), total=len(df), desc='Extracting Knowledge...'):
        text = row['Tokens']

        # Matching fragments for water network service operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnetwork\s+service\w*|\bnso\b|\bwater\s+technician\w*', text)
        match_2 = o(r'\bwater\b|\boperation[s]?\b|\bprocedure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'water network service operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for airworthiness certifications and directives
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bairworthiness\w*', text)
        match_2 = o(r'\bdirective\w*|\bcertificat\w*|\blegislation\w*|\bregulation\w*|\bcheck\w*|\bassess\w*|\bmonitor\w*|\bcompliance\w*|\bcompliant\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'airworthiness certifications and directives'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for engineering principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocess\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+background\w*|\bepc\b|\bepic\b|\bepci\b|\bengineer[i]?[n]?[g]?\s+principle\w*|\bprinciple[s]?\s+engineer\w*|\bmeng\b|\bbeng\b|\bunderstand[i]?[n]?[g]?\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+operation\w*|\bengineering\s+environment\w*|\bengineering\s+experience\w*|\bexperience\s+engineering\w*', text)
        match_2 = o(r'\bengineer\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\b3d\w*|\bmodel\w*|\bprogram\w*|\bconstruct\w*|\barchitect\w*|\bmechanical\w*|\bstructural\w*|\belectric\w*', text)
        match_4 = o(r'\bcyber\s+security\w*|\bcybersecurity\w*|\binformation\s+security\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'engineering principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gas engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+engineer\w*', text)
        match_2 = o(r'\bpipeline\w*|\bpipe[s]?\b|\bpipelay\w*', text)
        match_3 = o(r'\bgas\b', text)
        match_4 = o(r'\bengineer\b|\btechnician\w*|\brepair\w*|\bfix\w*|\bmaintain\w*|\bmaintenance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gas engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for well engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwell\s+engineer\w*|\bwell\s+intervention\w*|\bwell\s+operation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'well engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for water treatment health and safety procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+treatment\s+knowledge\w*|\bwater\s+treatment\s+skill\w*|\bwater\s+treatment\s+background\w*|\bwater\s+treatment\s+experience\w*|\bexperience\s+water\s+treatment\w*', text)
        match_2 = o(r'\bhealth\b|\bsafety\b', text)
        match_3 = o(r'\bwater\s+treatment\w*|\btreatment\s+water\w*|\bwater\s+technician\w*', text)
        match_4 = o(r'\bhealth\s+safety\w*|\bsafety\s+procedures\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'water treatment health and safety procedures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plumbing tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplumbing\b|\bplumber\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'plumbing tools'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for asbestos removal regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basbestos\s+removal\w*|\basbestos\s+survey\w*|\basbestos\s+analy\w*|\banalyze\s+asbestos\w*|\banalyse\s+asbestos\w*|\bremove\s+asbestos\w*|\basbestos\s+survey\w*|\basbestos\s+certificate\w*|\basbestos\s+consultan\w*', text)
        match_2 = o(r'(?<!water )\basbestos\b|\basbestos\b(?! other)\w*', text)
        match_3 = o(r'\bhealth\s+safety\w*|\bguideline\w*|\bregulation\w*|\blegislation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'asbestos removal regulations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for investment analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binvestment\s+analy\w*|\banalyze\s+investment\w*|\banalyse\s+investment\w*|\binvestigate\s+investment\w*|\binvestment\s+investigation\w*|\bassess\s+investment\w*|\binvestment\s+assess\w*|\breview\s+investment\w*|\binvestment\s+review\w*|\bmanage\s+investment\w*|\binvestment\s+manage\w*|\binvestment\s+strategy\w*|\bcompare\s+investment\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'investment analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for craftsmanship
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcraft\s+skill\w*|\bcraft\s+ability\w*|\bcrafting\b|\bcraft\s+good\w*|\bcraftsmanship\b|\bcraftsperson\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'craftsmanship'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electrical engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectrical\s+craft\w*|\belectric\s+craft\w*|\belectrical\s+engineer\w*|\belectricity\s+application\w*|\bapplication\s+electricity\w*|\belectronic[s]?\s+application\w*|\bapplication\s+electronic\w*|\binterpret\s+electric\w*|\belectrical\s+model\w*|\belectrical\s+code\w*', text)
        match_2 = o(r'\belectric\w*|\belectrical\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electrical engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mechanical engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmechanical\s+craft\w*|\banalys[e]?[i]?[n]?[g]?\s+mechanism\w*|\bdesign[i]?[n]?[g]?\s+mechanism\w*|\bmechanical\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+mechanism\w*|\bmaintain[i]?[n]?[g]?\s+mechanism\w*|\bmanufactur[i]?[n]?[g]?\s+mechanism\w*|\bmechanical\s+system\w*|\bmechanism\s+engineer\w*|\bmechanism\s+design\w*|\banalyze\s+mechanism\w*|\bmechanical\s+design\w*|\bmechanical\s+craftsperson\w*|\bmechanical\s+install\w*|\bmechanical\s+supervis\w*|\bmechanical\s+interface\w*', text)
        match_2 = o(r'\bmechanical\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mechanical engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mechatronics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmechatronic[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mechatronics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for civil engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcivil\s+engineer\w*|\bcity\s+plan\w*|\burban\s+plan\w*|\btunnelling\s+engineer\w*|\bcivil\s+\w+\s+engineer\w*', text)
        match_2 = o(r'\bengineer\w*', text)
        match_3 = o(r'\bcivil\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'civil engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for surface engineer
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsurface\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'surface engineer'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electric current
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectric[a]?[l]?\s+charge\w*|\belectric[a]?[l]?\s+current\w*|\belectric[a]?[l]?\s+flow\w*|\bpower\s+current\w*|\bvoltage\w*|\belectricity\s+charge\w*|\belectricity\s+flow\w*|\belectricity\s+service\w*|\belectricity\s+voltage\w*|\belectric[a]?[l]?\s+installation\w*|\belectric[a]?[l]?\s+engineer\w*|\belectric\s+voltage\w*|\belectric\s+service\w*|\belectrical\s+charge\w*|\belectrical\s+current\w*|\belectrical\s+flow\w*|\bhvdc\w*|\bhv\s+infrastructure\w*|\bhv\s+power\w*|\bpower\s+system\w*|\benergy\s+grid\w*|\bhv\s+grid\w*|\bmvdc\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electric current'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electric drives and motors
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectric[a]?[l]?\s+drive\w*', text)
        match_2 = o(r'\bvariable\s+speed\s+drive\w*|\bmotor\b|\bengine\w*', text)
        match_3 = o(r'\belectrical\w*|\belectric\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electric drives and motors'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electric heating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belectric\s+heat\w*|\belectrical\s+heat\w*|\belectricity\s+heat\w*', text)
        match_2 = o(r'\bheating\s+system\w*|\bheat\s+pump\w*|\bheat\s+system\w*', text)
        match_3 = o(r'\belectric\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electric heating system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for domestic heating systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+system\w*|\bheating\s+system\w*', text)
        match_2 = o(r'\bheating\w*|\bheat\b', text)
        match_3 = o(r'\bplumbing\w*|\bwater\s+system\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'domestic heating systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for type of heat pumps
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+pump\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'type of heat pumps'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of insulation material
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+recovery\w*|\bwaste\s+heat\w*|\bheat\s+waste\w*|\binsulation\w*|\binsulator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of insulation material'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for water desalination processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdesalination\w*', text)
        match_2 = o(r'\bwater\b|\bwastewater\w*|\bwaste\s+water\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'water desalination processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for renewable energy technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brenewable[s]?\s+energy\w*|\brenewable[s]?\s+power\w*|\brenewable\s+generation\w*|\bwind\s+energy\w*|\btidal\s+energy\b|\btidal\s+turbine\w*|\bwind\s+turbine\w*|\bwave\s+turbine\w*|\bsolar\s+energy\w*|\brenewable[s]?\s+engineer\w*|\brenewable[s]?\s+generation\s+energy\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'renewable energy technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electromagnetic compatibility and radio frequency testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemc\b|\brf\b|\bradio\s+frequency\w*|\belectromagnetic\s+compatibility\w*', text)
        match_2 = o(r'\bproject\w*|\btest\w*|\bplan\w*|\btechnical\s+specialist\w*|\bassess\w*|\baudit\w*|\bensure\w*|\bquality\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electromagnetic compatibility and radio frequency testing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ashrae testing principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bashrae\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ashrae testing principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for bsria and cibse commissioning standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbsria\b|\bcibse\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'bsria and cibse commissioning standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for water policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+law\w*|\bwater\s+legislation\w*|\bwater\s+act\w*|\bwater\s+regulation\w*|\bwater\s+policy\w*|\bwater\s+policies\w*|\bacop\b|\bl8\b|\bwater\s+\w+\s+law\w*|\bwater\s+\w+\s+legislation\w*|\bwater\s+\w+\s+act\w*|\bwater\s+\w+\s+regulation\w*|\bwater\s+\w+\s+policy\w*|\bwater\s+\w+\s+policies\w*|\bwaste\s?water\s+law\w*|\bwaste\s?ater\s+legislation\w*|\bwaste\s?water\s+act\w*|\bwaste\s?water\s+regulation\w*|\bwaste\s?water\s+policy\w*|\bwaste\s?water\s+policies\w*|\bwaste\s?water\s+\w+\s+law\w*|\bwaste\s?water\s+\w+\s+legislation\w*|\bwaste\s?water\s+\w+\s+act\w*|\bwaste\s?water\s+\w+\s+regulation\w*|\bwaste\s?water\s+\w+\s+policy\w*|\bwaste\s?water\s+\w+\s+policies\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'water policies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontrol\s+system\w*|\bmotor\s+control\s+panel\w*|\blocal\s+control\s+panel\w*|\bsystem[s]?\s+control\w*|\bcontrol\s+panel\w*', text)
        match_2 = o(r'\biacs\b|\bics\b|\blcp\b|\bmcc\b', text)
        match_3 = o(r'\bcontrol\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'control systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ICT system integration
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsystem[s]?\s+integrat\w*|\bintegrate\s+system\w*|\bintegrat[e]?[i]?[o]?[n]?\s+system\w*', text)
        match_2 = o(r'\bict\b|\bitc\b|\binformation\s+communication\s+technology\w*|\binformation\s+technology\s+communication\w*|\binformation\s+communication\s+system\w*|\binformation\s+technology\s+system\w*', text)
        match_3 = o(r'\bintagrat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ICT system integration'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for arc welding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmig\s+tig\s+arc\w*|\barc\s+weld\w*|\bmig\s+weld\w*|\btig\s+weld\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'arc welding'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mechanical tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmechanical\s+tool\w*|\bmechanical\s+equipment\w*|\bmechanical\s+machine\w*|\bmachine\s+type\w*|\btype[s]?\s+machine\w*|\brange\s+machine\w*|\btypology\s+machine\w*|\bmechanical\s+maintenance\w*|\bmachine\s+maintenance\w*|\bmaintain[i]?[n]?[g]?\s+machine\w*|\brepair[i]?[n]?[g]?\s+machine\w*', text)
        match_2 = o(r'\bmechanic\w*|\bmachine\w*', text)
        match_3 = o(r'\btool\w*|\bdevice\w*|\bequipment\w*|\bmerchandise\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mechanical tools'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for chemical products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchemical\s+product\w*|\bchemical\s+brand\w*|\bchemical\s+merchandise\w*|\bchemical\s+commodit\w*|\bchemical\s+stock\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'chemical products'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer insight
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnew\s+independent\s+customer\w*|\bnew\s+independent\s+client\w*|\bcustomer[s]?\s+insight\w*|\bclient[s]?\s+insight\w*|\bunderstand[i]?[n]?[g]?\s+customer\w*|\bunderstand[i]?[n]?[g]?\s+client\w*|\bcustomer[s]?\s+understand\w*|\bclient[s]?\s+understand\w*|\bcustomer[s]?\s+scope\w*|\bclient[s]?\s+scope\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer insight'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for acop legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bacop\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'acop legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hot and cold water systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+system[s]?\s+experience\w*|\bwater\s+system[s]?\s+background\w*|\bbackground\s+water\s+system\w*|\bexperience\s+water\s+system\w*', text)
        match_2 = o(r'\bhot\s+cold\b|\bcold\s+hot\b|\bhot\b|\bcold\b|\bwater\s+sample\w*', text)
        match_3 = o(r'water\w*', text)
        match_4 = o(r'\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hot and cold water systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for environmental science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment[a]?[l]?\s+engineer\w*|\benvironment[a]?[l]?\s+remediation\w*|\benvironment[a]?[l]?\s+protect\w*|\benvironment[a]?[l]?\s+scien\w*|\benvironment[a]?[l]?\s+advisor\w*|\benvironment[a]?[l]?\s+advice\w*|\benvironment[a]?[l]?\s+adviser\w*|\benvironment[a]?[l]?\s+consult\w*|\benvironment[a]?[l]?\s+discipline\w*', text)
        match_2 = o(r'(?<!work )(?<!working )(?<!challenge )(?<!challenging )(?<!team )(?<!commercial )(?<!business )(?<!community )environment\w*', text)
        match_3 = o(r'\bsustainability\w*|\bsustainable\w*|(?<!tower )\bremediation\w*|\bconservation\w*|\brestoration\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'environmental science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for environmental engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironment[a]?[l]?\s+improve\w*|\benvironment[a]?[l]?\s+engineer\w*|\benvironment[a]?[l]?\s+remed\w*|\benvironment[a]?[l]?\s+protect\w*|\benvironment[a]?[l]?\s+performance\w*|\benvironment[a]?[l]?\s+strategy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'environmental engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cutting technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpipe\s+cutting\b|\bpipe\s+cut\b|\bcut\s+pipe\w*|\bcutting\s+technologies\w*|\bcutting\s+machinery\w*|\bcutting\s+technology\w*|\bcutting\s+mechanis\w*|\btech\s+cutting\w*|\bhi\s+tech\s+cutting\w*|\bcut[t]?[i]?[n]?[g]?\s+material\w*|\bcut[t]?[i]?[n]?[g]?\s+metal\w*', text)
        match_2 = o(r'\bcutting\b|\bcut\b', text)
        match_3 = o(r'\bmetal\w*|\bpipe\b|\bpipeline\w*|\bpipelay\w*|\bpiping\w*|\bpipe\s+network\w*|\bmetal\w*|\bengineer\w*|\btechnician\w*|\belement\w*|\btechnique\w*|\bdevice\w*|\btool\w*|\bequipment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cutting technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drilling technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrilling\s+technologies\w*|\bdrilling\s+machinery\w*|\bdrilling\s+technology\w*|\bdrilling\s+mechanis\w*|\btech\s+drilling\w*|\bhi\s+tech\s+drilling\w*|\bknowledge\s+drilling\w*', text)
        match_2 = o(r'\bdrill\w*', text)
        match_3 = o(r'\btechnique\w*|\btechnolog\w*|\btechnician\w*|\belement\w*|\btechnique\w*|\bdevice\w*|\btool\w*|\bequipment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'drilling technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for decontamination techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdecontamination\w*|\bdecontaminate\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'decontamination techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for document management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdocument\s+control\w*|document\s+management\w*|document\s+schedule\w*|manag[e]?[i]?[n]?[g]?\s+document\w*|schedule\s+document\w*|maintain\s+document\w*|prepar[e]?[a]?[t]?[i]?[o]?[n]?\s+document\w*|provid[e]?[i]?[n]?[g]?\s+document\w*|control[l]?[i]?[n]?[g]?\s+document\w*|updat[e]?[i]?[n]?[g]?\s+document\w*|upload[i]?[n]?[g]?\s+document\w*|index[i]?[n]?[g]?\s+document\w*|\breview[i]?[n]?[g]?\s+document\w*|handl[e]?[i]?[n]?[g]?\s+document\w*|complete\s+document\w*|document[s]?\s+control\w*|fill[i]?[n]?[g]?\s+document\w*|documentation\s+control\w*|paperwork\s+control\w*|manag[e]?[i]?[n]?[g]?\s+paperwork\w*|paperwork\s+management\w*|handl[e]?[i]?[n]?[g]?\s+paperwork\w*|ensure\s+document\w*|ensure\s+paperwork\w*|documentation\s+compliance\w*|document\s+compliance\w*|management\s+document\w*|inspect\s+document\w*|inspect\s+paperwork\w*|master\s+document\w*|document[s]?[a]?[t]?[i]?[o]?[n]?\s+inspect\w*|paperwork\s+inspect\w*|qc\s+document\w*|qa\s+document\w*|qc\s+paperwork\w*|qa\s+paperwork\w*|project\s+document\w*|schedule\s+document\w*|tender[i]?[n]?[g]?\s+document\w*|supplier[s]?\s+document\w*|payment\s+document\w*|payment\s+paperwork\w*|document[a]?[t]?[i]?[o]?[n]?\s+payment\w*|writ[e]?[i]?[n]?[g]?\s+document\w*|document[a]?[t]?[i]?[o]?[n]?\s+project\w*|relevant\s+paperwork\w*|administration\s+record\w*|document[a]?[t]?[i]?[o]?[n]?\s+activity\w*|scope\s+work\s+document\w*|completing\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|handl[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|maintain\s+\w+\s+document\w*|maintain\s+\w+\s+paperwork\w*|complete\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+paperwork\w*|handl[e]?[i]?[n]?[g]?\s+\w+\s+paperwork\w*|ensure\s+\w+\s+document\w*|ensure\s+\w+\s+paperwork\w*|project\s+\w+\s+document\w*|writ[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|review[i]?[n]?[g]?\s+document\w*|prepar[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|manag[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|upload[i]?[n]?[g]?\s+\w+\s+document\w*|updat[e]?[i]?[n]?[g]?\s+\w+\s+document\w*|index[i]?[n]?[g]?\s+\w+\s+document\w*|maintain\s+\w+\s+document\w*|maintain\s+\w+\s+paperwork\w*|review[i]?[n]?[g]?\s+\w+\s+document\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'document management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maximo software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaximo\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'maximo software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for alfresco software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\balfresco\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'alfresco software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for technical terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+terminology\w*|\btechnical\s+expertise\w*|\btechnical\s+procedure\w*|\btechnical\s+input\w*|\btechnical\s+proposal\w*|\btechnical[l]?[y]?\s+proficien\w*|\btechnical\s+competence\w*|\btechnical\s+language\w*|\btechnical\s+lexicon\w*|\btechnical[l]?[y]?\s+competent\w*|\btechnical\s+lingo\w*|\btechnical\s+term\w*|\btechnical\s+terminology\w*|\btechnical\s+vocabulary\w*|\btechnical\s+information\w*|\btechnical\s+data\w*|\btechnical\s+datum\w*|\btechnical\s+raport\w*|\btechnical\s+document\w*|\btechnical\s+paperwork\w*|\btechnical\s+clarification\w*|\btechnical\s+concept\w*|\bexplain\s+technical\w*|\btechnical\s+report\w*|\btechnical\s+review\w*|\btechnical\s+leadership\w*|\btechnical\s+note\w*|\btechnical\s+knowledge\w*|\btechnical\s+manage\w*|\btechnical\s+design\w*|\btechnical\s+draw\w*|\btechnical\s+advice\w*|\btechnical\s+support\w*|\btechnical\s+project\w*|\btechnical\s+insight\w*|\btechnical\s+\w+\s+document\w*|\btechnical\s+\w+\s+experience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'technical terminology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rigging terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blifting\s+accessor\w*|\blift\s+accessor\w*|\blifting\s+equipment\w*|\blift\s+equipment\w*|\blifting\s+tool\w*|\blift\s+tool\w*|\brigging\w*|\bloler\b', text)
        match_2 = o(r'\brig\b|\brigging\b|\brigger\b', text)
        match_3 = o(r'\blift\w*|\bactivit\w*|\bterminolog\w*|\bexpertise\w*|\bknowledge\w*|\bexperience\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rigging terminology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for crane load charts
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrane\s+load\w*|\btower\s+crane\w*|\bmobile\s+crane\w*|\blift[i]?[n]?[g]?\s+load\w*', text)
        match_2 = o(r'\blift\w*|\brigging\w*|\brig\b|\bchart\w*', text)
        match_3 = o(r'\bcrane\b|\bcranes\b|\bload\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'crane load charts'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for construction methods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bengineering\s+construction\w*|\bengineer\s+construction\w*|\bconstruction\s+engineer\w*|\bconstruction\s+discipline\w*|\bconstruction\s+method\w*|\bconstruction\s+proces\w*|\bconstruction\s+system\w*|\bconstruction\s+procedure\w*|\bconstruction\s+method\w*|\bconstruction\s+approach\w*|\bconstruction\s+techniq\w*|\bconstruction\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+construct\w*|\bconstruction\s+experience\w*|\bconsultant\s+construction\w*|\bconstruction\s+consultant\w*|\bconsult[i]?[n]?[g]?\s+construction\w*|\bknowledge\s+construction\w*|\bconstruction\s+process\w*|\bshoring\w*', text)
        match_2 = o(r'\bconstruction\w*', text)
        match_3 = o(r'\bbuild\w*|\bstructure\w*|\bbridge\w*|\broad\w*|\bhighway\w*|\bmotorway\w*|\brail\w*|\bexcavation\w*|\bengineer\w*|\barchitect\w*|\bfabrication\s+manage\w*|\bfabrication\s+experience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'construction methods'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for construction industry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconstruction\s+practice\w*|\bconstruction\s+industry\w*|\bconsultant\s+construction\w*|\bconstruction\s+consultant\w*|\bconsult[i]?[n]?[g]?\s+construction\w*|\badvise\s+construction\w*|\badvice\s+construction\w*|\bconstruction\s+advice\w*|\bconstruction\s+advisor\w*|\bconstruction\s+adviser\w*|\bknowledge\s+construction\w*|\bconstruction\s+process\w*|\bconstruction\w+operation\w*', text)
        match_2 = o(r'\bconstruction(?! material)\w*|\brenovation\w*', text)
        match_3 = o(r'\bbuilder\w*|\bbuilt\w*|\bengineer\w*|\bfabricat\w*|\bconsult\w*|\badvise\b|\badvice\b|\badvisor\b|\badviser\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'construction industry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for meica commissioning standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmeica\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'meica commissioning standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for legal research
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blegal\s+research\w*|\banalyse\s+law[s]?\b|\banalyze\s+law[s]?\b|\blaw[s]?\s+analy\w*|\banalyse\s+regulation\w*|\bregulation\s+analy\w*|\banalyse\s+legislation\w*|\blegislation\s+analy\w*|\banalyze\s+regulation\w*|\banalyze\s+legislation\w*|\blegal\s+analy\w*|\bjudical\s+analy\w*|\bjudical\s+inquir\w*|\bjudical\s+investigat\w*|\bjudical\s+prob\w*|\blegal\s+inquir\w*|\blegal\s+investigat\w*|\blegal\s+prob\w*|\binvestigate\s+legal\w*|\binvestigate\s+law[s]?\b|\binvestigate\s+regulation\w*|\binvestigate\s+legislation\w*|\banalyse\s+policy\w*|\banalyze\s+policy\w*|\bpolicy\s+analy\w*|\bassess[i]?[n]?[g]?\s+policy\w*|\bassess[i]?[n]?[g]?\s+law[s]?\b|\bassess[i]?[n]?[g]?\s+regulation\w*|\bassess[i]?[n]?[g]?\s+legislation\w*|\bassess[i]?[n]?[g]?\s+policies\w*|\bpolicy\s+reserach\w*|\bresearch\s+policy\w*|\binvestigate\s+policy\w*|\bpolicy\s+investigat\w*|\banalyse\s+policies\w*|\banalyze\s+policies\w*|\bassess[i]?[n]?[g]?\s+policies\w*|\bpolicies\s+reserach\w*|\bresearch\s+policies\w*|\binvestigate\s+policies\w*|\bpolicies\s+investigat\w*|\bregulation\s+assessment\w*|\blegislation\s+assessment\w*|\blaw[s]?\s+assessment\w*|\bpolicy\s+assessmnet\w*|\bpolicies\s+assessment\w*', text)
        match_2 = o(r'\blegal\w*|\bpolicy\w*|\blegislation\w*|\bpolicy\w*', text)
        match_3 = o(r'\bresearch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'legal research'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gas contaminant removal process
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\s+decontamination\w*|\bgmru\b|\bmercury\s+remov\w*|\bgas\s+particle\s+filter\w*', text)
        match_2 = o(r'\bgas\b', text)
        match_3 = o(r'\bcontaminat\w*|\bcontaminant\w*|\bpollution\w*|\bdecontaminat\w*', text)
        match_4 = o(r'\bremove\w*|\bremoval\w*|\bcoalescer\w*|\bmanage\w*|\bmanaging\w*|\bdecontaminat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gas contaminant removal process'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for engineering procurement construction commissioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bepcc\b|\bepc\s+project\w*|\bengineering\s+procurement\s+construction\w*', text)
        match_2 = o(r'\bepc\b', text)
        match_3 = o(r'\bstandard\w*|\bcompliance\w*|\bregulation\w*|\bcomission\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'engineering procurement construction commissioning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oil rig legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boil\s+rig\w*|\boil\s+drill\w*|\boil\s+well\w*', text)
        match_2 = o(r'\benvironment[a]?[l]?\s+law\b|\benvironment[a]?[l]?\s+regulat\w*|\benvironment[a]?[l]?\s+laws\b|\benvironment[a]?[l]?\s+legislation\w*|\benvironment[a]?[l]?\s+guideline\w*|\blaw\b|\bregulation\w*|\blegislation\w*|\bguideline\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oil rig legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fuel gas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boxyacetylene\w*|\boxy\s+acetyle\w*|\boxy\s+-\s+acetyle\w*|\boxyhyrdo\w*|\boxy\s+hyrdog\w*|\boxy\s+-\s+hydrog\w*', text)
        match_2 = o(r'\bfuel\w*', text)
        match_3 = o(r'\bgas\w*|\boil\b|\bpetrol\b', text)
        match_4 = o(r'\bfuel(?! passion| ideas| idea)\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fuel gas'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for shale gas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bshale\b', text)
        match_2 = o(r'\bgas\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'shale gas'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for natural gas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnatural\s+gas\w*', text)
        match_2 = o(r'\bng\b', text)
        match_3 = o(r'\bgas\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'natural gas'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of polymers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpolymer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of polymers'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of fibre for polymer reinforcement
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcarbon\s+fibre\w*|\breinforce\s+polymer\w*|\bpolymer\s+reinforc\w*|\bplastic\s+fibre\w*|\breinforce\s+fibre\w*|\breinforcement\s+fibre\w*|\breinforcement\s+polymer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of fibre for polymer reinforcement'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laws on the transportation of dangerous goods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdangerous\s+good\w*|\bdanger\s+good\w*|(?<!non )(?<!non- )(?<!non - )(?<!non-)\bhazardous\s+good\w*|\bhazard\s+good\w*|\bdangerous\s+good[s]?\s+land\s+air\s+sea\w*', text)
        match_2 = o(r'\bregulat\w*|\bcompliance\w*|\blegislation\w*|\blaw[s]?\b|\bguideline\b', text)
        match_3 = o(r'\btransport\w*|\btransit\w*|\blogistic\w*|\bhaul\w*|\bdistribution\w*|\bstock\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laws on the transportation of dangerous goods'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for energy effciency
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+efficiency\w*|\benergy\s+waste\w*|\benergy\s+consumption\w*|\benergy\s+performance\w*|\besos\b|\bisoesos\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'energy effciency'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for property surveying
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproperty\s+survey\w*|\bhouse\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+property\w*|\bsurvey[i]?[n]?[g]?\s+house\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'property surveying'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for architectural theory
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barchitectural\s+technician\w*|\barchitectural\s+theory\w*|\barchitecture\s+theory\w*|\barchitecture\s+technician\w*', text)
        match_2 = o(r'(?<!data )(?<!datum )\barchitecture(?! data)(?! datum)\w*|(?<!data )(?<!datum )\barchitect(?<! data)(?<! datum)\w*', text)
        match_3 = o(r'\bbuild(?! career)\w*|\bhouse\w*|\bconstruction\w*|\brenovat\w*|\bdemoli\w*|\bstructure\w*|\btheory\w*|\bknowledge\w*|\bexperience\w*', text)
        match_4 = o(r'\bsecurity\s+architect\w*|\bdata\s+science\w*|\bdatum\s+scien\w*|\bdata\s+management\w*|\bdatum\s+management\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'architectural theory'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for architectural design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barchitectural\s+design\w*|\barchitectural\s+project\w*|\barchitecture\s+design\w*|\barchitecture\s+project\w*|\bbuild[i]?[n]?[g]?\s+design\w*|\bdesign[i]?[n]?[g]?\s+build(?! finance)\w*', text)
        match_2 = o(r'(?<!data )(?<!datum )\barchitecture(?! data)(?! datum)\w*|(?<!data )(?<!datum )\barchitect(?<! data)(?<! datum)\w*', text)
        match_3 = o(r'\bbuild(?! career)\w*|\bhouse\w*|\bconstruction\w*|\brenovat\w*|\bdemoli\w*|\bstructure\w*|\bdesign\w*|\bdesigner\w*|\bdesigning\w*|\bplan\w*', text)
        match_4 = o(r'\bsecurity\s+architect\w*|\bdata\s+science\w*|\bdatum\s+scien\w*|\bdata\s+management\w*|\bdatum\s+management\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'architectural design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for building codes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\build[i]?[n]?[g]?\s+home\w*|\bbuild[i]?[n]?[g]?\s+house\w*|\bbuilding\s+build\w*|\bbuild\s+build\w*|\bbuild[i]?[n]?[g]?\s+construction\w*|\bhouse\s+construct\w*|\bconstruct\s+house\w*|\bbuilding\s+warrant\w*|\bbuild\s+warrant\w*|\bbuild[i]?[n]?[g]?\s+code\w*|\bbuild[i]?[n]?[g]?\s+law\b|\bbuild[i]?[n]?[g]?\s+laws\b|\bbuild[i]?[n]?[g]?\s+regulat\w*|\bbuild[i]?[n]?[g]?\s+legislation\w*|\bbuild[i]?[n]?[g]?\s+rule\b|\bbuild[i]?[n]?[g]?\s+rules\b|\bconstruction\s+legislation\w*|\bbuild[i]?[n]?[g]?\s+standard\w*|\bconstruction\s+regulat\w*|\bconstruction\s+law[s]?\b|\bconstruction\s+rule\w*|\bconstruction\s+standard\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'building codes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maintenance operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaintenance\s+operation\w*|\bhandyman\b|\bmro\b|\bmaintenance\s+task\w*|\bstandard\s+maintenance\w*|\bmaintenance\s+experience\w*|\bmaintenance\s+expert\w*|\bmaintenance\s+special\w*|\bengineering\s+maintenance\w*|\bplanned\w+maintenance\w*|\bmaintenance\s+engineer\w*|\bmechanical\s+craftsperson\w*|\bservicing\s+work\w*|\bservice\s+work\w*|\binstallation\s+servic\w*|\bservice\s+engineer\b|\bmaintain\s+customer[s]?\s+equipment\w*|\bmaintain\s+client[s]?\s+equipment\w*|\bmaintain\s+service\w*|\bmaintenance\s+service\w*|\bequipment\s+maintenance\w*|\bmaintenance\s+technician\w*|\bmaintenance\s+activity\w*|\bexecute\s+maintenance\w*|\bsupport\s+maintenance\w*|\bbuilding\s+fabric\w*|\binstrument\s+uptime\s+goal\w*|\bmean\s+time\s+repair\w*|\bmttr\b', text)
        match_2 = o(r'\bmaintenance\w*', text)
        match_3 = o(r'\brepair\w*|\bfix\b|\bfixing\b|\bbuild(?! relationship)\w*|\brenovat\w*|\bdemoli\w*|\burbishment\w*|\bdiagnos\w*|\bcondition\w*|\breliability\w*|\bequipment\w*|\binstrument\w*|\bmonitor\w*|\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'maintenance operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control panel components
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperate[e]?[i]?[n]?[g]?\s+control\s+panel\w*|\bus[e]?[i]?[n]?[g]?\s+control\s+panel\w*|\brun[n]?[i]?[n]?[g]\s+control\s+panel\w*|\bcontrol[l]?[i]?[n]?[g]?\s+panel\s+experience\w*|\bexperience\s+control\s+panel\w*|\bcontrol\s+panel\s+operat\w*|\bcontrol\s+panel\s+experience\w*|\bcontrol\s+panel\s+use\w*|\bcontrol\s+panel\s+run\w*|\bcontrol\s+panel\s+experience\w*|\bcontrol\s+panel\s+knowledge\w*|\bexperience\s+control\s+panel\w*|\bknowledge\s+control\s+panel\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'control panel components'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydrogeology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrogeology\b|\bhydrogeological\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydrogeology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emergency shutdown systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besd\b|\bemergency\s+shutdown\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emergency shutdown systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for water reuse
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brain\s+water\s+harvest\w*|\bharvest\s+rain\s+water\w*|\bwater\s+reuse\w*|\bre\s+-\s+use\s+water\w*|\bwater\s+-\s+reuse\w*|\bwater\s+retaining\w*|\bwater\s+retention\w*', text)
        match_2 = o(r'\battenuation\w*', text)
        match_3 = o(r'\bwater\w*|\bwastewater\w*|\bwaste\s+water\w*|\breservoir\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'water reuse'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for qhse policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqhse\b|\bquality\s+health\s+safety\s+environment\w*|\bsheq\b|\bsafety\s+health\s+environment\s+quality\w*|\bhseq\b|\bhealth\s+safety\s+environment\s+quality\w*|\bqhsse\b|\bquality\s+health\s+safety\s+security\s+environment\w*|\benvironment[a]?[l]?\s+hse\w*|\behs\b|\benvironment[a]?[l]?\s+health\s+safety\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'qhse policies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydroelectricity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydro\s+power\w*|\bhydro\s+electri\w*|\bhydroelectric\w*|\bhydro\s+-\s+electri\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydroelectricity'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for power plant equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boperate\s+power\s+plant\w*|\bpower\s+plant\s+operat\w*|\bpower\s+plant\s+control\w*|\belectric[a]?[l]?\s+plant\s+control\w*|\belectric[a]?[l]?\s+plant\s+operat\w*|\boperate\s+electric[a]?[l]?\s+plant\w*|\belectric[a]?[l]?\s+plant\s+item\w*|\bpower\s+plant\s+item\w*', text)
        match_2 = o(r'\bpower\w*|\belectric\w*', text)
        match_3 = o(r'\bplant\w*', text)
        match_4 = o(r'\bequipment\w*|\btool\w*|\binstrument\w*|\bmachine\w*|\bitem[s]?\b|\bdevice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'power plant equipment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manufacturing of steam generators
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsteam\s+turbine\w*|\bsteam\s+generation\w*|\bsteam\s+generator\w*|\bsteam\s+\w+\s+generat\w*|\bsteam\s+\w+\s+turbine\w*', text)
        match_2 = o(r'\bproduct\w*|\bmanufactur\w*|\bfabricat\w*|\bassemb\w*|\bcreate\w*|\bdesign\w*|\bdesigning\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'manufacturing of steam generators'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biomas conversion
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+from\s+waste\w*|\bbiomass\s+conversion\w*|\bprocess[i]?[n]?[g]?\s+biomass\w*|\bbiomass\s+process\w*', text)
        match_2 = o(r'\bbiomass\w*', text)
        match_3 = o(r'\bconvert\w*|\bconverstion\w*|\btransform\w*|\benergy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biomas conversion'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for integrity management principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegrity\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+integrity\w*|\bproject\s+integrity\w*|\bintegrity\s+project\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'integrity management principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for flow assurance and other well services and operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bflow\s+assurance\w*|\bwell\s+completion\w*|\bassure\s+flow\w*|\bensure\s+flow\w*|\bfluid\s+intervention\w*|\bartificial\s+lift\w*|\bwell\s+logging\w*|\bwell\s+stimulation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'flow assurance and other well services and operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cost value reconciliation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcost\s+value\s+report\w*|\bcost\s+value\s+analy\w*|\banalyse\s+cost\s+value\w*|\banalyze\s+cost\s+value\w*|\bassess\s+cost\s+value\w*|\bcost\s+value\s+assess\w*|\bcost\s+value\s+reconcil\w*|\bcvr\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cost value reconciliation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electric generators
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpower\s+generator\w*|\belectric\s+generator\w*|\belectrical\s+generator\w*|\belectricity\s+generator\w*|\bmotor\s+generator\w*', text)
        match_2 = o(r'\belectric\w*', text)
        match_3 = o(r'\bgenerator\w*', text)
        match_4 = o(r'(?<!marine )\baggregat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4:
            matched_fragments.extend([match_2, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electric generators'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tree felling methods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfelling\w*|\bfell\s+tree\w*|\bcut\s+tree\w*|\blogging\b|\bfell\s+trees\w*|\bchop\s+down\s+tree\w*|\bchopping\s+down\s+tree\w*|\bcut\s+down\s+tree\w*|\bcutting\s+down\s+tree\w*|\bchop\s+tree\w*|\bchopping\s+tree\w*|\bfell\s+tree\w*|\btree[s]?\s+fell\w*|\bremove\s+tree\w*|\btree[s]?\s+remov\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tree felling methods'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for telemetry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btelemetry\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'telemetry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer interfaces
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer[s?]\s+interface\w*|\bclient[s]?\s+interface\w*|\bstakeholder[s]?\s+interface\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer interfaces'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for type of tyres
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmet\s+technician\w*|\bvehicle\s+technician\w*|\bmechanic\w*|\bmot\b|\bservice\w*|\brepair\w*|\bfix\b|\bfixing\w*|\bcar\b|\btruck\b', text)
        match_2 = o(r'\btyre\b|\btyres\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'type of tyres'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cost metrics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcost[s]?\b', text)
        match_2 = o(r'\bmetric\w*|\bparameter\w*|\bkpi\b|\bmeasure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cost metrics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for abe fermentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\babe\s+ferment\w*|\babe\s+process\w*', text)
        match_2 = o(r'\bacetone\b', text)
        match_3 = o(r'\bbutanol\b', text)
        match_4 = o(r'\bethanol\b', text)
        match_5 = o(r'\bferment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4 and match_5:
            matched_fragments.extend([match_2, match_3, match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'abe fermentation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for distillation techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\b|\bdistil\b|\bdistilator\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'distillation techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for crude oil distillation processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdistillation\w*|\bdistil\b|\bdistilator\b', text)
        match_2 = o(r'\bcdu\b|\bcrude\b|\boil\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'crude oil distillation processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for scada system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscada\b|\bsupervisory\s+control\s+data\s+acquisition\w*|\bsupervision\s+control\s+datum\s+acqui\w*|\bsupervisory\s+control\s+datum\s+acqui\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'scada system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oil and gas upstream lifecycle processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bup\s+stream\s+oil\w*|\bupstream\s+oil\w*|\bup\s+stream\s+gas\w*|\bupstream\s+gas\w*|\bupstream\s+engineer\w*', text)
        match_2 = o(r'\bup\s+stream\s+life\s+cycle\w*|\bup\s+stream\s+lifecycle\w*|\bupstream\s+life\s+cycle\w*|\bupstream\s+lifecycle\w*', text)
        match_3 = o(r'\boil\w*|\bgas\w*|\bcrude\w*|\boffshore\w*|\bonshore\w*', text)
        match_4 = o(r'\bupstream\b|\bup\s+stream\b|\bup\s?-\s?stream\b', text)
        match_5 = o(r'\boil\w*', text)
        match_6 = o(r'\bgas\w*', text)
        match_7 = o(r'\bbusiness\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        elif match_4 and match_7:
            matched_fragments.extend([match_4, match_7])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oil and gas upstream lifecycle processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oil and gas midstream lifecycle processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmid\s+stream\s+oil\w*|\bmidstream\s+oil\w*|\bup\s+stream\s+gas\w*|\bmidstream\s+gas\w*|\bmidstream\s+engineer\w*', text)
        match_2 = o(r'\bmid\s+stream\s+life\s+cycle\w*|\bmid\s+stream\s+lifecycle\w*|\bmidstream\s+life\s+cycle\w*|\bmidstream\s+lifecycle\w*', text)
        match_3 = o(r'\boil\w*|\bgas\w*|\bcrude\w*|\boffshore\w*|\bonshore\w*', text)
        match_4 = o(r'\bmidstream\b|\bmid\s+stream\b|\bmid\s?-\?stream\b', text)
        match_5 = o(r'\boil\w*', text)
        match_6 = o(r'\bgas\w*', text)
        match_7 = o(r'\bbusiness\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        elif match_4 and match_7:
            matched_fragments.extend([match_4, match_7])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oil and gas midstream lifecycle processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oil and gas downstream lifecycle processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdown\s+stream\s+oil\w*|\bdownstream\s+oil\w*|\bdown\s+stream\s+gas\w*|\bdownstream\s+gas\w*|\bdownstream\s+engineer\w*', text)
        match_2 = o(r'\bdown\s+stream\s+life\s+cycle\w*|\bdown\s+stream\s+lifecycle\w*|\bdownstream\s+life\s+cycle\w*|\bdownstream\s+lifecycle\w*', text)
        match_3 = o(r'\boil\w*|\bgas\w*|\bcrude\w*|\boffshore\w*|\bonshore\w*', text)
        match_4 = o(r'\bdownstream\b|\bdown\s+stream\b|\bdown\s?-\s?stream\b', text)
        match_5 = o(r'\boil\w*', text)
        match_6 = o(r'\bgas\w*', text)
        match_7 = o(r'\bbusiness\s+analy\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        elif match_4 and match_7:
            matched_fragments.extend([match_4, match_7])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oil and gas downstream lifecycle processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ethylene gas
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bethylene\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ethylene gas'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cartography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcartography\w*|\bcartographic\w*|\bcartographer\w*|\bmapmaker\w*|\bmak[e]?[i]?[n]?[g]\s+map[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cartography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geodesy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeodesy\w*|\bgeodetic\w*|\bgeodeist\b|\bsurvey[i]?[n]?[g]?\s+position\w*|\bposition[i]?[n]?[g]?\s+survey\w*', text)
        match_2 = o(r'\bgeodesy\w*|\bgeodetic\w*|\bgeodesist\w*', text)
        match_3 = o(r'\bsp\b|\bconstruction\s+survey\w*|\bsurvey[i]?[n]?[g]?\s+construction\w*|\bbuild[i]?[n]?[g]?\s+survey\w*|\bbuilt\s+survey\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geodesy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gematics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeomatic\w*|\bstudy[i]?[n]?[g]?\s+earth\s+surface\w*|\bexamin[e]?[i]?[n]?[g]?\s+earth\s+surface\w*|\bearth\s+surface\s+study\w*|\bearth\s+surface\s+examin\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gematics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geometry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeometric\w*|(?<!road )\bgeometry\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geometry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydrography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrography\w*|\bhydrographic\w*|\bhydrographer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydrography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for bathymetry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbathymetry\w*|\bbathymetric\w*', text)
        match_2 = o(r'\bbed[s]?\b|\bfloor[s]?\b', text)
        match_3 = o(r'\bwater\w*|\bmarine\w*|\bsea\w*|\bsubsea\w*|\bmaritime\w*|\bocean\w*|\briver\w*|\bstream\w*|\blake\w*', text)
        match_4 = o(r'\bstudy\w*|\bsurvey\w*|\bresearch\w*|\bsample\w*|\btest\w*|\bassess\w*|\banaly\w*|\bmonitor\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'bathymetry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hpht wells
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhpht\b', text)
        match_2 = o(r'\bwell\b|\bwells\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hpht wells'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputer\s+engineer\w*|\bcomputer\s+hardware\w*|\bcomputer\s+system\s+validat\w*|\bcomputer\s+software\s+validat\w*|\bengineer[i]?[n]?[g]?\s+computer\w*|integrat[e]?[i]?[n]?[g]?\s+hardware\s+software\w*|\bintegration\s+hardware\s+software\w*|\bintegrat[e]?[i]?[n]?[g]?\s+software\s+hardware\w*|\bintegration\s+software\s+hardware\w*|\bhardware\s+software\s+integrat\w*|\bsoftware\s+hardware\s+integrat\w*', text)
        match_2 = o(r'\bsoftware\w*', text)
        match_3 = o(r'\bhardware\w*', text)
        match_4 = o(r'\bengineer\w*|\bdeveloper\w*', text)
        match_5 = o(r'\bcomputer\w*', text)
        match_6 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        elif match_5 and match_6:
            matched_fragments.extend([match_5, match_6])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer programming
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bability\s+cod[e]?[i]?[n]?[g]?\w*|\bable\s+cod[e]?[i]?[n]?[g]?\w*|\bability\s+programming\w*|\bcod[e]?[i]?[n]?[g]?\s+ability\w*|\bcod[e]?[i]?[n]?[g]?\s+experience\w*|\bprogramming\s+experience\w*|\bexperience\s+cod[e]?[i]?[n]?[g]?\s+experience\s+programming\w*', text)
        match_2 = o(r'\bpython\w*|\bjava\b|\bnodejs\w*|\bnode\s+js\b|\bnode\.js\w*|\bnode\s+\.\s+js\w*|\bphp\b|\bajax\b|\bcss\b|\bcascad[e]?[i]?[n]?[g]?\s+style\s+sheet[s]?\b|\breactjs\b|\breact\s+js\b|\bc\s?#\w*|\bc\s?sharp\w*|\bc#\b', text)
        match_3 = o(r'\bsoftware\w*|\bexperience\w*|\bknowledge\w*|\bdevelop\w*|\bdesign\w*|\bfamiliar\w*|\bproficien\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer programming'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoftware\s+develop\w*|\bsoftware\s+engineer\w*|\bdevelop\s+software(?! draw)\w*|\bcreate\s+software(?! draw)\w*|\bengineer[i]?[n]?[g]?\s+software\w*|\bdevelop\s+interface\w*|\bdevelop\s+it\s+interface\w*', text)
        match_2 = o(r'\bsoftware\w*|\bprogramming\b|\bprogrammer\w*', text)
        match_3 = o(r'\bengineer\w*|\bdeveloper\w*|\bdevelopment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for application engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapp[s]?\s+develop\w*|\bapp[s]?\s+engineer\w*|\bapplication\s+develop\w*|\bapplication\s+engineer\w*|\bdevelop\s+app[l]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\b|\bcreate\s+app[l]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\b|\bengineer[i]?[n]?[g]?\s+app[l]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\b', text)
        match_2 = o(r'\bapplication\w*|\bapp\b', text)
        match_3 = o(r'\bdesign\w*|\bdevelop\w*|\bcreate\w*|\bprogramming\w*|\bcoding\w*|\bcode\b', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'application engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software ui design patterns
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+design\w*', text)
        match_2 = o(r'\binterface\w*|\bprogramming\w*|\bcode\b|\bsoftware\w*|\bweb\b|\bapplication\b|\bapp\b|\bcreate\w*|\bdevelop\w*|\bcoding\w*', text)
        match_3 = o(r'\bui\b|\buser\s+interface\w*', text)
        match_4 = o(r'\binterface\w*|\bprogramming\w*|\bcode\b|\bsoftware\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+software\w*|\bweb\s+develop\w*|\bwebsite\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+website\w*|\bapp\s+develop\w*|\bapplication\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+app\w*|\bcoding\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software ui design patterns'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for usability engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\busability\s+engineering\w*|\busability\s+accessability\s+standard\w*|\busability\s+standard\w*', text)
        match_2 = o(r'\busability\w*|\baccessability\w*', text)
        match_3 = o(r'\bui\b|\buser\s+interface\w*|\bdigital\s+design\w*|\bsoftware\s+engineer\w*|\bsoftware\s+develop\w*|\bapplication\s+engineer\w*|\bapplication\s+design\w*|\bsoftware\s+design\w*|\bdesign\s+app\w*|\bdesign\s+software\w*|\bcoding\w*|\bcode\b|\bweb\s+develop\w*|\bwebsite\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+website\w*|\bapp\s+develop\w*|\bapplication\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+app\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'usability engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for android operating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+design\w*|\bui\b|\buser\s+interface\w*|\bsoftware\s+engineer\w*|\bapplication\s+engineer\w*|\bapplication\s+design\w*|\bsoftware\s+design\w*|\bdesign\s+app\w*|\bdesign\s+software\w*|\bsystem\w*|\bapplication\w*|\bapp\b', text)
        match_2 = o(r'\bandroid\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'android operating system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ios operating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+design\w*|\bui\b|\buser\s+interface\w*|\bsoftware\s+engineer\w*|\bapplication\s+engineer\w*|\bapplication\s+design\w*|\bsoftware\s+design\w*|\bdesign\s+app\w*|\bdesign\s+software\w*|\bsystem\w*|\bapplication\w*|\bapp\b', text)
        match_2 = o(r'\bios\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ios operating system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for innovation processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!lead )\binnovation(?! embrace)\w*|\binnovative(?! oil|  oil| pharmaceutical)\w*|\bmodernization\w*|\bmodernisation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'innovation processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for areobic composting processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bareobic\s+compost\w*|\bcompost[i]?[n]?[g]?\s+areobic\w*', text)
        match_2 = o(r'\bcompost\w*', text)
        match_3 = o(r'\bareobic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'areobic composting processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for personnel management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpersonnel\s+logistic\w*|\bhuman\s+resource[s]?\s+administrator\w*|\bpersonnel\s+admin\w*|\bpersonnel\s+management\w*|\bpersonnel\s+oversight\w*|\bhr\s+administrator\b|\bpersonnel\s+supervis\w*|\bhuman\s+resource[s]?\s+oversight\w*|\bpersonnel\s+regulation\w*|\bhuman\s+resource[s]?\s+regulation\w*|\bhuman\s+resource[s]?\s+supervis\w*|\bhr\s+regulation\w*|\bhr\s+supervis\w*|\bhr\s+oversight\w*|\bmanag[e]?[i]?[n]?[g]?\s+personnel\w*|\bemployee[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+employee\w*|\bmanag[e]?[i]?[n]?[g]?\s+personnel\w*|\bhir[e]?[i]?[n]?[g]?\s+manage\w*|\bpersonnnel\s+opeartion\w*|\bpersonnel\s+resources\w*|\boperation[s]?\s+personnel\w*', text)
        match_2 = o(r'\bhuman\s+resources\w*|\bhr\b', text)
        match_3 = o(r'(?<!look )(?<!looking )(?<!seek )(?<!seeking )\brecruit[m]?[e]?[n]?[t]?\b(?! agenc[y]?[i]?[e]?[s]?|  agenc[y]?[i]?[e]?[s]?)\w*', text)
        match_4 = o(r'\brecruiting\b(?! agenc[y]?[i]?[e]?[s]?|  agenc[y]?[i]?[e]?[s]?)\w*', text)
        match_5 = o(r'\bhiring\w*|payroll\w*', text)
        match_6 = o(r'\bemployee\w*|\bpersonnel\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_6:
            matched_fragments.extend([match_2, match_3, match_6])
        elif match_2 and match_4 and match_6:
            matched_fragments.extend([match_2, match_4, match_6])
        elif match_2 and match_5 and match_6:
            matched_fragments.extend([match_2, match_5, match_6])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'personnel management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mechanical design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmechanical\s+design\w*|\bmechanical\s+project\w*|\bmachine\s+design\w*|\bmachine\s+project\w*', text)
        match_2 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b', text)
        match_3 = o(r'\bmachine(?! learn)\w*|\bmechanical\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mechanical design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rubber technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\belastomer[s]?\b|\brubber[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rubber technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for industrial tools
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bindustrial\s+tool\w*|\bindustrial\s+apparatu\w*|\bindustrial\s+mechanism\w*|\bindustrial\s+device\w*|\bindustrial\s+appliance\w*|\bfactory\s+made\s+tool\w*|\bfactory\s+made\s+apparatu\w*|\bfactory\s+made\s+appliance\w*|\bfactory\s+made\s+mechanism\w*|\bfactory\s+made\s+device\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'industrial tools'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for visualsoft software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvisualsoft\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'visualsoft software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for energy market
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+market\w*|\bpower\s+market\w*|\benergy\s+project\w*|\benergy\s+transition\w*|\benergy\s+industry\w*', text)
        match_2 = o(r'\beirgrid\b|\besb\b|\besbi\b', text)
        match_3 = o(r'\benergy\w*|\belectric\w*|\bpower\b', text)
        match_4 = o(r'\bgas\b|\boil\b|\benergy\b', text)
        match_5 = o(r'\bindustr\w*|\bmarket\w*|\bsector\w*|\btrade\b', text)
        match_6 = o(r'\bunderstand\w*|\bexpertise\w*|\bknowledge\w*|\bexperience\w*|\banaly\w*|\bassess\w*|\bmanage\b|\bmanaging\b|\bmanagement\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5 and match_6:
            matched_fragments.extend([match_4, match_5, match_6])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'energy market'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for north sea subsea decommissioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnssd\b|\bnorth\s+sea\s+transition\s+authority\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'north sea subsea decommissioning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for decommissioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdecommission\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'decommissioning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for primavera software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprimavera\b|\bp6\s+plan\s+tool\w*', text)
        match_2 = o(r'\bp6\w*', text)
        match_3 = o(r'\bschedule\w*|\bsoftware\w*|\btool\w*|\bplan\w*|\bexpeirence\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'primavera software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of corrosion
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brust\b|\brusty\b|\bcorroded\w*|\bcorrosion\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of corrosion'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for system intervention strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsystem\w*', text)
        match_2 = o(r'\bintervention\w*|\bintervene\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'system intervention strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for systems engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsystem[s]?\s+engineer\w*|\bsystem[s]?\s+configuration\w*|\bengineer[i]?[n]?[g]?\s+system\w*|\bsystem[s]?\s+architecture\w*|\barchitecture\s+system\w*|\bsystem\s+design\w*|\bdesign[i]?[n]?[g]?\s+system\w*|\bconfigur[e]?[a]?[t]?[i]?[o]?[n]?\s+system\w*|\bsystem\s+architecture\w*|\bengineer[i]?[n]?[g]?\s+\w+\s+system\w*|\barchitecture\s+\s+\w+system\w*|\bdesign[i]?[n]?[g]?\s+system\w*|\bcomputer\s+system\s+validation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'systems engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lightwell intervention systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blightwell\s+intervention\w*|\blight\s+well\s+intervention\w*|\blightwell\s+system\w*|\blight\s+well\s+system\w*', text)
        match_2 = o(r'\blwi\b', text)
        match_3 = o(r'\bmarine\w*|\bsubsea\w*|\boffshore\w*|\bwell\b|\bvessel\b|\bmaritime\b|\brig\b|\bintervention\w*|\bintervene\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'lightwell intervention systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for machinery load capacity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcheck[i]?[n]?[g]?s+load\w*|\bdetermin[e]?[i]?[n]?[g]?\s+load\w*|\bload[i]?[n]?[g]?\s+capacity\w*|\bmaximum\s+load\w*|\bpassive\s+load\w*|\bmachine\s+load\w*|\bmachinery\s+load\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'machinery load capacity'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ocean bottom node acquisition
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bobn\b|\bocean\s+bottom\s+node\w*|\bocean\s?-\s?bottom\s+node\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ocean bottom node acquisition'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geophysics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeophysicist\w*|\bgeophysic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geophysics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for petrophysics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpetrophysic\w*|\bpetrophysicist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'petrophysics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft project software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrosoft\s+project\w*|\bms\s+project\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft project software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geographic information systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeograph[y]?[i]?[c]?\s+information\s+system\w*|\bgis\s+datum\w*|\bgis\s+data\b|\bgis\s+consult\w*|\bgis\s+advis\w*|\bgis\s+advice\w*|\bconsult[i]?[n]?[g]?\s+gis\b|\badise\s+gis\b', text)
        match_2 = o(r'\bgis\b', text)
        match_3 = o(r'\bearth\w*|\bseismic\w*|\bgeograph\w*|\bgeolog\w*|\binformation\w*|\bsystem\w*|\bsoftware\w*|\bdatabase\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geographic information systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for machine learning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bml\b', text)
        match_2 = o(r'\bdata\b|\bdatum\w*|\banaly\w*|\bassess\w*|\bexperience\w*|\bcoding\w*|\bprogram\w*|\bcode\b|\bdevelop\w*|\bsoftware\w*|\bmodel\w*', text)
        match_3 = o(r'\bmachine\s+learn\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'machine learning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for military engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmilitary(?! service)\w*', text)
        match_2 = o(r'\bengineer\w*|\bmaintain\w*|\bmaintenance\w*|\bfix\b|\bfixing\b|\brepair\w*|\bequipment\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'military engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for raychem plug-in termination
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brpit\b|\brpits\b|\braychem\s+plug\w*', text)
        match_2 = o(r'\bpipeline\w*|\bpipelay\w*|\bpipe\b|\bpiping\b|\bexperience\w*|\bdevice\w*|\buse\w*|\bknowledge\w*|\bterminat\w*|\bprocess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'raychem plug-in termination'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for product structure modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduct\s+strucutring\w*|\bproduct\s+structure\s+model\w*|\bbill\s+material\w*|\bproduct\s+structure\w*', text)
        match_2 = o(r'\bbom\b', text)
        match_3 = o(r'\bmaterial\w*|\bproduct\w*|\bstructure\w*|\bstructuring\w*|\bprepare\w*|\bmanage\w*|\bmaintain\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'product structure modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hvr software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhvr\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hvr software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control environment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcontrol\s+environment\w*', text)
        match_2 = o(r'\bcoso\b', text)
        match_3 = o(r'\benvironment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'control environment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business integration strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegration\s+business\w*|\bbusiness\s+integrat\w*|\bintegrat[e]?[i]?[n]?[g]?\s+business\w*|\bintegration\s+\w+\s+business\w*|\bintegrat[e]?[i]?[n]?[g]?\s+\w+\s+business\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business integration strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for network communication
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnetwork\s+communication\w*|\bcommunication\s+network\w*|\binternetworking\b', text)
        match_2 = o(r'\bsoftware\w*|\bprogramming\w*|\bsystem[s]?\b|\buse\w*|\bcommunicate\w*|\bmanage\w*|\bexperience\w*|\bknowledge\w*|\bmonitor\w*|\bmanaging\w*|\bensure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'network communication'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for prerequisite programs procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprerequisite\s+program[s]?\s+procedure\w*', text)
        match_2 = o(r'\bprps\b|\bprp\b', text)
        match_3 = o(r'\bfsm\b|\bfsms\b|\bknowledge\w*|\bexperience\w*|\bprocess\w*|\bprocedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'prerequisite programs procedures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for insurance law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binsurance\s+claim\w*|\binsurance\s+law\w*|\bcommercial\s+insurance\w*|\binsurance\s+contract\w*|\binsurance\s+regulation\w*|\blegal\s+insurance\w*|\binsurance\s+issue\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'insurance law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ev charging technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bev\s+charge\w*|\belectric\s+vehicle\s+charge\w*|\bbp\s+pulse\w*', text)
        match_2 = o(r'\bev\b|\belectric\s+vehicle\w*', text)
        match_3 = o(r'\bcharger\w*|\bcharging\s+station\w*|\bcharge\s+station\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ev charging technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for technical drawing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+draw\w*|\bdefect\s+draw\w*|\bdraw[i]?[n]?g?\s+technical\w*|\btechnical\s+diagram\w*|\bscientific\s+draw\w*|\bschematic\s+draw\w*|\bspecialised\s+draw\w*|\bcad\s+draw\w*|\bmechanical\s+draw\w*|\bred\s+-\s+lin[e]?[i]?[n]?[g]?\s+draw\w*|\bred\s+lin[e]?[i]?[n]?[g]?\s+draw\w*|\bred-lin[e]?[i]?[n]?[g]?\s+draw\w*|\bengineer[i]?[n]?[g]?\s+draw\w*|\bstructural\s+draw\w*|\bdraughting\w*|\bisolation\s+diagram\w*|\bdraw[i]?[n]?[g]?\s+bim\w*|\bconstruction\s+draw\w*|\bvnetilation\s+flow\s+diagram\w*|\bpid\s+draw\w*|\bpid\s+diagram\w*|\bpfd\s+draw\w*|\bpfd\s+diagram\w*|\bpfs\s+draw\w*|\bpfs\s+diagram\w*|\bpefs\s+draw\w*|\bpefs\s+diagram\w*|\bpid\s+line\s+diagram\w*|\bprocess\s+flow\s+diagram\w*|\bpid\s+\w+\s+draw\w*|\bpid\s+\w+\s+diagram\w*|\bpfd\s+\w+\s+draw\w*|\bpfd\s+\w+\s+diagram\w*|\bpfs\s+\w+\s+draw\w*|\bpfs\s+\w+\s+diagram\w*|\bpefs\s+\w+\s+draw\w*|\bpefs\s+\w+\s+diagram\w*|\bpdb[s]?\s+\w+\s+draw\w*|\bpdb[s]?\s+\w+\s+diagram\w*|\bdraw[i]?[n]?[g]?\s+\w+\s+technical\w*', text)
        match_2 = o(r'\bpid[s]?\b|\bpefs\b|\bpfd[s]?\b|\bpfs\b', text)
        match_3 = o(r'\bcad\w*|\bengineer\w*|\bdesign\w*|\bdrawing\w*|\bdraw\w*|\bprepare\w*|\breport\w*|\bdocument\w*|\bschema\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'technical drawing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data governance practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+governance\w*|\bgovernance\s+data\b|\bgovernance\s+datum\b|\bdata\s+accessibility\w*|\bdatum\s+governance\w*|\bdatum\s+accessibility\w*|\baccessibility\s+data\b|\baccessibility\s+datum\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data governance practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for internet governance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bweb\s+accessibility\w*|\binternet\s+governance\w*|\bweb\s+governance\w*|\bgovernance\s+web\b|\bgovernance\s+webiste\w*|\bgovernance\s+internet\w*|\bwebsite\s+accessibility\w*|\binternet\s+accessibility\w*|\baccessibility\s+internet\w*|\baccessibility\s+website\w*|\baccessibility\s+web\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'internet governance'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for food storage
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bshop\w*|\bstore\w*|\bretail\w*|\bwholesale\w*|\bbutcher\w*|\bstorage\w*', text)
        match_2 = o(r'\bfood\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'food storage'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emission standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemission[s]?\s+standard\w*|\bemission[s]?\s+reduction\w*|\bemission[s]?\s+law\w*|\bemission[s]?\s+legislation\w*|\bemission[s]?\s+regulation\w*|\bemission[s]?\s+guideline\w*|\bemission[s]?\s+requirement\w*|\bemission[s]?\s+compliance\w*|\beuets\b|\beu\s+ets\b|\benergy\s+emission\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+protocol\w*|\bghg\b|\bgreenhouse\s+gas[s]?[e]?[s]?\s+regulation\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+legislation\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+standard\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+law\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+guideline\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+\w+\s+standard\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+\w+\s+regulation\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+\w+\s+legislation\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+\w+\s+guideline\w*|\bgreenhouse\s+gas[s]?[e]?[s]?\s+\w+\s+law\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emission standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sustainable finance (esg)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besg\b|\bsustainable\s+finance\w*|\bsustainable\s+investment\w*|\bsustainable\s+business\w*|\bresponsible\s+finance\w*|\bresponsible\s+business\w*|\bresponsible\s+inestment\w*|\benvironment[a]?[l]?\s+social\s+governance\w*|\benvironment[a]?[l]?\s+social\s+corporate\s+governance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sustainable finance (esg)'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fabric types
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfabric\s+maintenance\w*|\btype[s]?\s+fabric\w*|\bfabric\s+type\w*|\bbrand[s]?\s+fabric\w*|\bcategor[y]?[i]?[e]?[s]?\s+fabric\w*|\bvariet[y]?[i]?[e]?[s]?\s+fabric\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fabric types'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ergonomics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bergonomics\w*|\bergonomy\w*|\bergonomical\w*', text)
        match_2 = o(r'\bhuman\s+factor\w*|\bhuman\s+performance\w*', text)
        match_3 = o(r'\bhealth\s+safety\w*|\bhs\b|\bhseq\b|\bhse\b|\bhsec\b|\bhsse\b|\bhesa\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ergonomics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for iterative development
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\biterative\s+development\w*|\biterative\s+\w+\s+development\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'iterative development'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for refining processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brefining\w*|\binery\w*|\brafinery\w*|\brefinery\w*', text)
        match_2 = o(r'\boil\w*|\bchemical\w*|\bgas\w*|\boilgas\w*|\bgasoil\w*|\bshale\w*|\bchemic\w*|\bprocess\w*|\bprocedure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'refining processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for risk-based inspection process
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brisk\s?[-]?\s?based\s+inspect\w*', text)
        match_2 = o(r'\brbi\b', text)
        match_3 = o(r'\binspect\w*|\bmaintain\w*|\bmaintenance\w*|\brisk[s]?\b|\bassess\w*|\bsafety\w*|\bexperience\w*|\bknowledge\w*|\bprocess\w*|\bprocedure\w*|\bperform\w*|\bassess\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'risk-based inspection process'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for root cause analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\broot\s+cause\s+analy\w*', text)
        match_2 = o(r'\broot\s+cause\w*|\brcfa\b|\brca\b', text)
        match_3 = o(r'\bmanufactur\w*|\bindustry\w*|\bequipment\w*|\bplant\w*|\bfabricat\w*|\bfault\w*|\binspect\w*|\bmachine\w*|\bmaintenance\w*|\bmaintain\w*|\brepair\w*|\brisk\w*|\bassess\w*|\banaly\w*|\bperform\w*|\bidentify\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'root cause analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for delay analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdelay\b', text)
        match_2 = o(r'\banaly\w*|\binvestigat\w*|\bmonitor\w*|\bmanage\w*|\bmanaging\w*|\bassess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'delay analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data quality assessment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\w*|\bdatum\w*', text)
        match_2 = o(r'\bquality\w*|\bqc\b|\bqa\b', text)
        match_3 = o(r'\bassess\w*|\bensure\w*|\bensuring\w*|\bmanage\w*|\bmanaging\w*|\bmonitor\w*|\breview\w*|\binspect\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data quality assessment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ethical standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bethics\w*|\bethical\w*|\bethic[s]?\s+policy\w*|\badhere\s+ethic\w*|\bfollow\s+ethic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ethical standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for technical engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+engineer\w*|\bengineering\s+technology\w*', text)
        match_2 = o(r'\bengineer\w*', text)
        match_3 = o(r'\btechnical\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'technical engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for employment law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhuman\s+resource[s]?\s+polic\w*|\bhuman\s+resource[s]?\s+law\w*|\bhuman\s+resource[s]?\s+legislation\w*|\bhuman\s+resource[s]?\s+regulat\w*|\bhuman\s+resource[s]?\s+guideline\w*|\bhuman\s+resource[s]?\s+rule\w*|\bhuman\s+resource[s]?\s+statute\w*|\bhr\s+polic\w*|\bhr\s+law\w*|\bhr\s+legislation\w*|\bhr\s+regulat\w*|\bhr\s+rule\w*|\bhr\s+statute\w*|\bemployment\s+law\w*|\bemployment\s+polic\w*|\bemployment\s+legislation\w*|\bemployment\s+guideline\w*|\bemployment\s+regulat\w*|\bemployment\s+rule\w*|\bemployment\s+statute\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'employment law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fault reporting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfault\w*|\bincident\w*', text)
        match_2 = o(r'\breport\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fault reporting'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for international financial reporting standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breport[i]?[n]?[g]?\s+standard\w*|\breport[i]?[n]?[g]?\s+guideline\w*|\bstandard[s]?\s+report\w*|\bguideline\s+report\w*|\breport[i]?[n]?[g]?\s+regulation\w*|\bfrc\b', text)
        match_2 = o(r'\bfinance\w*|\baccounting\w*|\bbookkeep\w*|\bbookkeep\w*|\bbook\s+-\s+keep\w*|\bbook-keep\w*|\baccountant\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'international financial reporting standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for joint ventures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+venture\w*|\bjoint\s+venture\w*|\bnew\s+\w+\s+venture\w*', text)
        match_2 = o(r'\bjv\b', text)
        match_3 = o(r'\bbusiness\b|\baccounting\w*|\bfinance\b|\bbook\s?[-]?\s?keep[i]?[n]?[g]?\b|\btender\w*|\bventure\b|\bmerger\b|\bagreement\b|\bcontract\b|\bpartnership\w*|\blegal\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'joint ventures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mergers and acquisitions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmerger[s]?\s+acquistion\w*|\bacquisition[s]?\s+divestiture\w*|\bacqusition[s]?\s+merger\w*|\bdivestiture\s+acquistion\w*|\btechnical\s+transfer\w*|\btechnology\s+transfer\w*|\btech\s+transfer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mergers and acquisitions'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for offshore legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boffshore\s+legislation\w*|\boffshore\s+compliance\w*|\boffshore\s+law\w*|\boffshore\s+policy\w*|\boffshore\s+policies\w*|\boffshore\s+code\w*|\boffshore\s+regulation\w*|\bsubsea\s+legislation\w*|\bsubsea\s+law\w*|\bsubsea\s+policy\w*|\bsubsea\s+policies\w*|\bsubsea\s+code\w*|\bsubsea\s+regulation\w*|\bsubsea\s+compliance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'offshore legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for football tactics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsport\s+science\w*|\bcoach\w*|\btactic[s]?\b', text)
        match_2 = o(r'\bfootball\w*|\bsoccer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'football tactics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maritime legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarine\s+law\w*|\bmarine\s+guideline\w*|\bmarine\s+legislation\w*|\bmarine\s+regulation\w*|\bmaritime\s+law\w*|\bmaritime\s+legislation\w*|\bmaritime\s+guideline\w*|\bmaritime\s+regulation\w*|\bvessel\s+complaince\w*|\bmarine\s+complaince\w*|\bsubsea\s+complaince\w*|\bmarine\s+regulator\w*', text)
        match_2 = o(r'\bisps\b|\bism\b|\blegislation\w*|\bcompliance\w*|\bpolicy\w*|\bpolicies\w*', text)
        match_3 = o(r'\bmarine\w*|\bmaritime\w*|\bfleet\w*|\bsubsea\w*|\bvessel\w*', text)
        match_4 = o(r'\bmarine\w*|\bmaritime\w*|\bsubsea\w*', text)
        match_5 = o(r'\benvironment[a]?[l]?\s+legislation\w*|\benvironment[a]?[l]?\s+regulat\w*|\benvironment[a]?[l]?\s+guideline\w*|\benvironment[a]?[l]?\s+law[s]?\b|\benvironment[a]?[l]?\s+policy\w*|environment[a]?[l]?\s+policies\w*|\bconsenting\w*|\blicensing\w*|\bconsent\s+manage\w*', text)
        match_6 = o(r'\bwfd\b|\bhra\b|\bhras\b|\bmcz\b|\benvid\b|\bbat\b|\bbpeo\w*|\beia\b|\besia\b|\beias\b|\becias\b|\berms\b|\bcedm\b|\bism\w*', text)
        match_7 = o(r'\bpolicy\w*|\bpolicies\w*|\blegislation\w*|\blitigation\w*|\bregulation\w*|\bguideline\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        elif match_4 and match_6 and match_7:
            matched_fragments.extend([match_4, match_6, match_7])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'maritime legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for integrated safe systems of work
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bissow\b|\bintegrated\s+safe\s+system[s]?\s+work\w*|\bsafe\s+work[i]?[n]?[g]?\s+system\w*|\bssow\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'integrated safe systems of work'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for loss management practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bloss[e]?[s]?\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+loss\w*|\bhandl[e]?[i]?[n]?[g]?\s+loss\w*|\bassess[i]?[n]?[g]?\s+loss\w*|\bloss[e]?[s]?\s+assess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'loss management practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for diving equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdiv[e]?[i]?[n]?[g]?\s+equipment\w*|\bdrysuit\w*|\bequipment\s+div[e]?[i]?[n]?[g]?\w*|\bwetsuit\w*|\bscuba\w*', text)
        match_2 = o(r'\bdiv[e]?[i]?[n]?[g]?\b', text)
        match_3 = o(r'\bhelmet\b|\bequipment\w*|\bsuit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'diving equipment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for diving vessels systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdiv[e]?[i]?[n]?[g]?\s+vessel\w*|\bdiv[e]?[i]?[n]?[g]?\s+position\w*|\bdiv[e]?[i]?[n]?[g]?\s+support\w*|\bdiv[e]?[i]?[n]?[g]?\s+system\w*|\bdynamic\s+positioning\w*|\bsystem[s]?\s+div[e]?[i]?[n]?[g]?\w*|\bdp\s+equipment\w*|\bdp\s+engineer\w*', text)
        match_2 = o(r'\bdp\b', text)
        match_3 = o(r'\bvessel\w*|\bmarine\w*|\bfleet\w*|\bsusbsea\w*|\bpositioning\w*|\bdiv[e]?[i]?[n]?[g]?\b|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'diving vessels systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for api 6a specification for manufacture and testing of valves
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapi6a\b|\bapi\s+6a\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'api 6a specification for manufacture and testing of valves'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mathcad software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmathcad\w*|\bmath\s+cad\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mathcad software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for maximo software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaximo\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'maximo software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for survey techniques and methods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsurveyor[s]?\b|\bplan[n]?[i]?[n]?[g]?\s+survey\w*|\bsupervis[e]?[i]?[n]?[g]?\s+survey\w*|\bsurvey\s+supervis\w*|\bundertake\s+survey\w*|\bmanag[e]?[i]?[n]?[g]?\s+survey\w*|\bsurvey\s+manage\w*|\binspection\s+approach\w*|\binspection\s+capabilit\w*|\binspection\s+method\w*|\binspection\s+skill\w*|\bsurvey[s]?[i]?[n]?[g]?\s+approach\w*|\bsurvey[s]?[i]?[n]?[g]?\s+capabilit\w*|\bsurvey[s]?[i]?[n]?[g]?\s+method\w*|\bsurvey[s]?[i]?[n]?[g]?\s+skill\w*|\bsurvey[s]?[i]?[n]?[g]?\s+technique\w*|\bsurvey[s]?[i]?[n]?[g]?\s+engineer\w*|\bsurvey\s+co\s+-\s+ordinat\w*|\bsurvey\s+coordinat\w*|\bcoordinate\s+survey\w*|\bordinate\s+survey\w*|\bsurvey[s]?[i]?[n]?[g]?\s+method\w*|\bmethod[s]?\s+survey\w*|\bsignal\s+survey\w*|\bsignal\s+site\s+survey\w*', text)
        match_2 = o(r'\bsurvey\w*', text)
        match_3 = o(r'\bmethod\w*|\btechnique\w*|\bexperience\w*|\bknowledge\w*|\bperform\w*|\bmanage\w*|\bmanaging\w*|\bequipment\w*|\btool\w*|\bsensor\w*|\bconduct\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'survey techniques and methods'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for integrated risk management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegrated\s+risk\s+manage\w*', text)
        match_2 = o(r'\birm\b', text)
        match_3 = o(r'\brisk\w*|\bprocess\w*|\bprocedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'integrated risk management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for enterprise risk management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benterprise\s+risk\s+manage\w*', text)
        match_2 = o(r'\berm\b(?! group\w*|group)\w*', text)
        match_3 = o(r'\brisk\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'enterprise risk management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pipeline coating properties
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpipe\s+coat\w*|\bpipeline\s+coat\w*|\bpiping\s+coat\w*|\bcoat[i]?[n]?[g]?\s+pipe\w*|\bcoat[i]?[n]?[g]?\s+piping\w*', text)
        match_2 = o(r'\bcoat\b|\bcoataing\b|\bpolymer\s+lining\w*', text)
        match_3 = o(r'\bpipe\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pipeline coating properties'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for abaqus software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\babaqus\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'abaqus software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cesar ii software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcesar\s+ii\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cesar ii software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teamspirit software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bteamspirit\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'teamspirit software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for asset integrity management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basset[s]?\s+integrity\w*', text)
        match_2 = o(r'(?<!main )(?<!project )\baim\b(?! work)\w*', text)
        match_3 = o(r'\basset\w*|\bintegrity\w*', text)
        match_4 = o(r'\basset\w*', text)
        match_5 = o(r'\bintegrity\w*|\bintegral\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'asset integrity management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pollution legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpollution\s+control\w*|\bpollution\s+regulation\w*|\bpollution\s+legislation\w*|\bpollution\s+compliance\w*|\bpollution\s+law\w*|\bpollution\s+prevent\w*|\bcontrol[l]?[i]?[n]?[g]?\s+pollution\w*|\bmeasur[e]?[i]?[n]?[g]?\s+pollution\w*|\bmanag[e]?[i]?[g]?\s+pollution\w*|\bassess[i]?[n]?[g]?\s+pollution\w*|\bpollution\s+manage\w*|\bpollution\s+control\w*|\bpollution\s+monitor\w*|\bpollution\s+assess\w*|\bpollution\s+strandard\w*|\bstandard[s]?\s+pollution\w*|\bpollution\s+requirement\w*|\brequirement[s]?\s+pollution\w*', text)
        match_2 = o(r'\bpollution\w*|\bpollutant\w*', text)
        match_3 = o(r'\bregulation\w*|\bguideline\w*|\blegislation\w*|\bpolicy\w*|\bpolicies\w*|\blaw\b|\bcompliance\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pollution legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for international safety management code
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binternational\s+safe[t]?[y]?\s+manage[m]?[e]?[n]?[t]?\s+code\w*|\bism\s+code\w*|\bism\s+qualification\w*', text)
        match_2 = o(r'\bism\b', text)
        match_3 = o(r'\bmarine\w*|\bpollution\w*|\boffshore\w*|\bsubsea\w*|\bwell\b|\bmaritime\w*|\bisps\b|\bcips\b|\biso\b|\bcode\w*|\bprocedure\w*|\bpolicy\w*|\bcompliance\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'international safety management code'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pollution prevention
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpollution\s+prevent\w*|\bprevent[i]?[o]?[n]?\s+pollution\w*|\bmarpol\b', text)
        match_2 = o(r'\bpollution\w*|\bpollutant\w*', text)
        match_3 = o(r'\bprevent\w*|\bmitigat\w*|\bremediat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pollution prevention'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for creo software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcreo\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'creo software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for forest ecology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becology\w*|\becologist\w*|\becological\w*', text)
        match_2 = o(r'\bforest\w*|\bforestry\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'forest ecology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for power engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpower\s+engineer\w*|\belectricity\s+power\w*|\bpower\s+quality\w*|\bquality\s+power\w*|\bdynamic\s+power\w*|\bstatic\s+power\w*|\belectricity\s+interpret\w*|\bunderstand\s+electic\w*|\bpower\s+study\w*|\bstudy[i]?[n]?[g]?\s+power\w*|\bpower\s+electronic\w*|\bpower\s+generation\w*|\bpower\s+flow\w*|\bpower\s+system\w*', text)
        match_2 = o(r'\brms\b|\bemt\b|\bemtp\w*|\bcdeg[s]?\b|\bpowerfactory\b|\bemtdc\b|\bpscad\b', text)
        match_3 = o(r'\bpower\w*|\belectric\w*|\bengineer\w*|\bgrid\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'power engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geotechnical engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeotechnic[a]?[l]?\s+engineer\w*|\bgeotechnic[a]?[l]?\s+work\w*|\bgeotechnic[a]?[l]?\s+design\w*|\bgeotechnic[a]?[l]?\s+assess\w*|\bskill\s+geotechnical\w*|\bgeotechnical\s+datum\w*|\bgeotechnical\s+data\w*|\bsoil\s+mechanics\w*|\bcharacteristic[s]?\s+soil\w*|\bporperty\s+soil\w*|\bproperties\s+soil\w*|\bsoil\s+analy\w*|\banalyse\s+soil\w*|\banalysis\s+soil\w*|\banalyze\s+soil\w*|\brock\s+slope\w*|\bslope\s+stability\w*|\bdeep\s+shallow\s+foundation\w*', text)
        match_2 = o(r'\bgeotechnic\w*', text)
        match_3 = o(r'\bengineer\w*|\btechnician\w*|\bsciencist\w*|\bearthwork\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geotechnical engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plaxis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplaxis\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'plaxis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for wallap software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwallap\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'wallap software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for transportation engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransport[a]?[t]?[i]?[o]?[n]?\s+modeller\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+engineer\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+modelling\w*|\bplan[n]?[i]?[n]?[g]?\s+transport\w*|\btransport[a]?[t]?[i]?[o]?[n]?\s+plann\w*|\bhighway[s]?\s+engineer\w*|\broad[s]?\s+engineer\w*|\bstreet\s+design\w*|\bstreet\s+engineer\w*|\bstreetscape\s+engineer\w*|\bstreetscape\s+design\w*|\bdesign[i]?[n]?[g]?\s+streetscape\w*', text)
        match_2 = o(r'\bsustainable\s+transport\w*|\btransportation\s+system\w*|\btransport\s+project\w*|\btransport\s+engineer\w*|\btransportation\s+engineer\w*|\bengineer\s+transport\w*|\bengineering\s+transport\w*|\bransport[a]?[t]?[i]?[o]?[n]?\s+plan\b', text)
        match_3 = o(r'\brail\b|\brailway\w*|\bbridge\w*|\broad\b|\bairport\w*|\bhighway\w*|\bviaduct\w*|\btransportation\s+business\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'transportation engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for revit software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brevit\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'revit software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for type of wind turbines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bv\s+model\w*|\bhawt\b|\bh\s+rotor\w*|\bh\s+-\s+rotor\w*|\bsavoniu[s]?\w*|\bdarrieu[s]?\w*', text)
        match_2 = o(r'\bturbine\w*|\bwind\w*', text)
        match_3 = o(r'\bturbine\w*|\bmotor\w*|\bmechanism\w*', text)
        match_4 = o(r'\bwind\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'type of wind turbines'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for petrology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brock\s+engineer\w*|\bpetrolog\w*|\brock\s+study\w*|\bstudy[i]?[n]?[g]?\s+rock\b|\bcomposition\s+rock\w*|\brock\s+composition\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'petrology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for industrial relations legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bir\s+legislation\w*|\bir\s+regulation\w*|\bir\s+compliance\w*|\bir\s+guideline\w*|\bir\s+law[s]?\w*|\bir\s+coordination\w*|\bir\s+policy\w*|\bir\s+policies\w*|\bindustrial\s+relation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'industrial relations legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adult education
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badult\s+education\w*|\beducation\s+adult\w*|\badult\s+learn\w*|\badult\s+teach\w*|\badult\s+train\w*|\badult\s+pedagogy\w*', text)
        match_2 = o(r'\badult\w*', text)
        match_3 = o(r'\bteach\w*|\beducate\w*|\blecture\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'adult education'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cymcap software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcymcap\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cymcap software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for comsol software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomsol\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'comsol software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for urban planning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btown\s+plan\w*|\bplanning\s+town\w*|\burban\s+plan\w*|\bdesign[i]?[n]?[g]?\s+town\w*|\bcountry\s+plan\w*|\burban\s+design\w*|\burban\s+engineer\w*|\binfrastructure\s+town\s+plan\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'urban planning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for urban planning law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhighway[s]?\s+act\b|\bplanning\s+act\b|\btcpa\w*', text)
        match_2 = o(r'\btown\s+plan\w*|\bplanning\s+town\w*|\burban\s+plan\w*|\bdesign[i]?[n]?[g]?\s+town\w*|\bcountry\s+plan\w*|\burban\s+design\w*|\burban\s+engineer\w*', text)
        match_3 = o(r'\blaw[s]?\b|\bcompilance\w*|\blegislation\w*|\bregulation\w*|\bpolicy\b|\bpolicies\b|\bact\b', text)
        match_4 = o(r'\btcpa\b|\btwa\b|\bdco\b|\btwao\b|\bcpo\b', text)
        match_5 = o(r'\bconstuction\w*|\binfrastructure\w*|\bcivil\b|\bproperty\w*|\bbuild\w*|\barchitect\w*|\bstandard\w*|\bguideline\w*|\burban\w*|\bpolicy\w*|\bprocedure\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'urban planning law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for catchment management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcatchment\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+catchment\w*', text)
        match_2 = o(r'\bcatchment\w*', text)
        match_3 = o(r'\bwater\w*|\bwaste\s?water\w*|\bflood[i]?[n]?[g]?\b|\bmanage\w*|\bdesign\w*|\bdevelop\w*|\bstrategy\w*|\bmanaging\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'catchment management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cube software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcube\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cube software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for saturn software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsaturn\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'saturn software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for paramics software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bparamics\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'paramics software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emme software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemme\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emme software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ptv vissim software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvissim\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ptv vissim software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tuba software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btuba\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tuba software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for herpetology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bherpetolog\w*|\bamphibian[s]?\b|\breptile[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'herpetology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for climate change impact
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclimate\s+change\w*|\bglobal\s+warming\w*|\bclimate\s+risk\w*|\bclimate\s+assessment\w*|\bclimate\s+projection\w*', text)
        match_2 = o(r'\bconsult\w*|\badvice\w*|\badvise\w*|\badvisor\w*|\bimpact\w*|\bresilence\w*|\bremediat\w*|\bmitigat\w*|\bmanage\w*|\bassess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'climate change impact'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ict infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bict\s+experience\w*|\bexperience\s+ict\b|\bict\s+infrastructure\w*|\bitc\s+cabinet\w*|\bcommunication\s+room\w*|\blan\s+switch\w*|\bstructured\s+cabling\b|\bav\s+multimedia\w*', text)
        match_2 = o(r'\bict\b', text)
        match_3 = o(r'\bprogramming\w*|\bcoding\b|\bsoftware\w*|\bapplication\w*|\bweb\s+develop\w*|\bwebsite\s+develop\w*|\binfrastrucutre\w*|\btool\w*|\bservice\w*|\bnetwork\w*|\bhardware\w*|\bsoftware\w*|\bapplication\w*|\bapp\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ict infrastructure'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for moodle e-learningn platform
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmoodle\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'moodle e-learningn platform'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for online learning platforms and learning management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\be-learning\s+platform\w*|\be\s+-\s+learning\s+system\w*|\be-learningn\s+system\w*|\belearning\w*|\blearning\s+management\s+system\w*|\blearning\s+management\s+platform\w*', text)
        match_2 = o(r'\btraining\s+platform\w*|\belearning\s+platform\w*|\be\s+-\s+learning\s+platform\w*|\blearning\s+platform\w*|\blms\b', text)
        match_3 = o(r'\bsystem\w*|\bict\b|\bitc\b|\bweb\s+develop\w*|\bwebsite\s+develop\w*|\bsoftware\s+develop\w*|\bprogramming\b|\bcoding\b|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'online learning platforms and learning management systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for landscape architecture design and analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blandscap[e]?[i]?[n]?[g]?\s+plann\w*|\bplan[n]?[i]?[n]?[g]?\s+landscap\w*|\blandscap[e]?[i]?[n]?[g]?\s+architect\w*|\blandscap[e]?[i]?[n]?[g]?\s+design\w*|\bdesign[i]?[n]?[g]?\s+landscap\w*|\blandscap[e]?[i]?[n]?[g]?\s+analy\w*|\banalysis\s+landscap\w*|\banalyse\s+landscap\w*|\banalyze\s+landscap\w*|\bassessment\s+landscap\w*|\bassess[i]?[n]?[g]?\s+landscap\w*|\blvia\b', text)
        match_2 = o(r'(?<!technical )(?<!competitive )\blandscape\w*|\blandscaping\w*', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bdraw\w*|\bdraughting\w*|\bconstruction\w*|\bassess\w*|\banaly\w*|\bengineer\w*|\barchitect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'landscape architecture design and analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ornithology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bornitholog\w*|\bstudy[i]?[n]?[g]?\s+bird\w*|\bstudy[i]?[n]?[g]?\s+seabird\w*|\bbird\s+study\w*|\bbird\s+ecology\w*|\bseabird\s+study\w*|\bseabird\s+ecology\w*', text)
        match_2 = o(r'\bbird\w*|\bseabird\w*', text)
        match_3 = o(r'\bspecie\w*|\bwildlife\w*|\bhabitat\w*|\becology\w*|\benvironmnet\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ornithology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for applied zoology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bzoology\w*|\bzoologist\w*|\bzoological\w*|\banimal[s]?\s+biology\w*|\bbiology\s+animal\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'applied zoology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laser types
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaser\s+brand\w*|laser\s+categor\w*|laser\s+format\w*|laser\s+type\w*|laser\s+variet\w*|radiation\s+emission\s+brand\w*|radiation\s+emission\s+categor\w*|radiation\s+emission\s+format\w*|radiation\s+emission\s+variet\w*|laser\s+scan\w*|\bus[e]?[i]?[n]?[g]?\s+laser\w*|\boperate[e]?[i]?[n]?[g]?\s+laser\w*|\bus[e]?[i]?[n]?[g]?\s+\w+\s+laser\w*|\boperate[e]?[i]?[n]?[g]?\s+\w+\s+laser\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laser types'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electrical wiring and circuit diagrams
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsingle\s+line\s+diagram\w*|\bblock\s+cable\s+diagram\w*|\belectrical\s+installation\w*|\bcable\s+route\s+draw\w*|\bcircuit\s+diagram\w*|\bcircuit\s+blueprint\w*|\bcircuit\s+schema\w*|\bdiagram\s+circuit\w*|\belectric[a]?[l]?\s+diagram\w*|\belectric[a]?[l]?\s+schemat\w*|\belectronic\s+diagram\w*|\belectronic\s+schemat\w*|\belementary\s+diagram\w*|\bschedule\s+load\w*', text)
        match_2 = o(r'\bcable\b|\bwiring\w*|\bcircuit\b|\bsld\b', text)
        match_3 = o(r'\bdiagram\w*|\bdraw\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electrical wiring and circuit diagrams'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for integrated circuits
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintegrate\s+circuit\w*|\bheterogeneous\s+integrat\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'integrated circuits'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for solar energy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsolar\s+farm\w*|\bsolar\s+\w+\s+farm\w*|\bsolar\s+energy\w*|\bphotovoltaic[s]?\b|\bsun\s+energy\w*|\bsolar\s+thermal\s+energy\w*|\benergy\s+sun\b|\brenewable\s+solar\w*', text)
        match_2 = o(r'\bpv\b|\bste\b', text)
        match_3 = o(r'\bsolar\w*|\bsun\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'solar energy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rail infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brail\s+infrastructure\w*|\brail\s+system\w*|\brail\s+project\w*|\brail\s+structure\w*|\brail\s+project\w*|\bole\s+engineer\w*|\bole\s+design\w*|\boverhead\s+line\s+equipment\w*|\bsolid\s+state\s+interlock\w*|\brail\s+roughness\s+index\bolemi\b', text)
        match_2 = o(r'\brailroad\b|\brailway\b|\bcrossrail\w*', text)
        match_3 = o(r'\bconstruction\w*|\binfrastrucure\w*|\bengineering\w*|\brri\b|\bssi\b|\barchitect\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rail infrastructure'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for bridge engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbridge[s]?\s+technician\w*|\bviaduct[s]?\s+technician\w*|\bbridge[s]?\s+engineer\w*|\bviaduct[s]?\s+engineer\w*|\bbridge[s]?\s+infractrucutre\w*|\bviaduct[s]?\s+infrastructure\w*|\bbridge[s]?\s+code\w*|\bviaduct[s]?\s+code\w*|\bbridge[s]?\s+structure\w*|\bviaduct[s]?\s+structure\w*|\bbridge[s]?\s+construct\w*|\bviaduct[s]?\s+construct\w*|\btechnical\s+bridge\w*|\btechnical\s+viaduct\w*|\bbridge[s]?\s+manage\w*|\bviaduct[s]?\s+manage\w*|\bengineer[i]?[n]?[g]?\s+bridge\w*|\bengineer[i]?[n]?[g]?\s+viaduct[s]?\w*|\bbridge[s]?\s+design\w*|\bviaduct[s]?\s+design\w*|\bdesign[i]?[n]?[g]?\s+bridge\w*|\bdesign[i]?[n]?[g]?\s+viaduct[s]?\w*|\bproduction\s+bridge\w*|\bproduction\s+viaduct[s]?\w*|\brail\s+bridge\w*|\brail\s+viaduct\w*|\bbridge[s]?\s+inspection\w*|\bviaduct[s]?\s+inspection\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'bridge engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lusas software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blusas\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'lusas software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for building services engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmep\b', text)
        match_2 = o(r'\bengineer\w*|\bbim\b|\build\w*|\bconstruction\w*|\bdesign\w*|\bstandard\w*|\bprocedure\w*|\barchitect\w*|\bknowledge\w*|\bexperience\w*|\bdesign\w*|\bmanage\w*|\bmanaging\w*', text)
        match_3 = o(r'\bbuild[i]?[n]?[g]?\s+service\w*', text)
        match_4 = o(r'\bengineer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'building services engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pvsyst software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpvsyst\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pvsyst software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pvsol software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpvsol\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pvsol software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for energy storage systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benergy\s+storage\w*|\bbattery\s+type\w*|\bstore\s+energy\w*|\bbattery\s+storage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'energy storage systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for traffic engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+traffic\w*|\btraffic\s+manage\w*|\bdesign[i]?[n]?[g]?\s+traffic\w*|\btraffic\s+design\w*|\btraffic\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'traffic engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for topography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btopography\w*|\btopographic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'topography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for social interaction
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsocial\s+interaction\w*|\binteraction[s]?\s+social\w*|\binteract\s+social\w*|\bengag[e]?[i]?[n]?[g]?\s+colleague\w*|\bengag[e]?[i]?[n]?[g]?\s+employe\w*|\bengag[e]?[i]?[n]?[g]?\s+people\w*|\bsocially\s+interactive\w*|\bsocial\s+\w+\s+interact\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'social interaction'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for midas software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmidas\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'midas software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft solutions framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrosoft\s+solution[s]?\s+framework\w*|\bms\s+solution[s]?\s+framework\w*', text)
        match_2 = o(r'\bmsf\b', text)
        match_3 = o(r'\bframework\w*|\bmicrosoft\w*|\bms\b|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft solutions framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for bentley microstation software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrostation\w*', text)
        match_2 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bcreo\w*|\bbentley\w*|\bautodesk\w*|\bdraughting\w*|\bsoftware\w*|\bexpeirence\w*|\bknowledge\w*|\bdesign\w*|\buse\w*', text)
        match_3 = o(r'\bbentley\w*', text)
        match_4 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bcreo\b|\bautodesk\w*|\bmicrostation\w*|\bdraughting\w*|\bsoftware\w*|\bexpeirence\w*|\bknowledge\w*|\bdesign\w*|\buse\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'bentley microstation software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cae software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcae\b|\bcomputer\s+aid[e]?[d]?\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cae software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for creo software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcreo\b', text)
        match_2 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bbentley\w*|\bautodesk\w*|\bmicrostation\w*|\bdraughting\w*|\bsoftware\w*|\bexpeirence\w*|\bknowledge\w*|\bdesign\w*|\buse\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'creo software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for superstress software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsuperstress\w*', text)
        match_2 = o(r'\bmicrostation\w*|\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bcreo\w*|\bbentley\w*|\bautodesk\w*|\bdraughting\w*|\bexpeirence\w*|\bknowledge\w*|\bdesign\w*|\buse\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'superstress software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for staad software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstaad\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'staad software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputer\s+science\w*|\binformatic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for smart ticketing system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsmart\s+ticket\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'smart ticketing system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tekla software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btekla\b|\btedds\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tekla software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for scia software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscia\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'scia software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for csc fastrak
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcsc\s+fastrak\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'csc fastrak'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for health and safety
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhealth\s+safe\w*|\bhealthy\s+safe\w*|\bsafety\s+environment\w*|\bsafety\s+\w+\s+environment\w*', text)
        match_2 = o(r'\bhs\b|\bhseq\b|\bhse\b|\bhsec\b|\bhsse\b|\bhses\b', text)
        match_3 = o(r'\bhealth\w*|\bsafety\w*|\bperformance\w*|\bpolicy\w*|\bpolicies\w*|\bregulation\w*|\bguideline\w*|\blegislation\w*|\bprocedure\w*|\bexperience\w*|\bknowledge\w*|\bundesrtand\w*|\bfamiliar\w*|\brequirement\w*|\bcompliance\w*|\bcomply\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'health and safety'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for nuclear energy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnuclear\s+power\w*|\bnuclear\s+system\w*|\bnuclear\s+project\w*|\bnuclear\s+energy\w*|\bnuclear\s+\w+\s+system\w*|\bnuclear\s+\w+\s+project\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'nuclear energy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fluid mechanics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfluid\s+mechanic\w*|\bfluid\s+dynamic\w*|\bchoke\s+manage\w*|\bfluid\s+separat\w*', text)
        match_2 = o(r'\bcfd\b|\bmechanic\w*|\bpropert\w*', text)
        match_3 = o(r'\bfluid\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fluid mechanics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for heat transfer processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bheat\s+transfer\w*|\btransfer[i]?[n]?[g]?\s+heat\w*|\bconduction\b|\bconvection\b|\bradiation\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'heat transfer processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fluent software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfluent\b', text)
        match_2 = o(r'\bfluid\w*|\bcfd\b|\bsoftware\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fluent software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cfx software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcfx\b', text)
        match_2 = o(r'\bfluid\b|\bcfd\b|\bsoftware\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cfx software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cmm-star software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcmm\b', text)
        match_2 = o(r'\bstar\b', text)
        match_3 = o(r'\bfluid\b|\bcfd\b|\bsoftware\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cmm-star software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for private wire networks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprivate\s+wir[e]?[i]?[n]?[g]?\s+network\w*|\bprivate\s+wir[e]?[i]?[n]?[g]?\s+grid\w*|\bprivate\s+wir[e]?[i]?[n]?[g]?\s+project\w*|\bmicrogrid\w*|\bmicro\s+grid\w*', text)
        match_2 = o(r'\bengineer\w*|\btechnician\w*|\belectrician\w*|\bexperience\w*|\bknowledge\w*|\binstall\w*|\bconnect\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'private wire networks'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for safety engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsafety\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+safety\w*|\bsafe[t]?[y]?\s+instrument[e]?[d]?\s+function\w*|\bsafe[t]?[y]?\s+industrial\s+function\w*', text)
        match_2 = o(r'\bsif\b|\bsifs\b', text)
        match_3 = o(r'\brisk\w*|\bhazard\w*|\bsafety\w*|\bfault\w*|\bquality\w*|\bqc\b|\bqa\b|\bknowledge\w*|\bexperience\w*|\bprocedure\w*|\bprocess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'safety engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydraulics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydraulic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydraulics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydrology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydrolog\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydrology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydromorphology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydromorpholog\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydromorphology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydroecology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhydroecolog\w*|\bhydro-ecolog\w*|\bhydro\s+-\s+ecolog\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydroecology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for trendwatching
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfuture\s+trend\w*|\btrendwatch\w*|\btrend[s]?\s+watch\w*|\bmonitor[i]?[n]?[g]?\s+trend\w*|\bnew\s+trend\w*|\btrend[s]?\s+monitor\w*|\btrend[s]?\s+-\s+watch\w*|\btrend[s]?-watch\w*|\bmarket\s+trend\w*|\btrend\s+change\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'trendwatching'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for remote sensing techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremote\s+sensing\w*|\bremote\s+sense\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'remote sensing techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for remotely operated technologies and equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bremote[l]?[y]?\s+operated\s+vehicle\w*|\brov[s]?\s+intervention\w*|\brov[s]?\s+tool\w*|\bknowledge\s+rov[s]?\w*|\bremote\s+control\w*|\bunmanned\s+installation \w*', text)
        match_2 = o(r'\brov\b', text)
        match_3 = o(r'\bmarine\w*|\bsubsea\w*|\boffshore\w*|\bequipment\w*|\btools\w*|\bdevice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'remotely operated technologies and equipment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for writing techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwrit[e]?[i]?[n]?[g]?\s+skill\w*|\bwrit[e]?[i]?[n]?[g]?\s+technique\w*|writ[e]?[i]?[n]?[g]?\s+method\w*|\bmethod[s]?\s+writ[e]?[i]?[n]?[g]?\w*|\bwrit[e]?[i]?[n]?[g]?\s+practice\w*|\bcopy\s+type\w*|\btyp[e]?[i]?[n]?[g]?\s+copy\w*|\bnewsletter\s+writ[e]?[i]?[n]?[g]?\w*|\btechnical\s+write\b|\btechnical\s+writting\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'writing techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mist database
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmist\b', text)
        match_2 = o(r'\bdatabase\w*|\bmedical\w*|\bbacteria\w*|\bgenom\w*|\blab\b|\blaboratory\b|\bexperience\w*|\bknowledge\w*|\bdatum\w*|\bfamiliar\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mist database'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for basic offshore safety induction and emergency training
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbosiet\w*|\bebs\b', text)
        match_2 = o(r'\boffshore\w*|\bmarine\w*|\bonshore\w*|\bpipeline\w*|\bsubsea\b|\bmaritime\b|\bprocedure\w*|\bexperience\w*|\btrain\w*|\bknowledge\w*|\bcomply\w*|\bcompliance\w*|\bregualation\w*|\badhere\w*|\bfollow\w*|\bposses\w*|\bcertificat\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'basic offshore safety induction and emergency training'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cms database
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcms\b', text)
        match_2 = o(r'\bdatabase\w*|\bcontent\s+creation\w*|\bsoftware\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cms database'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pims software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpims\b|\bproject\s+information\s+manage[m]?[e]?[n]?[t]?\s+system\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pims software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for construction design and management regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'construction\s+design\s+management\w*|\bconstruction\s+environment[a]?[l]?\s+management\s+document\w*|\bunderstand[i]?[n]?[g]?\s+cdm\b|\bcdm\s+quality\w*|\bcdm\s+regulat\w*|\bcdm\s+legislation\w*|\bcdm\s+law[s]?\b|\bcdm\s+guideline\w*|\bcdm\s+compliance\w*', text)
        match_2 = o(r'\bcdm[s]?\b|\bcemd[s]?\b', text)
        match_3 = o(r'\bconstruction\w*|\barchitect\w*|\bhealth\s+safety\w*|\bcad\b|\bexperience\w*|\bknowledge\w*|\bstandard\w*|\bprocedure\w*|\bfollow\w*|\bengineer\w*|\bstructure\w*|\bdesign\w*|\bcomply\w*|\bcompliance\w*|\bfollow\w*|\bdocument\w*|\breport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'construction design and management regulations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for deck operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdeck\s+operation\w*|\bdeck\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+deck\w*|\bdeck\s+activity\w*|\bcoordinat[e]?[i]?[o]?[n]?\s+deck\w*|\bordinat[e]?[i]?[o]?[n]?\s+deck\w*|\bdeck\s+foreman\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'deck operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for permit to work systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpermit\s+work\s+system\w*', text)
        match_2 = o(r'\bptw\b', text)
        match_3 = o(r'\bpermit\w*|\bsystem\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'permit to work systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for personal protective equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bppe\b|\bpersonal\s+protective\s+equipment\w*|\bprotective\s+equipment\w*|\bpersonal\s+protection\s+equipment\w*|\brpe\s+inspection\w*', text)
        match_2 = o(r'\brpe\b', text)
        match_3 = o(r'\bequipment\w*|\bwear\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'personal protective equipment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for program management and its principals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprogram\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+program\w*|\bprogramme\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+programme\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'program management and its principals'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tableau software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btableau\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tableau software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data capture and management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+capture\s+manage\w*|\bdatum\s+capture\s+manage\w*', text)
        match_2 = o(r'\bdcm\b', text)
        match_3 = o(r'\bdata\w*|\bdatum\w*|\bcollect\w*|\bgather\w*|\bmanage\w*|\bmanaging\w*|\bexperience\w*|\bknowledge\w*|\bmonitor\w*|\bperform\w*|\bprocedure\w*|\boperat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data capture and management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for esdat database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besdat\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'esdat database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gint database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgint\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gint database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for holebase software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bholebase\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'holebase software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for equis database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bequis\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'equis database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for solarwind database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsolarwind\b|\bsolarwinds\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'solarwind database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mysql database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmysql\b|\bmy\s+sql\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mysql database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cisco sd-wan software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcisco\b', text)
        match_2 = o(r'\bsdwan\b|\bsd-wan\w*|\bsd\s+-\s+wan\w*|\bsd\s+wan\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cisco sd-wan software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for agile design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagility\w*|\bagile\w*', text)
        match_2 = o(r'\bdesign\w*|\bdesigning\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'agile design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for building information modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbuild[i]?[n]?[g]?\s+information\s+model\w*|\bclash\s+detection\w*|\bbim\s+manager\w*|\bbim\s+co\s+-\s+ordinat\w*|\bbim\s+coordinat\w*|\bcic\s+bim\w*', text)
        match_2 = o(r'\bbim\b', text)
        match_3 = o(r'\bdesign\b|\bdesigner\b|\bdesigning\b|\bconsutruction\w*|\bcad\b|\barchitecture\w*|\bcde\b|\blod\b|\bexperience\w*|\bknowledge\w*|\bsystem\w*|\bbuild\w*|\bprocedure\w*|\bmodel\w*|\bstandard\w*|\bbuild\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'building information modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for navisworks software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnavisworks\b|\bnaviswork\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'navisworks software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for civil 3d software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcivil[s]?\s+3d\w*|\bcivil3d\w*|\bcivils3d\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'civil 3d software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for epanet software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bepanet\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'epanet software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for swmm software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstorm\s+water\s+management\s+model\w*|\bswmm\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'swmm software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for infoworks software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binfoworks\b|\binfowork\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'infoworks software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computational fluid dynamics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputational\s+fluid\s+dynamic\w*|\bairflow\s+analy\w*|\banalysis\s+airflow\w*|\banalyse\s+airflow\w*|\banalyze\s+airflow\w*|\bfluid\s+behaviour\s+motion\w*', text)
        match_2 = o(r'\bcfd\w*', text)
        match_3 = o(r'\bfluid\w*|\bmodel\w*|\bwater\w*|\bexperience\w*|\bknowledge\w*|\bmodel\w*|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computational fluid dynamics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for scientific modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphysical\s+modelling\w*|\bscientific\s+model\w*|\bphysical\s+simulation\w*|\bscientific\s+simulation\w*|\bcomputer\s+model\w*|\bcomputer\s+simulation\w*|\bsimulation\s+software\w*|\bsimulation\s+system\w*|\bmathematic[s]?[a]?[l]?\s+model\w*|\bnumeric[a]?[l]?\s+model\w*|\bmathematic[s]?[a]?[l]?\s+simulation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'scientific modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sustainability accounting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsustainable\s+accounting\w*|\bclimat\s+change\s+accounting\w*|\bsustainability\s+accounting\w*|\bclimate\s+accounting\w*|\bsustainability\s+\w+\s+accounting\w*|\bsustainable\s+\w+\s+accounting\w*', text)
        match_2 = o(r'accounting\w*|\baccountant\w*|\bbook\s?[-]?\s?keep\w*', text)
        match_3 = o(r'\btcfd\b|\bsasb\b|\bgri\b|\bcdp\b|\bsustainable\w*|\bsustianability\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sustainability accounting'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for viewpoint for projects document management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\b4project[s]?\b|\b4\s+project[s]?\b|\bviewpoint\s+project[s]?\b|\bviewpointforproject[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'viewpoint for projects document management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aconex document management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baconex\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aconex document management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for risk identification
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bidentify\w*|\bclassify\w*|\binspect\w*|\bidentification\w*|\bclassification\w*|\bcheck\w*', text)
        match_2 = o(r'\brisk\w*|\bhazzard\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'risk identification'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hydraulic modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstormwater\s+runoff\w*', text)
        match_2 = o(r'\bhydraulic\w*|\bhydrology\w*|\bhydrological\w*', text)
        match_3 = o(r'\bmodel\w*|\bexperience\w*|\bknowledge\w*|\bunderstand\w*|\bfamilair\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hydraulic modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\w*|\bcompany\w*|\bfirm|\benterprise\w*|\bmarket\w*', text)
        match_2 = o(r'\banaly\w*|\basses\w*|\bresearch\w*|\bdetermine\w*|\bmodel\w*|\binvestigat\w*|\bmeet\w*|\bunderstand\w*|\bknowledge\w*', text)
        match_3 = o(r'\btrend\w*|\bdemand\w*|\bobjective\w*|\bstrateg\w*|\boutlook\w*', text)
        match_4 = o(r'\bbusiness\s+objective\w*|\bcompany\s+objective\w*|\bfirm\s+objective\w*|\benterprise\s+objective\w*|\benterprise\s+strategy\w*|\benterprise\s+\w+\s+strategy\w*|\bbusiness\s+strategy\w*|\bbusiness\s+\w+\s+strategy\w*|\bcompany\s+strategy\w*|\bcompany\s+\w+\s+strategy\w*|\bfirm\s+strategy\w*|\bfirm\s+\w+\s+strategy\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+business\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+\w+\s+business\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+company\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+\w+\s+company\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+enterprise\w*|\banaly[s]?[e]?[y]?[s]?[i]?[s]?\s+\w+\s+enterprise\w*|\bbusiness\s+analy\w*|\benterprise\s+analy\w*|\bcompany\s+analy\w*|\bfirm\s+analy\w*|\bbusiness\s+\w+\s+analy\w*|\benterprise\s+\w+\s+analy\w*|\bcompany\s+\w+\s+analy\w*|\bfirm\s+\w+\s+analy\w*|\bbusiness\s+\w+\s+objective\w*|\bcompany\s+\w+\s+objective\w*|\bfirm\s+\w+\s+objective\w*|\benterprise\s+\w+\s+objective\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        elif match_4:
            matched_fragments.extend([match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+support\w*|\bglobal\s+business\s+service\w*', text)
        match_2 = o(r'\bbusienss\w*', text)
        match_3 = o(r'\bproces\w*|\bprocedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for financial departamental processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinance\w*|\bfinancial\w*', text)
        match_2 = o(r'\bproces\w*|\bprocedure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial departamental processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sales, inventory & operations planning principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsiop\b|\bsale[s]?\s+inventory\s+operation[s]?\s+plan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sales, inventory & operations planning principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for control engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bengineer[i]?[n]?[g]?\s+control\w*|\bcontrol\s+engineer\w*|\bengineering\s+process\s+control\w*|\bprocess\s+control\w*|\bengineering\s+\w+\s+control\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'control engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for acoustics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bacoustic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'acoustics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for acoustical engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bacoustic[a]?[l]?\s+engineer\w*|\bengineer[i]?[n]?[g]?\s+accoustic\w*|\bnoise\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'acoustical engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for noise mapping
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnoise\s+map\w*', text)
        match_2 = o(r'\bdefra\b', text)
        match_3 = o(r'\bnoise\b|\bsoftware\w*|\bexperience\w*|\bknwoeldge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'noise mapping'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for autodesk software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautodesk\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'autodesk software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for opeanroad software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bopenroad\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'opeanroad software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microdrainage software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrodrainage\w*|\bmicro\s+drainage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microdrainage software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pub/sub architecture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpub\s?[\]?[/]?\s?sub\w*|\bpublish\s?[\]?[/]?\s?subscribe\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pub/sub architecture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for amtech software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bamtech\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'amtech software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dialux software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdialux\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'dialux software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pdms software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpdms\b', text)
        match_2 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bbentley\w*|\bautodesk\w*|\bmicrostation\w*|\bdraughting\w*|\bconstruction\w*|\barchitect\w*|\bsoftware\w*|\bknowledge\w*|\bexperience\w*|\buse\w*|\bdesign\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pdms software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for telecommunications engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btelephone\s+engineer\w*|\btelecommunication[s]?\s+engieer\w*', text)
        match_2 = o(r'\btelecommunication\w*|\btelecoms\w*|\btele-communication\w*|\btele\s+communication\w*|\btele\s+-\s+communication\w*', text)
        match_3 = o(r'\bengineer\w*|\btechnology\w*|\btelephone\w*|\btechnician\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'telecommunications engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for decision analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdecision\s+analy\w*|\banalyse\s+decision\w*|\banalyze\s+decision\w*|\banalysis\s+decision\w*', text)
        match_2 = o(r'\bdecision\s+make\w*|\bdecision\s+making\w*|\bmake\s+decision\w*|\bmaking\s+decision\w*|\bsupport[i]?[n]?[g]?\s+decision\w*|\bdecision\s+support\w*|\bdecision\s+advice\w*|\bdecision\s+advis\w*|\bdecision\s+process\w*', text)
        match_3 = o(r'\bdatum\w*|\bdata\w*|\banaly\w*|\bassess\w*|\bmanage\w*|\bmonitor\w*|\binvestigat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'decision analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for stata analysis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstata\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'stata analysis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sas analysis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsas\s+program\w*', text)
        match_2 = o(r'\bsas\b', text)
        match_3 = o(r'\bdata\b|\bdatum\w*|\bmodel\w*|\bdataset\w*|\bsoftware\w*|\bexpereince\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sas analysis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for spss analysis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspss\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'spss analysis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for e-views analysis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\beview[s]?\b|\be-view[s]?\b|\be\s+-\s+view[s]?\b|\be\s+view[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'e-views analysis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer aided facility management software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcafm\b|\bcomputer\s+aid[e]?[d]?\s+facility\s+manage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer aided facility management software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft power platforms software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpower\s+platforms\b', text)
        match_2 = o(r'\bpower\s+platform\b', text)
        match_3 = o(r'\bpowerbi\w*|\bpower\s+bi\w*|\bmicrosoft\w*|\bms\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft power platforms software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for itwin platform
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bitwin\s+platform\w*|\bitwin\s+services\w*', text)
        match_2 = o(r'\bitwin\w*', text)
        match_3 = o(r'\bbentley\b|\bdigital\s+twin\w*|\bdigitaltwin\w*|\bapi\b|\bplatform\w*|\bexperience\w*|\bservice\w*|\bknowledge\w*|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'itwin platform'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for esri software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\besri\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'esri software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for arcgis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barcgis\w*|\barc\s+gis\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'arcgis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for base mapping
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbase\s+map\w*|\bweb-base\s+map\w*|\bwebbase\s+map\w*|\bweb\s+-\s+base\s+map\w*|\bweb\s+map\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'base mapping'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microarchitectures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicroarchitecture\w*|\bmicro\s+architecture\w*|\bmicro-architecture\w*|\bmicro\s+-\s+architecture\w*|\bcomputer\s+organization(?! skill)\w*|\bcomputer\s+organisation(?! skill)\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microarchitectures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geoserver
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeoserver\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geoserver'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for javascript
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjavascript\w*|\bes6\b|\bes5\b|\beslint\w*|\bjquery\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'javascript'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for model view and controller framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmvc\b', text)
        match_2 = o(r'\bjavascript\w*|\bjs\b|\bframework\w*|\blibrary\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'model view and controller framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for symfony framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsymfony\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'symfony framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for zend framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bzend\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'zend framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laravel frwamework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaravel\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laravel frwamework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sencha framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsencha\b', text)
        match_2 = o(r'\bjavascript\w*|\bjs\b|\bframework\w*|\blibrary\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sencha framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for vue.js framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvuejs\b|\bvue\s+js\b|\bvue\.js\b|\bvue\s+\.js\b|\bvue\.\s+js\b|\bvue\s+\.\s+js\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'vue.js framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for graphic design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgraphic[a]?[l]?\b', text)
        match_2 = o(r'\bdesign\b|\bdesigning\w*|\bdesigner\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'graphic design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for grlweap software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgrlweap\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'grlweap software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for postgresql database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpostgresql\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'postgresql database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rest api
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brest\b|\brestful\b', text)
        match_2 = o(r'\bapi\b|\bapplication\s+programming\s+software\w*|\bprogramming\w*|\bsoftware\s+develop\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rest api'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for graphql api
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgraphql\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'graphql api'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft dataverse
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatavers[e]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft dataverse'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for user experience design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bux\s+design\w*', text)
        match_2 = o(r'\buser\s+experience\w*|\bux\b|\badobe\s+xd\w*', text)
        match_3 = o(r'\bprogramming\w*|\bcode\b|\bsoftware\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+software\w*|\bweb\s+develop\w*|\bwebsite\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+website\w*|\bapp\s+develop\w*|\bapplication\s+develop\w*|\bdevelop[i]?[n]?[g]?\s+app\w*|\bdesign\w*|\bmanage\w*|\bcreate\w*|\bmanaging\w*|\bcreating\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'user experience design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adobe illustrator
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badobe\s+illustrator\w*|\bphotoshop\s+illustrator\w*', text)
        match_2 = o(r'\badobe\b', text)
        match_3 = o(r'\bphotoshop\w*|\bxd\b|\bcreative\s+cloud\w*|\billustrator\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'adobe illustrator'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adobe xd
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badobe\s+xd\w*', text)
        match_2 = o(r'\badobe\b', text)
        match_3 = o(r'\bphotoshop\w*|\bilustrator\b|\bcreative\s+cloud\w*', text)
        match_4 = o(r'\bxd\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'adobe xd'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for asset management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanage\w*|\bmanaging\w*|\boversee\w*|\boversight\w*|\bmonitor\w*', text)
        match_2 = o(r'\basset\w*', text)
        match_3 = o(r'\bmaintenance\w*\|\bmaintain\w*|\bplant\w*|\bengineer\w*|\binspect\w*|\binvestigat\w*', text)
        if match_1 and match_2 and not match_3:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'asset management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for structural engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstructur[e]?[a]?[l]?\s+engineer\w*|\bstructural\s+design\w*|\bdesign[i]?[n]?[g]?\s+structur\w*|\bdesign[i]?[n]?[g]?\s+\w+\s+structur\w*', text)
        match_2 = o(r'\bweight\s+control\w*|\bstructural\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'structural engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for multi-stakeholder governance
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmultiple\s+stakeholder\w*|\bmulti\s+stakeholder\w*|\bmulti-stakeholder\w*|\bmulti\s+-\s+stakeholder\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'multi-stakeholder governance'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cdegs software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcdeg[s]?\b', text)
        match_2 = o(r'\belectric\w*|\bgrid\b|\bpower\b|\benergy\b|\btransmission\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cdegs software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for powerfactory software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpowerfactory\b|\bbdigsilent\b', text)
        match_2 = o(r'\belectric\w*|\bgrid\b|\bpower\b|\benergy\b|\btransmission\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'powerfactory software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emtdc software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemtdc\b', text)
        match_2 = o(r'\belectric\w*|\bgrid\b|\bpower\b|\benergy\b|\btransmission\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emtdc software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emtp software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemtp\b', text)
        match_2 = o(r'\belectric\w*|\bgrid\b|\bpower\b|\benergy\b|\btransmission\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emtp software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pscad software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpscad\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pscad software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for etap analysis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\betap\b', text)
        match_2 = o(r'\belectric\w*|\belectronic\w*|\bcircuit\w*|\bpower\b|\bgrid\b|\btransmission\b|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'etap analysis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aquatic species
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'aquatic\s+specie\w*|\bmarine\s+specie\w*|\bcrustacean[s]?\b|\bmollusc[s]?\b|\bsea\s+specie\w*|\bocean\s+specie\w*', text)
        match_2 = o(r'\bspecie\w*', text)
        match_3 = o(r'\bmarine\w*|\bsea\b|\bocean\b|\baquatic\w*|\bfish\w*|(?<!charles )\briver\w*|\blake\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aquatic species'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plankton production
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplankton\w*|\bphytoplankton\w*|\bzooplankton\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'plankton production'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fish biology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfish\s+science\w*|\bfish\s+biology\w*|\bbiology\s+fish\w*|\bscience\s+fish\w*|\bichthyology\w*', text)
        match_2 = o(r'\bfish\w*', text)
        match_3 = o(r'\becolog\w*|\biolog\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fish biology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for benthic zones
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbenthic\w*', text)
        match_2 = o(r'\bexperiment\w*|\bresearch\w*|\bstudy\w*|\bdata\b|\bdatum\w*|\blaboratory\w*|\blab\b|\bzone\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'benthic zones'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for alternative fuels
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\balternative\s+fuel\w*|\bhydrogen\w*|\bnatural\s+gas\w*|\bpropane\w*|\bbiodiesel\w*|\behanol\w*|\brenewable\s+diesel\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'alternative fuels'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for en eurocodes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\beurocode[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'en eurocodes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for facilities management in the organisation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfacilities\s+management\w*|\bfacility\s+management\w*', text)
        match_2 = o(r'\bfm\b', text)
        match_3 = o(r'\bfacilit[y]?[i]?[e]?[s]?\w*|\bperform\w*|\borganisation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'facilities management in the organisation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for linsig software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blinsig\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'linsig software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for trics software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btrics\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'trics software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for xero accounting software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bxero\b', text)
        match_2 = o(r'\baccounting\w*|\baccountant\w*|\bbook\s?[-]?\s?keep\w*|\boffice\s+administrat\w*|\boffice\s+clerk\w*|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'xero accounting software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for proarc document management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproarc\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'proarc document management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mapinfo software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmapinfo\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mapinfo software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for qgis software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqgis\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'qgis software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for indesign software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bindesign\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'indesign software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cads rc3d software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcads\s+-\s+rc\b|\bcads\s+rc\b|\bcads-rc\b|\bcads\s+rc3d\b|\b3d\s+rc\b|\b3-d\s+rc\b|\b3\s+-\s+d\s+rc\b', text)
        match_2 = o(r'\brc\b', text)
        match_3 = o(r'\bcad[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cads rc3d software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aecosim software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baecosim\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aecosim software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for google docs
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdoc[s]?\b', text)
        match_2 = o(r'\bgoogle\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'google docs'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for google sheets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsheet\b|\bsheets\b', text)
        match_2 = o(r'\bgoogle\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'google sheets'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for eiva software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\beiva\b', text)
        match_2 = o(r'\bmap\w*|\btopograph\w*|\bgeomorpholog\w*|\blaser\w*|\bdgps\b|\btotal\s+station\w*|\bsurvey\w*|\bsoftware\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'eiva software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for multibeam echosounder system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmbes\b|\bmulti\s?[-]?\s?beam\s+echo\s?sounder\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'multibeam echosounder system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for differential gps system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdgps\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'differential gps system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for real-time kinematic positioning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brtk\b', text)
        match_2 = o(r'\bdgps\w*|\bpositioning\w*|\btopograph\w*|\bgeomorpholog\w*|\blaser\w*|\btotal\s+station\w*|\bsurvey\w*|\bexperience\w*|\bknowledge\w*|\buse\w*|\bperform\w*|\bconduct\w*|\btest\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'real-time kinematic positioning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for metallurgy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmetallurg\w*|\bpropert[y]?[i]?[e]?[s]?\s+metal\w*|\bpopert[y]?[i]?[e]?[s]?\s+intermetal\w*|\bmetal[l]?[i]?[c]?\s+propert\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'metallurgy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tyvek material
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btyvek\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tyvek material'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer-aided manufacturing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcomputer\s+[-]?\s+aided\s+manufactur\w*', text)
        match_2 = o(r'\bcam\b', text)
        match_3 = o(r'\benginner\w*|\bdesign\w*|\bcad\b|\bexperience\w*|\bknowledge\w*|\bapply\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer-aided manufacturing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for windows presentation framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwpf\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'windows presentation framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fortran
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfortran\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fortran'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for c programming language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!\bgrade)(?<!\bgrade )(?<!\bgrade  )(?<!\bband)(?<!\bband )(?<!\bband  )\bc\b(?!\s*(\+|\#|sharp|eng|\+\+|\+\s+\+))', text)
        match_2 = o(r'\bprogramming\w*|\bcode\w*|\bcoding\w*|\bsoftware\w*|\bapp[s]?\b|\bdeveloper\w*|\blanguage\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'c programming language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for c sharp
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bc\s?#|\bc\s?sharp|\bc#', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'c sharp'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for c+
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bc\s?\+(?!\s?\+)', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'c+'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for c++
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bc\s*\+\s*\+\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'c++'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for architectural code
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barchitectur\w*', text)
        match_2 = o(r'\bcode\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'architectural code'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ventilation systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bventilation\s+system\w*|\blev\b|\bhvac\b|\bmechanical\s+ventilation\w*|\bsystem[s]?\s+ventilation\w*|\bventilation\s+flow\s+diagram\w*|\bfan\s+siz[e]?[i]?[n]?[g]?\b', text)
        match_2 = o(r'\bvfd\w*|\bbod\b', text)
        match_3 = o(r'\bventilation\w*|\bsystem\w*|\bexperience\w*|\bknowledge\w*', text)
        match_4 = o(r'\bcable\s+design\w*|\bcable\s+system\w*|\bcable\s+transmission\w*|\bhvdc\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ventilation systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for building research establishment environmental assessment method
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbreeam\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'building research establishment environmental assessment method'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for capital improvement plan
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcapital\s+improvement\s+plan\w*', text)
        match_2 = o(r'\bcip\b', text)
        match_3 = o(r'\bfinance\w*|\bfinancial\w*|\bbusiness\w*|\bcompany\w*|\bcapital\w*|\baccount\w*|\bprepare\w*|\bmanage\w*|\bmanaging\w*|\bcreate\w*|\bcreating\w*|\bdesign\w*|\bdesigning\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'capital improvement plan'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for single use technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsingle\s+use\s+technology\w*|\bsingle\s+use\s+system\w*|\bsingle\s+use\s+techniq\w*', text)
        match_2 = o(r'\bsut\b', text)
        match_3 = o(r'\bsystem\w*|\btechnology\w*|\bexperience\w*|\bknowledge\w*', text)
        match_4 = o(r'\bsingle\s+use\w*', text)
        match_5 = o(r'\btechnology\w*|\bsystem\w*|\btechnique\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_2 and match_4 and match_5:
            matched_fragments.extend([match_2, match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'single use technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for legal terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blegal\b|\blegislature\b|\blegislation\w*|\blawyer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'legal terminology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for medical terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmedical\s+writing\b|\bmedical\s+terminology\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'medical terminology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for environmental legislation and policy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+environment[a]?[l]?\s+datum\w*|\bmanag[e]?[i]?[n]?[g]?\s+environment[a]?[l]?\s+data\b|\benvironment[a]?[l]?\s+quality\w*|\benvironment[a]?[l]?\s+consult\w*|\benvironment[a]?[l]?\s+permit\w*', text)
        match_2 = o(r'(?<!global )\benvironment\w*', text)
        match_3 = o(r'\blaw\b|\bpolicy\b|\bpolicies\w*|\bcompliance\w*|\bregulation\w*|\brequirement\w*|\blegislation\w*|\blegislature\w*|\blawyer\w*|\blegal\w*|\bguideline\w*|\blitigation\w*', text)
        match_4 = o(r'\bwfd\b|\bhra\b|\bhras\b|\bmcz\b|\benvid\b|\bbat\b|\bbpeo\w*|\beia\b|\besia\b|\beias\b|\becias\b|\berms\b|\bcedm\b', text)
        match_5 = o(r'\blaw\b|\blegislation\w*|\bregulation\w*|\bguideline\w*|\blitigation\w*|\bpolicy\w*|\bpolicies\w*|\bassess\w*|\buse\w*|\butilise\w*|\bmanage\w*|\bmanaging\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'environmental legislation and policy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for crofting regulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcroft[i]?[n]?[g]?\s+regulation\w*|\bcroft[i]?[n]?[g]?\s+regulation\w*|\bcroft[i]?[n]?[g]?\s+law\w*|\bcroft[i]?[n]?[g]?\s+guideline\w*|\bcroft[i]?[n]?[g]?\s+legislation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'crofting regulation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for forestry regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforester\w*|\bforestry\w*', text)
        match_2 = o(r'\blaw\b|\bregulation\w*|\blegislation\w*|\blitigation\w*|\bguideline\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'forestry regulations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for commercial law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcommercial\s+contract\w*|\bcommercial\s+law\w*|\bcommercial\s+legal\b|\blitigation\w*|commercial\s+negotiat\w*|\bcommercial\s+lawyer\w*|\bcommercial\s+risk\w*|\bcontractual\s+commercial\w*|\bbusiness\s+law\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'commercial law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for carbon emissions management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcarbon\s+law[s]?\b|\bcarbon\s+policy\w*|\bcarbon\s+policies\w*|\bcarbon\s+regulation\w*|\bcarbon\s+guideline\w*|\bcarbon\s+audit\w*|\bcarbon\s+limit\w*|\blimit[i]?[n]?[g]?\s+carbon\w*|\bcarbon\s+reqirement\w*|\bcarbon\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+carbon\w*|\bcarbon\s+regulat\w*|\bregulate\s+carbon\w*|\breduce\s+carbon\w*|\breduction\s+carbon\w*|\bcarbon\s+engineer\w*|\bccus\b', text)
        match_2 = o(r'\bemisson\s+law\w*|\bemission\s+regulat\w*|\bemission\s+legislation\w*|\bregulate\s+emission\w*|\bmanag[e]?[i]?[n]?[g]?\s+emission\w*|\bemission\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+emission\w*|\bregulate\s+\w+\s+emission\w*', text)
        match_3 = o(r'\bcarbon\w*|\bccs\b|\bclimate\s+change\w*|\bgreenhouse\w*|\bghg\b|\baudit\w*', text)
        match_4 = o(r'\bcarbon\s+calculator\w*|\baudit[i]?[n]?[g]?\s+carbon\w*|\bcarbon\s+\w+\s+law[s]?\b|\bcarbon\s+\w+\s+policy\w*|\bcarbon\s+\w+\s+policies\w*|\bcarbon\s+\w+\s+regulation\w*|\bcarbon\s+\w+\s+guideline\w*', text)
        match_5 = o(r'\bccs\b|\badhere\w*|\bcomply\w*|\bcomplinace\w*|\bconduct\w*|\bperform\w*|\bmanage\w*|\bmanaging\w*|\bassess\w*|\baccord\w*|\bfollow\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'carbon emissions management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for contract law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproposal[s]?\s+engineer\w*|\bcontract[s]?\s+advisor\w*|\bcommercial\s+contract\w*|\blegal\s+contract\w*|\bcontract[s]?\s+advice\w*|\bclient\s+contract[s]?\b|\bcontract\s+finance\b|\bcontract[s]?\s+specialist\w*|\bcustomer[s]?\s+contract\w*|\bcontract[s]?\s+specialist\w*|\bsale[s]?\s+contract\w*|\bpurchase\s+agreement\w*|\bcontract[s]?\s+include\w*|\bcontract\s+document\w*|\bnec\s+[s]?[u]?[b]?\s?[-]?\s?contract[s]?\b|\bjoas\b|\bnec1\b|\bnec2\b|\bnec3\b|\bnec4\w*|\bnec\s+1\b|\bnec\s+2\b|\bnec\s+3\b|\bnec\s+4\b|\bcdrls\b|\bsdrls\b|\bnec\s+option\s+contract\w*|\bcontract\s+data\s+requirement\w*|\bcontract\s+datum\s+requirement\w*|\bsubcontract\s+data\s+requirement\w*|\bsub\s+-\s+contract\s+data\s+requirement\w*|\bsub\s+contract\s+data\s+requirement\w*|\bsubcontract\s+datum\s+requirement\w*|\bsub\s+-\s+contract\s+datum\s+requirement\w*|\bsub\s+contract\s+datum\s+requirement\w*|\bnec\s+option\s+\w+\s+contract\w*', text)
        match_2 = o(r'\bcontract\b|\bcontracts\b|\bcontractual\b', text)
        match_3 = o(r'\blaw\b|\bregulation\w*|\blawyer\b|\blegislation\w*|\blitigation\w*|\blegal\w*|\bguideline\w*|\brequirement\w*|\badvice\w*|\bguidance\w*|\badvisor\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'contract law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for corporate law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcorporate\s+policy\b|\bcorporate\s+law\w*|\bcorporate\s+tax\b|\bcorporate\s+taxes\w*|\bcorporate\s+counsel\w*|\bcorporate\s+regulat\w*|\bcorporate\s+legislation\w*|\bcorporate\s+guideline\w*|\bcorporate\s+governance\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'corporate law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for european banking regulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\beuropean\s+banking\w*|\beuropean\s+tax\w*|\beuropean\s+accounting\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'european banking regulation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data analytics and science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatum\s+science\w*|\bdata\s+science\w*|\bdata\s+analy\w*|\bdatum\s+analy\w*|\banalysis\s+data\b|\banalysis\s+datum\w*|\banalyse\s+data\b|\banalyse\s+datum\w*|\banalyze\s+data\b|\banalyze\s+datum\w*|\binterpret[a]?[t]?[i]?[o]?[n]?\s+datum\w*|\bdatum\s+interpret\w*|\banalyse\s+\w+\s+datum\w*|\banalysis\s+\w+\s+datum\w*|\banalyze\s+\w+\s+data\b|\banalyse\s+\w+\s+data\b|\banalysis\s+\w+\s+data\b|\banalyze\s+\w+\s+data\b|\binterpret\s+\w+\s+datum\w*', text)
        match_2 = o(r'\binterpret\w*|\banaly\w*|\binvestigat\w*', text)
        match_3 = o(r'\bdata\b|\bdatum\w*|\bdataset\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data analytics and science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data warehouse
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+warehouse\w*|\bdatum\s+warehouse\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data warehouse'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data protection policies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+protection\s+act\w*|\bdatum\s+protection\s+act\w*|\bdatum\s+protection\s+legislation\w*|\bdatum\s+protection\s+regulation\w*|\bdata\s+protection\s+legislation\w*|\bdata\s+protection\s+regulation\w*|\bgdpr\b|\bnist\b|\bhitech\b', text)
        match_2 = o(r'\bdata\s+protection\w*|\bdatum\s+protection\w*|\bdata\s+\w+\s+protection\w*|\bdatum\s+\w+\s+protection\w*|\bcybersecurity\w*|\bcyber\s+security\w*|\binformation\s+security\w*|\bdatum\s+loss\w*|\bdata\s+loss\w*', text)
        match_3 = o(r'\bphi\b|\bpii\b|\bregulated\s+data\w*|\bregulated\s+datum\w*|\bpci\b|\biso\b|\bdss\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data protection policies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data protection
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdata\s+protect\w*|\bdatum\s+protect\w*|\bdata\s+\w+\s+protect\w*|\bbdatum\s+\w+\s+protect\w*|\bdata\s+safe\w*|\bdatum\s+safety\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data protection'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for data loss prevention
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bprevent\w*|\bexperience\w*|\bknowledge\w*', text)
        match_2 = o(r'\bdlt\b|\bdlp\b|\bdata\s+loss\w*|\bdatum\s+loss\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'data loss prevention'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for working with data from emea
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemea\s+specific\b', text)
        match_2 = o(r'\bdatum\w*|\bdata\w*', text)
        match_3 = o(r'\bemea\s+data\w*|\bemea\s+datum\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'working with data from emea'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for intranet
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintranet\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'intranet systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for social media management and marketing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsocial\s+medium\w*|\bsocial\s+media\w*', text)
        match_2 = o(r'\bmarketing\b|\bmanagement\b|\bmanage\b|\bcampaign\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'social media management and marketing strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sarbanes-oxley requirements
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boxley\b\s+requirement\w*|\bknowledge\s+sox\b|\bexperience\s+sox\w*|\bexposure\s+sox\b', text)
        match_2 = o(r'\bsox\b', text)
        match_3 = o(r'\baccount\w*|\bbookkeep\w*|\btax\b|\btaxation\b|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sarbanes-oxley requirements'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for credit control processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcredit\s+limit\w*|\bcredit\s+control\w*|\bmanage\s+credit\w*|\bcredit\s+manage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'credit control processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for equal credit opportunity act
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becoa\b', text)
        match_2 = o(r'\blaw\b|\blegislation\b|\bpolicy\w*|\bpayment\w*|\bguideline\w*|\bregulation\w*|\bpolicy\b|\bpolicies\b|\bknowledge\w*|\bexperience\w*|\brequirement\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'equal credit opportunity act'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for credit card payments
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcredit\s+card\s+transaction\w*|\bcredit\s+card\s+payment\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'credit card payments'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for product or project lifecycle management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplm\b|\bproduct\s+lifecycle\w*|\bproduct\s+life\s+cycle\w*|\bproduct\s+life\s+-\s+cycle\w*|\bproduct\s+release\s+plan\w*|\bproject\s+lifecycle\w*|\bproject\s+life\s+cycle\w*|\bproject\s+life\s+-\s+cycle\w*|\basset\s+life\s+cycle\w*|\basset\s+lifecycle\w*|\basset\s+life\s+-\s+cycle\w*', text)
        match_2 = o(r'\blcm\b', text)
        match_3 = o(r'\bresearch\w*|\bproject\b|\bproduct\b|\bproducts\b|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'product or project lifecycle management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for project management and its principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanag[e]?[i]?[n]?[g]?\s+\bproject\w*|\bmanagement\s+project\w*|(?<!data )\bproject\s+manage\w*|\bgenerat[e]?[i]?[n]?[g]?\s+tor\b|\bgenerat[e]?[i]?[n]?[g]?\s+term[s]?\s+refine\w*|\bproject\s+engineer\w*|\blead[i]?[n]?[g]?\s+project\w*|\boversee[i]?[n]?[g]?\s+project\w*|\bsite\s+lead\w*|\bprepare\s+project\s+plan\w*|\bproject\s?[/]?\s?engineer[i]?[n]?[g]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+multiple\s+project\w*|\bcoordinat[e]?[i]?[n]?[g]?\s+project\w*|\bordinat[e]?[i]?[n]?[g]?\s+project\w*|\bproject\s+coordinat\w*|\bproject\s+co\s+-\s+ordinat\w*|\bproject\s+co-ordinat\w*|\bproject\s+planning\w*|\bproject\s+plan\s+control\w*|\bproject\s+plan\s+schedule\w*|\bproject\s+delivery\s+strategy\w*|\bmanag[e]?[i]?[n]?[g]?\s+multi\s+-\s+disciplinary\s+project\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+project\w*|\blead[i]?[n]?[g]?\s+\w+\s+project\w*|\boversee[i]?[n]?[g]?\s+\w+\s+project\w*|\bgenerat[e]?[i]?[n]?[g]?\s+\w+\s+tor\b|\bgenerat[e]?[i]?[n]?[g]?\s+\w+\s+term[s]?\s+refine\w*', text)
        match_2 = o(r'(?<!datum )\bproject\s+manage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2:
            matched_fragments.extend([match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'project management and its principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for diary management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdiary\w*', text)
        match_2 = o(r'\bmanage\w*|\bprepare\w*|\bmaintain\w*|\bperparation\w*|\bmanaging\w*|\bwrite\w*|\bwriting\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'diary management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for project management office
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+management\s+office\w*', text)
        match_2 = o(r'\bpmo\b', text)
        match_3 = o(r'\bmanage\w*|\bproject\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'project management office'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for line management and its principals
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!one )\bline\b\s+\bmanage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'line management and its principals'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for agile project management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproject\s+manage\w*|\bproduct\s+manage\w*|\bproject\s+methodolog\w*', text)
        match_2 = o(r'\bagile\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'agile project management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for agile development or methodologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagile\s+development\w*|\bagile\s+model\w*|\bagile\s+framwework\w*|\bagile\s+coach\w*|\bagility\s+method\w*|\bagile\s+method\w*|\bagile\s+method\w*|\bagile\s+techniques\w*|\bagile\s+software\s+developemnt\w*|\bknowledge\s+agile\w*|\bexperience\s+agile\w*|\bagile\s+experience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'agile development or methodologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for agile product lifecycle management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplm\b\s+\bagile\w*|\bagile\s+\bplm\b', text)
        match_2 = o(r'\bproduct\s+manage\w*|\bplm\b|\bproduct\s+lifecycle\w*', text)
        match_3 = o(r'\bagile\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'agile product lifecycle management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cloud technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcloud\s+service\w*|\bcloud\s+infrastructure\b|\bcloud\s+architecture\w*|\bcloud\s+software\w*', text)
        match_2 = o(r'\bcloud\b', text)
        match_3 = o(r'\bsoftware\w*|\bsystem\w*|\bprogramming\w*|\bdeveloper\w*|\baws\b|\boracle\b|\btechnology\w*|\barchitect\w*|\bserver\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cloud technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oracle cloud infrastructure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boracle\b\s+cloud\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oracle cloud infrastructure'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for change control process
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchange\s+control\s+workflow\w*|\bchange\s+control\s+process\w*|\bchange\s+control\s+management\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'change control process'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for programmable logic devices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprogramm[e]?[a]?[b]?[l]?[e]?\s+logic\s+digital\b|\bprogramm[e]?[a]?[b]?[l]?[e]?\s+logic\s+analogue\b|\bprogramm[e]?[a]?[b]?[l]?[e]?\s+logic\s+device\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'programmable logic devices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for simulation software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsimulation\s+package\w*|\bsimulation\s+software\w*|\bsimulation\s+program\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'simulation software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for circuit diagrams
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcircuit\b|\bcircuitry\b', text)
        match_2 = o(r'\belectronic\w*|\belectric\w*|\bassembl\w*|\bprogram\w*|\brepair\w*|\bdiagram\w*|\bcable\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'circuit diagrams'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for firmware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfirmware\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'firmware'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for policy analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bassess\w*|\banaly\w*|\binvestigat\w*|\bmonitor\w*|\binspect\w*|\bchange\w*|\breview\w*|\bguidance\w*|\bconsult\w*|\badvice\w*|\badvise\w*', text)
        match_2 = o(r'\bpolicy\w*|\bpolices\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'policy analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for debt collection techniques and systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcollection\s+debt\w*|\bdebt\s+collection\w*|\bcredit\s+collection\w*|\bcollect\s+credit\w*|\bcollect\s+loan\w*|\bfield\s+debt\s+solution\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'debt collection techniques and systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for debt classification
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\ballocation\s+debt\w*|\ballocate\s+debt\w*|\bdebt\s+allocation\w*|\bdebt\s+classification\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'debt classification'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for document management systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bedms\b|\bdms\b|\bsdms\b|\bdocument[s]?\s+management\s+system\w*|\bdocument[s]?\s+management\s+software\w*|\bdocument[s]?\s+control[s]?\s+system\w*|\bdocument[s]?\s+control\s+software\w*|\belectronic\s+file[s]?\s+manag\w*|\belectronic\s+document[s]?\s+manage\w*|\belectronic\s+paperwork\s+manage\w*|\belectronic\s+file\s+system\w*|\bdata\s+management\s+system\w*|\bdata\s+management\s+software\w*|\bdatum\s+management\s+system\w*|\bdatum\s+management\s+software\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'document management systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical examinations in dietetics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+nutrition(?! product)\w*', text)
        match_2 = o(r'\bdietetic\w*|\bdiet\w*', text)
        match_3 = o(r'\bclinical\w*|\bexamine\w*|\bstudy\w*|\bresearch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical examinations in dietetics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dietetics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdietetic[s]?\b', text)
        match_2 = o(r'\bnutrition\w*|\barrange\w*|\blist\w*', text)
        match_3 = o(r'\bdiet\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'dietetics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for techniques of blood sampling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphebotomist\b|\bphlebotomy\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'techniques of blood sampling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for home decoration techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdecorator\b', text)
        match_2 = o(r'\bhome\w*', text)
        match_3 = o(r'\bdecorat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'home decoration techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for computer aided design software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bcreo\w*|\bmicrostation\b|\bautodesk\b|\bcivil\s+3d\b|\bcomputer\s+aid[e]?[d]?\s+design\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'computer aided design software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for 3d modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'3d\s+model\w*|\b3\s+-\s+model\w*|\b3d\s+developer\w*|\b3d\s+tool\w*|\b3d\s+design\w*|\b3d\s+finite\s+element\w*|\b3d\s+\w+\s+tool\w*|\b3d\s+\w+\s+model\w*', text)
        match_2 = o(r'\b3d\b|\bdetailing\s+modelling\w*', text)
        match_3 = o(r'\bcad\b|\bautocad\b|\bcadd\b|\bmathcad\w*|\bacad\b|\bcreo\b|\bmicrostation\b|\bexperience\w*|\bsoftware\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = '3d modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cardiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcardiology\b|\bcardiologist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cardiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for welding techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwelder\b|\bwelding\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'welding techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for metal joining techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjoiner\b|\bweld\b|\bwelder\w*', text)
        match_2 = o(r'\bmetal\b|\bmetals\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'metal joining techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of wood
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjoiner\b|\btype\w*|\bexperience\w*|\bknowledge\w*|\bproduct\w*', text)
        match_2 = o(r'\bwood\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of wood'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for waste management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwaste\s+assessor\w*|\bwaste\s+assessment\w*|\bmanag[e]?[i]?[n]?[g]?\s+waste\w*|\bwaste\s+manage\w*|\breduce\s+waste\w*|\bwaste\s+disposal\w*|\bwaste\s+transfer\s+disposal\w*|\bredundant\s+material\w*|\bbiological\s+waste\w*|\bdisposal\s+waste\w*|\bdisposal\s+\w+\s+waste\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'waste management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pricing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpricing\s+tactic\w*|\bpricing\s+strateg\w*|\bpricing\s+analys\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pricing strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sourcing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'sourcing\s+solution[s]?\b|sourcing\s+strateg[y]?[i]?[e]?[s]?\w*|\bstrategic\s+insourcing\w*|sourcing\s+process\w*|\bstrategic\s+sourcing\w*|\bstrategic\s+outsourcing\w*|sourcing\s+process\w*|sourcing\s+specialist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sourcing strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for warehouse operations and safety regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgneral\s+warehouse\s+opeation\w*|\bwarehouse\s+operative\w*|\bwarehouseing\s+operative\w*|\bwarehouse\s+activit\w*', text)
        match_2 = o(r'\bwarehouse\w*', text)
        match_3 = o(r'\bsafety\w*|\bhs\b|\boperation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'warehouse operations and safety regulations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hazardous waste treatment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonitor\s+gas\s+leachate\w*|\bbiological\s+waste\w*', text)
        match_2 = o(r'\bhazard\w*|\bdanger\w*', text)
        match_3 = o(r'\bwaste\w*', text)
        match_4 = o(r'\bdisposal\w*|\btreatment\w*|\bmanage\w*|\bassess\w*|\bprocess\w*|\bprocedure\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hazardous waste treatment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cleaning techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcaretaker\w*|\bclean[i]?[n]?[g]\s+service\w*|\bclean[i]?[n]?[g]?\s+floor\w*|\bdusting\w*|\bscrubbing\w*|\bsweeping\w*|\bmopping\w*|\bwaxing\w*|\bbuffing\w*|\bclean[i]?[n]?[g]?\s+experience\w*|\bclean[i]?[n]?[g]?\s+\w+\s+experience\w*', text)
        match_2 = o(r'\bcleaning\w*|\bclean\w*', text)
        match_3 = o(r'\bwarehouse\w*|\bglassware\w*|\bsurface\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cleaning techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for housekeeping
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhousekeep\w*|\bhouse\s+keep\w*|\bhouse\s?[-]?\s?keep\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'housekeeping'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geomagnetism
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeomagnetism\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geomagnetism'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pharmacokinetics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpharmacokinetic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pharmacokinetics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for epidemiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bepidemiology\b', text)
        match_2 = o(r'\bpathogen\w*', text)
        match_3 = o(r'\bgenome\b|\bcell\b|\bgenetic\b|\bmolecular\b|\bstudy\w*|\banaly\w*|\bresearch\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'epidemiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for immunochemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunochemistry\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'immunochemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for immunobiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunobiology\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'immunobiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biogeochemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiogeochemist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biogeochemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for toxicology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btoxicology\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'toxicology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pharmacology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpharmacology\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pharmacology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for arboriculture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\barboriculture \b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'arboriculture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for psychiatry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpsychiatry\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'psychiatry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cardiovascular system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcardiovascular\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cardiovascular system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for respiratory medicine
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brespiratory\b|\blung\s+diseae\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'respiratory medicine'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for central nervous system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcns\b', text)
        match_2 = o(r'\bcardiovascular\w*|\brespiratory\b|\bknowledge\w*|\bexperience\w*|\bsystem\w*|\bneurolog\w*', text)
        match_3 = o(r'\bcentral\s+nervous\s+system', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3:
            matched_fragments.extend([match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'central nervous system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gut microbiome
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrobiome\w*', text)
        match_2 = o(r'\bstomach\w*|\bgut\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gut microbiome'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quantum mechanics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquantum\s+mechanic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'quantum mechanics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for photonics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphotonics\w*|\btransmission\s+spectr\w*|\boptical\s+waveguide\w*|\binsertion\s+loss\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'photonics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quantum optics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquantum\s+optics\w*|\bquantum\s+optical\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'quantum optics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electron lithography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blitograph\w*', text)
        match_2 = o(r'\bbeam\w*|\bebeam\w*|\belectron\w*|\bsilicon\w*|\boptic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electron lithography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for optics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boptics\w*|\boptic[a]?[l]?\s+lab\w*|\boptic[a]?[l]?\s+waveguide\w*|\bwaveguide\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'optics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for nanotechnology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmolecular\s+technolog\w*|\bnanotechnolog\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'nanotechnology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for metabolism
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmetabolism\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'metabolism'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for skin types and qualities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bskin\s+absorption\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'skin types and qualities'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for stem cells
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpluripotent\s+cell\b|\bstem\s+cell\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'stem cells'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for histology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhistology\b|\bhistological\s+sample\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'histology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pathology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpathology\b|\bpathological\s+sample\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pathology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical cytology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmedical\s+cytolog\w*|\bclinical\s+cytolog\w*', text)
        match_2 = o(r'\bcell\s+therapy\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*|\bperform\w*|\bdesign\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical cytology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cytopathology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcytopathology\b', text)
        match_2 = o(r'\bcytology\b', text)
        match_3 = o(r'\bpathology\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cytopathology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for immunotherapy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunotherapy\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'immunotherapy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for neuroscience
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bneuroscience\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'neuroscience'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hepatology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliver\s+biology\w*|\bhepatology\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hepatology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for immuno-oncology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmuno\s+oncology\w*|\bimmuno\s+-\s+oncology\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'immuno-oncology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for oncology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcancer\s+research\w*|\boncology\w*|\bcancer\s+science\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'oncology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for transplantation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransplantation\w*|\btransplant\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'transplantation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for urology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\burolog\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'urology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for parasitology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bparasitology\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'parasitology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for bioinformatics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbioinformatic\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'bioinformatics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biophysics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiophysic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biophysics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biochemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\biochemist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biochemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for solid state chemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsolid\s+phase\b|\bsolid\s+state\b', text)
        match_2 = o(r'\bchemical\w*|\bchemist\w*|\bproperty\w*|\bprocess\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'solid state chemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microparticles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicroparticle\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microparticles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fluorophores
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfluorophore\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fluorophores'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for in vitro ADME
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badme\b', text)
        match_2 = o(r'vitro\s?science\w*|\bin\s+-\s+vitro\s+science\w*|vitro\s?stud\w*|\bexperience\w*|\bknowledge\w*|\bstudy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'in vitro ADME'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for in vitro sciences
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'vitro\s?science\w*|\bin\s+-\s+vitro\s+science\w*|vitro\s?stud\w*', text)
        match_2 = o(r'\bvitro\b|\bin\s+-\s+vitro\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*|\bknowledge\w*|\bexperience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'in vitro sciences'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for in vivo sciences
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'vivo\s?science\w*|\bin\s+-\s+vivo\s+science\w*|\bvivo\s?stud\w*', text)
        match_2 = o(r'\bvivo\b|\bin\s+-\s+vivo\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'in vivo sciences'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for molecular biology and microbiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmolecular\s+biology\w*|\bmicrobiolog\w*|\bmicroorganis\w*|\bmicro\s+organism\w*|\bmicro\s+[-]?\s+organism\w*|\bmicrobial\w*|\bexperience\s+molecular\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'molecular biology and microbiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for molecular synthetic biology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmolecular\s+synthetic\s+biology\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'molecular synthetic biology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiology\b|\bbiologist\b|\bbiological\b|\bmicrobiolog\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeology\w*|\bgeologist\w*|\bgeoscience\w*|\bgeomechanic\w*|\bgeological\s+science\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geomechanics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgeomechanic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geomechanics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for physics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphysic\b|\bphysics\b|\bphysicist\b|\bbiophycis\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'physics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for chemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchemistry\b|\bchemist\b|\bbiochemist\w*|\bbiogeochemist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'chemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for geography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!multiple )\bgeography\b|\bbiogeochemist\w*|\bgeomechanic\w*', text)
        match_2 = o(r'\bmarketing\b', text)
        if match_1 and not match_2:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'geography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mathematics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmathematic[s]?\b|\bmath[s]?\b|\bmathematician\b|\bcalculation\b|\bmathematical\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mathematics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pharmaceutical chemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliquid\s+dossage\s+form\w*|\bsolid\s+dossage\s+form\w*|\bmedical\s+chemistry\w*|\bmedicinal\s+chemistry\w*|\bpharmaceutical\s+chemistry\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pharmaceutical chemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for yeast fermentation principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfermentation\w*', text)
        match_2 = o(r'\byeast\w*|\bbeer\w*|\bwine\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'yeast fermentation principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fermentation processes of food
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfermentation\w*', text)
        match_2 = o(r'\bfood\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fermentation processes of food'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fermentation processes of beverages
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfermentation\w*', text)
        match_2 = o(r'\bbevrage\w*|\bdrink\w*|\balcohol\w*|\bwine\w*|\bbeer\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fermentation processes of beverages'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lab scale fermentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfermentation\w*', text)
        match_2 = o(r'\blaboratory\b|\blab\b|\blarge\s+scale\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'lab scale fermentation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for skin absorption properties
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bskin\s+absorption\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'skin absorption properties'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical microbiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrobiolog\w*', text)
        match_2 = o(r'\bclinical\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical microbiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microbiology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrobiolog\w*', text)
        match_2 = o(r'\bcell\b|\bexperiment\w*|\bresearch\w*|\bstudy\w*|\banaly\w*|\bsample\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microbiology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biometrics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiometric\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biometrics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for flow cytometry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bflow\s+cytometer\w*|\bflow\s+cytometry\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'flow cytometry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for analytical chemistry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banalytical\s+chemist\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'analytical chemistry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for proteins and enzymes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprotein\s+characterization\w*', text)
        match_2 = o(r'\bprotein\w*|\benzym\w*', text)
        match_3 = o(r'\bbiology\w*|\bpharmacology\w*|\bchemistry\w*|\bchemical\w*|\blab\b|\blaboratory\w*|clinical\w*|\btest\w*|\bresearch\w*|\bstudy\w*|\bexperience\w*|\bknowledge\w*|\bexperiment\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'proteins and enzymes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sales strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarket\s+sale\s+analys\w*|\bsale\s+strateg\w*|\bsales\s+strateg\w*|\bstrategic\s+sale\w*|\bevaluate\s+sale\w*|\bsale\s+knowledge\w*|\bsales\s+knowledge\w*|\bsale\s+tactic\w*|\bsles\s+tactic\w*|\btactical\s+sale\w*|\btactic\s+sale\w*|\bsale\s+support\w*|\bsales\s+support\w*|\bsupport\s+sale\w*|\bsale\s+marketing\w*|\bsales\s+marketing\w*|\bsell\s+product\w*|\bsell\s+item\w*|\bsell\s+equipment\w*|\bsell\s+device\w*|\bsale\s+pipeline\w*|\bsales\s+pipeline\w*|\bcoordinate\s+sale\w*|\bmanag[e]?[i]?[n]?[g]?\s+sale\w*|\bsale\s+manage\w*|\bsale\s+specialist\w*|\bsale[s]?\s+manage\w*|\bsales\s+specialist\w*|\bsale\s+experience\w*|\bsales\s+experience\w*|\bexperience\s+sale\w*|\bsale\s+track\s+record\w*|\bsales\s+track\s+record\w*|\bsale\s+activity\w*|\bsales\s+activity\w*|\bsale\s+consult\w*|\bsales\s+consult\w*|\bconsult\s+sale\w*|\bequipment\s+sale\w*|\bdevice\s+sale\w*|\bitem\s+sale\w*|\bproduct\s+sale\w*|\bsale[s]?\s+action\s+plan\w*|\bsell\s+\w+\s+equipment\w*|\bsell\s+\w+\s+device\w*|\bsell\s+\w+\s+item\w*|\bsell\s+\w+\s+product\w*', text)
        match_2 = o(r'\bgssm\b', text)
        match_3 = o(r'\bsale\w*|\bmarketing\w*|\bexperience\w*|\bknowledge\w*|\bsoftware\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sales strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for medical sales industry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmedical\s+sale\b|\bmedical\s+sales\b|\bhospital\s+sale\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'medical sales industry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for accounting operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baccounting\b|\bbookkeep\w*|\baccount[s]?\s+payable\w*|\baccount[s]?\s+recivable\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'accounting operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for office administration
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boffice\s+administrat\w*|\boffice\s+organisation\w*|\boffice\s+admin\w*|\boffice\s+duty\w*|(?<!team )administrative\s+dut[y]?[i]?[e]?[s]?\b|\badministrative\s+responsibility\w*|\badministration\s+responsibility\w*|(?<!team )administration\s+dut[y]?[i]?[e]?[s]?\b|\badministration\s+skill\w*|\badministrative\s+skill\w*|\badministration\s+work\w*|\badministration\s+tool\w*|\badministrative\s+work\w*|\badministrative\s+tool\w*|(?<!team )administration\s+duties\w*|(?<!team )administration\s+duty\w*|\bgeneral\s+admin\w*|\bgeneral\s+administrat\w*|\boffice\s+clerk\w*|\bclerk\s+activit\w*|\bfile\s+file[s]?\b|\bfiling\s+file[s]?\w*|\bfile\s+document\w*|\badministrative\s+support\w*|\badministrative\s+assistant\w*|\badministrative\s+experience\w*|\badmin\s+assist\w*|\bassist\s+office\w*|\boffice\s+assist\w*|\bcommercial\s+administrator\w*|\boffice\s+\w+\s+manage\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'office administration'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for national generally accepted accounting principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgaap\b', text)
        match_2 = o(r'\baccounting\b|\bbookkeep\w*|\bfamiliar\w*|\bknowledge\w*|\bexperience\w*|\bcomply\w*|\bcompliance\w*|\bstandard\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'national generally accepted accounting principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for audit techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btechnical\s+audit\w*|\bquality\s+audit\w*|\bauditor\w*|\bstrategic\s+review\s+audit\w*|\binternal\s+audit\w*|\bcarry\s+audit\w*', text)
        match_2 = o(r'\baudit\b', text)
        match_3 = o(r'\binspect\w*|\bassess\w*|\btechnique\w*|\bsupport\w*|\bconduct\w*|\bperform\w*|\bmanage\w*|\bmanaging\w*|\bquality\w*|\bassess\w*|\bhelp\w*|\bprocess\w*|\bprocedure\w*|\bcarry\w*|\breport\w*|\bdocument\w*|\bpaperwork\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'audit techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for digital marketing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdigital\s+marketing\w*', text)
        match_2 = o(r'\bdigital\w*|\bonline\w*', text)
        match_3 = o(r'\bmarketing\w*', text)
        match_4 = o(r'\bstrateg\w*|\bconsult\w*|\bguidance\w*|\badvise\w*|\badvice\w*|\bsupport\w*|\bdesign\w*|\bcreate\w*|\bdesigning\w*|\bplan\w*|\bsale\w*|\bselling\w*|\bproduct\w*|\bservice\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'digital marketing strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for emea marketing strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bemea\b', text)
        match_2 = o(r'\bmarketing\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'emea marketing strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer segmentation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer\s+segment\w*|(?<!range )\bmarket\s+segment\w*|\bclient\s+segment\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer segmentation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for immunology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautoimmunity\w*|\bantibodies\b|\bantibody\b|\bimmunology\b|\bimmuno\s+oncology\w*|\bantibody\s+analytics\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'immunology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical immunology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautoimmunity\w*|\bantibodies\b|\bantibody\b|\bimmunology\b|\bimmuno\s+oncology\w*', text)
        match_2 = o(r'\bclinical\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical immunology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical immunopathology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\w*', text)
        match_2 = o(r'\bpatholog\w*', text)
        match_3 = o(r'\bautoimmunity\w*|\bantibodies\b|\bantibody\b|\bimmunology\b|\bimmuno\s+oncology\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical immunopathology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for diagnostic immunology techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimmunodiagnostic\w*', text)
        match_2 = o(r'\bdiagnos\w*', text)
        match_3 = o(r'\bautoimmunity\w*|\bantibodies\b|\bantibody\b|\bimmunology\b|\bimmuno\s+oncology\w*', text)
        match_4 = o(r'\btest\w*|\bconduct\w*|\bperform\w*|\bsample\w*|\bassess\w*|\bresearch\w*|\bstudy\w*|\btechnique\w*', text)
        if match_1 and match_4:
            matched_fragments.extend([match_1, match_4])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'diagnostic immunology techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for financial technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+technology\w*|\bfintech\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business intelligence
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bintelligence\s+manager\w*|\bbusiness\s+intelligence\w*|\bmarket\s+intelligence\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business intelligence'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business model
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+model\w*|\bbusiness\s+design\w*|\bmodel\s+business\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business model'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for succession planning
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsucession\w*', text)
        match_2 = o(r'\bplan\w*|\bstrategy\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'succession planning'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for business management principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbusiness\s+management\w*|\bmanag[e]?[i]?[n]?[g]?\s+business\w*|\bmanag[e]?[i]?[n]?[g]?\s+new\s+business\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'business management principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rural business management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagriculture\w*|\brural\w*|\bagricultural\w*', text)
        match_2 = o(r'\bmanage\w*|\bmanaging\w*', text)
        match_3 = o(r'\bsuiness\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rural business management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for omnichannel approach
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bomnichannel\w*|\bomni\s+channel\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'omnichannel approach'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for channel marketing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchannel\s+marketing\w*|\bchannel\s+campaign\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'channel marketing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for procurement legislation and lifecycle
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocurement\s+sourcing\w*|\bprocurement\s+purchasing\w*|\bprocurement\s+specification\w*|\bprocurement\s+analy\w*|\banalyse\s+procurement\w*|\banalyze\s+procurement\w*|\bprocurement\s+specialist\w*|\bprocurement\s+advice\w*|\bprocurement\s+consult\w*|\bprocurement\s+advisor\w*|\bprocurement\s+adviser\w*|\bperform\s+procurement\w*|\bprocurement\s+strategy\w*|\bmanag[e]?[i]?[n]?[g]?\s+procurement\w*|\bprocurement\s+manage\w*|\bpurchase\s+item\w*|\bpurchase\s+equipment\w*|\bproject\s+procurement\w*|\bcorporate\s+procurement\w*|\bprocurement\s+process\w*|\bdesign\s+procurement\w*|\bconstruction\s+procurement\w*|\bprocurement\s+construction\w*|\bcommodity\s+sourc\w*|\bcommodity\s+strategy\w*|\bprocurement\s+organi\w*|\borganize\s+procurement\w*|\borganise\s+procurement\w*|\bmanag[e]?[i]?[n]?[g]?\s+procurement\w*|\bprocurement\s+manage\w*|\bprocurement\s+need\w*|\bsupport[i]?[n]?[g]?\s+procurement\w*|\bfm\s+procurement\w*|\bmaterial\s+movement\s+ticket\w*|\bensure\s+\w+\s+procurement\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+procurement\w*|\banalyse\s+\w+\s+procurement\w*|\banalyze\s+\w+\s+procurement\w*|\borganise\s+\w+\s+procurement\w*|\borganize\s+\w+\s+procurement\w*|\bdevelop[i]?[n]?[g]?\s+\w+\s+procurement\w*|\bsupport[i]?[n]?[g]?\s+\w+\s+procurement\w*', text)
        match_2 = o(r'\bprocurement\w*|\btender\w*', text)
        match_3 = o(r'\bproject\w*|\bmanage\w*|\bcontract\b|\bcontracting\b|\bmanaging\w*|\bsupport\w*|\bhandle\w*|\bhandling\w*|\bexperience\w*|\bknowledge\w*', text)
        match_4 = o(r'\bprocurement\w*|\btender\w*|\bdocument\w*|\breport\w*|\bprepar\w*|\bwrit\w*|\bpaperwork\w*', text)
        match_5 = o(r'\brqt\b|\bitt\b|\bitb\b|\brfq\b|\bpfi\b|\bppp[s]?\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'procurement legislation and lifecycle'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer service
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer[s]?\s+support\w*|\bcommunicate\s+customer\w*|\bcomplaint\s+investigation\w*|\bcommunicate\s+client\w*|\bcustomer[s]?\s+communicate\w*|\bcustomer[s]?\s+survey\w*|\bcustomer[s]?\s+assist\w*|\bassist[a]?[n]?[c]?[e]?\s+customer\w*|\bclient[s]?\s+assist\w*|\bassist[a]?[n]?[c]?[e]?\s+client\w*|\bcustomer[s]?\s+service\w*|\bcustomer[s]?\s+advice\w*|\bclient[s]?\s+advice\w*|\badvise\s+customer\w*|\badvise\s+client\w*|\bclient[s]?\s+services\b|\bconsult\s+customer\w*|\bconsult\s+client\w*|\bcustomer\s+-\s+service\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer service'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for consultation methods
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!education )\bconsultancy(?! firm| company)\w*|\bconsultation(?! firm| company)\w*|\badviser\b|\badvisor\w*|\bconsult[i]?[n]?[g]?\b|\badvisory\s+work\w*|\badvise\s+customer\w*|\badvise\s+client\w*|\badvise\s+stakeholder\w*|\bcustomer[s]?\s+advice\w*|\bclient[s]?\s+advice\w*|\bstakeholder[s]?\s+advice\w*|\bprovide\s+consultation\w*|\bgive\s+consultation\w*|\bconsult\s+others\w*|\bconsultation\s+method\w*|\bprovide\s+advice\w*|\bgive\s+advice\w*|\bgive\s+guidance\w*|\bprovide\s+guidance\w*|\btechnical\s+guidance\w*|\bconsultation\s+practic\w*|\bconsult\s+business\w*|\bconsult\s+manager\w*|\bconsult\s+supervisor\w*|\bconsult\s+skill\w*|\bconsultation\s+skill\w*|\bconsultative\s+ability\w*|\bconsultative\s+skill\w*|\bconsultation\s+support\w*|\bconsult[a]?[n]?[c]?[y]?\s+strategy\w*|\bconsult[a]?[n]?[c]?[y]?\s+service\w*|\bmake\s+recommendation\w*|\bprovide\s+recommend\w*|\brecommend\s+solution\w*|\bmake\s+\w+\s+recommendation\w*|\brecommend\s+\w+\s+solution\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'consultation methods'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biomedical engineering techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiomedical\s+engineer\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biomedical engineering techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer relationship management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrm\b|\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+stakeholder\w*|\bcustomer\s+relationship\s+manage\w*|\bcustomer\s+relationship\s+database\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+stakeholder\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+customer\w*|\bmanag[e]?[i]?[n]?[g]?\s+relationship\s+client\w*|\benhanc[e]?[i]?[n]?[g]?\s+relationship\s+client\w*|\bnurture\s+\w+\s+relationship\w*|\bliais[e]?[o]?[n]?\s+customer\w*|\bliais[e]?[o]?[n]?\s+client\w*|\bliais[e]?[o]?[n]?\s+stakeholder\w*|\bstakeholder\s+management\w*|\bliais[e]?[o]?[n]?\s+\w+\s+customer\w*|\bliais[e]?[o]?[n]?\s+\w+\s+client\w*|\bliais[e]?[o]?[n]?\s+\w+\s+stakeholder\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+client\w*|\bliais[e]?[i]?[n]?[g]?\s+customer\w*|\bliais[e]?[i]?[n]?[g]?\s+\w+\s+stakeholder\w*', text)
        match_2 = o(r'\bcustomer[s]?\s+relationship\w*|\bstakeholder[s]?\s+relationship\w*|\bclient[s]?\s+relationship\w*|\brelationship\s+key\s+stakeholder\w*|\brelationship\s+key\s+client\w*|\brelationship\s+key\s+customer\w*|\brelationship\s+key\s+external\s+client\w*|\brelationship\s+key\s+external\s+customer\w*|\brelationship\s+key\s+external\s+stakeholder\w*|\bclient[s]?\s+\w+\s+relationship\w*|\bcustomer[s]?\s+relationship\w*', text)
        match_3 = o(r'\bmanage\w*|\bestablish\w*|\bhandle\w*|\bmanaging\w*|\bhandling\w*|\bsupport\w*|\boversee\w*|\boversight\w*|\bactivity\w*|\bduty\w*|\bperform\w*|\bconduct\w*|\bexperience\w*|\bknowledge\w*|\bresponsib\w*', text)
        match_4 = o(r'\bcustomer[s]?\s+manage\w*|\bclient[s]?\s+manage\w*|\bstakeholder[s]?\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+customer\w*|\bmanag[e]?[i]?[n]?[g]?\s+client\w*|\bmanag[e]?[i]?[n]?[g]?\s+stakeholder\w*', text)
        match_5 = o(r'\brelation\w*', text)
        match_6 = o(r'\bcustomer\w*|\bclient\w*|\binternal\w*|\bexternal\w*', text)
        match_7 = o(r'\brelation[s]?[h]?[i]?[p]?\s+manag\w*|\bmanag[e]?[i]?[n]?[g]?\s+relation\w*|\bmanagement\s+relation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        elif match_6 and match_7:
            matched_fragments.extend([match_6, match_7])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer relationship management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for service line management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bservice\s+line(?! with)\w*', text)
        match_2 = o(r'\bmanage\w*|\bsupervis\w*|\bmonitor\w*|\boversee\w*|\boversight\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'service line management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ITC networking hardware
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+technology\w*|\bitc\b', text)
        match_2 = o(r'\bhardware\w*|\bdevice\w*|\bequipment\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ITC networking hardware'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for security in buildings
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsecurity\w*', text)
        match_2 = o(r'\bbuilding\w*|\boffice\b|\bfacility\b|\bfacilities\b|\bpremises\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'security in buildings'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cyber security
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcyber\s+security\w*|\bcybersecurity\w*|\bfirewall\s+admin\w*|\bfirewall\s+configur\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cyber security'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for information security strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'information security strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for attack vectors (cybersecurity)
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bict\b', text)
        match_2 = o(r'\bthreat\w*|\bmalware\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'attack vectors (cybersecurity)'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for cloud access security broker
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bexperience\w*|\bknowledge\w*|\bcloud\w*|\bmonitor\w*|\bsecurity\w*|\bset\w*|\bcreate\w*|\bdesign\w*|\bmaintain\w*|\bmanage\w*|\bmanaging\w*|\boperat\w*|\bsystem\w*', text)
        match_2 = o(r'\bcasb\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'cloud access security broker'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for secure web gateway
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binformation\s+security\b|\bcyber\s+security\w*|\bcybersecurity\w*|\bexperience\w*|\bknowledge\w*|\bcloud\w*|\bmonitor\w*|\bsecurity\w*|\bset\w*|\bcreate\w*|\bdesign\w*|\bmaintain\w*|\bmanage\w*|\bmanaging\w*|\boperat\w*|\bsystem\w*', text)
        match_2 = o(r'\bswb\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'secure web gateway'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laboratory systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+systems\b|\blaboratory\s+system\b|\blab\b\s+systems\b|\blab\b\s+system\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laboratory systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laboratory techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+technique\b|\blab\b\s+technique\b|\btechnical\s+laboratory\b|\btechnical\s+lab\w*|\blaboratory\s+experience\w*|\blab\b\s+experience\w*|\blaboratory\s+skill\w*|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+lab\b|\b[c]?[o]?\s?[-]?\s?ordinat[e]?[i]?[n]?[g]?\s+laboratory\w*|\blab\b\s+skill\w*|\blaboratory\s+industry\w*|\blab\b\s+industry\w*|\blaboratory\s+sector\w*|\blab\b\s+sector\w*|\bwork[i]?[n]?[g]?\s+laboratory\w*|\bwork[i]?[n]?[g]?\s+lab\b|\blaboratory\s+technician\w*|\blab\s+technician\w*|\blaboratory\s+procedure\w*|\blab\s+procedure\w*|\blaboratory\s+protocol\w*|\blab\s+protocol\w*|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+lab\b|ordinat[e]?[i]?[n]?[g]?\s+\w+\s+laboratory\w*', text)
        match_2 = o(r'\blab\b|\blaboratory\w*', text)
        match_3 = o(r'\bexperiment\w*|\bresearch\w*|\bstudy\w*|\bexperience\w*|\bknowledge\w*|\btechnique\w*|\bmethod\w*|\bsetting\w*|\bprocedure\w*|\bprotocol\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laboratory techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pkpd modelling
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpkpd\b|\bpk\s+-\s+pd\w*|\bpk\s+pd\w*', text)
        match_2 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bmodel\w*|\bdrug\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pkpd modelling'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for medical laboratory technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+technology\w*|\blab\b\s+technology\w*|\blaboratory\s+tech\b|\blab\b\s+tech\b', text)
        match_2 = o(r'\bmedical\w*|\bclinical\w*', text)
        match_3 = o(r'\blab\b|\blaboratory\w*', text)
        match_4 = o(r'\btechniques\w*|\binstrument\w*|\btechnolog\w*|\bgear\w*|\bimplement\w*|\bequipmnet\w*|\bdevice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'medical laboratory technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for laboratory information management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaboratory\s+information\s+management\s+system\w*|\blaboratory\s+information\s+management\s+software\w*', text)
        match_2 = o(r'\blims\b', text)
        match_3 = o(r'\blaboratory\b|\blab\b|\bsoftware\b|\bexperience\w*|\bknowledge\w*|\buse\w*|\butilise\w*|\bmanage\w*|\bmanaging\w*|\bdata\w*|\bdatum\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'laboratory information management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for treasury management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btreasury\s+management\s+system\w*|\btreasury\s+management\s+software\w*|\bwallstreet\s+suite\w*', text)
        match_2 = o(r'\btms\b', text)
        match_3 = o(r'\btreasury\w*|\bcash\s+flow\w*|\baccount\w*|\bbill\b|\bbank[i]?[n]?[g]?\w*|\bexperience\w*|\bknowledge\w*|\buse\w*|\butilise\w*|\bsoftware\w*|\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'treasury management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for automated analysers in the medical laboratory
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blab\b\s+automation\w*|\bautomate\s+\blab\b|\blaboratory\s+automation\w*|\bautomate\s+laboratory\w*', text)
        match_2 = o(r'\bautomat\w*', text)
        match_3 = o(r'\banaly\w*|\bassess\w*|\bresearch\w*|\bstudy\w*', text)
        match_4 = o(r'\blab\w*|\blaboratory\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'automated analysers in the medical laboratory'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for materials science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmaterial\s+science\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'materials science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for international taxation of transfer prices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransfer\s+pricing\w*|\btransfer\s+price\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'international taxation of transfer prices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for international trade
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bglobal\s+trade\b|\binternational\s+trade\b|\bforeign\s+trade\b', text)
        match_2 = o(r'\bimport\b|\bimporting\b|\bimportation\b', text)
        match_3 = o(r'\bexport\b|\bexporting\b|\bexportation\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'international trade'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for trading law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btrade\s+law\w*|\btrade\s+compliance\w*|\btrade\s+legislation\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'trading law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supply chain principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupply\s+chain\w*', text)
        match_2 = o(r'\breactive\s+line\w*|\breactive\s+\w+\s+line\w*', text)
        match_3 = o(r'\bdevelop\w*|\bassess\w*|\breview\w*|\banaly\w*|\bplan\w*|\bsupply\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'supply chain principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for supply chain management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsupply\s+chain\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+supply\s+chain\w*|\bsupply\s+chain\s+regulat\w*|\bsupply\s+chain\s+supervis\w*|\bsupervise\s+supply\s+chain\w*|\bsupply\s+chain\s+oversight\w*|\bsupply\s+chain\s+oversee\w*|\boversee\s+supply\s+chain\w*|\bsupply\s+chain\s+guideline\w*|\bsupply\s+chain\s+legislation\w*|\bsupply\s+chain\s+law\w*|\bassist\s+supply\s+chain\w*|\bcategory\s+management\w*', text)
        match_2 = o(r'\bscm\b', text)
        match_3 = o(r'\bsupply\s+chain\w*|\bknowledge\w*|\bexperience\w*|\boperation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'supply chain management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mining engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bengineer\w*', text)
        match_2 = o(r'(?<!datum )(?<!data )\bmining\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mining engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for optomechanical or optoelectronic devices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bopto\s?[-]?\s?mechanic\w*|\bopto\s?[-]?\s?electronic\w*|\boptical\s+product\w*|\boptical\s+system\w*', text)
        match_2 = o(r'\bspectromet\w*', text)
        match_3 = o(r'\bengineer\w*|\binstrument\w*|\bdevice\w*|\bmaintenance\w*|\brepair\w*|\bfix\w*|\bmaintain\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'optomechanical or optoelectronic devices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pharmaceutical products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpharmaceutical\s+bioproduction\w*|\bpharmaceutical\s+production\w*|\bpharmaceutical\s+product\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pharmaceutical products'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pharmaceutical processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrug\s+discovery\w*|\bdrug\s+development\w*|\bdiscover\s+drug\w*|\bdrug\s+manufacture\w*|\bmanufacture\s+drug\w*|\bpharmaceutical\s+proces\w*|\bmanufacture\s+new\s+drug\w*|\bdevelop\s+\w+\s+drug\w*|\bdiscover\s+\w+\s+drug\w*', text)
        match_2 = o(r'\bdrug\b|\bpharmaceutic\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*|\bassess\w*|\banaly\w*|\bassay\w*|\bexperiment\w*', text)
        match_4 = o(r'\bhair\s+sample\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pharmaceutical processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for chemical processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsynthesis\s+purification\b|\bpharmaceutical\s+bioproduction\w*|\bchemical\s+processes\b|\bchemical\s+process\b|\bchemicals\s+application\w*|\bchemical\s+test\w*|\bchemical\s+analy\w*|\bchemical\s+qualoity\w*|\bchemical\s+injection\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'chemical processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aerosol generating procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baerosol\w*', text)
        match_2 = o(r'\bgenerate\w*|\bproduce\w*|\bproduction\w*|\bgeneration\w*|\bsynthesis\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aerosol generating procedures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for binary alignment mapping
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbinary\s+align[m]?[e]?[n]?[t]?\s+map\w*', text)
        match_2 = o(r'\bbam\b', text)
        match_3 = o(r'\bgene\b|\bgenome\b|\bgenetic\w*|\bbioproduction\w*|\bpharmaceutical\w*|\bexperience\w*|\bknowledge\w*|\bproces\w*|\bprocedure\w*|\btest\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'binary alignment mapping'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for genetic engineering and genetics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgenetic\s+analys\w*|\bgene\s+editing\b|\bdna\b\s+sequencing\b|\brna\b\s+sequencing\b|\banalyse\b\s+gene\b|\banalyse\s+genes\b|\brdna\b\s+sequencing\b|\bsequencing\s+plasmid\w*|\bgenetic\s+evaluation\w*|\bgenomic\s+evaluation\w*|\bquantitative\s+genetic\w*', text)
        match_2 = o(r'\bkras\b', text)
        match_3 = o(r'\bgene\b|\bcell\b|\bprotein\b|\bexperience\w*|\bknowledge\w*|\bproces\w*|\bprocedure\w*|\btest\w*', text)
        match_4 = o(r'\bprogramming\b|\bprogrammer\b', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'genetic engineering and genetics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ngs technique
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bngs\s+techniuqe\w*|\bngs\s+technology\w*|\bngs\s+approach\w*|\bnext\s+generation\s+sequenc\w*', text)
        match_2 = o(r'\bngs\b', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|\bgene\b|\bgenome\b|\bsequencing\b|\btest\w*|\bproces\w*|\bprocedure\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ngs technique'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical biology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+biolog\w*|\bclinical\s+microbiolog\w*|\binterventional\s+biolog\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical biology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for food policy and science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfood\s+industry\w*', text)
        match_2 = o(r'\bfood\b', text)
        match_3 = o(r'\bprotection\w*|\bsafety\w*|\bprotect\w*|\bscience\w*|\bpolicy\w*|\bcomply\w*|\blegislation\w*|\blaw\w*|\bquality\w*|\bqc\b|\bqa\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'food policy and science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for digital pcr
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdpcr\b|\bdigital\s+\bpcr\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'digital pcr'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for pcr technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpcr\b|\bpolymer[a]?[s]?[e]?chain\s+react\w*|\bqpcr\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'pcr technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for fibre optics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfiber\s?[-]?\s?optic\w*|\bfibre\s?[-]?\s?optic\w*|\boptic[a]?[l]?\s+fibre[s]?\b|\boptic[a]?[l]?\s+fiber[s]?\b|\bglass\s+filament\w*|\bglass\s+thread[s]?\b|\bplastic\s+filament\w*|\bplastic\s+thread[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'fibre optics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lasers
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blaser\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'lasers'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for petroleum
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpetroleum\s+expert\w*|\boil\s+expert\w*|\bpetroleum\s+process\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'petroleum'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for crystallization process
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrystallization\w*', text)
        match_2 = o(r'\bproces\w*|\bprocedure\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'crystallization process'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for injection moulding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\binjection\w*', text)
        match_2 = o(r'\bmould\w*|\bstretch\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'injection moulding'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for blow moulding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bblow\w*', text)
        match_2 = o(r'\bmould\w*|\bstretch\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'blow moulding'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for semiconductors
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsemiconduct\w*|\bsemi\s?[-]?\s?conduct\w*', text)
        match_2 = o(r'\bcrystallization\w*|\bexperience\w*|\bknowledge\w*|\bperform\w*|\bproces\w*', text)
        match_3 = o(r'\bfpga\b', text)
        match_4 = o(r'\blithography\w*', text)
        match_5 = o(r'\bcircuit\w*|\bsilicon\w*|\bwafer\w*|mask\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'semiconductors'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for therapeutic knowledge
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btherapeutic\s+knowledge\w*|\btherapeutic\s+expertise\w*|\btherapeutic\s+experience\w*|\btherapeutic\s+\w+\s+knowledge\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'therapeutic knowledge'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adeno-associated virus vector
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baav\b', text)
        match_2 = o(r'\bgene\w*|\bvector\w*|\bknowledge\w*|\bexperience\w*|\bviral\w*|\btherapy\w*|\btest\w*|\bresearch\w*|\banaly\w*|\bdrug\w*|\bphramaceutic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'adeno-associated virus vector'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for knowledge about dna and rdna
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdna\b|\brna\b|\brdna\b|\boligonucleotide\w*|\bolgio\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'knowledge about dna and rdna'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for macrophages
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmacrophage\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'macrophages'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for rtx toxin
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brtx\b', text)
        match_2 = o(r'\btoxin\w*|\bbacteria\w*|\bexperience\w*|\bknowledge\w*|\btest\w*|\bresearch\w*|\banaly\w*|\bdrug\w*|\btherapy\w*|\bpharmaceutic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'rtx toxin'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for spectroscopy
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspectroscop\w*|\buv\b\s+\bvis\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'spectroscopy'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for orthopaedics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphysiotherapy\b', text)
        match_2 = o(r'\borthopaedic\w*', text)
        match_3 = o(r'\bphysical\b|\bphysician\b|\btherapy\w*|\bplan\w*|\brehabilitat\w*|\bpatient\w*', text)
        match_4 = o(r'\bjoint\b|\bjoints\b', text)
        match_5 = o(r'\bspine\b|\bneck\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'orthopaedics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for value added tax law
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bvat\b', text)
        match_2 = o(r'\btax\b|\btaxation\b|\btaxes\b|\bcalcluat\w*|\breturn\w*|\bexperience\w*|\bknowledge\w*|\breturn\w*|\bclaim\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'value added tax law'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tax legislation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btax\s+legislation\w*|\buk\s+tax\b|\boverseas\s+tax\b|\bnordic\s+tax\w*', text)
        match_2 = o(r'\btax\b|\btaxes\b|\btaxation\b', text)
        match_3 = o(r'\blaw\b|\blaws\w*|\blawyer\b|\blitigation\w*|\blegislation\w*|\bregulation\w*|\bguideline\w*|\bcompliance\w*|\bcomply\w*|\bcalculate\w*|\breturn\w*|\bpolicies\w*|\bpolicy\w*|\brequirement\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tax legislation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mdx inspection technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmdx\b', text)
        match_2 = o(r'\bdrug\w*|\bknowledge\w*|\bexperience\w*|\btechnique\w*|\buse\w*|\banaly\w*|\bassess\w*|\bapply\w*|\binspect\w*|\btechnolog\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mdx inspection technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drug administration regulations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdrug\s+administration\s+regulat\w*|\bfda\b', text)
        match_2 = o(r'\bdrug\w*', text)
        match_3 = o(r'\bregulation\w*|\blegislation\w*|\bcompliance\w*|\badministrat\w*|\bregulat\w*|\brequire\w*|\bpolicy\w*|\bpolicies\w*|\bcomply\w*|\biso\b|\bfda\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'drug administration regulations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for corrective action and preventive action
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcapa\b|\bcorrective\s+preventive\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'corrective action and preventive action'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for statistical quality control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatiscial\s+analys\w*', text)
        match_2 = o(r'\bstatistic\w*|\bstatistical\w*', text)
        match_3 = o(r'\bqc\b|\bqa\b|\bcomply\w*|\bcompliance\w*|\bquality\w*|\bperformance\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'statistical quality control'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for statistics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?!national )\bstatistic\w*|\bstatistical\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'statistics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biostatistics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiostatistic\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biostatistics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality assurance procedures and methodologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquality\s+control\w*|\bquality\s+assurance\w*|\bvalidate\s+quality\w*|\bvalidation\s+quality\w*|\btechnical\s+assurance\w*|\bvalidat[e]?[i]?[o]?[n]?\s+\bverif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\w*|\bverif[y]?[i]?[c]?[a]?[t]?[i]?[o]?[n]?\s+validat[e]?[i]?[o]?[n]?\w*|\binspection\s+procedure\w*|\bmonitoring\s+procedure\w*|\bquality\s+check\w*|\bquality\s+control\s+procedure\w*|\bquality\s+manage\w*|\bmanage\s+quality\w*|\bsupervision\s+procedure\w*|\bquality\s+procedure\w*|\bqc\s+co\s?[-]?\?ordinat\w*|\bqa\s+co\s?[-]?\?ordinat\w*', text)
        match_2 = o(r'\bqc\b|\bqa\b|\bquality\w*', text)
        match_3 = o(r'\bhealth\s+safety\w*|\bhse\b|\bhs\b|\bhsse\b|\bqhse\b', text)
        match_4 = o(r'\bpqp\b|\bitp\b|\bcra\b|\bmdr\b|\bsdrl\b|\bmrb\b', text)
        match_5 = o(r'\bquality\w*|\binspection\w*|\bknowledge\w*|\bexperience\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'quality assurance procedures and methodologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality control systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bquality\s+control\s+system\w*|\bqc\b\s+system\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'quality control systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aseptic processing procedures
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baseptic\w*|\basepsis\w*', text)
        match_2 = o(r'\bproces\w*|\bknowledge\w*|\bprocedure\w*|\btechnique\w*|\bmanufactur\w*|\bexperience\w*|\btechnique\w*|\btechnolog\w*|\battention\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aseptic processing procedures'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sap
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsap\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sap'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for jd edrward oracle software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjde\b\s+\boracle\b|\bjd\b\s+\bedward\b\s+\boracle\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'jd edrward oracle software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for phoenix software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphoenix\s+software\w*|\bphoenix\s+system\w*|\bphoenix\s+program\w*|\buse\s+phoenix\w*', text)
        match_2 = o(r'\bphoenix\w*', text)
        match_3 = o(r'\bprogram\w*|\bsoftware\w*|\bexperience\w*|\bsystem\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'phoenix software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for phenix software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphenix\s+software\w*|\bphenix\s+program\w*|\bphenix\s+system\w*|\buse\s+phenix\w*', text)
        match_2 = o(r'\bphenix\w*', text)
        match_3 = o(r'\bprogram\w*|\bsoftware\w*|\bexperience\w*|\bsystem\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'phenix software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft access database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicrosoft\s+access\b|\bms\s+access\b|\bmicrosoft\s+office\s+access\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft access database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ruby
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bruby\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ruby'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for scala
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscala\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'scala'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for angular framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bangular\b', text)
        match_2 = o(r'\bframework\w*|\bexperience\w*|\bknowledge\w*|\bweb\w*|\bsoftware\w*|\bcode\w*|\bprogram\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'angular framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microsoft azure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bazure\b', text)
        match_2 = o(r'\bmicrosoft\b|\bms\b|\bcloud\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microsoft azure'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for html markup language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhtml\d?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'html markup language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for python
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpython\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'python'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for .net
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!asp\. )(?<!asp\.)(?<!asp \.)(?<!asp \. )(?<!asp)(?<!asp )[\.]?net\d?\b(?!zero| zero| zero \w*|gain| gain| gain \w*|-zero| -zero| - zero)\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = '.net'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)
        
        # Matching fragments for asp.net
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\basp\.net\w*|\basp\s+\.\s+net\w*|\basp\s+\.net\w*|\basp\.\s+net\w*|\baspx\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'asp.net'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for java
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjava\b', text)
        match_2 = o(r'\bwest\s+java\w*', text)
        if match_1 and not match_2:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'java'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for node js
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnodejs\w*|\bnode\s+js\b|\bnode\.js\w*|\bnode\s+\.\s+js\w*|\bnode\s+\.js\w*|\bnode\.\s+js\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'node js'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for javascript redux library
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bredux\w*', text)
        match_2 = o(r'\bjava\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\blibrary\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'javascript redux library'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ajax
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bajax\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ajax'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for php
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bphp\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'php'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sql
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsql\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sql'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sql server data management software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsql\b\s+server\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sql server data management software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for css
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcss\b|\bcascad[e]?[i]?[n]?[g]?\s+style\s+sheet[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'css'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for react
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\breactjs\b|\breact\s+js\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'react'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for non sql databases
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bnosql\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'non sql databases'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for hadoop
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhadoop\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'hadoop'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mongodb database management system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmongodb\b|\bmongo\s+db\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mongodb database management system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for graphql
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgraphql\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'graphql'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apache maven
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapache\b', text)
        match_2 = o(r'\bmaven\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'apache maven'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apache kafka
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapache\b', text)
        match_2 = o(r'\bkafka\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'apache kafka'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for apache HTTP server
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapache\b', text)
        match_2 = o(r'\bserver\b|\bhttp\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'apache HTTP server'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for spring framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspring\b', text)
        match_2 = o(r'\bjava\b|\bframework\w*|\bsoftware\w*|\bprogramm\w*|\bcode\b|\bcoding\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'spring framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for microservices architecture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmicroservice\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'microservices architecture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for junit testing framework
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjunit\b', text)
        match_2 = o(r'\bjava\b|\bframework\w*|\bsoftware\w*|\bprogramm\w*|\bcode\b|\bcoding\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'junit testing framework'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for robotic process automation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brpa\b', text)
        match_2 = o(r'\bprogramming\b|\bcoding\b|\bcode\b|\brobotic\w*|\bprocess\w*|\bprocedure\w*|\bsoftware\w*|\bprogramm\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'robotic process automation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for devops methodology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevops\b|\bdev\s+ops\b|\bdevop\b|\bdev\s+op\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'devops methodology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for unix operating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bunix\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'unix operating system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for linux operating system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blinux\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'linux operating system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for extensible markup language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bxml\b', text)
        match_2 = o(r'\bcoding\b|\bcode\b|\bmarkup\w*|\bsoftware\w*|\bprogramm\w*|\bexperience\w*|\bknowledge\w*|\bprogram\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'extensible markup language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for teamcity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bteamcity\b', text)
        match_2 = o(r'\bcoding\b|\bcode\b|\bsoftware\w*|\bprogramm\w*|\bexperience\w*|\bknowledge\w*|\bprogram\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'teamcity'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for jira project management tool
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bjira\b', text)
        match_2 = o(r'\bcoding\b|\bcode\b|\bmanage\w*|\bmanging\w*|\bproject\w*|\bexperience\w*|\bknowledge\w*|\bprogram\w*|\bsoftware\w*|\btool\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'jira project management tool'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for amazon web services
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\baws\b|\bamazon\s+web\s+service\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'amazon web services'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for grafana analyst tool
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgrafana\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'grafana analyst tool'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for depomod modelling software
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdepomod\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'depomod modelling software'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for italian language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+itallian\w*|\bwrit[e]?[i]?[n]?[g]?\s+itallian\w*|\bitallian\s+language\w*|\bcommunicate\s+itallian\w*|\bcontact\s+itallian\w*|\bfluent\s+itallian\w*|\bitallian\s+customer\w*|\bitallian\s+client\w*', text)
        match_2 = o(r'\bitallian\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'italian language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for spanish language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+spanish\w*|\bwrit[e]?[i]?[n]?[g]?\s+spanish\w*|\bspanish\s+language\w*|\bcommunicate\s+spanish\w*|\bcontact\s+spanish\w*|\bfluent\s+spanish\w*|\bspanish\s+customer\w*|\bspanish\s+client\w*', text)
        match_2 = o(r'\bspanish\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'spanish language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for polish language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+polish\w*|\bwrit[e]?[i]?[n]?[g]?\s+polish\w*|\bpolish\s+language\w*|\bcommunicate\s+polish\w*|\bcontact\s+polish\w*|\bfluent\s+polish\w*|\bpolish\s+client\w*|\bpolish\s+customer\w*', text)
        match_2 = o(r'\bpolish\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'polish language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for french language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+french\w*|\bwrit[e]?[i]?[n]?[g]?\s+french\w*|\bfrench\s+language\w*|\bcommunicate\s+french\w*|\bcontact\s+french\w*|\bfluent\s+french\w*|\bfrench\s+customer\w*|\bfrench\s+client\w*', text)
        match_2 = o(r'\bfrench\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'french language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for korean language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+korean\w*|\bwrit[e]?[i]?[n]?[g]?\s+korean\w*|\bkorean\s+language\w*|\bcommunicate\s+korean\w*|\bcontact\s+korean\w*|\bfluent\s+korean\w*|\bkorean\s+customer\w*|\bkorean\s+client\w*', text)
        match_2 = o(r'\bkorean\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'korean language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for german language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak\s+german\w*|\bwrit[e]?[i]?[n]?[g]?\s+german\w*|\bgerman\s+language\w*|\bcommunicate\s+german\w*|\bcontact\s+german\w*|\bfluent\s+german\w*|\bgerman\s+client\w*|\bgerman\s+customer\w*', text)
        match_2 = o(r'\bgerman\b', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'german language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for arabic language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+arabic\w*|\bwrit[e]?[i]?[n]?[g]?\s+arabic\w*|\barabic\s+language\w*|\bcommunicate\s+arabic\w*|\bcontact\s+arabic\w*|\bfluent\s+arabic\w*', text)
        match_2 = o(r'\barabic\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'arabic language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for russian language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+russian\w*|\bwrit[e]?[i]?[n]?[g]?\s+russian\w*|\brussian\s+language\w*|\bcommunicate\s+russian\w*|\bcontact\s+russian\w*|\bfluent\s+russian\w*|\brussian\s+customer\w*|\brussian\s+client\w*', text)
        match_2 = o(r'\brussian\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'russian language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for japanese language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+japanese\w*|\bwrit[e]?[i]?[n]?[g]?\s+japanese\w*|\bjapanese\s+language\w*|\bcommunicate\s+japanese\w*|\bcontact\s+japanese\w*|\bfluent\s+japanese\w*|\bjapanese\s+customer\w*|\bjapanese\s+client\w*', text)
        match_2 = o(r'\bjapanese\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'japanese language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for swedish language
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bspeak[i]?[n]?[g]?\s+swedish\w*|\bwrit[e]?[i]?[n]?[g]?\s+swedish\w*|\bswedish\s+language\w*|\bcommunicate\s+swedish\w*|\bcontact\s+swedish\w*|\bfluent\s+swedish\w*|\bswedish\s+customer\w*|\bswedish\s+client\w*', text)
        match_2 = o(r'\bswedish\w*', text)
        match_3 = o(r'\blanguage\w*|\bspeak\w*|\bproficiency\w*|\blevel\w*|\bfluent\w*|\bfluency\w*|\bproficient\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'swedish language'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for human resources management and department processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhr\b\s+policy\w*|\bhr\b\s+policies\b|\bhuman\s+resource[s]?\s+policy\b|\bhuman\s+resource[s]?\s+admin\w*|\bhr\s+admin\w*|\bhuman\s+resource[s]?\s+policies\b|\bhuman\s+resource[s]?\s+management\w*|\bhr\b\s+management\w*|\bhr\b\s+process\w*|\bhuman\s+resource[s]?\s+process\w*|\bhuman\s+resource[s]?\s+advisor\w*|\bhuman\s+resurce[s]?\s+adviser\w*|\bhr\b\s+advisor\w*|\bhr\s+adviser\w*|\bhr\s+regulation\w*|\bhr\s+guidance\w*|\bhuman\s+resource[s]?\s+guidance\w*|\bhuman\s+resource[s]?\s+regulation\w*|\bhr\s+file\w*|\bhr\s+report\w*|\bhuman\s+resource[s]?\s+file\w*|\bhuman\s+resource[s]?\s+report\w*|\bhr\s+experience\w*|\bhr\s+knowledge\w*|\bexperience\s+hr\b|\bknowledge\s+hr\b|\bhuman\s+resource[s]?\s+experience\w*|\bhuman\s+resource[s]?\s+knowledge\w*|\bexperience\s+human\s+resource\w*|\bknowledge\s+human\s+resource\w*', text)
        match_2 = o(r'\bsafeguarding\w*|\bpersonnnel\s+opeartion\w*|\boperations\s+administrator\w*|\bpersonnel\s+resources\w*|\boperation[s]?\s+personnel\w*|\bdepartment[a]?[l]?\s+process\w*|\bdepartment[a]?[l]?\s+operation\w*', text)
        match_3 = o(r'\bhr\b|\bhuman\s+resources\w*|\bmanage\w*|\bmanaging\w*|\badvise\w*|\badvice\w*|\bconsult\w*|\bsupport\w*|\bduty\w*|\bexperience\w*|\bperform\w*|\bknowledge\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'human resources management and department processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software frameworks
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoftware\b\s+development\b|\bapplication\s+development\b|\bapp\b\s+development\b|\bsoftware\s+engineer\w*|\bsoftware\s+framework\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software frameworks'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software development life cycle
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsdlc\b|\bsdlcm\b|\bsoftware\s+development\s+life\s+cycle\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software development life cycle'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for downstream processing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdownstream\s+processing\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'downstream processing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software design methodologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscrum\b|\bagile\s+methodolog\w*', text)
        match_2 = o(r'\bagile\b', text)
        match_3 = o(r'\bsoftware\b|\bdigital\s+design\w*|\bui\b|\buser\s+interface\w*|\bapplication\w*|\bapp\b|\bdevelop\w*|\bprogram\w*|\bcode\b|\bcoding\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software design methodologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software configuration management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bscm\b', text)
        match_2 = o(r'\bprogramming\w*|\bsoftware\w*|\bcoding\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software configuration management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for software interaction design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsoftware\s+interaction\s+design\w*', text)
        match_2 = o(r'\binteraction\s+design\w*', text)
        match_3 = o(r'\bprogram\w*|\bcode\b|\bcoding\w*|\bsoftware\w*|\bdevelop\w*|\bwebsite\w*|\bweb\b|\bapplication\w*|\bapp\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'software interaction design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for levels of software testing
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bunit\s+integration\w*|\buser\s+acceptance\w*|\bintgration\s+testing\w*|\bsystem\s+testing\w*|\bsoftware\s+test\w*|\btest[i]?[n]?[g]?\s+software\w*|\bsoftware\s+compliance\w*|\bsoftware\s+quality\w*', text)
        match_2 = o(r'\blevel\w*|\bstage\w*', text)
        match_3 = o(r'\bsoftware\w*', text)
        match_4 = o(r'\btest\w*|\bassess\w*|\bquality\w*|\bqc\b|\bqa\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'levels of software testing'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for user experience design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bux\b\s+design\b|\buser\s+experience\s+design\w*|\bdesign[i]?[n]?[g]?\s+user\s+experience\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'user experience design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for application lifecycle management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapplication\s+life\s?[-]?\s?cycle\w*|\bapp\b\s?[-]?\s?lifecycle\w*', text)
        match_2 = o(r'\balm\b', text)
        match_3 = o(r'\bapplication\b|\bapp\b|\bknowledge\w*|\bexperience\w*|\bfamilair\w*|\bmanage\w*|\bmanaging\w*|\bperform\w*|\buse\w*|\bsystem\w*|\bsoftware\w*|\btool\w*|\bplan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'application lifecycle management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for algorithms
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\balgorithm\b|\balgorithms\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'algorithms'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for principles of artificial intelligence
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bartificial\s+intelligence\b', text)
        match_2 = o(r'\bai\b', text)
        match_3 = o(r'\bsoftware\w*|\bcoding\w*|\bsystem\w*|\bexperience\w*|\bprogram\w*|\bknowledge\w*|\bcode\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'principles of artificial intelligence'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ai product development
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bartificial\s+intelligence\w*|\bai\b', text)
        match_2 = o(r'\bproduct\w*|\bservice\w*', text)
        match_3 = o(r'\bdevelop\w*|\bcreate\w*|\bdesign\w*|\brelease\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ai product development'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for netsuite pick pack and ship
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bpick\s+pack\s+ship\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'netsuite pick pack and ship'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for development security and operations practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevsecop[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'development security and operations practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for kubernetes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bkubernete[s]?\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'kubernetes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for applicant tracking systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bapplicant\s+track[i]?[n]?[g]?\s+system\w*', text)
        match_2 = o(r'\bats\b', text)
        match_3 = o(r'\bexperience\w*|\bknowledge\w*|\brecruit\w*|\bhire\w*|\bhiring\w*|\bapplicant\w*|\bsystem\w*|\bsoftware\w*|\buse\w*|\bfamiliar\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'applicant tracking systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for radiological photography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimaging\b|\bimage\w*|\bphotograph\w*', text)
        match_2 = o(r'\bray\b|\bxray\b|\bradiolog\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'radiological photography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for veterinary clinical sciences
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!sruc )\bveterinary(?! services group)\w*|\bvet\s+scien\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'veterinary clinical sciences'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for good manufacturing practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgood\s+manufactur[e]?[i]?[n]?[g]?\s+principles\w*|\bgood\s+manufactur[e]?[i]?[n]?[g]?\s+practice\w*|\bgmp\s+environment\w*', text)
        match_2 = o(r'\bgmp\b|\bgmps\b|\bcgmp\b|\bcgmps\b', text)
        match_3 = o(r'\bmanufactur\w*|\bfabricat\w*|\bcomply\w*|\bcomplinace\w*|\bknowledge\w*|\baware\w*|\bexperience\w*|\benvironment\w*|\bpractice\w*|\bprocedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'good manufacturing practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manufacturing processes
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bprocess\s+scientist\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+manufactur\w*|\bpassion\s+manufactur\w*|\bprocess\s+development\s+scientist\w*|\b[c]?[o]?\s?[-]?\s?ordinate\s+\w+\s+manufactur\w*', text)
        match_2 = o(r'\bmanufactur[e]?[i]?[n]?[g]?\w*|\bplant\b|\bfabricat[e]?[i]?[o]?[n]?\w*', text)
        match_3 = o(r'\bprocess\w*|\bprocedur\w*|\bexperience\w*|\benvironment\w*|\bknowledge\w*|\bunderstand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'manufacturing processes'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for current good manufacturing practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcgmp\b|\bcgmps\b|\bcurrent\s+good\s+manufacturing\s+principle\w*|\bcurrent\s+good\s+manufacturing\s+practice\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'current good manufacturing practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manufacturing plant operations and equipment
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmanufacturing\s+equipment\s+engineer\w*|\bplant\s+equipment\w*|\bmanufactur[e]?[i]?[n]?[g]?\s+operations\w*', text)
        match_2 = o(r'(?<!lean )\bmanufacturing\w*|\bplant\w*', text)
        match_3 = o(r'\bequipment\w*|\boperat\w*|\btool\w*|\bdevice\w*|\bmachine\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'manufacturing plant operations and equipment'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for lean principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blean\s+manufactur\w*|\blean\s+principle\w*|\blean\s+manage\w*|\blean\s+leader\w*', text)
        match_2 = o(r'\bmanufactur\w*|\bknowledge\w*|\bfamiliar\w*|\bexperience\w*|\bmethod\w*', text)
        match_3 = o(r'\bpdca\b|\bace\b|\bwcm\b|\bsix\s+sigma\w*|\bspc\b|\b6\s+sigma\w*|\b8d\b|\b5s\b|\blean\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'lean principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical science
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+research\w*|\bclinical\s+science\w*|\bclinical\s+knowledge\w*|\bclinical\s+experinece\w*|\bclinical\s+\w+\s+knowledge\w*|\bdeliver[i]?[n]?[g]?\s+clinical\s+train\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical science'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical research document
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcrd\b', text)
        match_2 = o(r'\bresearch\w*|\bclinical\w*|\bstrategy\w*|\blaboratory\b|\blab\b|\bdocument\w*|\bpaperwork\w*|\bhandle\w*|\banaly\w*|\bprepare\w*|\bpreparation\w*|\bknowledge\w*|\bexperience\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical research document'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical coding
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+administration\w*|\bclinical\s+coding\w*|\bclinical\s+coder\b|\bdiagnostic\s+coder\b|\bdiagnostic\s+coding\w*|\bclinical\s+code(?! conduct)\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical coding'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for standards for organizing clinical trial data
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsdtm\b', text)
        match_2 = o(r'\bprogram\w*|\bstatisctics\b|\bstatistical\b|\bdata\w*|\bdatum\w*|\bcode\b|\bcoding\b|\bmodel\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\baware\w*|\bunderstand\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'standards for organizing clinical trial data'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for adam datasets
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\badam\b', text)
        match_2 = o(r'\bprogrammer\w*|\bdataset\w*|\bstatisctics\b|\bstatistical\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'adam datasets'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for tables listings and figures for clinical study report
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btfls\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'tables listings and figures for clinical study report'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical data interchange standards consortium standards
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcdisc\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical data interchange standards consortium standards'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for good clinical and laboratory practices
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bich\w*|\bglp\b|\bgcp\b|\bgood\s+clinical\s+practice\w*|\bgood\s+laboratory\s+practice\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'good clinical and laboratory practices'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical reports
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+datum\b|\bclinical\s+data\b|\bclinical\s+document\w*|\bclinical\s+research\s+associate\w*|\bclinical\s+report\w*|\bclinical\s+outcome[s]?\s+assessment\w*', text)
        match_2 = o(r'\bclinical\s+trial\w*|\bclinical\s+test\w*|\bclinical\s+study\w*', text)
        match_3 = o(r'\blaboratory\w*|\blab\b|clinical\w*|\btest\w*|\bstudy\w*|\bresearch\w*|\bdrug\w*|\breport\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical reports'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dissemination strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdissemination\w*', text)
        match_2 = o(r'\bstrateg\w*|\bplan\w*|\bexperience\w*|\bknowledge\w*|\bmethod\w*|\bpublish\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'dissemination strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ppi business system
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bppi\b\s+business\w*|\bpractical\s+process\s+imporov\w*', text)
        match_2 = o(r'\bppi\b', text)
        match_3 = o(r'\bbusiness\w*', text)
        match_4 = o(r'\bsystem\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ppi business system'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for inventory management rules
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'manag[e]?[i]?[n]?[g]?\s+inventory\w*|\binventory\s+manage\w*|\bbuyer\s+planner\w*|\bplan\s+inventory\w*|\binventory\s+plan\w*|\binventory\s+optimal\w*|\binventory\s+control\w*|\boptimal\s+inventory\w*|\bmanag[e]?[i]?[n]?[g]?\s+stock\w*|\bstock\s+manage\w*|\boptimal\s+stock\w*|\bplan\s+stock\w*|\bstock\s+plan\w*|\bupdate\s+inventory\w*|\bcatalog[u]?[e]?\s+inventory\w*|\binventory\s+catalog\w*|\bmanag[e]?[i]?[n]?[g]?\s+stock\w*|\bstock\s+manage\w*|\bstock\s+check\w*|\bcheck[i]?[n]?[g]?\s+stock\w*|\bstock\s+adjustment\w*|\badjust[i]?[n]?[g]?\s+stock\w*|\bmaterial\s+manage\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+stock\w*|\bmanag[e]?[i]?[n]?[g]?\s+\w+\s+inventory\w*', text)
        match_2 = o(r'\bschedul\w*|\bplan\w*|\bmanage\w*', text)
        match_3 = o(r'\binventory\w*|\bstock\w*|\bresource\w*|\brestock\w*|\bmmt[s]?\b|\bmaterial\w*', text)
        match_4 = o(r'\btraining\s+program\w*|\btrainer\w*|\btraining\s+plan\w*', text)
        if match_1 and not match_4:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and not match_4:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'inventory management rules'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for standard operating procedure
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstandard\s+operation\s+procedure\w*|\bstandard\s+operat[e]?[i]?[n]?[g]?\s+procedure\w*|\brelevant\s+sop[s]?\b|\bcompany\s+\bsop[s]?\b|\bcurrent\s+\bsop[s]?\b|\buse\s+\bsop[s]?\b|\bsop[s]?\b\s+policy\w*|\bfollow\s+sop[s]?\b|\baccordance\s+sop[s]?\b|\baccording\s+sop[s]?\b|\badhere\s+sop[s]?\b|\brevise\s+sop[s]?\b|\bsop[s]?\s+\w+\s+regulation\w*', text)
        match_2 = o(r'\bsop\b', text)
        match_3 = o(r'\bprocedure\w*|\bregulat\w*|\bguideline\w*|\bpolicy\w*|\bstandard\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'standard operating procedure'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for category management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcategory\s+management\w*', text)
        match_2 = o(r'\bcm\b', text)
        match_3 = o(r'\bstrategy\w*|\bprocurement\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'category management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for clinical trial regulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bclinical\s+trial\w*', text)
        match_2 = o(r'\bcomply\w*|\bcompliance\w*|\brequirement\w*|\bpolicy\w*|\bregulat\w*|\blegislation\w*|\bpolicies\w*|\bstandard\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'clinical trial regulation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for agriculture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagriculture(?! rural invite)\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'agriculture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for environmental management monitors
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\benvironmental\s+management\w*', text)
        match_2 = o(r'\b(?<!team )(?<!work )(?<!organisation )(?<!corporate )(?<!company )\benvironment\w*|\bwildlife\w*', text)
        match_3 = o(r'\bmanage\w*|\bmonitor\w*|\bassess\w*|\bconserv\w*|\banaly\w*|\bparameter\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'environmental management monitors'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for wildlife projects
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwildlife\w*|\bnature\s+conservation\w*', text)
        match_2 = o(r'\bproject\w*|\bstudy\w*|\bresearch\w*|\bassess\w*|\banaly\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'wildlife projects'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for conservation techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconservation\s+management\w*|\bconservation\s+techniq\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'conservation techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for climate smart agriculture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bagriculture(?! rural invite)\w*|\bagricultural\w*', text)
        match_2 = o(r'\bclimate\s+change\w*|\bclimate\s+smart\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'climate smart agriculture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sustainable development in agriculture
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsustainable\w*|\bsustainability\w*', text)
        match_2 = o(r'\bagriculture(?! rural invite)\w*|\bagricultural\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sustainable development in agriculture'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for ecology and its principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\becologist[s]?\b', text)
        match_2 = o(r'\becology\w*|\becological\b', text)
        match_3 = o(r'\bconsult\w*|\bresearch\w*|\bscientist\w*|\badvisor\w*|\badviser\w*|\bassess\w*|\badvice\w*|\badvise\w*|\bprinciple\w*|\bknowledge\w*|\bscience\w*|\bexperience\w*|\bprocedure\w*|\bstudy\w*|\bsurvey\w*|\bresearch\w*|\bfamiliar\w*|\bundersand\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'ecology and its principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for forestry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bforester\w*|\bforestry\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'forestry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for sustainable forest management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bsustainable\s+forest\w*', text)
        match_2 = o(r'\bsustainability\w*|\bsustainable\w*', text)
        match_3 = o(r'\bforest\w*|\bwoodland\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'sustainable forest management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for plant disease control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdisease\w*', text)
        match_2 = o(r'\bplant\b|\bplants\b', text)
        match_3 = o(r'\bcontrol\w*|\bassess\w*|\bdata\w*|\bdatum\w*|\bmanage\w*|\bmanaging\w*|\bcontrol\w*|\bknowledge\w*|\bexperience\w*|\bunderstand\w*|\bfamiliar\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'plant disease control'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for aquatic ecology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwater\s+ecosystem\w*', text)
        match_2 = o(r'\becology\w*|\becological\b|\becologist\w*', text)
        match_3 = o(r'\bmarine\b|\bmaritime\w*|\bsea\b|\bocean\w*|\bsubsea\w*|\baquatic\b|\bwater\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'aquatic ecology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for marine biology and technology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\balgal\w*|\bmacroalgal\w*|\bshellfish\w*|\bmarine\s+research\w*|\bmarine\s+scien\w*|\bmarine\s+biolog\w*|\bsubsea\s+engineer\w*|\bsubsea\s+product[s]?\w*|\bmarine\s+technology\w*|\bsubsea\s+system\w*|\bmarine\s+system\w*|\bmarine\s+environment\w*|\bmarine\s+aquaculture\w*|\bmarine\s+ecolog\w*|\baquaculture\s+environment\w*|\bmarine\s+hardware\w*|\bsubsea\s+biolog\w*|\bsubsea\s+research\w*|\bsubsea\s+technolog\w*|\bsubsea\s+hardware\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'marine biology and technology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for marine engineering
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarine\s+engineer\w*|\bocean\s+engineer\w*|\bmarine\s+road\s+engineer\w*|\bmarine\s+highway\s+engineer\w*|\bmarine\s+operation\w*|\bmarine\s+structure\w*|\bsubsea\s+irm\b', text)
        match_2 = o(r'\bmarine\w*|\bocean\b|\boceanic\b|\bsubsea\w*', text)
        match_3 = o(r'\bengineer\w*', text)
        match_4 = o(r'\bquay\s+wall[s]?\b|\bpier\b|\bjetty\b|\brevetment[s]?\b|\bfendering\b|\bscour\s+protect\w*|\blinkspan[s]?\w*|\blink\s+-\s+span[s]?\b|\blink-span[s]?\b', text)
        match_5 = o(r'\bdesign\w*|\bengineer\w*|\bmarine\w*|\bocean\b|\boceanic\b|\bsubsea\w*|\bfamiliar\w*|\bknowledge\w*|\bexperience\w*|\buse\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'marine engineering'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of tidal stream generators
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btidal\s+energy\b|\btidal\s+turbine\b', text)
        match_2 = o(r'\btidal\b|\bwave\w*', text)
        match_3 = o(r'\benergy\w*|\bgenerat\w*|\bturbine\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of tidal stream generators'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for offshore renewable energy technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btidal\s+energy\b|\btidal\s+turbine\w*|\bwind\s+turbine\w*|\bwave\s+turbine\w*|\bexperience\s+renewable\w*|\bknowledge\s+renewable\w*', text)
        match_2 = o(r'\brenewable\s+energy\w*|\brenewable\s+power\w*', text)
        match_3 = o(r'\boffshore\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'offshore renewable energy technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for seaweed
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmacroalgae\b|\bseaweed\b|\bmacrophyte\w*|\bbenthic\s+diatom\w*|\balgal\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'seaweed'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for horticulture principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhorticulture\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'horticulture principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for horticultural design
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bhorticulture\b|\bhorticultural\b', text)
        match_2 = o(r'\bdesign\w*|\bplan\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'horticultural design'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for animal species and animal nutrition
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\s+caretaker\w*|\banimal\s+nutrition\w*|\bhabitat\s+specie\w*', text)
        match_2 = o(r'\banimal\w*', text)
        match_3 = o(r'\bnutrition\w*|\bspecie\w*|\bhealth\w*|\bwell\s?[-]?\s?being\w*|\bsafety\w*|\bcaretake\w*|\bfeed\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'animal species and animal nutrition'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for upland hill systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bupland\w*', text)
        match_2 = o(r'\blivestock\w*|\bsystem\w*|\banimal\w*|\bfarm\w*|\bcrofter\w*|\bhill\w*|\bagriculture\w*|\bagricultural\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'upland hill systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for animal species
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brural\s+animal\s+health\b|\banimal\s+specie\w*|\banimal\s+caretaker\w*|\bspecie[s]?\s+animal\w*|\bhabitat\s+specie\w*', text)
        match_2 = o(r'\bspecie\w*|\bspiecies\w*', text)
        match_3 = o(r'\becolog\w*|\benvironment\w*|\bhabitat\w*|\bmarine\w*|\bsea\b|\bocean\b|\bbiodiversity\w*', text)
        match_4 = o(r'\bbat[s]?\b|\botter[s]?\b|\breptile\w*|\bamphibian\w*|\bvole[s]?\b|\bbird[s]?\b', text)
        match_5 = o(r'\banimal\w*|\bwork\w*|\bfamiliar\w*|\bknowledge\w*|\bexperience\w*|\btend\w*|\bhandle\w*|\bcare\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'animal species'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for animal welfare
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!alert )\banimal\s+welfare\w*|\bwelfare\s+animal\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'animal welfare'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for veterinary terminology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!sruc )\bveterinary\b|\banimal\s+health\w*|\bvet\s+scien\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'veterinary terminology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for veterinary pathology and cytology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bveterinary\s+pathologist\b|\bveterinary\s+cytologist\b', text)
        match_2 = o(r'\bveterinary\w*', text)
        match_3 = o(r'\bpathology\w*|\bpathological\s+sample\w*|\bcytology\w*|\bpathogen\w*|\bviral\w*|\bvirus\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'veterinary pathology and cytology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for horse racing regulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\brace\s?horse\w*', text)
        match_2 = o(r'\bhandle\w*|\btend\w*|\bcare\w*|\bregulat\w*|\blegislat\w*|\blaw\w*|\bpolicy\w*|\bpolicies\w*|\bmanage\w*|\bmanaging\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'horse racing regulation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for precision livestock farming
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bplf\b\s+technology\w*|\bprecision\s+farm[i]?[n]?[g]?\s+technolog\w*', text)
        match_2 = o(r'\bplf\b|\bprecision\s+farm\w*|\bsmart\s?[-]?\s+farm\w*', text)
        match_3 = o(r'\banimal\w*|\blivestock\w*|\bknowledge\w*|\bsystem\w*|\bexperience\w*|\bunderstand\w*|\bfamiliar\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'precision livestock farming'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for the science of cultivated meat
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bqultivate\s+meat\b', text)
        match_2 = o(r'\bqcultivate\b|\bstem\s+cell\w*', text)
        match_3 = o(r'\bmeat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'the science of cultivated meat'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for livestock reproduction
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\banimal\s+breeding\b|\blivestock\s+breeding\w*', text)
        match_2 = o(r'\breproduct\w*', text)
        match_3 = o(r'\banimal\w*|\blivestock\w*|\bknowledge\w*|\bexperience\w*|\btechnique\w*|\btechnolog\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'livestock reproduction'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for livestock
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\blivestock\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'livestock'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for monogastric digestive systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmonogastric\b', text)
        match_2 = o(r'\banimal\w*|\blivestock\w*|\bdigestive\w*|\bsystem\w*|\bexpereince\w*|\bknowledge\w*|\bunderstand\w*|\bfamiliar\w*|\bcare\w*|\bdigestion\w*|\bstomach\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'monogastric digestive systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for dairy products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdairy\b', text)
        match_2 = o(r'\blivestock\w*|\banimal\w*|\bproduct\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'dairy products'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for publishing market and industry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstudy\s+publisher\b|\breport\s+publisher\b|\bstudy\s+publishing\b|\breport\s+publishing\b', text)
        match_2 = o(r'\bpublish\w*', text)
        match_3 = o(r'\bexperience\w*|\bknowledge\w*|\bsupport\w*|\bconsult\w*|\bhelp\w*|\bunderstand\w*|\bfamiliar\w*|\bactivit\w*|\bmarket\w*|\bindustry\w*|\bstrateg\w*|\bplan\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'publishing market and industry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for medical statistics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistic\w*|\bstatistical\w*', text)
        match_2 = o(r'\bbiolog\w*|\bmedical\b', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'medical statistics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for product data management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bproduct\s+datum\w*|\bproduct\s+data\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'product data management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for customer data management
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bcustomer\w*|\bclient\w*', text)
        match_2 = o(r'\bdata\w*|\bdatum\w*', text)
        match_3 = o(r'\bmanage\w*|\bhandle\w*|\buse\w*|\boversee\w*|\bmanaging\w*|\bexperience\w*|\bknowledge\w*|\buse\w*|\banaly\w*|\bassess\w*|\btest\w*|\binvestigat\w*', text)
        match_4 = o(r'\bcdm(?! legislation| regulation)\b', text)
        match_5 = o(r'\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bunderstand\w*|\bsoftware\w*|\bsystem\w*|\buse\w*|\bdata\w*|\bdatum\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        elif match_4 and match_5:
            matched_fragments.extend([match_4, match_5])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'customer data management'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for electronic design automation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bschematic\s+capture\b', text)
        match_2 = o(r'\belectronic\w*', text)
        match_3 = o(r'\bdesign\w*|\bcreate\w*', text)
        match_4 = o(r'\bautomat\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3 and match_4:
            matched_fragments.extend([match_2, match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electronic design automation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for statistical process control
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstatistic\w*|\bstatistical\w*', text)
        match_2 = o(r'\bproces\w*|\bprocedure\w*', text)
        match_3 = o(r'\bcontrol\w*|\bquality\w*|\bdevelop\w*|\bassess\w*|\banaly\w*|\binvestigat\w*|\bknowledge\w*|\bexperience\w*|\bunderstand\w*|\bfamiliar\w*', text)
        if match_1 and match_2 and match_3:
            matched_fragments.extend([match_1, match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'statistical process control'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for new product or service development
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdevelop\s+new\s+product\w*|\blaunch\s+product\w*|\blaunch\s+new\s+product\w*|\bintroduce\s+new+\s+product\w*|\bintroduction\s+new\s+product\w*|\bintroduce\s+product\w*|\bintroduction\s+product\w*|\bproduct\s+introduction\w*|\blaunch\s+novel\s+product\w*|\bdevelop\s+product\w*|\blaunch\s+service\w*|\blaunch\s+new\s+service\w*|\blaunch\s+novel\s+service\w*|\bproduct\s+launch\w*|\bservice\s+launch\w*|\bfuture\s+product\s+development\w*|\bpromot[e]?[i]?[g]?\s+new\s+product\w*', text)
        match_2 = o(r'\bnpd\b|\bmpis\b|\bnpi\b', text)
        match_3 = o(r'\bresearch\w*|\bproject\w*|\bproduct\b|\bdevelopemnt\w*|\brepair\w*|\bmaintain\w*|\bexperience\w*|\bknowledge\w*|\bunderstand\w*|\bfamiliar\w*|\bproces\w*|\bprocedure\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'new product or service development'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for biotechnology
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbiotechnology\w*|\bbiotech\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'biotechnology'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for financial analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+analy\w*|\bfinance\s+analy\w*|\banalyse\s+financ\w*|\banalyze\s+financ\w*|\bbase\s+analy\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for financial forecasting
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinancial\s+forecast\w*|\bfinance\s+forecast\w*|\bforecast\s+financ\w*|\bforecasting\s+financ\w*|\bops\s+forecast\w*|\bforecast\s+budget\w*|\bbudget\s+forecast\w*', text)
        match_2 = o(r'\bquater[l]?[y]?\s+forecast\w*|\bforecast[s]?\s+update\w*|\bupdate\s+forecast\w*|\bforecast[s]?\s+review\w*', text)
        match_3 = o(r'\bfinance\w*|\bfinancial\w*|\bconduct\w*|\bcalculat\w*|\bperform\w*|\banaly\w*|\bwork\w*|\bmanage\w*|\binvestigat\w*|\bassess\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial forecasting'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for financial planning and analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bfinance\s+plan\w*|\bfinancial\s+plan\w*|\bplan\s+financ\w*', text)
        match_2 = o(r'\bfpa\b', text)
        match_3 = o(r'\bplann\w*|\bfinance\b|\bplan\b|\banaly\w*|\bexperience\w*|\bknowledge\w*|\bproces\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial planning and analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for brand marketing techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbrand\b', text)
        match_2 = o(r'\bmarketing\b|\bcampaign\w*|\btalent\s+acquisition\w*|\badvertis\w*|\bambassador\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'brand marketing techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for imaging techniques
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bimaging\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'imaging techniques'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for stock keeping unit
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bstock\s+keeping\s+unit\w*', text)
        match_2 = o(r'\baccounting\b|\bbookkeep\w*|\baccount\w*|\bknowledge\w*|\bexperience\w*|\bfamiliar\w*|\bunderstand\w*|\bunit\w*|\bprocess\w*', text)
        match_3 = o(r'\bsku\b', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'stock keeping unit'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for marketing mix and market entry strategies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarketing\b|\bmarketing\s+plan\w*|\bcampaign\s+officer\w*', text)
        match_2 = o(r'\bcampaign\w*|\badvertising\w*|\bstrategy\w*|\bprepare\w*|\bexperience\w*|\bknowledge\w*|\bfamiliar\w*|\bdesign\w*|\bcreate\w*|\bresponsibl\w*|\bprocess\w*', text)
        match_3 = o(r'\bgssm\b', text)
        match_4 = o(r'\bsale\w*|\bmarketing\w*|\bknowledge\w*|\bexperience\w*|\bunderstand\w*|\bfamiliar\w*|\bprocess\w*|\bsocial\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'marketing mix and market entry strategies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for market analysis
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmarket\w*', text)
        match_2 = o(r'\banaly\w*|\bassess\w*|\btest\w*|\bperformance\w*|\bresearch\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'market analysis'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for databases
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdatabase\w*|\bdata\s+query\w*|\bdatum\s+query\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'databases'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for multidimensional expressions
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmdx\b', text)
        match_2 = o(r'\bquery\b|\bqueries\b|\bexpression\w*|\bexperience\w*|\bknowledge\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'multidimensional expressions'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for banking activities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bbank[i]?[n]?[g]?\s+activit[y]?[i]?[e]?[s]?\w*|\bbank[i]?[n]?[g]?\s+account\w*|\bbank[i]?[n]?[g]?\s+admin\w*|\bbank[i]?[n]?[g]?\s+system\w*|\bbank[i]?[n]?[g]?\s+platform\w*', text)
        match_2 = o(r'\bbank[i]?[n]?[g]?\b|(?<!world )\bbank(?! holiday)(?! holidays)\w*', text)
        match_3 = o(r'\baccount\w*|\bbill\w*|\btransaction\w*|\bexperience\w*|\bknowledge\w*|\bprocedure\w*|\bpolicy\w*|\bprocess\w*|\brule\w*|\bcomply\w*|\bcomplinace\w*|\bstandard\w*|\bactivit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'banking activities'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for e-commerce systems
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\be\s?[-]?\s?comm\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'e-commerce systems'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for transportation principles
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\btransport\w*|\bmove[m]?[e]?[n]?[t]?\s+good\w*|\bmove[m]?[e]?[n]?[t]?\s+people\w*', text)
        match_2 = o(r'\bmanagement\b|\bprinciple\w*|\bprocedure\w*|\bmethod\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'transportation principles'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for warehouse operations
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bwarehouse\s+operation\w*|\bwarehouse\s+manage\w*|\bmanage[m]?[e]?[n]?[t]?\s+warehouse\w*|\bmanaging\s+warehouse\w*|\bwarehouse\s+function\w*|\bwarehouse\s+practice\w*|\boperation\s+warehouse\w*|\bfunction\s+warehouse\w*|\bstoreman\b', text)
        match_2 = o(r'\bwarehouse\w*', text)
        match_3 = o(r'\bexperience\w*|\bknowledge\w*|\bunderstand\w*|\boperation\w*|\bprocedure\w*|\bactivit\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'warehouse operations'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for machinery load capacity
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bload\s+requirement\w*|\bline\s+load\b|\btest[i]?[n]?[g]?\s+load\w*|\bmaximum\s+load\b|\bequipment\s+load\b|\bmachinery\s+load\b|\bload\s+capacity\w*|\bensure\s+load\b', text)
        match_2 = o(r'\bequipment\w*|\bmachinery\w*|\bcrane\w*|\bmaintenance\w*|\bknowledge\w*|\bunderstand\w*|\bexperience\w*|\bcheck\w*|\bensure\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'machinery load capacity'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for offshore constructions and facilities
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\boffshore\s+construction\w*|\boffshore\s+facilit\w*|\bsubsea\s+construction\w*|\boil\s+platform\w*|\boffshore\s+structure\w*|\bsubsea\s+structure\w*|\boffshore\s+build\w*|\bsubsea\s+build\w*|\boffshore\s+certificat\w*|\boffshore\s+wind\w*|\boffshore\s+project\w*|\bsubsea\s+project\w*|\boffshore\s+energy\w*|\bsubsea\s+energy\w*|\boffshore\s+manage\w*|\boffshore\s+resource\w*|\bmarine\s+construction\w*|\bsubsea\s+products\w*|\bsubsea\s+irm\b', text)
        match_2 = o(r'\bwind\s+farm\w*|\brig\b|\bplatform\b|\bwell\b', text)
        match_3 = o(r'\boffshore\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'offshore constructions and facilities'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for construction products
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bconstruction\s+product\w*|\bconstruction\s+brand\w*|\bconstruction\s+commodit\w*|\bconstruction\s+good\w*|\bconstruction\s+merchandise\w*|\bconstruction\s+stock\w*|\bconstruction\s+equipment\w*|\bconstruction\s+tool\w*|\bbuild[i]?[n]?[g]?\s+product\w*|\bbuild[i]?[n]?[g]?\s+brand\w*|\bbuild[i]?[n]?[g]?\s+commodit\w*|\bbuild[i]?[n]?[g]?\s+good\w*|\bbuild[i]?[n]?[g]?\s+merchandise\w*|\bbuild[i]?[n]?[g]?\s+stock\w*|\bbuild[i]?[n]?[g]?\s+equipment\w*|\bbuild[i]?[n]?[g]?\s+tool\w*', text)
        match_2 = o(r'\bconstruction\w*', text)
        match_3 = o(r'\bproduct\w*|\bmerchandise\w*|\bmaterial\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'construction products'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for types of pipelines
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'(?<!sale )(?<!great )\bpipeline\w*|\bsvt\s+transformation\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'types of pipelines'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for automation technologies
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bautomat[e]?[i]?[o]?[n]?\s+technolog\w*|\belectrical\s+automat\w*|\belectric\s+automat\w*|\bprocess\s+automat\w*|\bautomat[e]?[i]?[o]?[n]?\s+process\w*|\bfield\s+automat\w*|\bautomat[e]?[i]?[o]?[n]?\s+specialist\w*|\bautomat[e]?[i]?[o]?[n]?\s+engineer\w*|\bautomat[e]?[i]?[o]?[n]?\s+method\w*|\bmethod\s+automat\w*|\bopportunity\s+automat\w*|\bautomat[e]?[i]?[o]?[n]?\s+opportunit\w*|\bmarketing\s+automation\w*|\bautomat[e]?[d]?[i]?[o]?[n]?\s+\w+\s+solution\w*|\bautomat[e]?[i]?[o]?[n]?\s+\w+\s+method\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'automation technologies'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)
        
        # Matching fragments for in vitro diagnosctic regulation
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bivdr\b|\bivd\b|\bvitro\s+bioequivalence\w*|\bbioequivalence\s+test\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'in vitro diagnosctic regulation'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for mass spectrometry
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bmass\s+spectromet\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'mass spectrometry'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for chromatography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bchromatograph\w*|\bchromatography\s+software\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'chromatography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for gas chromatography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bgas\b\s+chromatograph\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'gas chromatography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for liquid chromatography
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bliquid\s+chromatograph\w*|\bhlpc\b', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'liquid chromatography'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for drug metabolism and pharmacokinetics
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'\bdmpk\b', text)
        match_2 = o(r'\bscientist\w*|\bresearch\w*|\bdrug\w*|\bdata\w*|\bdatum\w*|\bexperience\w*|\bknowledge\w*', text)
        match_3 = o(r'\bdrug\w*|\bmedicine\w*', text)
        match_4 = o(r'\bmetabolism\w*|\bpharmacokinetic\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        elif match_3 and match_4:
            matched_fragments.extend([match_3, match_4])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'drug metabolism and pharmacokinetics'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality standards for crisis intervention
        matched_fragments = []
        match_1 = o(r'\bcrisis\b', text)
        match_2 = o(r'\bmanage\b|\bmanagement\b|\bmanaging\b|\bmechanism\w*|\bstrateg\w*|\bintervention\w*|\bcommunicat\w*|\bplan\w*', text)
        if match_1 and match_2:
            matched_fragments.extend([match_1, match_2])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'crisis intervention'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality standards for financial capability
        matched_fragments = []
        match_1 = o(r'\bfinancial\s+abilit\w*|\bfinance\s+abilit\w*|\bfinancial\s+acumen\w*|\bfinance\s+acumen|\bfinancial\s+capabilit\w*|\bfinance\s+capabilit\w*|\bfinancial\s+knowledge\w*|\bfinance\s+knowledge\w*|\bfinancial\s+understand\w*|\bfinance\s+understand\w*|\bunderstand[i]?[n]?[g]?\s+financ\w*|\bfinancial\s+skill\w*|\bfinance\s+skill\w*|\bfinancial\s+litera\w*|\bfinance\s+litera\w*|\bfinancial\s+expertise\w*', text)
        if match_1:
            matched_fragments.append(match_1)
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial capability'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for quality standards for financial markets
        matched_fragments = []
        match_1 = o(r'\bfinancial\w+market\w*|\bfinance\s+market\w*|\bfiscal\s+market\w*|\bmoney\s+market\w*|\blisting\s+rule\w*', text)
        match_2 = o(r'\bpublic\s+listing\w*|\bpublic\s+offering\w*|\bipo\b|\bstock\s+market\w*|\bstock\s+exchange\w*', text)
        match_3 = o(r'\bexpertise\w*|\bknowledge\w*|\bunderstand\w*|\bassess\w*|\banaly\w*|\badvice\w*|\badvise\w*|\bmanaging\b|\bmanage\b|\bmanagement|\bhelp\w*|\bassist\w*|\bexperience\b', text)
        if match_1:
            matched_fragments.append(match_1)
        elif match_2 and match_3:
            matched_fragments.extend([match_2, match_3])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'financial markets'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)

        # Matching fragments for manage electronic communication
        matched_fragments = []  # Reset matched fragments
        match_1 = o(r'electronic\s+communicat\w*|\bdigital\s+communicat\w*|\belectronic\s+contact\w*|\bdigital\s+contact\w*|\belectronic\s+converstaion\w*|\bonline\s+communicat\w*|\belectronic\s+mail\s+service\w*|\bemail\s+service|\bhandl[e]?[i]?[n]?[g]?\s+email\w*|\bmanag[e]?[i]?[n]?[g]?\s+email\w*|\belectronic\s+information\w*|\bdigital\s+information\w*', text)
        if match_1:
            matched_fragments.extend([match_1])
        if matched_fragments:
            k = k + 1
            df.loc[index, 'knowledge_' + str(k)] = 'electronic communication'
            df.loc[index, 'knowledge_' + str(k) + '_fragments'] = join_lists(matched_fragments)
        k = 0
    return df
